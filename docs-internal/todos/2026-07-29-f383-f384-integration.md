# F383 + F384 Integration

## Goal

Integrate the complete F383 and F384 history onto the latest local `master`,
resolve every divergence by source-backed semantics, and produce enough
database, API, web, browser, and data-readback evidence to decide whether the
result is safe to merge into `master`.

## Scope

- In scope: Git ancestry and divergence, semantic merge resolution, migration
  compatibility, F383/F384 regressions, Picshare runtime readback, evidence
  reporting, and progress-index synchronization.
- Out of scope: F385, resource providers, resource requests, unrelated
  refactors, private-remote credential sign-off, pushing, and merging into
  `master`.
- Protected unrelated files in the original checkout: `check2.mjs` and
  `docs/todos/2026-07-29-agent-session-isolation.md`.

## Clarifications And Assumptions

- Confirmed: integration starts from local `master@68aabfa7f99aa0a478eefdbcf09197ada700aafd`.
- Confirmed: feature branch is
  `codex/f384-repository-analysis@1524df449c2a3c72844ad9421921b62c959eb9bf`.
- Confirmed: local `master` has 8 unique commits and the feature branch has 54.
- Confirmed: the feature branch contains `dbce7a7f`, `cc87c2d8`, `5bffc4a6`,
  and `1524df44`; local `master` contains none of them.
- Assumption: an isolated Git worktree is the safest integration surface while
  another active task writes the original checkout. The integration branch
  remains a normal local branch and the original untracked files stay untouched.

## Workflow Routing

`routing: long-goal + code-graph + single-writer integration + noisy-tools; the
merge crosses schema, API, web, runtime, and evidence boundaries, while all
high-output verification is captured under
/tmp/codex-tool-runs/svton/f383-f384-integration/.`

## Functional TODO Breakdown

### F1. Establish a safe, source-backed integration baseline

Purpose: prove the exact histories and semantic impact before changing the
integration branch.

| ID | Status | Atomic TODO | Context Boundary | Evidence |
|----|--------|-------------|------------------|----------|
| F1.1 | done | Enumerate both sides of the divergence and analyze each of the 8 `master`-only commits. | Git history and changed-path inspection only. | `w001-git-impact-audit.md`: one patch-equivalent pair, six later Pi fixes, merge commit, and no master-only Devpilot production/migration overlap. |
| F1.2 | done | Create the integration branch from the latest local `master` without touching the original checkout. | Git branch/worktree only. | `codex/devpilot-f383-f384-integration@68aabfa7`. |
| F1.3 | done | Build a source/test impact map for migrations, backend, web routes, release execution, and repository analysis. | Read-only graph and source verification. | CodeGraph index plus W001-W003 maps identify schema/API/web/runtime entry points, test gates, and evidence gaps. |

### F2. Preserve both histories and resolve integration semantics

Purpose: merge the complete F383/F384 chain while retaining all valid
`master` behavior.

| ID | Status | Atomic TODO | Context Boundary | Evidence |
|----|--------|-------------|------------------|----------|
| F2.1 | done | Merge `codex/f384-repository-analysis` as a complete branch. | Git merge in the isolated worktree. | Merge commit `0887483e`; second parent is fixed feature head `1524df44`. |
| F2.2 | done | Resolve every conflict from real data models and call chains. | Conflicted production/test/docs paths only. | Preserved the post-equivalent-commit `master` Pi fixes in 16 paths; regenerated `pnpm-lock.yaml` from all merged manifests. |
| F2.3 | done | Add focused regression coverage for any semantic resolution not already covered. | Tests adjacent to affected contracts. | Activated all 6 web spec files with Vitest, fixed the exposed dependency-label regression, and passed 25/25 tests. |
| F2.4 | done | Confirm the final graph contains local `master` plus F383/F384 ancestors. | Git ancestry only. | `master`, `dbce7a7f`, `cc87c2d8`, `5bffc4a6`, and `1524df44` are all ancestors of `0887483e`. |

### F3. Prove database and service compatibility

Purpose: ensure schema history and backend contracts work from clean and
upgrade states without weakening permissions, IDs, or redaction.

| ID | Status | Atomic TODO | Context Boundary | Evidence |
|----|--------|-------------|------------------|----------|
| F3.1 | done | Validate and generate Prisma clients. | Devpilot Prisma packages only. | Prisma validate/generate pass on the integrated tree. |
| F3.2 | done | Run all migrations against a disposable fresh database. | Disposable database resources only. | Fresh MySQL 8 reaches 55/55 migrations. |
| F3.3 | done | Upgrade a disposable integration-prebaseline database. | Baseline snapshot plus new migrations only. | Local `master@68aabfa7` reaches 50/50, then the integrated tree upgrades to 55/55 with the same schema hash as a fresh database. |
| F3.4 | done | Run F383/F384 backend, permission, forged-ID, and secret-redaction tests. | Targeted API suites. | Green: F383 7 suites/38, F384 7/45, permission+forged-ID+redaction 14/164, release coordinator DB 28/28, and dependency resolver DB 4/4. |
| F3.5 | done | Run API type-check, build, and merge-impact regression. | Devpilot API workspace. | API type-check/build pass; Pi Core 330 files/1841 tests, Pi Client 12/267 plus strict type-check, Pi Web 4/25, and nestjs-http 1/3 all pass. |
| F3.6 | done | Remove repository-analysis suggestion secrets from stored analysis evidence and API responses without applying redaction placeholders as executable config. | Repository parser suggestions, run responses, and review persistence only. | Response redaction, write-side sanitization, unsafe-command omission, apply-time rejection, Date preservation, and a legacy cleanup migration are covered by 37 targeted tests and a 54-to-55 migration sentinel proof with zero residual secret matches. |

### F4. Prove web behavior and Picshare runtime integrity

Purpose: verify real project, repository-analysis, release, execution, audit,
and deep-link behavior rather than relying on builds alone.

| ID | Status | Atomic TODO | Context Boundary | Evidence |
|----|--------|-------------|------------------|----------|
| F4.1 | done | Run F384 web tests, web type-check, build, and affected regression. | Devpilot web workspace. | Devpilot Web 6 files/25 tests, type-check, and production build all pass. |
| F4.2 | done | Exercise Picshare overview, 6/6 readiness, analysis details/history, review/application, app-service/environment, audit, release, and execution deep links. | Running integration build and existing Picshare data only. | Browser rerun after the Date fix proves 6/6, valid history timestamps, 5 persisted review decisions, environment/application/service views, 13 audit events, F383 6/6 release stages, and exact deployment deep links. |
| F4.3 | done | Verify forged IDs fail accurately and UI actions do not fake or silently swallow outcomes. | Browser plus API readback. | Forged project, repository run, deployment run, cross-project run, and release plan return exact 404; browser paths show explicit not-found states without list fallback. |
| F4.4 | done | Confirm Picshare branch/commit, associations, F383 execution records, F384 analysis records, and zero secret leakage through API/database/log/audit readback. | Read-only Picshare/API/database inspection. | Picshare stayed clean at `master@8e7c465d`; API/DB association readback matches all known IDs; repository API, suggestion JSON, audit rows, and runtime logs report zero sensitive findings. |

### F5. Produce merge-readiness evidence and stop

Purpose: leave a committed integration branch with an explicit, auditable
decision.

| ID | Status | Atomic TODO | Context Boundary | Evidence |
|----|--------|-------------|------------------|----------|
| F5.1 | done | Write the integration acceptance report with commands, commits, conflicts, screenshots, logs, and limitations. | `docs-internal/devpilot/f383-f384-integration-report.md`. | Report contains the merge verdict, ancestry, 17 conflict paths, test/migration/runtime evidence, security proof, cleanup, and external boundary. |
| F5.2 | done | Synchronize only the relevant TODO/progress indexes without starting F385. | Existing Devpilot indexes. | Progress/TODO indexes, requirements summary, and F383/F384 anchors now route to the integration report. |
| F5.3 | done | Check diff hygiene and all touched production TS/TSX files against the 200-line ceiling. | Final branch diff only. | `git diff --check` passes; 10 integration-authored production files are at most 200 lines (largest 195). The 13 production Pi conflict paths are byte-identical to local master and introduce no content delta. |
| F5.4 | done | Commit all integration work and issue the evidence-backed merge/no-merge verdict. | Final Git state and report. | Final report verdict: can merge to master; the closing evidence commit contains this TODO/report/index synchronization. No push or master merge is performed. |

### F6. Rebuild merged master and prove a new Picshare release

Purpose: validate the merged `master@036b704b` from fresh images and a new,
disposable six-stage Picshare release without reusing the historical plan as
proof.

`routing: long-goal + focused cross-module fix + code-graph + noisy-tools; one
writer owns codex/devpilot-master-local-deploy-smoke, and all runtime evidence
is isolated under /tmp/codex-tool-runs/svton/devpilot-master-local-deploy-smoke/.`

| ID | Status | Atomic TODO | Context Boundary | Evidence |
|----|--------|-------------|------------------|----------|
| F6.1 | done | Rebuild API/Web images from exact merged master and prove F383/F384 runtime readiness on a cloned database. | Isolated worktree, images, Compose project, and cloned database only. | Images and running containers carry source label `036b704b`; canonical Picshare repository analysis succeeds on exact `master@8e7c465d`, and project readiness is 6/6. |
| F6.2 | done | Preserve release-only deploy configuration when F384 repository suggestions are applied. | Repository-analysis suggestion apply path and focused tests only. | Commit `17652567`; 7 suites/48 tests and API type-check pass. Rebuilt API, fresh analysis `cms690icn001a10067rtj8g6p`, and preview prove all 6 stages plus required backend-health-to-admin-deploy edge survive application. |
| F6.3 | in_progress | Rebuild the fix and complete a new six-stage Picshare release plus idempotent repeat in a disposable target. | Task-owned SSH/Docker/repository/database resources only. | Disposable SSH/DinD/MySQL/Redis/repository target is online. Plan `cms69g0pw0023ea2ob7hifvy0` proved real schema execution and downstream blocking, exposing that F383 dropped F384 `workingDirectory`; focused propagation fix passes 3 suites/50 tests and type-check. |
| F6.4 | pending | Verify browser deep links, exact execution/deployment details, audits, zero leak, cleanup, and final F385 gate. | Isolated runtime and evidence report only. | Pending. |

## Verification Plan

- Capture all noisy commands under
  `/tmp/codex-tool-runs/svton/f383-f384-integration/`.
- Run Git ancestry and diff hygiene, Prisma validation/generation, fresh and
  baseline-upgrade migrations, targeted and affected tests, API/web type-check
  and builds, browser regression, API/database readback, and redaction checks.
- Re-run any failing product path after an in-scope fix until it passes or an
  unavoidable external blocker is demonstrated.

## Change Log

- 2026-07-29: Created the integration plan from verified local Git facts.
- 2026-07-29: Routed write work to an isolated worktree because another active
  task owns the original checkout.
- 2026-07-29: Created merge commit `0887483e`; resolved 16 Pi migration
  conflicts to the `master` semantic superset and regenerated the lockfile for
  the combined manifests.
- 2026-07-29: Activated the Devpilot Web Vitest suite and fixed the dependency
  description spacing regression found by the previously inert tests.
- 2026-07-29: Added F3.6 after source-backed security review confirmed that
  historical repository-analysis run details returned literal credentials in
  nested command values.
- 2026-07-29: Closed the database and service proof on 55 migrations, including
  fresh/upgrade schema parity, legacy credential cleanup, release DB suites,
  repository-analysis redaction suites, and API type-check/build.
- 2026-07-29: Browser acceptance found and closed a Date-preservation
  regression, then proved real Picshare overview/repository/audit/environment/
  release/deployment flows and exact forged-ID failures.
- 2026-07-29: API/DB/log readback returned zero sensitive findings; the source
  acceptance database remained at 54 migrations while only the clone reached
  55, and all disposable runtime resources plus sensitive temp files were
  removed.
- 2026-07-29: Final ancestry, diff, scope, documentation routing, screenshot,
  cleanup, no-upstream/no-remote-branch, and integration-authored 200-line
  gates passed. Verdict: can merge to master; stop before push or merge.
- 2026-07-29: A fresh merged-master runtime proved F384 suggestion application
  could erase F383-only deploy fields; added F6 to fix and re-run a genuinely
  new six-stage Picshare release before deciding whether F385 may start.
- 2026-07-29: The first disposable release correctly blocked all downstream
  stages after a real schema command failure; the failure exposed and fixed
  missing F384 working-directory propagation into F383 server-command stages.
