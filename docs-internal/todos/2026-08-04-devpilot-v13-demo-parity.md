# Devpilot V13 Demo 1:1 Parity Closure

## Goal

在保留 F386-F412 已完成领域模型和历史证据的前提下，把当前 V13 worktree 的功能、页面结构、交互状态、真实执行链路和浏览器结果完整对齐 V13 canonical spec 与 `delivery-versions-v9.html?v=13.0`，最终让用户能够从项目接入开始，经发布单构建、Staging、Production、环境升级/回退和域名入口完成可审计、可从浏览器访问的真实闭环。

## Scope

- In scope: V13 项目目录/接入、项目交付首页、发布单四步、BuildRun/Manifest、Staging/Production、审批、环境版本、项目设置、51/15 门禁、真实构建/部署/路由、日志、UI/交互、无障碍、Docker/browser E2E、文档和最终验收。
- Out of scope: 未经用户授权的 push、PR、合并 master、外部生产环境变更；伪造 Provider/部署/健康检查成功；用静态截图或 mock API 替代真实行为。
- Protected checkout: `/Users/zhaoxingbo/Workspace/ai-driven/svton` 只读；实现只写 `/Users/zhaoxingbo/Workspace/ai-driven/svton-devpilot-project-delivery-v13`。
- Visual truth: V13 Demo 项目内容区、交互、状态和视觉层级 1:1 对标；平台仍可保留真实跨项目模块，但不得破坏 Demo 的项目中心 IA。

## Clarifications And Assumptions

- Confirmed: F386-F412 是历史实现范围完成记录，不代表本次严格 1:1 parity 验收通过。
- Confirmed: canonical spec 优先级高于 Demo 中的旧文案或纯 mock 行为；两者冲突时记录差异并按 canonical spec 实现。
- Confirmed: Demo HTML SHA-256 为 `523080f43d935dba737fdfc0013f5133dc140c6d19936077692dfa556b549b0a`。
- Confirmed: canonical spec SHA-256 为 `a491e9f5e9f583bf92fc56ef804a0884f5ab65bd93156a318b809f2b5b605393`。
- Confirmed: 当前 Docker 未配置 `RELEASE_BUILD_EXECUTION_ENABLED` 和 `RELEASE_STAGING_DEPLOYMENT_ENABLED`；当前 release-order 执行数据为 0 BuildRun/Manifest/DeploymentRun/ReleaseRun/EnvironmentVersion。
- Confirmed: 当前 exact-artifact executor 只验证并物化 ZIP，不启动工作负载、注入配置、配置入口或执行浏览器探测。
- Confirmed: 一个 checkout 同时只允许一个 write worker；可并行的只读审查或独立 worktree 写入必须由主会话明确调度。

## Workflow Routing

`routing: long-goal + orchestrator-board + atomic workers + single-writer + noisy-tools; 主会话维护目标、依赖和验收，worker 每次只处理一个 Fxxx，完整日志保存在 /tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/。`

## Authoritative Inputs

- V13 Demo: `/Users/zhaoxingbo/.codex/visualizations/2026/07/31/019fb7eb-9f49-77b0-af1d-f50f9c4316ce/delivery-center-html/delivery-versions-v9.html`
- Canonical spec: `/Users/zhaoxingbo/.codex/visualizations/2026/07/31/019fb7eb-9f49-77b0-af1d-f50f9c4316ce/delivery-center-html/devpilot-project-delivery-v13-canonical-spec.md`
- 1:1 acceptance: `../devpilot/project-delivery-v13-demo-parity-acceptance.md`
- Historical implementation ledger: `2026-08-03-devpilot-project-delivery-v13.md`
- Historical progress/evidence: `../devpilot/progress/project-delivery-v13.md`

## Dependency Rules

- F414-F419 establish project identity and project-home contracts before release UI consumes them.
- F420-F430 establish release state, gates and immutable build behavior before real Staging/Production execution.
- F431-F442 establish real runtime, approval and environment-version execution before positive E2E.
- F443-F451 may proceed after the referenced backend/read-model contract exists; visual workers must not invent data contracts.
- F452-F458 are closure gates and may not be marked done using mocked success states.

## Current Parity Verdict

- Verdict: **not accepted**. The current branch has the core additive domain model and
  several usable project/release screens, but it does not yet provide the complete
  real deployment chain or 1:1 V13 information/interaction/visual parity.
- Remaining execution scope: 47 pending atomic slices after F413 — project entry/home
  6, release/gates/build 11, real Staging/Production 10, environment governance 9,
  UI/accessibility 4, and runtime/E2E/final closure 7.
- Acceptance state at freeze: 350 checks total; 4 baseline-integrity checks recorded,
  346 functional/UI/runtime/final checks remain open. Historical F386-F412 evidence
  does not auto-satisfy any open check.

## Functional TODO Breakdown

### P0. Parity Contract And Project Entry

| ID   | Status | Atomic TODO                                                                                                       | Context Boundary                                                                                                                                                           | Acceptance                                                                                                                                                                                                                         |
| ---- | ------ | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F413 | done   | 固化本次审计差距、Demo/spec 哈希、剩余 TODO 和逐项验收清单。                                                      | Docs only；不改产品行为。                                                                                                                                                  | AC-000～AC-006；本文件与 parity acceptance 文件存在且互相引用。                                                                                                                                                                    |
| F414 | done   | 让“生成新项目”和“三步接入已有项目”最终创建同一种 READY Project、唯一 Staging/Production baseline 和首个配置修订。 | 单一 `ProjectGovernanceFinalizationService` owner；生成请求必须带持久 attempt key，数据库 claim 只选中一个唯一 attempt artifact，失败 sibling 不删除 winner；未改发布 UI。 | AC-PROJ-001～006；真实 MySQL 两条入口均为 READY、1+1 baseline、2 个 R1/current，响应丢失重放、异输入拒绝、artifact ownership、回滚/幂等/并发通过。                                                                                 |
| F415 | done   | 把仓库识别结果整理为项目类型、架构、部署方案、组件、路径、构建输出和运行方式的结构化可编辑合同。                  | Repository-analysis typed read/review model + race-safe immutable review snapshot；快照后 generic apply 无副作用 409，finalize 拒绝状态漂移；不改项目首页。                | AC-INTAKE-001～007；真实仓库分析可核对和调整，不显示原始 JSON；确定性 MySQL 竞态/无变更、Git/Browser 证据完成。                                                                                                                    |
| F416 | done   | 锁定 finalized 项目的 canonical repository identity，并拒绝构建来源与锁定身份漂移。                               | Repository connection/intake/build source policy；READY legacy 无可验证 current identity revision 时 fail closed。                                                         | AC-IDENTITY-001～005；独立修正已补齐 audit reason、冻结 BuildRun 展示、保守 migration 和全证据 hash inventory；二次复核后仅精确 v1/null 允许 legacy 展示回退，未知/畸形快照 fail closed；真实 MySQL/Git/runtime/Browser 通过。     |
| F417 | done   | 补齐项目目录的单一状态筛选语义、最近活动排序和 Demo 信息密度。                                                    | Project-directory query/presenter/Web list。                                                                                                                               | AC-DIR-001～010；后续修正要求 exact current version 与同 team/project/Production 的 active Site/domain 同时成立；真实 MySQL、hydration 首帧、认证 Browser Console 0、无交叉 evidence inventory 和 1484×1324 同 viewport 对照通过。 |
| F418 | done   | 纠偏项目首页 frozen intake 与跨 Tab 创建发布单主操作。                                                            | 单一 finalized-snapshot projector + always-mounted create modal owner；不进入 F419/F420+。                                                                                 | AC-HOME-001～005；active-child Tabs 两页签共享同一创建 owner，intake 只接受 exact succeeded finalization/result/review truth；两套真实 MySQL、认证 API/Browser、Console 0、no-create 计数及 1484×1324 截图通过。                   |
| F419 | done   | 实现发布单列表搜索、状态筛选、最近执行排序、构建/部署计数和最后执行步骤。                                         | ReleaseOrder list/read model/query/Web list；独立复核纠偏只收紧追加式关系，不进入 F420+。                                                                                  | AC-HOME-006～013；Deployment 验证 Manifest↔BuildRun 四维归属，ReleaseRun 验证 Manifest 四维归属与 frozen digest；fresh MySQL 对抗关系漂移、当前 tree 的逐截图 Browser raw evidence 绑定及 accepted/rejected inventory 通过。       |

### P1. Release Order State, Gates And Build

| ID   | Status | Atomic TODO                                                                                                           | Context Boundary                                                        | Acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | ------ | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F420 | done   | 建立 ReleaseOrder 追加式生命周期派生规则，分别表达草稿、构建、预发、待审批、生产中、生产成功、失败和撤回。            | 单一 list/detail projector + audited withdraw；不进入 F421 resumeStep。 | AC-ORDER-001～005；初审 P0=0/P1=2/P2=1 已按 active baseline、exact Production governance、canonical response 修正；7 suites/34 real-MySQL tests、16 accepted Browser captures、认证 create/withdraw/adversarial SHA inventories 通过，独立修正复审 P0/P1/P2=0/0/0。                                                                                                                                                                                                            |
| F421 | done   | 修复 `resumeStep`，按最远真实执行证据恢复到 preflight/build/staging/production。                                      | ReleaseOrder detail projection/route tests；不进入 F422。               | AC-ORDER-006～010；F420 valid lifecycle events 最大阶段 + strict presenter；fresh MySQL 11/11、认证 API、16 accepted Browser、duplicate-step P1 修正；final independent seal review P0/P1/P2/P3=0/0/0/0。                                                                                                                                                                                                                                                                      |
| F422 | done   | 将四个普通 Tab 改为连接步进条，并展示完成、当前、等待和阻断状态。                                                     | Release detail shell only。                                             | AC-ORDER-011～016；A→B stale-scope 与 same-scope list-after-POST 两个 P1 均已修正；9 files/61 tests、认证 Browser 10/10、同输入 Product Design QA、correction2 与 final independent reviews 全部通过，最终 P0/P1/P2/P3=0/0/0/0。                                                                                                                                                                                                                                               |
| F423 | done   | 把“构建最新代码”放到详情页主要位置，并按是否已冻结 Production 制品控制动作。                                          | Release detail header/action policy.                                    | AC-BUILD-001～004；单一共享 Build controller、服务端逐次解析默认分支、scoped `releaseRuns` 冻结事实；认证 Browser 证明双击 1 个空体 POST、冻结态 0 POST、1484/820 无溢出；9 files/64 tests 与最终源码门禁通过，独立复审 P0/P1/P2/P3=0/0/0/0。                                                                                                                                                                                                                                  |
| F424 | done   | 在前置检查首屏展示 15 个 MVP 能力组摘要，并保留 51 项完整目录下钻。                                                   | Gate catalog presenter/Web summary。                                    | AC-GATE-001～006；精确校验 15 组/51 项/39 MVP/12 Target、10-11-20-10、持久化六态与九项 Commit MVP 结论；首屏四类摘要、具名 51 项 Modal、逐项 Provider/原因/证据/时间完整；17 focused + 31 Web regression + 19 API tests、11 real-MySQL fixture、type/lint/i18n/build、1484/821/820 Browser 与同输入视觉对照通过；独立复审 8 files/33 tests、P0/P1/P2/P3=0/0/0/0；不进入 F425 服务端阻断。                                                                                      |
| F425 | done   | 将门禁结果接入 Build、Staging、Production 服务端阻断策略，区分技术阻断、业务证据、人工确认和 unavailable。            | Gate decision policy/API；不改视觉。                                    | AC-GATE-007～014；Build 9、Staging 5、Production 19 项 MVP 技术门禁由同一策略 fail closed，P03 仅作业务证据；精确输入/actor/决定快照追加持久化并在运行预留事务内单次 claim，人工确认仅作用于同输入的 canonical manual 门禁；Browser 证明页面与实际 Build 拒绝决定同为 8 项、同 inputHash，422 后 BuildRun 仍为 0；11 suites/51 API tests、7 suites/25 real-MySQL tests、3 files/18 Web tests、type/lint/format/Prisma/build/CodeGraph 通过，最终独立复审 P0/P1/P2/P3=0/0/0/0。 |
| F426 | done   | 为受控本地/隔离构建执行器建立明确的 runtime profile、存储卷、超时和并发配置，并在 V13 Docker 验收环境显式启用。       | Build executor/config/compose。                                         | AC-BUILD-005～010；`controlled-local-v1` 双开关与专用卷、整次运行/命令超时、取消、进程组、单进程并发 2、恢复与 CAS 终态已闭环；基础配置继续 fail closed。14 suites/44 focused、4 suites/11 real-MySQL、4 Docker runtime、1 authenticated HTTP composition、加固镜像启动与 CodeGraph 均通过；独立复审 P0/P1/P2/P3=0/0/0/0。                                                                                                                                                     |
| F427 | done   | 收敛环境无关制品合同，只打包声明的组件输出和 provenance，拒绝越界 symlink/特殊文件及把环境变量烘焙为伪同一 Manifest。 | Artifact packaging/build config。                                       | AC-BUILD-011～018；V4 输入快照、声明输出 create-once 聚合/组件 ZIP、内容索引与来源 provenance、结构化秘密/越界/symlink/特殊文件拒绝、稳定同输入 Digest 与 baked public env 区分已闭环；18 suites/67 focused、4 suites/33 repository、7 suites/19 real-MySQL、3 suites/9 hardened Docker、1 suite/2 authenticated HTTP 及静态/CodeGraph 全绿，独立纠错复审 P0/P1/P2/P3=0/0/0/0。                                                                                                |
| F428 | done   | 补齐构建记录的 revision、Commit、Manifest、结果、耗时和逐次日志抽屉 UI。                                              | Build step/read model/log drawer。                                      | AC-BUILD-019～025 全部通过；commit `92575cc2` 修复 scope 后六列四态（成功/失败/运行中/取消）表格、独立“日志”入口、右侧 Drawer（精确 BuildRun 元数据 + 脱敏日志）、`buildRunId` 深链刷新保留、非法 buildRunId fail closed、重复访问不覆盖日志/Manifest、390x844 无溢出均已 Browser 认证捕获；仅平台性 favicon.ico 404，0 unexpected console。证据 `/tmp/codex-tool-runs/svton/f428/f428-browser-evidence.json` 与 `f428/browser/f428-db-evidence.log`。                                                                                                                                                                                                                                                                                                       |
| F429 | done   | 为发布单详情聚合 BuildRun、Manifest、DeploymentRun、ReleaseRun 证据，移除项目一级重复的制品/部署主入口。              | Release detail read model/navigation。                                  | AC-ORDER-017～021；精确 team/project/order/Build/Manifest/approved Staging proof/ReleaseRun/Production DeploymentRun 聚合，重复 Staging/Production 历史只追加；专业 `view=deployments&runId` 深链与 legacy redirect 保留，项目一级仅保留发布单/环境版本；独立复审 P0/P1/P2/P3=0/0/0/0，fresh Browser 1484x1324、API 200/404、0 console error/warning 全部通过。                                                                                                                |
| F430 | done   | 统一发布相关状态和动作的中文语义，消除 raw `completed/upgrade/recovery` 与模糊词。                                    | Release/env-version messages and presenters。                           | AC-COPY-001～008；集中 presenter、稳定客户端错误键、精确 BuildRun/Manifest/DeploymentRun/ReleaseRun 与“预发（Staging）/生产（Production）”语义；3060 条 zh/en key/ICU parity、54 files/205 tests、生产构建、双语运行时切换与 7 张 Browser 截图通过；独立终审 P0/P1/P2/P3=0/0/0/0。                                                                                                                                                                                             |

### P2. Real Staging And Production Execution

| ID   | Status      | Atomic TODO                                                                                                        | Context Boundary                             | Acceptance                                                                                                                                                                                                                            |
| ---- | ----------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F431 | done        | 定义并实现 exact-Manifest Deployment Provider port，使 DeploymentRun 能把制品交给真实目标而非只解压目录。          | Deployment provider contract/adapter。       | AC-STG-001～006；默认关闭的 filesystem/SSH profile、服务端与远端双 Digest 校验、同 Manifest 两次真实 SSH 激活、无 checkout/pull/build、完整 scope/failed/unknown 负向矩阵及独立 READY 复审通过。                                      |
| F432 | done        | 在部署执行时解析并注入当前环境配置修订、普通变量、Secret 引用和资源连接信息，且不泄漏明文。                        | Deployment input snapshot/config injection。 | AC-STG-007～013；冻结安全输入哈希，受管引用仅在门禁后解密，环境/资源/目标作用域重校验，配置/Secret/资源/目标漂移在 Provider 前阻断；8 个 CodeGraph 受影响套件 33/33（真实 MySQL、真实 SSH、认证 HTTP）及独立 READY 0/0/0/0 终审通过。 |
| F433 | done        | 让 Staging 启动真实工作负载并执行服务级健康检查，失败必须保留日志且不生成成功环境版本。                            | Staging runtime/health adapter。             | AC-STG-014～020；exact-Manifest 前后端/worker/静态服务、托管进程/命令生命周期、目标机回环 HTTP/进程探针、失败诊断与 EnvironmentVersion CAS 边界闭环；真实 MySQL 10/10、SSH 2/2、认证 HTTP 1/1，独立 READY 0/0/0/0。                   |
| F434 | done        | 为每个 Staging DeploymentRun 增加独立日志/证据抽屉和“部署”动作，禁止重新构建。                                     | Staging Web only。                           | AC-STG-021～027；按冻结 Demo 登记当前制品/部署次数/生产前置摘要、逐次技术结果与业务验证区分、日志 Drawer、行级重复部署及完整五态+空态矩阵与 390 响应式；浏览器证明部署次数 3→4、构建数不变；v2 独立复审 READY，P0/P1/P2=0。                                     |
| F435 | done        | 把 Production 确认改成始终弹出的快照确认 Dialog，展示环境、版本、Build、Manifest、配置和策略。                     | Production confirmation Web。                | AC-PROD-001～009。实现完成、单测/类型/构建/i18n/格式化全部通过（focused 9 测试、全量 57 文件 222 测试、3107 消息 parity）；AC-PROD-001～003 已勾选，AC-PROD-004～009 已用 commit bfbfc805 的认证 CDP Browser 证据勾选（dialog 六字段/取消不建行/确认仅建 1 ReleaseRun+1 审批/390x844 无溢出），见 /tmp/codex-tool-runs/svton/f435/f435-browser-evidence.json。 |
| F436 | done        | 在项目上下文内完成生产审批申请、批准/拒绝、执行和状态回流，不要求用户跳到全局模块再手工返回。                      | Release approval bridge/project UI。         | AC-PROD-009～016。项目上下文审批卡片（批准/拒绝+必填理由驳回、审批人/时间/意见、执行生产发布+双提交保护）、全局模块 release 类别本地化+禁用全局执行（请在项目发布上下文执行）+`?id=` 深链聚焦；服务端不变量测试（拒绝/过期/已消费/输入漂移不可执行、并发收敛 1 个 DeploymentRun、review 路由 team_admin 403）；认证 1484x1324 浏览器证据（pending→批准→approved→执行接线→全局同态→深链聚焦）；执行点击命中真实服务端 422 门禁拒绝并回显到卡片，DeploymentRun 创建属于 F437 生产执行门禁（D06/D09 硬编码 unavailable），F436 未改动。focused API 23 测试、focused Web 4 文件 19 测试、类型/构建/i18n(3120)/diff 全通过。 |
| F437 | done        | 让 Production 使用同一 Deployment Provider 启动真实工作负载，并消费冻结快照而非读取漂移后的当前配置。              | Production executor/transaction。            | AC-PROD-017～024。execute 从冻结 ReleaseRun（configRevisionId+resource/route/policy snapshot）构建 deploymentInput+workload 并透传给共享 exact-Manifest executor/provider（复用 ReleaseDeploymentInputService.prepare 的冻结路径 + ReleaseProductionWorkloadService）；D06/D09 在 admit+finalize 显式 deferral（preflight 仍 unavailable，D17 保持 finalize 真实、D20 也 finalize deferral）；confirm 冻结 policySnapshot 的 releaseProtection 对标准合成策略默认 verified、真实策略行无标记时 fail closed；真实门禁+真实 provider 集成测试（新 spec，RUN_F437_PRODUCTION_REAL_GATE_INTEGRATION=1，真实 MySQL）证明成功路径（DeploymentRun/healthProbe/EnvironmentVersion/指针/审批消费/ReleaseRun succeeded）、D06/D09 双端 deferral+preflight unavailable、健康失败（日志保留、指针不动、ReleaseRun failed）。运行栈认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f437/f437-browser-evidence.json`：生产步骤 execute→success，ReleaseRun cmshqi5xu… succeeded、DeploymentRun cmshqj5jb… 经 ssh-v1 真实启动工作负载并 healthProbe passed、审批消费、EnvironmentVersion 前进、指针移动；commandPlan checkout/pull/build=false。focused API（real-gate 3 + production 相关 16）全过；API/Web type-check+build 过；i18n 3120 parity；diff check 两次干净。 |
| F438 | done        | 在生产部署成功后把站点/域名入口指向新 Production 运行目标，并对最终站点 URL 做真实 DNS/TLS/HTTP 探测，失败不得标记发布成功。 | Route activation/probe boundary。            | AC-PROD-025～031（AC-PROD-029 除外：演示环境无真实域名可服务，已诚实记录 blocker）。execute 成功路径新增路由激活 + 真实探测：匹配 Site（同 project+environment+domain）在 completion 同一事务写 Site.routeSwitch + append-only SiteRouteSwitchRun；路由声明域名但无匹配 Site → 拒绝/失败（fail closed）；`SiteFinalProbeService` 真实 `dns.promises.resolve`（记录+状态+时间戳）、真实 `tls.connect` `<domain>:443`（证书有效期或明确 unavailable）、真实 `http(s).get` 最终 URL（statusCode + body sha256 签名，最终主机不可达时诚实回退到 route proxyTarget 并记录实际探测 URL）；unavailable 显式诚实、绝不作假通过；HTTP 非 2xx/证书过期无效 → 完成前抛错不标记成功；`DeploymentRun.result.siteProbe`/`routeSwitch` 归属精确 DeploymentRun，Site 行同步 dns + tls.probe；D14 无新鲜真实 DNS 探测 → unavailable、D15 过期证书/invalid 探测 → blocked（admit/finalize 显式 deferral，preflight 能力真值不变）。真实 MySQL 集成（RUN_F438_SITE_ROUTE_ACTIVATION_INTEGRATION=1，3/3）：成功切换+探测证据、HTTP 500 硬失败（运行失败/指针不动/Site 不变/ReleaseRun failed）、站点缺失门禁拒绝；D14/D15 门禁 honesty 单测 8/8；presenter/site service 单测全过。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f438/f438-browser-evidence.json`：生产步骤展示绑定 DeploymentRun cmshsghrd… 的站点/DNS/TLS/HTTP 区块（切换已生效、DNS 已解析 198.18.11.9、TLS 不可用 ECONNRESET、HTTP 通过 200+签名，探测目标本地 http://127.0.0.1:23992/ 已如实记录）。focused API（新增 23）+ 既有 15 集成、Web 236 全量、API/Web type-check+build、i18n 3145 parity、diff check×2 全过（repository-analysis-run.service.spec.ts 为 HEAD 遗留失败，非本 worker 引入）。 |

| F439 | done        | 为 Production 创建独立恢复发布申请，允许从历史成功环境版本产生新的 approval、DeploymentRun 和 EnvironmentVersion。 | Recovery ReleaseRun/API/policy。             | AC-ENVVER-011～017；不复用已消费审批。recovery-confirm API（preview/confirm，POST `:envId/recovery/*`）从历史成功生产版本创建 recovery ReleaseRun（mode recovery、sourceReleaseRunId 链到历史版本的 releaseRun）+ 全新 OperationApproval（action project.release_order.deploy_production_recovery、category release、targetType release_run、risk high、inputHash 来自确认时刻当前配置快照、consumedAt null），幂等键并发收敛、漂移 409 强制重确认；Production 执行强制 run.mode=recovery（关闭非 recovery/已消费审批背书恢复的 latent path），sourceVersionId 缺失时由 run.sourceReleaseRunId→EnvironmentVersion 服务端推导；恢复 ReleaseRun 走既有生产执行路径（validateProduction+reservation+deploy+probe+complete），F437/F438 证据原样工作；证据 ownership 接受 recovery action。Web：Production 卡历史版本「回退」打开 Demo 对齐对话框（「回退 Production 环境版本」/呼出「回退会创建新的恢复部署」/「恢复到」历史成功版本默认最近/只读「当前环境版本」/「创建恢复部署」），确认后跳转到来源发布单生产步骤聚焦 recovery ReleaseRun，审批卡片以「生产恢复审批」+「回退」徽标呈现、批准后「执行生产恢复」；证据入口/回退徽标 i18n zh/en。测试：真实 MySQL 集成 5/5（创建 recovery run+新 approval 未消费→批准→执行→新 DeploymentRun+EnvironmentVersion kind recovery+指针移动+审批消费+ReleaseRun succeeded；已消费/非 recovery run 拒绝；配置漂移强制新确认；unknown/foreign/current source 拒绝；并发收敛 1 个 run）；既有 production 相关集成 12 全绿、API 全量 267 suites/1600 通过（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 失败）、Web 61 files/244 tests、API+Web type-check+build、i18n 3153 parity、diff check×2。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f439/f439-browser-evidence.json`（6 组截图/DOM/text + sha256，Console 0、failed 0、recovery preview/confirm/actions 201）：从历史成功版本 cmshsele4006… 打开回退对话框→创建恢复部署→生产步骤「生产恢复审批」批准→「执行生产恢复」→成功；DB 证明 ReleaseRun cmshul4c30…（mode recovery/succeeded/sourceReleaseRunId cmshsek25…）、approval cmshul4c50…（deploy_production_recovery/release/high/approved/consumedAt 18:27:00/新鲜 inputHash）、DeploymentRun cmshul852…（rollback/completed/DNS resolved/routeSwitch switched）、EnvironmentVersion cmshul9e70…（kind recovery、previousVersionId 指向恢复前 current、指针移动到它）。运行栈门禁证据 TTL（metrics 5min/connectivity 15min/route 1h）已刷新（沿用 F437/F438 的环境 fixture 维护方式）；第二轮生产恢复可端到端执行，AC-ENVVER-013 成立。Next → F440。 |
| F440 | done        | 补齐 Production ReleaseRun/DeploymentRun 的日志抽屉、审批证据和失败恢复动作。                                      | Production evidence Web。                    | AC-PROD-032～038；每次运行不可覆盖。Production step 重构为 Demo v9 结构（context strip 当前线上版本/正在交付的版本/待办事项/发布顺序 + 状态变体 stage-callout + stage-summary 当前线上版本/本发布单制品/生产前置条件 + manifest picker + 审批卡片 + release-run card strip + 六列部署表 部署运行|构建/Manifest|执行结果|验证结论|耗时/时间|操作，验证结论来自技术/业务证据，操作含 日志+专业深链+聚焦，无重部署）。新增 per-run `ReleaseProductionLogDrawer`（脱敏日志/结构化证据/失败诊断/技术+业务结论/站点探测），经 step-content+detail-panel 复用 `deploymentRunId` URL 深链刷新保留；证据 API 现在返回 deployment runs 的 `logs`+`result`（select+presenter+web type，无需新端点）。六态矩阵独立色调（等待审批 warning/批准 info/拒绝 danger/执行中 progress/成功 success/失败 danger）+ ReleaseRun card running pulse + 拒绝/失败 恢复入口（`?view=environment-versions`，复用 F439 恢复，无新恢复 API）。AC-PROD-036：门禁拒绝 stage token 本地化（releaseProductionGateDenied）、probe 错误只显示本地化 不可用+次要 code、绝不渲染 errorCode/errorMessage/deployment.error raw。AC-PROD-037：每状态恰好一个 data-primary（空闲→申请生产审批/等待审批 disabled+批准/已批准→执行/执行中 disabled/失败拒绝→前往环境版本管理恢复），测试断言恰好一个有效 primary。验证：focused API 11、focused Web 5 文件、Web 全量 62 文件 252 tests、API 全量 267 passed（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 失败）、API+Web type-check+build、i18n 3210 parity、diff check 干净。Next → F441。 |

### P3. Environment Versions And Project Governance

| ID   | Status  | Atomic TODO                                                                                                | Context Boundary                                 | Acceptance                                        |
| ---- | ------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------- |
| F441 | done        | 完善环境版本读模型，展示当前版本、来源发布单、Manifest、最近运行和完整变更历史。                           | EnvironmentVersion read API/Web cards。          | AC-ENVVER-001～006。环境版本页重构为 Demo v9 对齐读模型。Server：list 读路径用 exactCurrentEnvironmentVersion（completed/!dryRun/source=release_order/精确 scope）重校验 currentEnvironmentVersionId，不可证明则 fail closed 置 null、完整历史照常返回（`environment-version-read.utils.ts` + `currentEnvironmentVersionId` 包装，repository select 补充 teamId/projectId/currentEnvironmentVersion 全字段）；单测 8/8 fail-closed 矩阵 + 真实 MySQL 集成 5/5（per-version payload 字段/链/reverse-chrono/指针指向 failed run → current null）。Web：environment-versions-panel 重构为 Demo 卡（页头 环境版本+helper+共 N 个环境徽标；每环境卡=环境名+当前部署徽标/尚无可追溯、四事实 已部署版本/来源发布单 (releaseOrder.id·releaseVersion)/Artifact Manifest+B BuildRun #revision、最近发布时间 finishedAt??createdAt 本地化；动作 回退（outline，Production 走 F439 恢复对话框、Staging 直接执行，无上一版本时禁用）+升级版本 primary+生产审批提示；Production callout；环境变更记录表 caption 最近环境版本变更，列 环境|动作|版本变化(X→Y 链)|制品(short)|结果(成功/历史记录)|时间）保持 F439 recovery 接线与 production approval-required copy；summary 组件改为四事实布局；environmentVersionKindDeploy 值 部署→发布 对齐 Demo 动作列。i18n +23 键 zh/en parity 3231。验证：focused Web 7 tests、Web 全量 62 files/254 tests、API 全量 268 passed（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 失败）、API+Web type-check+build、i18n parity、diff check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f441/f441-browser-evidence.json`（f441/browser/ 6 artifacts + sha256）：Staging/Production 两卡当前部署徽标+四事实+回退/升级版本，变更记录表 6 列 9 行 7 条 `4.34.0 → 4.34.0` 链，current=API 重校验值（Staging cmshhefxd001vvwrcbdoot2kj / Production cmshul9e7009hny7b6remtdur），窄屏 390x844 无横向溢出，Console 0/failed 0。Next → F442（按环境候选过滤+默认推荐）。 |
| F442 | done        | 按环境过滤可升级/可回退候选，默认推荐最新合格制品或上一次成功版本。                                        | EnvironmentVersion candidate policy/Web select。 | AC-ENVVER-007～010；不接受文本版本/未知镜像。Server：`candidates()` 返回按环境拆分 `{ staging, production }`，production 用策略服务导出的 `hasVerifiedStagingProof`（artifactVerified + manifestId + manifestDigest 全匹配）过滤到仅 Staging 证明候选（deploymentRuns 子查询补 `result` + `dryRun=false`，source release_order/completed/baselineRole staging 保持）；staging 不按证明过滤；list 原样透传。Web：panel 按 `candidates[baselineRole]` 渲染每环境升级 select，Production 选项带「 · 预发部署成功」后缀，无候选时 disabled select + 本地化占位「暂无符合条件的候选版本」；回退统一走对话框——Staging 复用同一 EnvironmentRecoveryDialog 的 direct-execute 模式（Demo 对齐，确认后仍走原 F437 直执行路径），Production 保持 F439 recovery-run 流程；恢复对话框默认选项带「上一次成功」标签、callout「回退会创建新的恢复部署，默认推荐该环境上一次成功版本，不改写或删除当前部署历史。」，标题改为通用「回退环境版本」。i18n +3 键（candidateProductionSuffix/noCandidates/recoveryDefaultRecommend）zh/en parity 3234。验证：focused API 集成 8/8（含 cross-project/failed build/canceled order 排除 + staging-proof 过滤 + list 每环境数组）、release-order-lifecycle 集成 7/7、focused Web 16/16（每环境列表/production 后缀/空占位/默认=上一次成功/current 排除/无自由文本/Staging 对话框直执行）、Web 全量 62 files/262 tests、API 全量 268 passed（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 失败）、API+Web type-check+build、i18n parity、diff check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f442/f442-browser-evidence.json`（f442/browser/ 6 artifacts + sha256）：Staging 升级下拉无后缀、Production 下拉 `4.34.0 · BuildRun #1 · 预发部署成功`（staging-proven `cmshhebzv0017vwrc0p1iyl7p`）、Staging+Production 回退对话框默认选项带「上一次成功」+ callout、current 均被排除、无自由文本输入、Console 0/failed 0（空候选占位真实数据不可观察，由 focused Web 测试覆盖）。Next → F443。 |
| F443 | done   | 将项目设置改为独立页面和环境内子导航：部署目标、资源绑定、变量与密钥、域名与入口、保护规则。               | Settings IA/routes/layout。                      | AC-SET-001～009；不再用单个超长抽屉承载全部内容。settings 重构为三区左导航（项目识别/环境配置/发布规则），资源/Webhook/项目资料仅保留深链；环境配置区=env-switcher（staging/production）+四事实 env-summary（环境角色/部署目标/当前版本·运行状态标签/保护等级）+config-revision 条（当前生效配置 R{n}·date + 不可变身份 key·角色·发布顺序已锁定，诚实取自 config-revisions API）+五个子区（部署目标/资源绑定/变量与密钥/域名与入口/保护规则）承载原 460px 长抽屉全部内容（绑定服务器、资源计数/引用、环境变量/Secret 引用、路由快照/站点、策略引用/身份/写操作/复制/同步），共享草稿单点「创建配置修订」；深链 `?section=environments&env=<key>&envTab=<tab>` 刷新精确恢复；运行状态（部署次数）在 env-summary 以 运行状态 标签 + 查看环境版本/查看部署记录 跨引用链接呈现（?view=environment-versions / ?view=deployments 未动）；子区专业模块链接 /servers /resource-instances /keys /sites /operation-approvals 本地化文案。i18n +79 keys zh/en parity 3289。验证：focused Web 4 新 spec（settings 内容三区+深链、area switcher/四事实/修订条/子区/深链恢复/无抽屉、5 子区内容+模块链接、route-utils env/envTab 解析与隔离）42 tests + project/[id] 全量 53 files/251 tests + Web 全量 279 tests 全绿、Web type-check、web build ✓ Compiled successfully、i18n parity、git diff --check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f443/f443-browser-evidence.json`（f443/browser/ settings/production/variables/protection 4 组 + sha256，document 200，console 仅 favicon 404 噪音，failed 0）：settings 页显示三区+env-switcher+四事实+config-revision 条（staging R1·2026-08-06 20:17、production R1·2026-08-06 22:11）+五个子区，部署目标子区真实服务器 F432 HTTP SSH target（解绑/绑定服务器）+前往服务器模块，资源绑定子区引用编辑器+前往资源实例模块，变量与密钥子区环境变量/资源实例注入 F432 HTTP database/密钥变量 HTTP_DEPLOY_SECRET/Secret 引用，保护规则子区策略引用+环境身份与生命周期+环境操作+复制/同步+前往审批记录；四个深链组合刷新精确恢复；全部捕获 role=dialog=0（无长抽屉）；390x844 无横向溢出。运行栈说明：web dev server 需以 NEXT_PUBLIC_API_URL=http://localhost:3131 启动（API CORS_ORIGIN=http://localhost:3121）；F443 `next build` 覆盖 .next 后需重启 dev server。Next → F444。 |
| F444 | done    | 固化 Staging/Production baseline 身份和生命周期，阻止无保护归档、重复 baseline 或 finalized 后静默换 key。 | ProjectEnvironment CRUD/policy/UI。              | AC-SET-010～016；普通显示名/配置仍按修订维护。Server：`assertArchiveAllowed` 三守卫（基线角色/存在运行·绑定·版本/角色最后活动环境），PUT `status:archived` 与 DELETE 同守卫，DB `@@unique([projectId,baselineRole])` 兜底不动；re-finalize（governance-baseline upsert update 分支不再写 name/status）与 sync-from-project 种子不再复活归档环境；显示名/描述改为修订化身份——`EnvironmentConfigRevision.updateIdentity` 追加不可变修订（复制当前快照、CAS 不变、`displayName`/`displayDescription` 新列承载，migration 20260808080000 回填存量），同事务审计 `identity.update`（含 reason）/`key.update`/`archive`；`syncFromProject` 对 governed 项目跳过 dev/test 种子；key 锁定服务端规则=任意 DeploymentRun（含 failed）或 identityLockedAt。Web：governed 项目 settings 环境区隐藏 新建环境（`isGovernedEnvironmentSet`）；锁定指示按 `deploymentRunCount>0||identityLockedAt`（env-summary/保护规则身份行/治理区对齐）；保护规则编辑表单暴露 环境名称/环境描述/变更原因，基线环境无归档入口与 status 选项。i18n +3 键 zh/en parity 3292。验证：focused API 5 spec 57 tests（归档三守卫+PUT 旁路+failed-run key 锁+身份修订 CAS/审计+baseline 不复活+governed sync 跳过）、project-environment+project 全量 27 suites/162 tests、API 全量 269 suites/1629 passed（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 失败；release-build-artifact-cancellation 并行超时属 flaky，单跑通过）、focused Web 4 spec 21 tests、project/[id] 全量 55 files/261 tests、Web 全量 67 files/289 tests、API+Web type-check+build（web ✓ Compiled successfully）、i18n parity、git diff --check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f444/f444-browser-evidence.json`（f444/browser/ settings/protection/edit/narrow 4 组 + sha256）：settings 环境区仅 staging/production 两环境切换按钮、无 新建环境 入口（hasCreate=false）、config-revision 条 当前生效配置+不可变身份、创建配置修订条；保护规则子区 环境身份与生命周期+已锁定+环境操作、基线环境无 归档 按钮、无 archived 状态选项；编辑表单 环境名称/环境描述/变更原因 齐备（修订化保存面）；390x844 scrollWidth=390 无横向溢出。运行时 API/DB 证据：DELETE/PUT 归档 staging+production 均 400（基线环境不允许归档）、PUT key 400（key 已锁定）、sync-from-project 后仍仅 2 基线环境（无 dev/test）、f444-probe 自定义环境 PUT name/description/reason → 修订 R1 携带 displayName/displayDescription + 审计 identity.update(medium)/archive(high) 两行。运行栈说明：F444 `next build` 覆盖 .next 后需重启 dev server（与 F443 同）。Next → F445。 |
| F445 | done    | 实现按环境绑定/替换/复用部署目标服务器或 Provider target，并给出连通性校验。                               | Environment target binding。                     | AC-SET-017～024。Server：绑定写入加固——bindServer 接受并校验 providerKey（ssh-v1/local-filesystem-v1 白名单）/root（ssh-v1 必须 `isSafeReleaseDeploymentSshRoot`）/targetRef/共享范围（只接受同项目环境，越界 400），持久化前真实连通性检查（`ServerConnectionCapabilityService.verifyCapability`，不可达 409、ssh-v1 认证失败 409，诚实错误），role 枚举 `deployment`→`deploy` 归一化；FROZEN GUARD `assertNotFrozen`——任何 DeploymentRun 引用绑定（`params.deploymentInput.target.bindingId` 或该环境+服务器发布运行）后 bind(替换)/unbind 409；unbind 改为软归档（status:archived + 审计，listServers/listTargets 只返回 active，历史运行经 bindingId 可追溯）；新端点 `GET /project-environments/:id/targets` 用与部署路径同一解析（从 `selectReleaseDeploymentTarget` 提取 `matchReleaseDeploymentTargetBindings`，ssh-v1 安全 root）返回 provider-matched 当前目标（providerKey+targetRef+root+server+status+versionHash+共享范围）+全部活动绑定。门禁：D07 由 `serverBindings[0]` 改为 provider-matched（decisionTarget.providerKey 由 staging deploy 传入，缺省按唯一 provider 推断），offline→blocked server_not_online、无绑定/重复→unavailable fail-closed。Web：部署目标子区重构为 Demo 表格（组件/运行目标/区域·命名空间/规模/状态，运行目标显示真实 targetRef、规模无数据时诚实 不适用、状态取服务器行），当前生效 徽标标 matched 行，调整目标 打开可审计编辑面（provider/root/targetRef/共享范围勾选，保存走绑定 API+守卫，默认隔离提示），共享范围行显式展示；env-summary 部署目标事实显示 `ssh-v1 · stg-web` 当前目标摘要；helper 对齐 Demo `当前组件实际运行位置；环境身份固定，目标配置按修订留痕。`。i18n +25 键 zh/en parity 3317。验证：focused API 新增 2 spec（binding-service 13 tests：冻结 bind/unbind 拒绝、连通性失败、软归档+幂等、共享范围越界、provider 匹配 current-target 解析/多 provider 未解析、角色归一化；input-service duplicate 分支 1）+ 更新 service.spec/gate spec（D07 offline-blocked/missing fail-closed/duplicated 3 用例），project-environment+release-delivery 71 suites/371 tests、API 全量 272 suites/1650 passed（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 失败，与 F444 记录一致）、focused Web 4 spec 23 tests、project/[id] 全量 57 files/267 tests、Web 全量 69 files/295 tests、API+Web type-check+build（web ✓ Compiled successfully）、i18n parity、git diff --check×2 干净。日志 `/tmp/codex-tool-runs/svton/f445/`。Next → F446。             |
| F446 | done    | 实现按环境绑定资源实例和共享范围，区分基础设施生命周期与项目引用所有权。                                   | Resource binding/control-plane bridge。          | AC-SET-025～032。Server：PRODUCTION ANTI-SHARE RULE——resolver（`environment-config-reference-resolver.service.ts`）对 `baselineRole==='production'` 且 `sharedEnvironmentIds.length>1` 的引用 400（`EnvironmentConfigRevisionService.create` 环境 select 增加 baselineRole 并传入 scope）；新增 `environment-config-reference-resolver.service.spec.ts` 直测跨项目/跨团队 managed_resource+resource_instance 引用拒绝、共享环境集合越权、资源所属环境超出共享范围、共享范围缺当前环境、low-risk 共享拒绝、生产 anti-share（共享拒绝/专用放行/非生产共享放行）；revision service spec 新增 create 三用例（stale CAS → Conflict 且不落 revision/审计、anti-share 在写路径生效、baselineRole 传入 resolver scope）；release-delivery 新增 `release-deployment-input-snapshot.utils.spec.ts` 钉住 F432 冻结（id/kind/name/status/environmentId/sharedEnvironmentIds/versionHash、stateHash 覆盖引用、确定性排序、冻结后实例变更不影响既有快照）。Web：资源绑定子区重构——Demo 六列 资源需求/来源组件/绑定方式/资源实例/共享与隔离/校验（`EnvironmentResourceBindingTable`，来自当前不可变修订引用 + 实际实例行联接），每行校验 已验证/需重新绑定/共享范围外/违反生产隔离；绑定方式 select（绑定已有实例/换绑到其他实例/解除绑定/使用允许共享的实例）+ 共享与隔离 select（环境专用/仅非生产共享·逻辑隔离/Production 专用（强制），生产强制环境专用、use-shared 禁用）写回草稿（model 纯函数 `applyBindingMethod/applySharingMode/applySharedEnvironmentToggle/applyRebind`）走修订化保存；真实健康接线——托管资源行显示 ManagedResource.status/endpoint/lastSyncAt + `GET /resource-control/connection-runs` 最近探测（连接正常/连接失败/无连接探测/读取失败诚实 连接状态不可用），resource_instance 只显示生命周期状态；冻结修订徽标（当前生效 · 修订 R{n} · hash8）；两条 Demo callout；无任何创建/释放入口（AC-SET-027 边界保持）；i18n +29 键 zh/en parity 3346。验证：focused API 3 spec 21 tests（resolver 10 + revision create 4 + snapshot 5）+ project-environment+release-delivery 73 suites/388 tests、API+Web type-check 干净、API nest build + web next build ✓ Compiled successfully、focused Web 4 spec（model 12/table 8/editor 4/tabs 更新 2）+ project/[id] 全量 60 files/292 tests、i18n parity、git diff --check×2 干净。日志 `/tmp/codex-tool-runs/svton/f446/`。Next → F447。             |
| F447 | done    | 完善普通变量、Secret 引用、配置 revision/CAS、导入预览和跨环境复用交互。                                   | Environment config governance。                  | AC-SET-033～041；Secret 只保存引用。Server：`EnvironmentConfigRevision.changeSummary` 新列（migration 20260809100000，additive）承载 变更说明，`create`/`updateIdentity` 持久化（reason 即身份修订的 changeSummary）；新增 `POST /project-environments/:id/config-revisions/copy`（AC-SET-036）——多目标环境跨环境复用普通变量+Secret 引用，逐环境走与 `create` 完全相同的 append-only+CAS+同事务审计写路径（目标 CAS 读取后传入，stale 逐环境记录 冲突 不 abort 其余），仅限同项目环境（越界 400），结果逐环境 ok/error。Web：变量与密钥子区重构为 Demo 六列表 键/组件作用域/来源/环境值·引用/要求/校验（`EnvironmentEnvVarsTable`）——来源严格三分类 环境值/密钥引用/资源绑定生成，Secret 引用只渲染 `vault://name@id8 · ••••••••` 掩码，未进入当前修订的引用诚实标 待生效，当前生效 · 修订 R{n} 徽标，Demo 快照 callout；`.env` 导入分类（AC-SET-035）——`isSensitiveEnvKey` 启发式（`*_SECRET/*_PASSWORD/*_TOKEN/*_KEY/CREDENTIAL/API_*` 等）把疑似敏感 KEY 标 疑似敏感 · 建议密钥中心 并从 plainVars 提交中排除（预览列表+排除横幅），invalid/dup/conflict 行保持；跨环境复用弹窗（`EnvironmentEnvCopyDialog`，多选同项目环境+预览将复制内容+逐环境结果）；修订历史列表（`EnvironmentConfigRevisionHistory`：R/来源/时间/变更说明/创建人+当前徽标）与 env-summary 修订条补 来源/变更说明/创建人（AC-SET-039）；secret-ref 勾选/编辑器/导入/审查/暂存面保持。脱敏（AC-SET-040）：redaction spec 钉住 revision/审计/compat `config.envVars` 镜像永不带密钥值。i18n +42 键 zh/en parity 3388。验证：focused API 4 spec（service changeSummary+copy 9、redaction 4、更新 identity 1）+ project-environment 9 suites/100 tests、API 全量 269 suites/1678 passed（仅 HEAD 遗留 repository-analysis-run.service.spec.ts 7 failed，与 F444/F445/F446 记录一致）、focused Web 8 spec 45 tests、project/[id] 全量 66 files/328 tests、API+Web type-check 干净、API nest build + web next build ✓ Compiled successfully、i18n parity、git diff --check×2 干净。日志 `/tmp/codex-tool-runs/svton/f447/`。Next → F448。 |
| F448 | done    | 完善站点、域名、DNS、TLS、代理目标和流量入口的真实绑定/验证状态。                                          | Site/route/DNS/TLS project settings。            | AC-SET-042～050；配置状态与运行状态分离。Server：routeSnapshot 结构化 per-entry 模型 `entries[{domain,path,component,port,tlsMode}]`（`normalizeRouteSnapshot` 扩展 + `RouteEntry` 类型），校验 domain 必填/port 1-65535/tlsMode 枚举（非法 400），保留 domains[]/proxyTarget 平铺向后兼容（无 entries 时按域名派生逐行，component:port 形态的 proxyTarget 提取组件）；append-only CAS 与冻结快照语义未动（spec：per-entry create 持久化 + stale CAS Conflict 不落 revision/审计 + 历史不可变——两次 create 严格单调、历史行快照逐字保留、无 update/upsert/updateMany、对已取代修订 stale 写仍 Conflict；updateIdentity 冻结快照逐字复制）。Web：域名与入口子区重构为 Demo 六列表 域名/Path/目标组件/TLS/DNS/外部探测（`settings-env-routes.model.ts` 纯模型：entries↔Site 联接（primaryDomain/aliases）、DNS/TLS/外部探测诚实三态（resolved→DNS 已生效/failed→未生效/无证据→不可用；probe valid→托管证书·有效/invalid→无效/valid+未过期→证书有效（含到期）/过期→已过期）、外部探测取最新生产 DeploymentRun `result.siteProbe.http`（HTTP 200·时间）否则 Site.lastSyncAt 否则不可用、每入口行 D14/D15/D16 门禁就绪（就绪/阻断·原因/不可用，与 D14-D16 gate provider 相同 fail-closed 策略从 Site 探测数据推导，AC-SET-048））；当前生效 徽标 + 两条 Demo callout + 已绑定站点列表；添加入口 弹窗（`settings-env-entry-modal.tsx`：Host/Path/目标组件与端口 select web : 3000|api : 8080/TLS select 绑定托管证书|绑定已有证书资产，同域名替换写回草稿）经创建配置修订保存（AC-SET-043/046）；探测证据 深链 `?view=deployments&runId=`（AC-SET-049），落点 DeploymentRunDetails 复用 `ReleaseSiteProbeEvidence` 渲染结构化 DNS/TLS/HTTP 证据；`ProjectSite` 类型补齐 dns/tls/routeSwitch/lastSyncAt/syncError/aliases（API 本就返回）。i18n +57 键 zh/en parity 3445。验证：focused API 2 spec 30 tests（utils entries 6 + service per-entry/CAS/历史不可变 5 + 既有）、project-environment 9 suites/110 tests、release-delivery gate/route-activation 4 suites/21 tests 保持、focused Web 5 spec 28 tests、project/[id] 全量 68 files/345 tests、API+Web type-check 干净、API nest build + web next build ✓ Compiled successfully、i18n parity、git diff --check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f448/f448-browser-evidence.json`（f448/browser/ 8 artifacts + sha256，web 3121 重启于 F448 build 后、api 3131、devpilot-f434-mysql 3334 db svton，login n08uxlx08cj@f416-git.example）：Production 域名与入口子区六列表 + 行 `demo.f437.example / / / web : 3000 / 证书有效·于 2026-09-07 / DNS 已生效·于 2026-08-07 / HTTP 200·于 2026-08-07 + 探测证据`、当前生效 徽标、两条 Demo callout、已绑定站点；添加入口 弹窗（标题 添加 Production 入口、Host/Path/目标组件与端口 web : 3000|api : 8080、TLS 绑定托管证书|绑定已有证书资产、callout 本页不创建 DNS、证书或代理配置）；真实 UI 保存 R5→R6 追加（DB 修订行 routeSnapshot.entries 含 web:3000）；门禁行 `D14 就绪 / D15 不可用·TLS 探测未完成（fixture 真实握手 probe ECONNRESET，诚实）/ D16 就绪`；探测证据 深链跳转部署记录视图并渲染 站点/路由切换 DNS/TLS/HTTP 完整探测块；390x844 无横向溢出（scrollWidth=390）、Console 0/failed 0。Next → F449。 |
| F449 | done    | 将当前生效发布策略作为只读主视图，修改通过新修订流程；标准发布可执行，高级策略真实缺失时 fail closed。     | Release policy settings/capability。             | AC-POLICY-001～010。Server：per-env max-1-run 并发守卫（AC-POLICY-006）——新增 `release-run-concurrency.utils.ts`：confirm（`release-production.repository.ts` 与 `environment-version-recovery.repository.ts`）在事务内先锁发布单行、再锁目标环境行（FOR UPDATE）串行化，存在 `awaiting_approval|running` ReleaseRun 即 409 `生产环境已有进行中的发布运行（标准发布/恢复发布 · status），同一环境同时只允许一个运行；请等待其完成或先撤销后重试`；幂等重放优先——同 idempotencyKey 返回既有运行；standard↔recovery 互相阻断、运行解决后放行（集成测试钉住）。冻结诚实化：changeWindow/freezePolicy 保持 null，D13 门禁（M10 provider 读 releaseProtection）合成标准策略 → verified、真实修订无显式结论 → fail-closed（新 `release-gate-approval-capability.provider.spec.ts` 3 用例 + 集成 pin synthetic/real 快照）。Web：设置 发布规则 区重构为 Demo policySettingsV12——生效徽标 `policy-r{n} · 当前生效`（合成 系统默认）、快照哈希 mono（AC-POLICY-001）、生效条 标准发布 · 日期 · 由 {启用人} 启用、四事实（发布顺序/制品策略/生产保护/并发控制）、标准发布策略卡、生产发布门禁表（预发验证成功/配置就绪/人工审批/部署后验证 · 已启用+当前生效）、Demo callout（新修订只影响之后创建的运行…）、发布策略卡（标准可执行/金丝雀·蓝绿·自动放量 能力未就绪+缺失能力 mono 清单+只读提示，无选择器）、目标规则区块（变更窗口/冻结期 Provider 未接入保持空值，实际以 D13+冻结 CAS 执行，不假装生效）。i18n +33 键 zh/en parity 3477。验证：focused API（生产/恢复/执行策略 4 个 MySQL 集成 spec 26 tests——含 max-1-run 守卫、幂等重放、D13/freeze pin、CAS append-only 保持；release-delivery 全量 66 suites/302 tests 绿）+ focused Web 2 spec 10 tests（policy tab 6：徽标/哈希/事实/标准卡/门禁表/callout/策略卡/合成默认/保存态/错误态/无选择器；hook 4：CAS payload/合成省略 CAS/stale 错误）、project/[id] 全量 70 files/355 tests、API+Web type-check 干净、API nest build + web next build ✓ Compiled successfully、i18n parity、git diff --check 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f449/f449-browser-evidence.json`（f449/browser/ 8 artifacts + sha256，web 3121/api 3131 从 delivery-v13 worktree 重启、devpilot-f434-mysql 3334 db svton，login n08uxlx08cj@f416-git.example）：设置 发布规则 区先显示 系统默认 · 当前生效 + `default-standard-policy-v1` + 四事实 + 标准卡 + 四行门禁表（已启用 当前生效）+ callout + 三张高级策略卡（能力未就绪 + 缺少能力清单 + 只读提示）+ 目标规则诚实区块；真实 UI 点击 保存标准策略修订 → 徽标变 `policy-r1 · 当前生效` + 生效条 `标准发布 · 2026-08-07 17:12 · 由 F434 Browser Reviewer 启用` + 快照哈希 `986e99904ce3…`（DB `ReleasePolicyRevision` R1 行一致、项目指针更新）；390x844 scrollWidth=390 无横向溢出；Console 0 / failed 0。Next → F450。 |

### P4. Visual, Interaction And Accessibility Parity

| ID   | Status  | Atomic TODO                                                                                                                     | Context Boundary                     | Acceptance                             |
| ---- | ------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | -------------------------------------- |
| F450 | done    | 统一项目目录、接入、交付首页、发布详情、环境版本和设置页的 V13 spacing、density、typography、radius、border、color 和主次动作。 | Project-delivery visual layer only。 | AC-UI-001～010；1484×1324 同状态对照。目录 /projects（AC-UI-001）：header 双入口保持但收敛为单主次（主=接入已有项目 primary 置首，次=生成新项目 outline；Demo:736 单 primary 创建项目 vs 规范 §3.4 repo-first 双来源，决策=接入已有项目 primary，选择已记录 acceptance），行加 project-avatar（Demo:732 38px 品牌色圆角方块+图标）、DirectoryCell 全列改 cell-label/cell-main/cell-sub（label 桌面也可见，F417 五列头/空态语义与测试未动）、directory-summary 压缩为 Demo 紧凑形式（px-4 py-3 text-lg vs 原 px-5 py-4 text-2xl）；hydration 修复：use-projects `validating` 以 mounted 门控，消除 aria-busy 首帧 SSR/client 不匹配（/projects Console 0）。三步接入（AC-UI-002）：/projects/create 对齐 Demo 密度（max-w-3xl、表单 p-5、错误区 role=alert 内联、footer 压缩），stepper 改 Demo 式图标（check/circle 状态+步骤 01）、connect 步骤加 small helpers（intakeNameHelper/intakeBranchHelper/intakeRepositoryHelper），3 步语义与 F415/F416 immutable-snapshot 契约未动；删除经 rg 验证完全无引用的 5 步遗留 import/components+hooks+types+utils（保留 redirect stub page.tsx），review/finalize 冻结逻辑未动。交付首页（AC-UI-003）：主会话裁决保持——weak summary 留在首页（项目形态/环境就绪/资源绑定/Site 入口），context strip 仍在生产步骤（F440），仅验证密度。发布单列表（AC-UI-004）：列头与 Demo:653 一致，8 项粒度状态筛选保留（规范 F419 允许），labels 均为用户可理解中文（草稿/构建中/预发（Staging）发布/等待审批/生产（Production）发布中/已发布/失败/已撤回），行密度已验证。详情壳（AC-UI-005）四步 stepper 真实数据；构建/预发/生产步骤壳验证密度+空态换共享组件。环境版本（AC-UI-009）与设置页（AC-UI-010）F440-F449 已对齐仅复验。i18n +7 键 zh/en parity 3484。验证：focused Web 10 files/28 tests + projects 全量 77 files/374 tests 绿、Web type-check 干净、web next build ✓ Compiled successfully、i18n parity、git diff --check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f450/f450-f451-browser-evidence.json`（f450/browser/ 21 artifacts + sha256，web 3121 重启于 F450 build 后、api 3131、devpilot-f434-mysql 3334 db svton，login n08uxlx08cj@f416-git.example）：目录 接入已有项目 primary 置首+avatar 行+紧凑摘要+五列头；/projects/create 三步 stepper+helper+表单栅格；交付首页 weak summary（项目形态/环境就绪/资源绑定/Site 入口）+发布单/环境版本 tabs+创建发布单 primary+无 context strip；发布单列表 8 项筛选+2 行真实数据（4.34.0 生产发布中 / 4.34.0-empty 草稿）；详情壳四步 stepper（前置检查已完成/构建制品已完成/预发已完成/生产发布中）+context strip+审批卡真实数据；build step 六列构建记录表（含 f428-seed-canceled/running/failed 真实行）；staging step Manifest select+五列部署记录表；全部页面 Console 0/failed 0。Next → F452/F453（与主会话定序，F452 accessibility 优先）。 |
| F451 | done    | 补齐所有空态、阻断、运行中、成功、失败、审批和能力未就绪状态，并使用真实 fixture 而非页面硬编码。                               | Web state components/fixtures。      | AC-UI-011～018。新共享状态组件 `apps/devpilot-web/src/components/ui/`：EmptyState（title/description/action+dashed 变体，Demo `.empty` 风格）与 BlockedState（reason+recovery action，danger callout，role=alert）经 index.ts 统一导出。应用：三个步骤壳（build/staging/production）内联虚线空态全部替换为共享 EmptyState（AC-UI-011/012），build 空态带明确下一动作 构建最新代码（复用 header 同 hook，无新逻辑）；发布单列表空态增加 创建发布单 动作（onCreate 可选 prop 由 ProjectDeliveryRoute→Content→Panel 传递，打开既有 ReleaseOrderCreateModal，未筛选+有回调时才渲染，spec 保持绿）（AC-UI-011）；失败发布单行 failureLabelKey 由裸文本改共享 BlockedState（原因=服务端 failureKind 本地化文案，恢复=同行 查看发布单，AC-UI-013）；gate panel loading 换共享 LoadingState（AC-UI-012）；运行中状态由 StatusTag progress 呼吸点+disabled 提交按钮表达（AC-UI-014，无需新 RunningIndicator——StatusTag pulse 已覆盖）；成功/失败/审批状态继续由真实 hooks/SWR+StatusTag 六态渲染（AC-UI-015/016/017）；遗留 release-stage-actions.component.tsx 硬编码 zh（重试/跳过（可选）/重新请求审批/重新生成待审批）i18n 化（releaseStageAction* 4 键，?view=deployments 深链仍可见）。全部状态来自真实 fixture 数据（F434 种子 f428-seed-canceled/running/failed build、DeploymentRun/ReleaseRun 记录、审批卡），无页面硬编码。i18n zh/en parity 3484。验证：focused Web 10 files/28 tests + projects 全量 77 files/374 tests 绿、Web type-check、web build ✓、i18n parity、git diff --check×2 干净。认证 1484x1324 浏览器证据 `/tmp/codex-tool-runs/svton/f450/f450-f451-browser-evidence.json`（f450/browser/ 21 artifacts + sha256）：发布单列表 2 行真实状态（生产发布中/草稿）、详情壳四步 stepper 各状态徽标+审批卡+失败恢复入口、build step 六列表含已取消/失败/运行行、staging 五列部署记录、Console 0/failed 0。Next → F452/F453（与主会话定序）。 |
| F452 | pending | 完成键盘、焦点、ARIA、Dialog/Drawer、错误提示、对比度和中英文可访问名称验收。                                                   | Accessibility only。                 | AC-A11Y-001～012。                     |
| F453 | done    | 完成窄屏、常规桌面和 1484×1324 参考 viewport 的响应式与无横向溢出验收。                                                         | Responsive/layout tests。            | AC-UI-019～025。focused Web spec `f453-responsive.spec.tsx`（7 tests：release-order-list-row 最近执行/环境发布行 truncate+title 完整值访问、staging Manifest select shortId 选项+select title 保留完整值、Modal `max-w-[calc(100vw-32px)]`+内部 overflow-auto、Drawer `min(760px,100vw)`+内部滚动、目录五列头 lg-only、stepper `max-[820px]:flex-col` 堆叠、build/staging/production/create-finalize 表格 `overflow-x-auto` 包裹）。源码改动：release-order-list-row.tsx 最近执行/部署文本 truncate+title；release-order-staging-step.tsx select 选项 shortId（>16 字符截 12+…）+select title 完整值（原生 select 保留，F434 候选源 spec 未动）。验证：focused 7 tests + 相关 7 files/35 tests + Web 全量 84 files/405 tests 绿、Web type-check、web next build ✓ Compiled successfully、i18n parity 3485、git diff --check×2 干净。认证浏览器证据 `/tmp/codex-tool-runs/svton/f453/f453-browser-evidence.json`（f453/browser/ 32 artifacts + sha256，web 3121/api 3131/devpilot-f434-mysql 3334，login n08uxlx08cj@f416-git.example）：1484x1324/1280x800/390x844 三 viewport × 10 页面 = 30/30 断言 `documentElement.scrollWidth==clientWidth`（表格容器内滚动除外，逐容器记录）；390x844 build 日志 Drawer 与门禁目录 Dialog 全宽打开、页面无溢出、内容内部滚动；parity 页面 Console 0 错误（登录后 /dashboard 一次性 5 个 dashboard-widget 403 已注明非 parity）。Next → F454。 |

### P5. Runtime, E2E And Final Closure

| ID   | Status  | Atomic TODO                                                                                                                               | Context Boundary                      | Acceptance                                         |
| ---- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------------------------------------------------- |
| F454 | done    | 建立 project/network/volume 命名空间隔离的 V13 parity Docker stack、真实示例仓库、目标工作负载、域名和 allowlist 可重复 seed/reset 工具。 | Test infrastructure only。            | AC-E2E-001～006；不污染开发/生产数据。             |
| F455 | done    | 跑通“接入仓库→识别→配置→发布单→构建→Staging→Production→浏览器访问”正向 E2E。                                                              | Cross-layer positive E2E。            | AC-E2E-007～015；Browser/API/DB/log 四类证据一致。 |
| F456 | done    | 跑通同发布单多次构建、同 Manifest 多次预发、环境升级和 Staging/Production 回退 E2E。                                                      | Version/history E2E。                 | AC-E2E-016～023。驱动 `scripts/parity-version-history-e2e.mjs`（19 steps 全 OK；idempotent reset+seed + F455 正向链复用为基态，原 F455 证据先存档 `f456/f455-positive-e2e-evidence.before-rerun.json`）：第二次构建 B2 `cmsjc7367008zfq99wqcbby09`→新 M2 `cmsjc738z0092fq99s3jm3vez`（digest 确定性）；同一 M1 两次 Staging 部署 D2 `cmsjc73bk00apfq995eqndlko`（构建数不变 2）；Staging upgrade→EV `cmsjc73eg00ayfq99qks5b9wk`、recovery→EV `cmsjc73fh00b4fq99mvq75api`；Production upgrade（ReleaseRun `cmsjc73gh00b7fq992duj6skw`+审批 `cmsjc73gj00b9fq99y9x00a33`→execute→EV `cmsjc73sc00effq99r5ej2v4m`）与 recovery（恢复 ReleaseRun `cmsjc73tr00ekfq994pye13i1`+审批 `cmsjc73ts00emfq99c12xkhlo`→execute→EV `cmsjc744h00hsfq99eba317db`）均成功且指针移动；Staging `deploy→deploy→upgrade→recovery` + Production `deploy→deploy→upgrade→upgrade→recovery` previousVersionId 链全对、current 指针==链末、API/DB 一致；浏览器（CDP 1484x1324 认证）发布单详情 2 BuildRun/2 Manifest/Staging 4 次部署/Production ReleaseRun 3 个（upgrade+recovery 回退审批卡），`?view=environment-versions` 变更记录 发布/升级/回退 链，build/staging/production 日志抽屉与环境版本变更记录全开。零代码改动（F439/F441/F442 语义直接成立；仅记录 lifecycle 读模型对 recovery 审批 action 的展示差异，非阻塞）。验证：链 exit 0、每 AC 映射证据、无代码改动故无 focused test/type-check/build、`git diff --check`×2 干净；日志与证据 `/tmp/codex-tool-runs/svton/f456/`（f456-version-history-evidence.json + browser/ sha256 清单）。Next → F457。 |
| F457 | done    | 跑通权限、重复、异幂等键并发、配置漂移、门禁失败、Provider 关闭、Digest 错误、审批拒绝、探测失败和全证据凭据扫描负向 E2E。                | Negative/security E2E。               | AC-E2E-024～035。驱动 `scripts/parity-negative-e2e.mjs`（45 steps 全 OK，exit 0）：无仓库连接项目 build 422 RELEASE_GATE_BLOCKED（C01 repository_not_connected，0 BuildRun，决策持久化）；连接 failed → 必需门禁失败 422 且决策持久化（C01 repository_verification_failed）；Provider 关闭 → release-policy 能力列表 unavailable + preview/confirm 422 release_strategy_capabilities_unavailable；跨项目/跨发布单 Manifest staging 404；Digest 篡改 staging 422；配置漂移旧确认 execute 422；审批 rejected/expired/consumed execute 422（三次均无 DeploymentRun）；并发 confirm 同/异幂等键 → 各仅 1 个 ReleaseRun（409 max-1-run），并发 execute → 仅 1 个 DeploymentRun（409/422 loser）；健康检查失败 → DeploymentRun failed WORKLOAD_HEALTH_FAILED、ReleaseRun failed、指针未移动；DNS/TLS/HTTP 探测失败（404）→ 不标记最终成功、0 SiteRouteSwitchRun、指针未移动；MEMBER/跨团队 403（读 200、写全部 403）；全链 12 类证据凭据扫描 0 意外敏感命中（仅 compose 声明 + 0600 runtime.env 设计内交付）。修复真实缺口 2 处（最小+spec）：`packages/nestjs-http` mapPrismaError 在 string meta.target 的 P2002 上抛 TypeError 杀死整个 API 进程（并发 execute 复现 2 次）→ 防御性分支 + `prisma-error.util.spec.ts` 4 tests（package 2 suites/7 tests 绿，fix dist 热拷贝进运行中的 parity-api）；D05/D07/D08/D18 fixture 证据 TTL 过期 → driver 在每次生产 execute 前刷新时间戳（语义不变，门禁真实 fail-closed 先被观测）。验证：driver exit 0、每 AC 映射拒绝+DB 状态、API type-check 干净（api/web 源零改动）、`git diff --check`×2 干净；日志/证据 `/tmp/codex-tool-runs/svton/f457/`（f457-negative-e2e-evidence.json + db.dump.sql/api-container.log/web-container.log + 崩溃复现 repro 系列）。Next → F458。 |
| F458 | done    | 逐页执行 V13 视觉回归，保存 Demo/实际同 viewport 对照、DOM、交互录屏或步骤证据并关闭所有结构差异。                                        | Browser visual acceptance only。      | AC-VIS-001～012。Demo（frozen delivery-versions-v9.html，本机 8642 端口，hash/state JS API 驱动 17 个状态）⇄ parity web（http://localhost:4131，parity-project-0001/parity-order-0001）每页 1484x1324 截图+DOM+innerText：项目目录 ALIGNED（双入口 canonical §4.2 批准）、接入三步 MINOR（step1 字段走 spec §4.2 填写路径；step2/3 真实 contract+快照 SHA，已记录）、发布单列表 ALIGNED（粒度状态筛选 F419 + §4.3 弱摘要 canonical 批准）、前置检查 ALIGNED（结构一致，fixture 门禁状态为数据差异）、构建 ALIGNED（修复 1 处：步骤标题补 `main · 自动取最新 Commit` pill，release-order-build-step.tsx + zh/en messages）、Staging MINOR（验证列更丰富）、Production ALIGNED（context strip F440 + 密集证据表 + 审批卡 canonical 批准）、环境版本 MINOR（目标 Manifest 选择器）、项目识别 MINOR（repository-identity 模型；组件表为数据缺失）、环境五子区 MINOR（操作列/修订历史/保护空态为数据驱动）、发布规则 ALIGNED（能力卡 F449）。34 张截图 luminance/唯一性/页面标记 0 问题（shot-verification.json）；修复后 web 重建（NEXT_PUBLIC_API_URL=4132 host build → parity-web 镜像）并重截图验证；scratch step3 草稿项目+容器内克隆目录已清理还原 fixture。验证：focused vitest 14/14、web tsc --noEmit 干净、next build 成功、git diff --check×2 干净。证据/日志 `/tmp/codex-tool-runs/svton/f458/`（f458-visual-regression-evidence.json + demo/web 17 页截图+DOM+text + notes/f458-comparison-notes.md + shot-verification.json）。Next → F459。 |
| F459 | done    | 独立完成产品、UX、无障碍、安全、领域一致性和数据泄漏对抗审查，并修复有效发现。                                                            | Independent audit/review。            | AC-REVIEW-001～008；审查人与实现 worker 分离；恢复发布后生产证据不匹配显示缺陷已修复（approval action 按 run.mode 匹配），F455/F456 全链 rerun 无异常，axe 0/0。 |
| F460 | pending | 同步用户指南、迁移说明、TODO/progress/roadmap/board，运行全量 gate 并形成最终 parity verdict。                                            | Docs/full verification/final report。 | 所有 AC 均 pass；不存在未解释 partial/missing。    |

### Registered Post-Parity Hardening

| ID   | Status  | Atomic TODO                                                                                                                                                                                                              | Context Boundary                                                                            | Acceptance                                                                                                                             |
| ---- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| F461 | pending | 建立 durable repository-analysis execution：原子 DB claim/lease（owner/attempt/expiry）、heartbeat、stale takeover、CAS terminalization、exactly-one suggestion snapshot，以及 restart/multi-replica recovery。          | Repository-analysis execution ownership only；排在既定 parity slices 之后，不在 F415 实现。 | 真实 MySQL 双 worker、cancel/timeout/retry/stale-writer 证明。                                                                         |
| F462 | pending | 建立 repository Git egress/SSRF policy：批准 provider/self-hosted allowlist、DNS/IP resolution and pinning、拦截 loopback/link-local/private/metadata/redirect/rebinding，并规范 SSH/scp URL。                           | Repository Git network boundary only；排在既定 parity slices 之后，不在 F415 实现。         | 真实负向网络与 URL 规范化测试。                                                                                                        |
| F463 | pending | 建立 legacy repository identity inventory 和 collision-safe remediation：先 dry-run 报告 canonical/alias/provider/current-revision 冲突，再以可审计、幂等、可恢复事务创建 identity + initial revision 并逐项 reconcile。 | 历史 READY/connected 项目迁移 only；F416 只 fail closed，不推断或自动修复 legacy 身份。     | fresh/upgrade MySQL inventory、collision stop、idempotent apply/replay、partial-failure recovery、audit 和 post-migration build 证明。 |

## Worker Granularity Contract

- 一个 worker 只领取一个 Fxxx；若 Fxxx 仍需要跨越两个独立上下文，先由主会话拆成 `Fxxx.a/Fxxx.b` 再启动。
- Worker prompt 必须写明：允许路径、禁止路径、依赖、验收 AC、验证命令、结果文件、停止条件。
- Worker 不读取旧会话，不递归创建后继 worker，不修改自己范围外的 TODO 状态。
- 同 checkout 任意时刻只允许一个 active write worker；只读产品/领域/测试审查最多可并行三个。
- 每个 Fxxx 的顺序固定为：标记 `in_progress` → 改动 → 聚焦验证 → 浏览器/运行时证据（如适用）→ 记录 evidence → 标记 `done` → 原子提交。

## Verification Plan

- 每个 Fxxx：聚焦测试、相关 type-check/build、`git diff --check`、变更文件职责检查；高噪声输出隔离到 `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/`。
- 跨层行为：不得以 unit/type-check 替代 Docker+API+DB+Browser 证据。
- 视觉：同 viewport、同业务状态、同数据语义比较 Demo 与实际实现；先确认页面正确再接受截图。
- 真实发布：必须证明工作负载启动、配置注入、路由生效、HTTP/TLS 探测和浏览器访问；仅物化 ZIP 不算部署成功。
- 安全：Secret/token/credential 不得进入日志、快照明文、页面或证据包。

## Change Log

- 2026-08-04: F413 完成；根据 V13 Demo/canonical spec 与当前 worktree/API/DB/Docker/browser 审计，建立严格 1:1 parity 后继计划和验收清单。三路独立只读审查确认无需新增 F-ID，并细化越界 symlink、隔离 reset、异幂等键并发和全证据凭据扫描验收。F386-F412 保留为历史完成范围，不再作为最终 parity verdict。
