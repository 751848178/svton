# Devpilot V13 Project Delivery Progress

## Current State

- Program state: the historical F386-F412 implementation/evidence scope is complete, but strict V13 Demo 1:1 functional, runtime and UI parity is active as F413-F460. Use `../project-delivery-v13-demo-parity-acceptance.md` and `../../todos/2026-08-04-devpilot-v13-demo-parity.md` for the current verdict; do not interpret this historical completion as final parity acceptance. External push, PR, merge and production rollout remain out of scope.
- Branch: `codex/devpilot-project-delivery-v13`.
- Worktree: `/Users/zhaoxingbo/Workspace/ai-driven/svton-devpilot-project-delivery-v13`.
- Base: `b6c3488743be13eacf4320f685da927488490113`.
- Integrated fixes: `17652567` then `ef1a47cb` (local cherry-pick commits `7d9d580c`, `724abfef`).
- Original checkout protection checkpoint: `master@b6c3488`, 131 status entries, SHA-256 `1024cd00f74bfe32689bff198cdef74942ccff7b26d30f7d28a2609c52d0ab16`.

## Source-Backed Baseline

- Existing Project create and default-environment seed are not one transaction; defaults are `dev/test/staging/prod`.
- Repository analysis has real connect/run/review/apply evidence and start idempotency, but no canonical repository uniqueness or idempotent finalize record.
- Existing ReleasePlan/Stage/Attempt/Event, approvals, CAS and leases are reusable execution foundations.
- Release orchestration defaults off; live SSH defaults off; deployment defaults dry-run.
- ReleaseOrder, releaseVersion, BuildRun, ArtifactManifest, ReleaseRun, EnvironmentVersion and 51/15 catalog are absent from the authorized base.
- Attempt `artifacts[]` is JSON evidence, not an immutable Manifest; normal live deployment may pull a branch instead of enforcing stored Commit.
- Web project detail has eight peer tabs; `/projects/import` is five-step and explicitly unverified; release capability lookup fails open.

## Architecture Contract

- Additive schema only until dual-read/write parity; no physical rename or history deletion.
- Project intake owns lifecycle/identity/finalize; repository analysis remains evidence provider.
- Build owns immutable BuildRun/Manifest and never depends on deployment.
- Deployment consumes exact Manifest plus frozen environment revision and cannot build/pull.
- Successful DeploymentRun produces append-only EnvironmentVersion; recovery creates a new run/version.
- Production proves same-project/same-Manifest Staging success, freezes snapshots, then awaits approval before DeploymentRun.
- Unknown/expired Provider evidence is unchecked/unavailable, never passed; MVP executes standard release only.

## Slice Status

| Range | State | Outcome                                                                                                                               |
| ----- | ----- | ------------------------------------------------------------------------------------------------------------------------------------- |
| F386  | done  | Isolated baseline, TODO/progress/migration contract and baseline verification complete.                                               |
| F387  | done  | Additive intake/identity/finalization/config revision schema and preflight reports complete.                                          |
| F388  | done  | Transactional intake API, duplicate guard, idempotent finalize and recovery complete.                                                 |
| F389  | done  | ACL-filtered directory read model, search/filter/baseline/Production/activity summary complete.                                       |
| F390  | done  | Product/API/MySQL acceptance passed; its three named browser screenshots are a preserved historical evidence gap.                     |
| F391  | done  | Delivery/settings hosts, two-item primary IA and legacy deep-link adapters verified.                                                  |
| F392  | done  | Additive delivery schema and conservative legacy/unverified migration report verified.                                                |
| F393  | done  | ACL/idempotent ReleaseOrder API and default create/list Web verified with zero implicit build.                                        |
| F394  | done  | Server-resolved exact-Commit builds, isolated evidence and success-only immutable Manifests.                                          |
| F395  | done  | Four-step accessible detail, stable refresh/deep-link recovery and isolated BuildRun logs.                                            |
| F396  | done  | Repeatable exact-Manifest Staging DeploymentRuns with no Git, checkout or implicit build.                                             |
| F397  | done  | Production same-Manifest proof, frozen snapshots, approval and concurrent idempotency.                                                |
| F398  | done  | Append-only environment versions, controlled upgrade and recovery rollback.                                                           |
| F399  | done  | Manage Project consolidation and ordinary/professional route reachability.                                                            |
| F400  | done  | Audited immutable environment config and reference governance with locked identity.                                                   |
| F401  | done  | Versioned 51/15 catalog passed API/tests; its referenced browser screenshot is a preserved historical gap.                            |
| F402  | done  | Freshness-aware M01-M05 Commit/Build provider adapters and immutable evidence drill-down.                                             |
| F403  | done  | Environment-scoped M06-M09 Deploy providers with redaction and expiry boundaries.                                                     |
| F404  | done  | M10-M15 Promote adapters with technical/manual separation and fail-closed traffic capability.                                         |
| F405  | done  | Immutable standard release policy and explicit advanced-strategy capability gates.                                                    |
| F406  | done  | Read-only legacy compatibility, non-destructive archive and governed-path write closure.                                              |
| F407  | done  | Bilingual runtime, terminology parity and user/migration documentation verified.                                                      |
| F408  | done  | One coherent Docker/MySQL/Redis/browser delivery lifecycle passed end to end.                                                         |
| F409  | done  | Cross-layer ACL, concurrency, failure, compatibility, provider and redaction negatives passed.                                        |
| F410  | done  | Independent review, persisted gate facts, security hardening and final delivery audit passed.                                         |
| F411  | done  | Over-limit tests split, API lint restored, historical board records repaired and PR gates rerun.                                      |
| F412  | done  | Current intake and 51-gate behavior re-proved with Browser+DOM and API+MySQL successor evidence; board/docs repaired.                 |
| F418  | done  | Project home correction now freezes intake to exact succeeded finalization evidence, and keeps one cross-tab create owner.            |
| F419  | done  | Release-order list now uses an ACL-scoped server read model, bounded rows and scope-safe Web cache/refetch behavior.                  |
| F420  | done  | Release orders expose one eight-state lifecycle projector plus audited, idempotent withdrawal boundaries; correction rereview passed. |
| F421  | done  | Server-owned furthest valid evidence drives resumeStep; final independent seal review passed P0/P1/P2/P3=0/0/0/0.                     |
| F436  | done  | In-project production approval: project-context approve/reject + execute after approval, preserved global module with release labels + `?id=` deep link, and server-enforced approval/execution invariants. |
| F437  | done  | Production real execution with frozen snapshot: execute consumes the frozen ReleaseRun config revision + resource/route/policy snapshots to prepare deploymentInput+workload and runs the shared exact-Manifest provider, D06/D09 deferred (explicit reasons) at admit+finalize with preflight still unavailable, D13 releaseProtection default-verified for standard synthetic policy, and real-gate+real-provider success/health-failure proven by focused MySQL integration tests plus authenticated 1484x1324 browser execute→success evidence. |
| F438  | done  | Production route activation + real site probes: on production success the matching Site is pointed at the new DeploymentRun target (Site.routeSwitch + append-only SiteRouteSwitchRun in the same completion transaction), real DNS/TLS/HTTP probes of the final site URL run before completion and are bound to the exact DeploymentRun (siteProbe/routeSwitch blocks), probes/switch fail closed (unavailable is explicit, never a false pass), D14/D15 gates are data-honest, and the production Web step renders the site/DNS/TLS/HTTP evidence section. |
| F439  | done  | Production recovery release: recovery-confirm API creates a NEW recovery ReleaseRun (mode `recovery`, `sourceReleaseRunId` chained to the historical version's run) + a NEW OperationApproval (action `project.release_order.deploy_production_recovery`, category release, targetType release_run, risk high, fresh inputHash from the confirmation-time current-config snapshot, consumedAt null), idempotent/concurrent-convergent, drift forces a fresh confirm (409/422, AC-ENVVER-015), and the recovery ReleaseRun flows through the existing production execute path (validateProduction now enforces mode=recovery, sourceVersionId is derived server-side from the run when absent) so F437/F438 frozen-snapshot + route-activation/probe evidence works unchanged; Production recovery Web dialog (Demo parity) + 生产恢复审批 card + per-change evidence entry, zh/en parity. Next → F440 (Production evidence logs drawer). |
| F440  | done  | Production evidence Web: production step restructured to the Demo v9 structure — context strip (当前线上版本/正在交付的版本/待办事项/发布顺序) + state-variant stage-callout (生产发布/生产发布等待审批/生产发布执行中/当前线上版本运行正常/该版本曾在线上运行/生产发布失败) + stage-summary (当前线上版本/本发布单制品/生产前置条件) + release-run card strip (发布运行/目标环境/冻结制品/状态/创建时间) + six-column deployment table (部署运行|构建/Manifest|执行结果|验证结论|耗时/时间|操作, 验证结论从技术/业务证据推导, 操作=日志+专业深链+聚焦) — AC-PROD-032/033/038. New per-run `ReleaseProductionLogDrawer` (redacted logs, structured evidence, failure diagnostics, technical/business conclusion, site probe, professional link) wired through step-content + detail-panel via the existing `deploymentRunId` URL deep-link (opens on refresh); evidence API now returns deployment-run `logs`+`result` via select+presenter+web type, no new endpoint — AC-PROD-034. Six-state matrix with distinct tones (`releaseApprovalStateTone`/`releaseRunStateTone`: 等待审批 warning, 批准 info, 拒绝 danger, 执行中 progress, 成功 success, 失败 danger), ReleaseRun running pulse indicator, and a recovery link for failed/rejected runs routed to the existing Environment Versions recovery (`?view=environment-versions`, no new recovery API) — AC-PROD-035. Raw-code cleanup: gate-denial stage token and server errors localized via stable `releaseProductionErrorLabelKey` keys, site probe errors show localized 不可用 with the raw code only as secondary detail, errorCode/errorMessage/deployment.error never rendered — AC-PROD-036. Exactly one `data-primary` action per state (idle→申请生产审批, awaiting→等待生产审批 disabled+批准, approved→执行, running→disabled, failed/rejected→前往环境版本管理恢复) with tests asserting a single enabled primary — AC-PROD-037. Verification: focused API 11 tests (presenter logs/result + controller), focused Web 5 files, full Web 62 files/252 tests, full API 267 suites passed (only documented HEAD-legacy `repository-analysis-run.service.spec.ts` failure), API+Web type-check + builds, i18n zh/en parity 3210 messages, `git diff --check` clean. Runtime/browser evidence at `/tmp/codex-tool-runs/svton/f440/f440-browser-evidence.json` (if captured). Next → F441 (EnvironmentVersion read model). |

## Evidence

- F437 production real-execution evidence: focused real-gate + real-provider MySQL integration tests (`apps/devpilot-api/src/release-delivery/environment-version-production-real-gate.integration.spec.ts`, `RUN_F437_PRODUCTION_REAL_GATE_INTEGRATION=1`) pass 3/3 (success path: DeploymentRun created + workload started + `healthProbe` evidence + EnvironmentVersion advanced + pointer moved + approval consumed + ReleaseRun succeeded; D06/D09 deferred at admit+finalize with preflight gate evaluations still `unavailable`; health-failure keeps failed DeploymentRun logs, leaves the pointer unmoved, and marks the ReleaseRun failed). The existing production integration suites (16 tests) and staging suites (10 tests) still pass. API type-check + build, Web type-check + build, i18n parity 3120 and `git diff --check` pass. Authenticated 1484x1324 browser execute→success evidence is captured at `/tmp/codex-tool-runs/svton/f437/f437-browser-evidence.json` with artifacts in `/tmp/codex-tool-runs/svton/f437/browser/` (DOM/text/screenshot + cdp console evidence + DB evidence log `f437-db-evidence.log` and API evidence `api-execute-evidence.json`): the production step execute button drives the real API which consumes the frozen snapshot, starts a real ssh-v1 workload, and advances the environment version.
- F438 production route-activation + site-probe evidence: `EnvironmentVersionService.execute` production success now runs route activation + real probes before completing the DeploymentRun. Activation resolves the matching Site (same team+project+environment, `primaryDomain` ∈ frozen `routeSnapshot.domains`) and writes `Site.routeSwitch` + an append-only `SiteRouteSwitchRun` inside the same completion transaction (run refused/failed if the route declares domains but no matching Site exists). `SiteFinalProbeService` runs a REAL `dns.promises.resolve`, REAL `tls.connect <domain>:443`, and REAL `http(s).get` of the final URL (statusCode + sha256 body signature) with an honest documented fallback to the route `proxyTarget`; `unavailable` is explicit and never a false pass, and a definitive negative (HTTP non-2xx, expired/invalid TLS cert) throws before completion so the run is NOT marked successful. `DeploymentRun.result.siteProbe`/`routeSwitch` are bound to the exact run and the Site row also carries `dns` + `tls.probe`. Gates D14/D15 are data-honest (D14 unavailable without a fresh real DNS probe, D15 blocked on expired/invalid certs; admit/finalize defer the explicit unavailable reason codes, preflight capability truth unchanged). Real-MySQL integration (`apps/devpilot-api/src/release-delivery/site-route-activation.integration.spec.ts`, `RUN_F438_SITE_ROUTE_ACTIVATION_INTEGRATION=1`) passes 3/3 (success switch + real probe evidence; HTTP-500 probe hard-fail → run failed/pointer unmoved/Site unchanged/ReleaseRun failed/approval unconsumed; site-removed → RELEASE_GATE_BLOCKED with no DeploymentRun). D14/D15 gate honesty spec (8 tests), site activation service spec (5 tests) and presenter spec (3 tests) pass; existing 15 production integration tests and all 69 site/release-delivery unit suites stay green (full API 267 suites/1600 tests, only the pre-existing `repository-analysis-run.service.spec.ts` stale-harness failure remains, unrelated to F438). API+Web type-check + build, i18n parity (3145 messages), Prettier and `git diff --check` (twice) pass. Authenticated 1484x1324 browser evidence `/tmp/codex-tool-runs/svton/f438/f438-browser-evidence.json` (artifacts `/tmp/codex-tool-runs/svton/f438/browser/`, DB log `f438/db-evidence.sql` + execute evidence `f438/runtime/execute-evidence.json`): the production step shows the site/DNS/TLS/HTTP section bound to DeploymentRun `cmshsghrd001zayiz1dd1ss0n` — 路由切换 已切换 (target `ssh://deploy@127.0.0.1:2225/config/f434-browser`, proxyTarget `http://127.0.0.1:23992`), DNS 已解析 (real `198.18.11.9`), TLS 不可用 (ECONNRESET, honest), HTTP 通过 200 + Body 签名 `sha256:ecba021…` (probed URL = local target `http://127.0.0.1:23992/`, final URL `https://demo.f437.example` attempted first and documented). AC-PROD-025..028/030/031 are checked; AC-PROD-029 (browser loads the FINAL site page) remains OPEN because the demo environment cannot serve the fixture final domain `demo.f437.example` (no sudo to add hosts/443) — the local route target page is served and honestly probed instead. F438 is `done`; F439 production recovery is next.
- F439 production recovery evidence: a new recovery-confirm API (`POST /projects/:id/delivery/environment-versions/:env/recovery/{preview,confirm}`, `environment-version-recovery.repository.ts`) resolves a historical production EnvironmentVersion (≠ current; unknown/foreign/current rejected), reuses the production preview machinery against the CURRENT config snapshot for a fresh `inputHash`, and creates a NEW `ReleaseRun` (mode `recovery`, `sourceReleaseRunId` = historical version's run, own idempotencyKey) + NEW `OperationApproval` (action `project.release_order.deploy_production_recovery`, category release, targetType release_run, risk high, `inputHash` = fresh snapshot hash, `consumedAt` null) atomically; concurrent same-key confirms converge to one run (P2002 re-read), stale expectedInputHash → 409 「生产配置或策略已漂移，请重新确认最新恢复快照」(AC-ENVVER-015), and the recovery ReleaseRun flows through the existing production execute path — `validateProduction` now requires `run.mode === 'recovery'` for production recovery (closing the latent path where an unconsumed upgrade approval could back a recovery, AC-ENVVER-014) and the service derives `sourceVersionId` from `run.sourceReleaseRunId → EnvironmentVersion` when the web approval card executes with only the releaseRunId. `exactProductionProof`/evidence ownership accepts the recovery action so the recovery run + its DeploymentRun appear in the order's production evidence (F436-style approve/execute card). Web: the Production environment-version card's 回退 button opens the Demo-parity dialog (「回退 Production 环境版本」, callout 「回退会创建新的恢复部署」, select 「恢复到」 defaulting to the last successful historical version, readonly 「当前环境版本」, confirm 「创建恢复部署」), which on confirm navigates to the source order's production step focused on the new recovery ReleaseRun; the approval card renders 「生产恢复审批」+ 「回退」 badge and its execute posts `{kind:'recovery', releaseRunId}`. Real-MySQL integration `environment-version-recovery.integration.spec.ts` (`RUN_ENVIRONMENT_VERSION_RECOVERY_INTEGRATION=1`) passes 5/5: create recovery run + fresh unconsumed approval → approve → execute → new DeploymentRun (mode rollback) + EnvironmentVersion kind recovery + previousVersionId = prior current + pointer moved + approval consumed + ReleaseRun succeeded + history append-only; consumed/non-recovery ReleaseRun cannot back a recovery; config drift forces a fresh confirm (stale hash 409, execute 422); unknown/foreign/current sourceVersionId rejected; concurrent recovery converges to one run. Existing production integration suites (12 tests) stay green; full API unit run 267 suites/1600 tests (only the HEAD-legacy `repository-analysis-run.service.spec.ts` harness failure remains), Web full suite 61 files/244 tests, API+Web type-check + build, i18n parity (3153 messages), `git diff --check` twice. Authenticated 1484x1324 browser evidence `/tmp/codex-tool-runs/svton/f439/f439-browser-evidence.json` (artifacts `/tmp/codex-tool-runs/svton/f439/browser/`, DB proof `f439/db-evidence.sql`): from historical success version `cmshsele4006vgrgisp0kq0sc` the dialog created recovery ReleaseRun `cmshul4c30062ny7b2wkafp6m` (mode recovery/succeeded, sourceReleaseRunId `cmshsek25003ngrgivf2uxcsd`) + new approval `cmshul4c50064ny7boud4pgns` (deploy_production_recovery/release/high, inputHash `2c81169f…` fresh, consumedAt `2026-08-06T18:27:00.933Z`), approved in project context (生产恢复审批 card), executed → DeploymentRun `cmshul852007xny7bts158xs2` (rollback/completed, DNS resolved, routeSwitch switched — F438 probe path intact) + EnvironmentVersion `cmshul9e7009hny7b6remtdur` (kind recovery, previousVersionId `cmshsgj18003jayiz34hj3thw`, pointer moved to it) + evidence entry; Console 0, failed network 0, recovery preview/confirm/actions returned 201. Runtime gate evidence TTLs (metrics 5 min / connectivity 15 min / route 1 h) were refreshed for the running demo stack (same class of MySQL-only fixture maintenance F437/F438 used); a second production recovery ran end-to-end, so AC-ENVVER-013 is claimed.
- F418 project-home evidence: `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/f418/` contains the baseline summary proof.
- F440 production evidence Web evidence: focused API tests pass 11/11 — `release-order-evidence.presenter.spec.ts` (5 tests, incl. new "exposes sanitized logs and the raw structured result for every DeploymentRun"), `release-order-evidence.controller.spec.ts` (2), `release-order-evidence-production-ownership.spec.ts` (4). The evidence API (select `release-order-evidence.prisma.ts` adds `logs: true`; `release-order-evidence.presenter.ts` now returns `logs`+`result` per DeploymentRun) feeds the new per-run `ReleaseProductionLogDrawer` (redacted logs, structured evidence, failure diagnostics, technical/business conclusion, site probe, professional link) and the six-column production deployment table. Focused Web tests pass 5 files (production step incl. single-primary + stage-summary/context-strip + no-raw-code gate-denial; approval card incl. localized gate denial + rejected→Environment Versions recovery link; evidence lists incl. six-column headers, release-run card strip, running in-flight indicator, failed→recovery link, site probe no-raw-code; candidate sources; new production log drawer incl. deep-link open + retry-in-place). Full Web suite passes 62 files/252 tests; full API suite passes 267 suites/1601 tests with only the documented HEAD-legacy `repository-analysis-run.service.spec.ts` failure (7 tests, unrelated to F440). API+Web type-check and builds pass; i18n zh/en parity 3210 messages (`apps/devpilot-web/scripts/verify-message-parity.mjs`); `git diff --check` clean twice. Verification logs under `/tmp/codex-tool-runs/svton/f440/`. Browser/runtime evidence: authenticated 1484x1324 CDP capture at `/tmp/codex-tool-runs/svton/f440/f440-browser-evidence.json` (26 hashed artifacts) proves the context strip + stage-summary + six-column table, awaiting-approval/approved/succeeded states with single primary action, per-run log drawer + deploymentRunId deep-link reopen, narrow no-overflow, zero unexpected console errors, and a frozen Demo production-step structural comparison (f440-demo-comparison.md); DOM grep shows no raw gate/probe codes. AC-PROD-032..038 checked; AC-PROD-035 failed/rejected shots not separately captured (no failed fixture) but covered by focused Web tests + DOM assertions. Independent review then exposed an active-child cross-tab create no-op and mutable intake projection; `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/f418-correction/` contains the correction's focused regressions, two fresh real-MySQL suites, type/build/lint/i18n/format/CodeGraph gates, sanitized no-create counts, zero-console Browser facts and the accepted 1484×1324 Environment Versions dialog screenshot. Intake now requires exact succeeded finalization/result/review evidence and fails closed without mutable config fallback; one always-mounted route owner serves both tabs.
- F441 EnvironmentVersion read model evidence: the environment-versions list read path now re-validates `currentEnvironmentVersionId` with `exactCurrentEnvironmentVersion` semantics (completed / !dryRun / source=release_order / exact team+project+environment+order+manifest scope) and fails closed to null when not provable, while the full reverse-chronological version history is always returned — `environment-version-read.utils.ts` (`currentEnvironmentVersionId` wrapper) + `environment-version-read.repository.ts` select extended with `teamId`/`projectId`/full-fidelity `currentEnvironmentVersion` (releaseOrder/artifactManifest/deploymentRun incl. source/status/dryRun), wired in `EnvironmentVersionService.list`. Unit spec `environment-version-read.utils.spec.ts` passes 8/8 (pointer mismatch / null / non-completed / dry-run / non-release_order source / foreign environment / foreign manifest → null; exact → id). Real-MySQL integration (`RUN_ENVIRONMENT_VERSION_INTEGRATION=1`) 5/5: the two new tests assert per-version payload fields (releaseOrder.id+releaseVersion, manifest digest, buildRun revision, deploymentRun createdAt/finishedAt, previousVersionId chain, effectiveAt reverse-chrono, validated current) and pointer-at-failed-run → current null with history retained. Web: `environment-versions-panel.tsx` restructured to Demo v9 cards — page head 环境版本 + helper + 共 N 个环境 badge; per-env card (环境名 + 当前部署 badge or 尚无可追溯; facts 已部署版本=发布版本号 {version}, 来源发布单=releaseOrder.id·releaseVersion, Artifact Manifest=id + BuildRun #{revision}·digest, 最近发布时间=deploymentRun.finishedAt??createdAt localized; actions 回退 outline — Production opens the F439 recovery dialog, Staging executes directly, disabled without a previous version — + 升级版本 primary with the production approval-required note); Demo callout; 环境变更记录 table caption 最近环境版本变更 with columns 环境|动作|版本变化(X→Y via previousVersionId, falls back to Y)|制品(manifest short)|结果(成功 current / 历史记录)|时间(effectiveAt localized); `environment-version-summary.tsx` reworked to the four-fact layout (also used in the F439 recovery dialog); `environmentVersionKindDeploy` value aligned to Demo 发布/Release. No web type change needed (releaseOrder already in payload). i18n +23 environmentVersion* keys, zh/en parity 3231. Verification: focused Web 7 tests (panel facts/actions/table/chain/dialog + summary facts/finishedAt-preference), full Web 62 files/254 tests, full API 268 suites/1617 tests (only the HEAD-legacy `repository-analysis-run.service.spec.ts` failure remains), API+Web type-check + builds, i18n parity, `git diff --check` clean twice; logs under `/tmp/codex-tool-runs/svton/f441/`. Runtime/browser evidence: authenticated 1484x1324 CDP capture at `/tmp/codex-tool-runs/svton/f441/f441-browser-evidence.json` (artifacts `/tmp/codex-tool-runs/svton/f441/browser/` — env-versions + narrow DOM/text/png, sha256 inventory) on the running stack (web 3121 / api 3131 / devpilot-f434-mysql 3334): Staging + Production cards both show 当前部署 badge and the four facts (来源发布单 `cmshheb5r0007vwrcaft5uhsx · 4.34.0`, Artifact Manifest `cmshhebzv0017vwrc0p1iyl7p` + BuildRun #1, 最近发布时间 localized), 回退 + 升级版本 actions, and the 环境变更记录 table with caption + six columns + 9 rows incl. 7 `4.34.0 → 4.34.0` chain rows; API current values match the exact-validation result (Staging `cmshhefxd001vvwrcbdoot2kj`, Production `cmshul9e7009hny7b6remtdur`); narrow 390x844 has no horizontal overflow (scrollWidth=390); console errors 0, failed requests 0. AC-ENVVER-001..006 checked with F441 evidence notes; Next → F442 (per-environment candidate filtering with default recommendation — AC-ENVVER-007..010).
- F419 release-order list evidence: `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/f419/` contains focused API/Web tests, the fresh MySQL two-query/read-snapshot integration proof, static gates, CodeGraph coverage, authenticated private-cache API results, accepted/rejected SHA-256 inventories and five accepted 1484×1324 Browser captures. The captures prove total 52/bounded 50/default tie ordering, normal plus literal-safe search, five persisted status counts, Build/Manifest/deployment/latest-step projections, exact detail navigation and no stale rows across a team switch. Browser JavaScript console errors are zero; the expected ACL summary 404 retries during the negative isolation probe remain explicit network evidence.
- F419 independent-review correction: the SQL owner now accepts Deployment facts only through an exact Manifest↔BuildRun id/team/project/release-order join and returns only the validated BuildRun id. ReleaseRun execution events require exact Manifest team/project/release-order ownership plus frozen digest equality, retaining the exact Production baseline environment. Raw-SQL unit tests and fresh tmpfs MySQL adversarial rows for cross-order/project/team and digest drift passed without changing sort/lastExecution/deployment/latest or the two-query bound. Exact staged-tree Browser evidence under `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/f419-correction/` physically separates accepted/rejected captures and binds every screenshot to raw DOM, console, network, events, fixture IDs, URL, time, viewport and artifact hashes.
- F420 release-order lifecycle evidence: the initial independent review failed with P0=0/P1=2/P2=1 because inactive baselines could contaminate lifecycle/F419 execution facts, orphan or mismatched Production DeploymentRuns could be promoted, and create/withdraw returned divergent public shapes. The correction requires active baseline joins everywhere, keeps every newest invalid Production fact visible as `failed/production/evidence_mismatch`, restricts governance to the exact running/succeeded ReleaseRun plus reviewed approval, and uses one canonical detail presenter for create/replay/get/withdraw. Focused API tests passed 13 suites/61 tests; seven real-MySQL suites passed 34 tests, including archived Staging/Production, non-baseline, wrong-role and nine Production-linkage negatives; both API/Web type-checks, builds and lints, Web tests and i18n also passed. Exact correction source tree `6722d15d05cb1dacae7d7cee456e5084d2bd7d99` is bound by source bundle SHA-256 `4c46a2c99679c78f8e5163b0dae4d97b561043716900f6753c4d90e2c1374db8`. Correction Browser manifest SHA-256 `ab4836881c4e9064ae618d7042aa6088e99fdeeb1538e32ebb8d640b66432524` and inventory SHA-256 `e88ba6c2cbaace89666305d51699909b35b8208b205c1e03a316768ccd66a2a4` bind 16 accepted, zero rejected, unique 1484×1324 screenshots, zero hash mismatch, zero unexpected console errors and zero secret leaks; the isolation 404/two console errors remain expected. Authenticated create/replay, canonical withdraw and adversarial inventories are SHA-256 `8c7e47b07dd12df41d50d90d6fdc6c1c9a1987cd7acd6274a24ac92b9af00921`, `9940d3b41eced7afaf321ffa7bc9d7d1921175e844399ad2aedcbeb000cfacd5` and `f6642cec4bc74dfe51614194dfb9c22650970d520983553320683b5a59708109`. The old `d9f7...` Browser manifest is retained only as a superseded baseline. Independent correction rereview `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/workers/f420-correction-independent-rereview-report.md` passed P0/P1/P2=0/0/0; report SHA-256 `4ce4792e0ea2ebf6a870a11255821b5e2b3348ca1e61cf725da736530d0c5b7a`, result SHA-256 `aa84f48f9e61ae1869586f70d4380f099108080bd17655e609b1b5d1e1e25760`. F419 lastExecution still ignores `ReleaseRun.updatedAt`.
- F421 release-order resume evidence: detail now projects the maximum structurally accepted phase from F420's validated `lifecycle_events`, independent of newest event, status, timestamp, persisted status and raw counts; strict presentation rejects null, unknown and case-drift values. A fresh tmpfs MySQL 8 database applied every migration and passed 11/11 adversarial tests for all four phases, later retries, Production evidence mismatch, invalid/dry-run/inactive/non-baseline/wrong-scope/foreign-Manifest/digest-drift evidence and withdrawal history. Authenticated HTTP proved all resume values plus 403/404 scope isolation. Browser evidence under `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/f421/browser/` has 16 accepted and zero rejected 1484×1324 JPEG captures: four defaults, missing/invalid/valid/refresh, valid-first duplicate fail-closed, withdrawal, invalid evidence, unknown order, foreign build focus and reopen-to-server-resume. It records 13 unique hashes; `01/02/16` and `03/04` are expected identical final UI groups, not falsely claimed unique states. Root Browser assertions passed with zero unexpected console/exception entries, one expected canceled router navigation, exact isolation 404 and zero secret matches. Independent review's duplicate-step P1 was corrected by requiring exactly one `step`; correction rereview passed P0/P1/P2/P3=0/0/0/0 (`f421-independent-review-correction-report.md`, SHA-256 `70c446d3a6e47db2b4cdf758ddb30849d4909a0f0bd916ae98a0b055327f4022`). Final independent seal review passed P0/P1/P2/P3=0/0/0/0; report SHA-256 `dee364061281bed3a861e8b6d0a6741d9526e6075af587fb55547d156ee12ab0`, result SHA-256 `404a85eb48cc1fd7153f1c44e610730ee47ec1e756f4c5827291230211986b64`.
- Long-goal board: `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/board.json`.
- Phase 0 source audit: `.../artifacts/p0-source-audit.md`.
- Architecture brief: `.../artifacts/p0-architecture-brief.md`.
- Serialized backlog: `.../artifacts/p0-slice-backlog.md`.
- Dependency install: `/tmp/codex-tool-runs/svton/f386-install-20260803-115615.log`.
- Prisma generate/validate: `/tmp/codex-tool-runs/svton/f386-prisma-generate-20260803-115714.log`, `/tmp/codex-tool-runs/svton/f386-prisma-validate-final-20260803-115731.log`.
- Initialization build and type-checks: `/tmp/codex-tool-runs/svton/f386-init-build-final-20260803-115737.log`, `/tmp/codex-tool-runs/svton/f386-api-typecheck-20260803-115805.log`, `/tmp/codex-tool-runs/svton/f386-web-typecheck-20260803-115813.log`.
- Focused integration fixes: `/tmp/codex-tool-runs/svton/f386-focused-tests-20260803-115839.log` — 3 suites, 35 tests passed.
- CodeGraph initialized with 3,358 files, 37,526 nodes and 128,155 edges; pending changes zero.
- F387 Prisma and API checks: `/tmp/codex-tool-runs/svton/f387-prisma-validate-replay-20260803-121740.log`, `/tmp/codex-tool-runs/svton/f387-api-typecheck-replay-20260803-121757.log`.
- F387 preflight fixtures: `/tmp/codex-tool-runs/svton/f387-tests-replay-20260803-121757.log` — 2 suites, 14 tests passed.
- F387 real MySQL migration: `/tmp/codex-tool-runs/svton/f387-migrate-deploy-empty-20260803-121147.log`, `/tmp/codex-tool-runs/svton/f387-migrate-upgraded-fixture-20260803-121334.log`, `/tmp/codex-tool-runs/svton/f387-upgraded-fixture-assertions-20260803-121349.log`; two ambiguous environments were retained with NULL baseline role and no implicit lifecycle/backfill rows.
- F388 focused unit/regression tests: `/tmp/codex-tool-runs/svton/f388-unit-tests-final-20260803-123710.log` — 8 suites, 39 tests passed.
- F388 real MySQL transaction acceptance: `/tmp/codex-tool-runs/svton/f388-finalization-integration-acceptance-20260803-123802.log` — 7 integration tests passed, including rollback/retry and concurrent finalize.
- F388 API gates: `/tmp/codex-tool-runs/svton/f388-api-typecheck-replay-20260803-124121.log`, `/tmp/codex-tool-runs/svton/f388-api-build-replay-20260803-124121.log`.
- F389 unit/regression tests: `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f389-unit-tests-search-summary.log` — 3 suites, 12 tests passed.
- F389 real MySQL directory isolation/search: `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f389-mysql-search-integration-retry.log` — 2 integration tests passed; disposable container removed.
- F389 API gates and graph: `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f389-api-typecheck-search-summary.log`, `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f389-api-build.log`, `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f389-codegraph.log`.
- F390 focused regression: `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f390-api-unit-latest.log` — 5 API tests; `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f390-web-unit-latest.log` — 6 Web tests; API/Web type-check and build logs use the matching `f390-*-latest/final.log` names.
- F390 historical browser evidence gap: `f390-browser/01-project-directory-empty.png`, `02-analysis-review.png` and `03-baseline-confirmation.png` are three 5,702-byte near-white files with identical SHA-256 `e01ab5497ed33f5960c63aef2c484b77a3834123df61c2914cce5def3921dd1e`; they are preserved and are not accepted as page evidence. F412 successor evidence `f412/evidence-manifest.json` plus `f412/browser/01-project-directory.png` through `07-project-release-list.png` verifies current behavior and does not reconstruct the historical screenshots.
- F390 real MySQL evidence: project `cmscs55sy000azibq1is4a4dg` is ready at commit `85fad682d21785cf83cc48a911e993c049750356`, has one locked canonical identity, one successful finalization, exactly one active Staging and Production baseline, and the latest successful run is not overridden by an older failed run.
- F391 route regressions: `/tmp/codex-tool-runs/svton/long-goals/devpilot-project-delivery-v13/f391-web-unit-final.log` — 2 files, 12 tests; Web type-check/build passed in `f391-web-typecheck-after-build.log` and `f391-web-build-final.log`; CodeGraph selected the compatibility spec.
- F391 browser evidence: `.../f390-browser/06-f391-project-delivery.png` and `07-f391-manage-project.png`; default release-order view, two-item primary navigation, truthful environment-version empty state, settings sections and repository/environment/deployment legacy deep links all passed with preserved focused IDs and clean final-page consoles.
- F392 schema gates: `f392-prisma-validate.log`, `f392-prisma-generate.log`, `f392-api-typecheck.log`, `f392-api-build.log` and `f392-api-unit.log` under the long-goal directory all passed; CodeGraph selected the migration-report spec.
- F392 real MySQL: `f392-migrate-empty.log` and `f392-migrate-upgraded.log` passed. Empty schema has all six delivery tables; same digest produced two independent manifests for two builds; duplicate project releaseVersion failed. Upgrade fixture retained legacy plan/deployment links as NULL, observed-only digest as evidence, zero synthesized orders/manifests/environment versions, and preserved unrelated existing index/FK drift.
- F393 API evidence: `f393-api-unit-final.log` — 2 suites/7 tests; `f393-api-mysql-integration-final.log` — 3 real-MySQL tests including concurrent identical create convergence; API type-check/build passed.
- F393 Web/browser evidence: `f393-web-unit-final.log` — 2 files/12 tests, Web type-check/build passed; `.../f390-browser/08-f393-release-order.png` shows the real `2.4.1` draft with 0 builds/0 manifests. Sequential replay kept one card, conflicting note stayed in the dialog with an explicit error, and the final console was clean.
- F394 API evidence: `f394/api-release-suite-final.log`, `f394/api-integration-final.log`, `f394/api-typecheck-final.log` and `f394/api-build-final.log` cover executor/config/controller/service/repository behavior plus real MySQL revision allocation and success-only Manifest persistence. Executor tests prove deterministic archive digest, minimal environment, checkout path confinement and token redaction.
- F394 browser/MySQL evidence: `.../f390-browser/09-f394-build-runs.png`, `10-f394-build-success.png`, `f394/mysql-browser-evidence.log` and `f394/mysql-final-build-evidence.log`. The server resolved `main@85fad682d21785cf83cc48a911e993c049750356`; the release order reached 5 independent BuildRuns and 4 Manifests, failed revision 3 retained zero Manifest, revisions 4/5 produced the same deterministic digest, and the final browser console had no error/warn entries.
- F395 API/Web evidence: `f395/api-unit-final.log`, `f395/api-typecheck-final.log`, `f395/web-unit-final.log`, `f395/web-typecheck-final.log` and matching build logs verify the scoped detail route, server-derived preflight/resume state, stable route normalization and dual-language build.
- F395 browser evidence: `.../f390-browser/11-f395-four-step-detail.png` and `12-f395-build-log-drawer.png`. Opening an order without `step` restored `step=build`; ArrowRight moved the selected ARIA tab and URL to Staging; invalid `step` normalized to build and removed a bogus buildRunId; the exact Build #5 log dialog survived refresh with one dialog, `aria-selected=true` and no console error/warn.
- F396 API evidence: `f396/api-unit-final.log`, `f396/api-integration-final.log`, `f396/api-typecheck-final.log` and `f396/api-build-final.log` cover same-order/successful-build Manifest validation, repeatable DeploymentRuns, exact byte digest verification, archive traversal rejection boundary, failure persistence and real MySQL no-rebuild invariants.
- F396 browser/MySQL evidence: `.../f390-browser/13-f396-repeat-staging.png` and `f396/mysql-browser-evidence.log`. Two clicks against Build #5 produced two completed DeploymentRuns with the same Manifest and Commit, `dryRun=0`, `artifactVerified=true`, `checkout=false`, `build=false`; BuildRun count remained exactly 5 and the browser console was clean.
- F397 API evidence: `f397/api-release-tests.log`, `f397/api-integration-final.log`, `f397/api-type.log` and `f397/api-build.log` cover exact same-Manifest Staging proof, immutable Production snapshots, approval binding, transactional concurrent idempotency, config drift, cross-project lookup and unknown Digest rejection. The real MySQL integration converged two concurrent requests to one ReleaseRun and one pending approval.
- F397 Web/browser evidence: `f397/web-release-tests.log`, `f397/web-type.log`, `f397/web-build.log` and `.../f390-browser/14-f397-production-confirmation.png`. The Production-only action required an explicit confirmation of environment, release version, Build/Commit, Manifest/Digest, config revision and policy snapshot; the real browser created ReleaseRun `cmscwe4ze000bgm3ufy9gdn0i` in `awaiting_approval` with pending approval `cmscwe4zl000dgm3u234b2dym`, both bound to the same input hash.
- F398 API evidence: `f398/api-release-suite.log`, `f398/api-integration.log`, `f398/staging-integration.log`, `f398/api-type-final.log` and `f398/api-build.log`. Real MySQL proved three Staging upgrade/recovery requests appended three distinct DeploymentRuns and EnvironmentVersions, preserved the previous-version chain, rejected arbitrary history IDs, and allowed Production only through a matching approved unconsumed ReleaseRun; success consumed the approval and completed the ReleaseRun.
- F398 Web/browser/MySQL evidence: `f398/web-type-final.log`, `f398/web-build.log`, `f398/mysql-browser-evidence.log` and `.../f390-browser/15-f398-environment-versions.png`. The real flow appended deploy → upgrade → recovery for Staging, approved the F397 Production request in the governance UI, then created the Production current version. The project has four immutable versions backed by four distinct DeploymentRuns; the browser console had no errors or warnings.
- F399 regression evidence: `f399-api-tests.log`, `f399-web-tests.log`, matching API/Web type-check/build logs, and browser screenshots `16-f399-manage-project.png` plus `17-f399-professional-deployment.png`. All five Manage Project sections were reachable with stable query URLs, legacy `tab=resources` redirected to the secondary page, and a focused legacy DeploymentRun preserved its ID under `view=deployments`. The audit also fixed malformed ICU Webhook text and exposed the ReleaseRun approval through the professional deployment projection; a fresh browser tab then had no error/warn logs.
- F400 API evidence: `f400/api-unit.log`, API/Web type-check and build logs, focused Web ESLint, `cas-a.log`, `cas-b.log`, `cross-project.log`, and `secret-plaintext.log`. Eleven focused tests cover the permission controller, stable snapshot hashing, strict references, global validation, shared-risk declaration, pre-deployment key mutability and post-deployment key lock. Against real MySQL, two writes from the same R5 pointer produced exactly one R6 (201) while the loser failed with 409 drift; cross-project resource input and a Secret plaintext field both failed with 400.
- F400 surviving evidence: `f400/mysql-evidence.log` records immutable revision history, locked environment key, ordinary variables, safe Secret metadata and the Staging/Production shared Redis scope. The previously named `f390-browser/18-f400-config-governance.png` is absent, so it is now an explicit historical evidence gap rather than a browser-proof claim; it was not reconstructed. The database stores only Secret id/name/type in the revision and audit metadata, while the compatibility `config.envVars` mirror remains available to the existing deployment injector.
- F401 catalog evidence: `f401/api-tests.log` — 3 suites/17 tests; API/Web type-check and production builds plus focused Web ESLint passed. The authenticated API returned catalog `v13.2026-08-03` with exact Commit/Build/Deploy/Promote counts 10/11/20/10, all 51 unique checks, 15 capability groups and 51 `unavailable` evaluations while no provider is connected; cross-project lookup returned 404.
- F401 historical browser evidence gap: the referenced `f390-browser/19-f401-gate-catalog.png` does not exist and was not fabricated. F412 successor evidence `f412/browser/08-release-preflight.png`, `09-gate-catalog-expanded.png`, their DOM/console/network companions, `f412/database-api/gates-api-summary.json` and `f412/evidence-manifest.json` verifies the current page at the repaired current implementation; it does not restore the missing F401 artifact.
- F402 provider evidence: `f402/api-tests-final.log` — 4 suites/14 tests; API/Web type-check and production builds plus focused Web ESLint passed. Positive fixtures prove M01-M05 checked evidence, negative Build/test/security/Manifest results block, expired repository/analysis evidence becomes unchecked, and missing merge/CI/diff/security providers remain unavailable. Execution isolation/redaction is now explicitly separated from Secret/SAST/vulnerability scanning.
- F402 real browser/MySQL evidence: `f402/mysql-evidence.log`, `f402/browser-dom.txt` and `.../f390-browser/20-f402-commit-build-providers.png`. The real project evaluates C01/C05/B02/B09 as checked from exact repository, analysis, Build #5 and Manifest rows; C08/B03 are unchecked because the fixture has no supported lockfile/test provider; the other 45 checks, including all M04 scanner checks, remain unavailable. Evidence refs, checkedAt/expiresAt and precise reasons are visible, and the console is clean.
- F403 provider evidence: `f403/api-tests-final.log` — 5 suites/20 tests; API type-check/build and Web type-check passed. Positive fixtures prove M06-M09 config, Secret, deployment target, server/resource connectivity, capacity, migration and backup evidence; plaintext fields, cross-environment rows and failed evidence block; expired connectivity/metrics/migration/backup becomes unchecked; missing providers remain unavailable.
- F403 real browser/MySQL evidence: `f403/mysql-evidence.log`, `f403/browser-dom.txt` and `.../f390-browser/21-f403-deploy-providers.png`. D01 reads the real non-dry-run exact-Manifest Staging DeploymentRun, D02 reads immutable config R6 and its environment-scoped resource reference, and D03 resolves one Secret using only id/project/environment/name/type fields. The database has zero active server bindings, connection probes, capacity snapshots or backup runs, and no migration-diff provider, so D05 and D07-D12 remain unavailable rather than passing. The browser console is clean.
- F404 provider evidence: `f404/api-tests-final.log` — 4 suites/16 tests; API type-check/build and Web type-check passed. Positive fixtures cover approval/protection, DNS/TLS/routes, workload/HTTP, observability/metrics, recovery and retained evidence. Drift, ownership, expired certificates/approvals, failed probes/metrics and corrupt recovery block; time-sensitive DNS/HTTP/observability evidence expires; M15 remains unavailable without traffic/abort/rollback providers.
- F404 real browser/MySQL evidence: `f404/mysql-evidence.log`, `f404/browser-dom.txt` and `.../f390-browser/22-f404-promote-providers.png`. The real approval is approved, consumed and input-hash-bound, but D13 remains unchecked because no change-window/freeze provider conclusion exists. Production has no Site/DNS/TLS, health/workload/HTTP probe, observability metric, prior stable version or recovery-compatibility evidence, so those checks remain unavailable. P03 is manual evidence only; P10 alone is checked from the retained ReleaseRun→approval→Manifest→DeploymentRun→EnvironmentVersion chain. The catalog totals 51 checks with 39 unavailable and a clean browser console.
- F405 policy evidence: `f405/focused-tests.log`, `f405/integration-tests.log`, `f405/prisma-migrate-deploy.log`, `f405/api-build.log` and `f405/web-build.log`. An additive MySQL migration creates append-only `ReleasePolicyRevision` history and a project current pointer. Serializable row locking plus compare-and-swap lets only one stale-pointer competitor append. Production preview/confirmation accepts only `standard`, freezes the exact strategy revision into snapshot v2 and includes it in the approval input hash. Canary, blue-green and automatic traffic are rejected before persistence with explicit missing traffic/workload/metric/pause/abort/rollback capabilities.
- F405 browser/MySQL evidence: `f405/mysql-evidence.log`, `f405/browser-dom.txt`, `f405/production-policy-dom.txt`, `f405/23-f405-release-policy-viewport.png`, `f405/24-f405-advanced-strategies.png` and `f405/25-f405-production-policy-snapshot.png`. The authenticated Manage Project page created immutable R1, shows Standard as executable and all three advanced strategies as capability-unavailable with concrete provider keys. The real Production preview displays `R1 / standard / 986e9990…`; MySQL confirms the current pointer and immutable audit event.
- F406 compatibility evidence: `f406/focused-tests-final.log`, `f406/integration-test.log`, `f406/api-build.log` and `f406/web-build.log`. Governed or archived projects fail before the legacy branch/commit deployment service can create a checkout/build plan. The compatibility adapter classifies missing-Manifest history as `legacy_unverified`, preserves run/log references and never synthesizes a Digest. Project DELETE now performs a serializable archive transaction, retaining environments, runs and logs and emitting `project.archive` audit evidence; the real integration test reads all retained evidence after archive.
- F406 browser/MySQL evidence: `f406/mysql-evidence.log`, `f406/browser-dom.txt`, `f406/legacy-deployment-blocked-dom.txt`, `f406/26-f406-read-only-compatibility.png` and `f406/27-f406-legacy-deployment-blocked.png`. The professional deep link is read-only and reports 7 retained runs, 1 legacy-unverified run and zero synthetic manifests. All six exact-Manifest runs have `checkout=false`, `pull=false`, `build=false`. The Applications legacy wizard receives the concrete server rejection before creating any service DeploymentRun; the browser console has no error/warning entries.
- F407 parity/documentation evidence: `f407/api-build.log`, `f407/web-build.log`, `f407/browser-en-dom.txt`, `f407/production-en-dom.txt`, `f407/28-f407-release-policy-en.png` and `f407/29-f407-production-en.png`. The recursive parity check covers 2,796 zh/en leaf messages and ICU placeholder names. Runtime cookie switching renders release-policy capability reasons and Production policy snapshots in English without losing the Chinese default. The bilingual user guide and migration guide document release-order terminology, immutable Manifests, environment versions, archive/compatibility behavior and why advanced strategies remain unavailable.
- F408 complete lifecycle evidence: `f408/settings-dom.txt`, `f408/builds-dom.txt`, `f408/staging-dom.txt`, `f408/production-request-dom.txt`, `f408/approval-dom.txt`, `f408/production-deployed-dom.txt`, `f408/recovery-dom.txt`, `f408/mysql-evidence.log`, screenshots `30-f408-multiple-builds.png` through `33-f408-recovery.png`, and `f408/browser-console.json`. ReleaseOrder `2.4.2` retained the verified `main@85fad682...` repository/settings baseline, created two independent successful BuildRuns and two immutable Manifests, repeated Build #2's exact Manifest twice in Staging, consumed one high-risk approval for Production, and appended a Staging recovery version. MySQL ties four EnvironmentVersions to four distinct completed non-dry-run DeploymentRuns; every command plan has `checkout=false`, `pull=false`, `build=false`. The browser console contained no errors or warnings.
- F409 cross-layer negative evidence: `f409/focused-negative-tests.log` — 11 suites/74 tests; `f409/mysql-evidence.log`, browser DOM captures, screenshots `34-f409-cross-team-denied.png` through `38-f409-secret-redaction.png`, `f409/browser-console-fixed.json`, and `f409/web-build.log`. A real other-team Project with zero current-user membership rendered only “project not found” and retained zero release/deployment rows. Controlled Build #3 failed with no Manifest, while restored configuration produced successful Build #4. Concurrent ReleaseOrder/Production integration, exact-artifact boundaries, legacy compatibility, provider freshness/failure and deployment/Secret redaction all passed. The compatibility view remains read-only with no synthesized Manifest; 4xx load errors are rendered in UI state without polluting the console, while network/5xx errors are still reported. Browser and MySQL expose only Secret id/name/type plus masked value, with no plaintext field or test database credential.
- F410 final evidence: `f410/review-code-structure.md`, `review-domain.md`, `review-ux-a11y.md`, `review-security.md`, `accessibility-audit.json`, `final-browser-acceptance.json`, `mysql-post-browser-invariants.tsv`, screenshots `40-f410-gate-catalog-a11y.png` through `46-f410-production-build-environment-history.png`, and the final Prisma/type/build/test/lint logs. `GateEvaluation` now persists append-only, versioned, evidence-bound facts: the final project has 51 latest gate conclusions (9 passed, 3 pending, 1 needs-human, 38 unavailable) plus 2 retained superseded evaluations after repository evidence changed. Security review removed full API-environment inheritance from Git subprocesses and a real browser re-analysis still pinned `85fad682...` and cleaned its disposable checkout. API focused/regression verification passed 38 suites/185 tests, real MySQL integration passed 9 suites/23 tests, Web passed 11 files/54 tests, Prisma validate/generate/migrate, both type-checks and production builds passed, and i18n parity covers 2,798 leaf messages. The API lint gap recorded here was subsequently resolved by F411; this sentence remains the historical F410 boundary rather than claiming it passed at F410 time.
- F411 PR-readiness evidence: `f411/` contains `v13-added-maintained-source-lines.tsv` (198 added maintained source files, 0 over 200 lines), split-test logs (4 suites/12 tests), `api-lint-final.log` (exit 0; 0 errors/412 warnings), per-line warning provenance (`api-lint-warning-origins-final.tsv`: 379 baseline, 33 existing V13, 0 introduced by F411), Prisma validate/generate/deploy/status against a disposable MySQL 8 database with all 59 migrations, API type/build and full 201 suites/1,310 tests, real MySQL 10 suites/23 tests, Web lint/type/build and 11 files/54 tests, plus 2,798-message zh/en key/ICU parity. The two V13 lint errors were fixed with a precise `CurrentConfigRevision` alias; `runtime-transpile-equivalence.txt` proves byte-identical JavaScript versus parent `f7071a1b`. The board marks p0-worktree-init recovered by F386 and reconstructs F390-F399 result files only from existing commits/logs; unavailable original worker fields and named screenshots are explicit evidence gaps. The current production shell opened live with an empty error/warn console, but authenticated browser smoke was blocked by the Browser URL security policy; `browser-smoke-attempt.md` records the limitation and does not claim a completed authenticated rerun. The preserved F410 browser JSON/screenshots remain ancestor evidence for project directory, release detail/51 gates, environment history and zh/en switching.
- F412 current successor evidence: `f412/evidence-manifest.json` binds nine valid, unique 1280×720 screenshots to exact capture HEAD, URL, timestamp, DOM, console/network and API/DB paths; `screenshot-validation.json` records size, SHA-256 and luminance/pixel-distribution checks. The real local Git fixture finalized idempotently to one ready project, one locked canonical identity and exactly one valid Staging plus Production baseline with no dev/test/QA. The first 51-gate browser attempt exposed an API 500 when optional route domains were absent; failure evidence is preserved as `f412/browser/failure-preflight-catalog-error.*`, fix commit `7d43f40a` added a regression test, and the repeated Browser/API/MySQL proof shows 51 unique gates, 10/11/20/10 phases, 15 capability groups and zero missing-provider false passes. All F412 data is explicitly tagged and isolated; the successor evidence validates the present implementation, not historical capture time.
- F414 project-governance evidence: generator and three-step repository intake now depend on the project-domain `ProjectGovernanceFinalizationService`; READY transition, one active Staging plus Production baseline, immutable R1 revisions/current pointers and governance audit commit in one serializable transaction. Generated work requires a durable session-scoped attempt key, replays after response loss and rejects changed input. A persisted `claimed → selected` artifact record plus unique attempt ZIP files makes the selected artifact the single retry source: a failing concurrent attachment cannot delete or replace the winner, while pre-selection failure removes only its own file. `f414/svton/f414-second-review-final-mysql-integration-20260804-131325.log` records 6 suites/13 executed tests on a freshly migrated disposable MySQL 8 database; `f414/svton/f414-second-review-final-mysql-invariants-20260804-131410.log` proves the 10-column claim schema/FK, selected/Project SHA equality, READY, 1+1 baselines, two R1 revisions, one finalize audit and zero residual claimed rows. Final focused verification passed API 9 suites/19 tests, Web 1 file/3 tests, Prisma validation/generation, both type-checks and both production builds. `f414/svton/f414-second-review-final-complete-import-graph-20260804-130858.log` traverses all 1,130 API source TS files and 3,871 relative-import edges, records 10 unrelated pre-existing cycles separately, and reports zero F414-affected cycles or forbidden direction violations.
- F415 repository-intake contract evidence: repository connection now produces typed components, dependency/resource requirements and immutable review snapshots instead of exposing raw parser payloads. Private-repository credentials remain request-scoped and are represented only by managed-reference metadata; retries preserve the pinned commit while finalization binds the exact snapshot and input hash. Real Git fixtures covered API/Web/Worker detection and repository immutability; disposable MySQL integration covered snapshot idempotency, recovery and finalization convergence; runtime browser evidence covered failure/retry, dependency blocking and recovery, frozen-review refresh/back behavior, English localization and finalization success. Independent review rejected baseline `c53363ce` for permitting legacy generic apply after snapshot creation. The corrective implementation serializes both apply paths on the analysis-run row, rejects that mutation before any platform/suggestion/connection/audit write, renders snapshot-frozen decisions, and rejects finalization when suggestion/reference/connection state drifts. Deterministic MySQL waiter ordering, exact no-mutation state, and deliberate drift 409 with zero governance writes passed; accepted and rejected logs are indexed in `f415/provenance-manifest.json`. F461 durable multi-replica analysis leasing and F462 Git egress/SSRF hardening are registered as post-parity work only and are not implemented by F415.
- F416 canonical repository identity evidence: finalization now creates one immutable provider/key/URL identity plus an append-only R1 current revision. Explicit high-risk branch revision uses expected-revision CAS and idempotency, retains the exact operator reason in the same-transaction high-risk audit, and updates connection branch/SHA only after real Git verification. Connection, analysis start/retry and BuildRun reservation lock the Project row and reject provider/key/URL/revision ownership drift, duplicate identity and active-analysis races before credential persistence, Git checkout or executor work. Historical BuildRun presentation reads validated v2 provider/URL/revision/branch/SHA from its immutable input snapshot; mutable joined identity data is used only for exact numeric legacy v1/null rows. A second independent review caught unknown, missing and malformed snapshot versions borrowing mutable joins; the bounded follow-up now returns no joined repository provenance for those shapes. Independent correction added an audit-write rollback proof and a real pre-F416 MySQL upgrade split: only aligned connected+verified identities with equal non-null branch fields, conservative canonical URL equivalence and non-zero 40/64-hex commit receive R1; all unverifiable/drift fixtures retain a null current revision. The earlier Browser audit screenshot without a reason is superseded by the corrected runtime audit JSON. Existing real Git/Browser locked replacement, exact-R2 BuildRun and zero-secret evidence remains valid. F463 is registered for inventory, collision stop and audited idempotent remediation only; it is not implemented by F416.

- F422 completion evidence: the release-order detail shell renders the frozen connected four-step visual contract with official Phosphor icons, truthful lifecycle/evidence summaries, distinct viewed/resume/latest phases, complete tab keyboard semantics and canonical singular `step=build` routing. Collision-safe project+order identities, per-channel generation invalidation, response-ownership checks and a keyed detail subtree close A→B stale-state boundaries; accepted POST mutations also invalidate every older same-scope list generation before publishing their BuildRun. The deterministic deferred L1→POST→L1 reproduction and full 9-file/61-test suite pass. Root-run authenticated Browser accepted all 10 cases, including canonicalization, cross-scope fail-closed behavior, keyboard/ARIA semantics and the 820/821 px responsive boundary; same-input Product Design comparisons passed against the frozen Demo. Correction2 and final independent reviews both report P0/P1/P2/P3=0/0/0/0 with self-hashed evidence artifacts. F422 is `done`; F423 action policy, F424 gate counts and F429 aggregation remain outside its scope.
- F423 completion evidence: one release-detail-owned Build controller now supplies both the primary header action and the Build step, so the body has no duplicate action and synchronous duplicate activation is rejected before the first await. The accepted request uses no version, Commit, environment or note input; the server's existing Build reservation path resolves the current default-branch ref on every invocation. `detail.counts.releaseRuns > 0` is the scoped frozen-Production fact: the header shows the frozen badge, native-disabled action, title and `aria-describedby` reason while the immutable ReleaseRun snapshot remains untouched. Authenticated Browser at 1484x1324 proved one enabled header action, one no-body POST after double click, canonical `step=build`, no modal and zero frozen POST; 820x1324 retained one action with no horizontal overflow. Same-state Product Design comparisons and a clean Browser console passed. Root final gates passed 4 focused files/19 tests, TypeScript, scoped ESLint/Prettier, 2,971-message i18n parity, production build and diff check; the independent review additionally passed 9 files/64 tests and reported P0/P1/P2/P3=0/0/0/0. F423 is `done`; F424 gate counts, F425 gate enforcement and F429 aggregation remain outside its scope.
- F424 completion evidence: Preflight now uses the persisted release-gate catalog as its only first-screen fact source: a frozen-structure callout, `15 组 / 51 项 / 检查结论`, and four representative source/CI, impact, Secret/security and configuration/resource summaries replace the three generic baseline cards without adding another Build action. The presentation model validates all 51 ordered IDs, 10/11/20/10 phases, 39 MVP plus 12 Target ownership, M01-M15 uniqueness, summary counts and evaluated-to-persisted status mapping before the nine MVP Commit checks can produce a positive Build conclusion; C04 remains Target-only and any missing, duplicate, foreign or malformed fact fails closed. The full 760px named Modal keeps all 15 derived group states and every C01-P10 row with status, Provider, reason, evidence, checked and expiry fields, while scoped request generations reject cross-order, stale-success and stale-error publication. Root gates passed 4 focused files/17 tests, 7 Web regression files/31 tests, 5 API suites/19 tests, the real-MySQL authenticated fixture's 11 tests, TypeScript, scoped ESLint/Prettier, 3,005-message i18n parity and production builds. Authenticated Browser proved one real 200 gate request, zero refetch on disclosure, 51 unique ordered rows, accessible Escape/focus restoration, zero console errors/warnings, no overflow and the exact 1484 plus 821/820 `3/2/4 → 1/1/2` responsive transition; the frozen/reference same-input comparison passed. The independent final review passed 8 files/33 tests and reported P0/P1/P2/P3=0/0/0/0. Evidence is bound in `f424/runtime/browser-evidence.json`. F424 is `done`; F425 service-side enforcement and F429 aggregation remain outside its scope.
- F425 completion evidence: one canonical decision model now derives the exact Build 9, Staging 5 and Production 19 MVP technical-gate sets from the catalog; missing, stale, closed or malformed Provider facts fail closed, warning remains non-blocking, P03 remains business evidence only, and persisted manual confirmation can satisfy only the same-input canonical manual definition. Append-only `ReleaseGateDecision` rows bind actor, exact target/action input, full outcome and evaluation snapshot; request-key replay rejects actor/input/outcome drift, while BuildRun, Staging DeploymentRun and Production reservation/final version advancement claim the exact allowed decision inside their write transactions. Production evidence is bound to the exact Production environment/config/Manifest/ReleaseRun/current DeploymentRun, with old attempts suppressed and only D17/D20 explicitly deferred before execution; the final decision requires real current-run evidence. Catalog preview and Build execution share one exact source target, including a deterministic unavailable marker that cannot fall back to an historical BuildRun. Authenticated Browser proved a real Gate 200 with 8 blockers, an actual Build 422 alert, BuildRun count 0 and two actor-bound decisions with identical inputHash and blocker IDs. Root verification passed 11 suites/51 focused API tests, 7 suites/25 real-MySQL tests, 3 files/18 Web tests, API/Web TypeScript, changed-file ESLint/Prettier, Prisma validation, production builds, source line ceilings, `git diff --check` and a synced CodeGraph caller/impact audit; the final independent rereview reported P0/P1/P2/P3=0/0/0/0. F425 is `done` and is succeeded by F426; F429 aggregation remains outside this slice.

- F426 completion evidence: base env/compose remains explicit `false/disabled`; only the inert-until-selected `v13-acceptance` overlay enables the double-consent `controlled-local-v1` profile, dedicated work/artifact volume, bounded six-key child environment, whole-run and command deadlines, process-group TERM-to-KILL cancellation, single-process max concurrency and startup recovery. BuildRun snapshot V3 freezes the non-sensitive runtime descriptor inside the complete input hash; transactional revision allocation and terminal compare-and-set give cancel/timeout/success one durable winner, and artifact streaming/hash cleanup observes abort without leaving partial output. Runtime evidence honestly records `untrustedSandbox: false`, the disposable API-container trust boundary and shared-process/shared-network limitations. Root verification passed 14 focused suites/44 tests, 4 real-MySQL suites/11 tests, 4 hardened Docker executor tests, one compiled-image authenticated JWT/Authz/Git/MySQL/HTTP Build-cancel composition, current compiled API health/startup, TypeScript, ESLint, Prettier, build, source line ceilings, compose render, diff check and a synced CodeGraph mapping of all 18 affected tests. Evidence manifest `/tmp/codex-tool-runs/svton/f426/evidence-manifest.json` has SHA-256 `9666e60e6a57aab669b3da7f85f95eead8ec261a444d1751ae630121068a8f4d`; independent final review passed P0/P1/P2/P3=0/0/0/0 with result self-hash `db07f8e878a04e4aaeac9ea3738d48216eba5a4039b68803ae8ce2dc9d3bf045`. F426 is `done`; F427 artifact-contract work is next. General-purpose untrusted-code sandboxing, multi-replica scheduling, F425+F426 production admission, fresh remote dependency download, push/PR/merge and production enablement remain outside this slice.

- F427 completion evidence: Build input snapshot V4 binds stable repository/runtime/gate inputs, declared component outputs and explicit public build values without one-use gate decision IDs. Packaging publishes one create-once aggregate ZIP plus deterministic component ZIPs and per-file content indexes; every Manifest item binds URI, Digest, source Commit, inputHash and immutable provenance. Collection rejects repository-wide fallback, output overlap, traversal, symlinks, special files, `.env`/credential names, token/private-key content and structured JSON/YAML secret keys. Staging and Production resolve the same Manifest, while baked public values produce a different environment fingerprint and archive Digest. Manifest publication and Build reservation share ReleaseOrder-to-Project lock order; real-MySQL same-input and reserve-versus-publish concurrency tests close reproducibility/deadlock boundaries. Root verification passed 18 focused suites/67 tests, 4 repository suites/33 tests, 7 real-MySQL suites/19 tests, 3 hardened Docker suites/9 tests, authenticated HTTP success/replay/cancel 1 suite/2 tests, API build/type/lint, changed-file formatting, diff check, source ceilings and CodeGraph mapping of all 13 affected tests. Evidence manifest `/tmp/codex-tool-runs/svton/f427/evidence-manifest.json` has SHA-256 `25b9eb43f22e16b81456a6215736d20adb8921fd2d6c416c5615e5b80bf4f591`; independent correction review passed P0/P1/P2/P3=0/0/0/0 with result self-hash `7338b3b24408a81aa0601493ee6a67b5a4df8007a756c40de05f93b29894924a`. F427 is `done`; F428 Build history/log UI is next. The executor remains controlled-local for trusted disposable fixtures, not an untrusted-code sandbox; existing pre-F427 project build configs require re-review/re-application to gain declared `artifactPaths`; legacy single-ZIP reads remain compatibility-only. Push, PR, merge and production enablement remain outside this slice.
- F428 code/runtime-ready evidence: Build history is a private/no-store lightweight read model, bounded to 50 only on the Build page while Staging/Production retain the complete lightweight Manifest candidate set. Exact team/project/order/BuildRun detail supplies the right-side Drawer; deep-link loading, retryable failure, explicit 404 normalization and stale-response ownership are separate states. Six Demo-aligned columns keep Build ID primary; queued/canceled/running/failed/succeeded tones are explicit. Every success, command failure, cancellation and startup recovery stores an exact log reference plus pinned terminal result; the presenter joins and re-sanitizes legacy lines and error messages, including PEM, YAML block/indentless list and inline structured secrets. Final current-tree checks pass API release-delivery 49 suites/194 tests, Web 43 files/178 tests, real MySQL runtime 6/6 plus build integration 3/3, authenticated HTTP 3/3, both production builds/type-checks/lints, 3,028-message i18n parity, Prettier/diff/source ceilings and CodeGraph 19 affected tests. Source/runtime independent review has no remaining P0/P1/P2 finding. A real authenticated Browser run exposed a scope-identity bug: the build step compared a two-part scope against the four-part hook scope, so the table never rendered; fixed at `92575cc2` and the table now renders all state rows. F428 authenticated Browser evidence (worker f428-browser-evidence-resume) is complete at commit `92575cc2`: the six Demo-aligned columns render all four states (R1 succeeded + Manifest, R2 failed, R3 running, R4 canceled), every row has an independent 日志 action, the right-side Drawer on succeeded `cmshhebl10014vwrcsqghxnpa` shows exact metadata + sanitized logs, the `&buildRunId=` deep-link survives refresh, invalid buildRunId fails closed, repeated visits leave stored logs (logSummary SHA-256 `19c985e668fdb9547c6ac94638f2d5bc559e48b1e093121845d677d1c459e7ca`) and the single Manifest unchanged, and 390x844 shows no page overflow (table scrolls in overflow-x-auto). Only console error is the platform-wide missing `/favicon.ico` (0 unexpected). Evidence manifest `/tmp/codex-tool-runs/svton/f428/f428-browser-evidence.json` (SHA-256 `28c3a5089dd1214539480ceb976778ce8b789f9dc1969b13b5fdd05c498c35e8`) and DB log `/tmp/codex-tool-runs/svton/f428/browser/f428-db-evidence.log`; AC-BUILD-019～025 all PASS. The 2026-08-05 blocker record at `/tmp/codex-tool-runs/svton/f428/browser-evidence-final.json` is superseded. F428 is `done`; F429 copy semantics is next. Push, PR, merge and production enablement remain outside this slice.
- F429 completion evidence: one private/no-store release-order aggregate now owns exact BuildRun/Manifest evidence, repeated Staging DeploymentRuns and governed Production ReleaseRuns with every nested Production DeploymentRun. Parent candidates are ownership-filtered before bounded presentation; nested Production history remains complete. Production proof is the exact immutable approval snapshot and rejects dry-run, environment id/team/project/baseline-role, result, Manifest, digest or input drift. Web stale-response guards bind actor/team/project/order across candidate and aggregate reads; Build Manifest items remain nested under the exact BuildRun. Canonical `view=deployments&runId` links and the legacy redirect preserve the professional read-only deployment surface without restoring artifact/deployment as project-primary navigation. Independent final rereview passed 9 API suites/37 tests including real MySQL and authenticated HTTP, 13 Web files/61 tests, both type-checks, scoped format/diff/source ceilings, and reported P0/P1/P2/P3=0/0/0/0. Root final gates then passed Web 50 files/196 tests, both production builds/type-checks/lints, 3,031-message i18n parity and scoped Prettier. A fresh in-app Browser at 1484x1324 visibly proved the exact Build/Manifest, two Staging, one governed Production with two nested deployments, two professional links, legacy redirect, API 200/foreign 404 with `private, no-store` and `Vary: Authorization, X-Team-Id, Cookie`, and 0 console errors/warnings. Browser evidence is self-hashed at `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/workers/f429-browser-acceptance-root-retry-evidence.json` (`df71895202692a0baf6b76c6d20fa62b7cd88affa9b0733cacc2c0ff49998d08`). F429 is `done`; F430 copy semantics is next. F428's separate AC-BUILD-019～025 Browser matrix remains open. Push, PR, merge and production enablement remain outside this slice.
- F430 completion evidence: one centralized release-copy presenter now maps release-order, execution, ReleaseRun, approval, deployment-stage, risk, environment-version-kind and environment-role values to typed locale keys, with unknown inputs failing closed to localized unknown labels instead of raw codes. User-visible objects consistently use 发布单, 发布版本号 and 环境版本; professional evidence preserves exact BuildRun/Manifest/DeploymentRun/ReleaseRun terms; Staging/Production are rendered as 预发（Staging）/生产（Production） in zh and normal title case in en. Stable client error keys replace hardcoded scope-mismatch copy. Root final gates passed Web type-check, 3,060-message zh/en key plus ICU parity, 54 files/205 tests, lint with 0 errors, production build, Prettier, diff check, copy-contract and source ceilings; CodeGraph mapped all 31 changed TS/TSX files to all 9 changed tests. Fresh authenticated in-app Browser evidence proved zh→en→zh runtime switching on the same release entities, exact professional IDs and localized statuses/roles, no uppercase/raw Production regression, and 0 console errors/warnings across 7 hash-bound screenshots. The independent final review reported P0/P1/P2/P3=0/0/0/0 and AC-COPY-001～008 all PASS; evidence selfHash is `8f433ab852de9efb49b92d95a4a46d719843414cf1660f015e4ba5a0e302380c`. F430 is `done`; F431 exact-Manifest Deployment Provider is next. F428's separate AC-BUILD-019～025 Browser matrix remains open. Push, PR, merge and production enablement remain outside this slice.
- F431 completion evidence: one exact-Manifest Deployment Provider port now separates server-side artifact resolution/Digest recomputation from target execution. The default-off configured provider selects either an atomic filesystem release target or `ssh-v1`; the SSH adapter uploads the exact ZIP through SFTP, recomputes SHA-256 on the isolated target, verifies the archive, atomically activates a run-specific release plus `active.json`, cleans partial activation, and emits provider/target/receipt evidence without invoking Git, checkout, pull or build. Staging reasserts Manifest plus related BuildRun team/project/order/succeeded scope, freezes provider identity and target in each fresh DeploymentRun, and rejects unknown, foreign, failed and scope-drift inputs before creating a run. Root verification passed 6 focused suites/17 tests, real-MySQL 2/2, real-SSH 1/1, authenticated JWT/Authz/MySQL/artifact-storage/SSH HTTP composition 1/1 including two deployments after the Git source was removed and six negative requests, API type-check/build, changed-file lint with 0 warnings/errors, repository-wide lint with 0 errors, Compose render, Prisma validation, Prettier, diff check, source ceilings and CodeGraph coverage of all 9 affected tests. The full API suite passed 258 suites/1,527 tests and retains one unrelated seven-test `repository-analysis-run.service.spec.ts` stale-harness baseline failure that reproduces standalone with no F431 diff. The correction independent review reported READY, P0/P1/P2/P3=0/0/0/0 and AC-STG-001～006 all PASS; evidence selfHash is `2f566954a4ca99c07ae6e40c04a1cf6aef2c45b04289f127c8328a4d32168251`. Evidence manifest `/tmp/codex-tool-runs/svton/f431/evidence-manifest.json` has SHA-256 `1a1f102126b505782f8861345d5c5b05726ac4386735e9059de13abb7ab87e13`. F431 is `done`; F432 environment configuration, ordinary-variable, managed-Secret-reference and resource-connection injection is next. Workload startup/health remains F433; SSH host-key pinning remains hardening before external production use. F428's separate AC-BUILD-019～025 Browser matrix remains open. Push, PR, merge and production enablement remain outside this slice.

- F432 completion evidence: every Staging request now passes the existing gate before reading managed input, then resolves the current environment's immutable configuration revision, ordinary variables, scoped Secret references, environment-bound resource connection data and exactly one active deployment target binding. DeploymentRun persists only a secret-safe snapshot: revision ID/revision/snapshot hash/content-state hash, variable keys, Secret/resource ciphertext-version hashes, resource environment scope, target binding/server/provider/target/version hash and one canonical input hash; decrypted values and SSH credentials remain ephemeral. The reservation transaction locks the environment, reasserts team/project/environment scope and recomputes the full input hash before creating the run, so configuration, Secret, resource or target drift produces zero Provider calls. Filesystem and SSH targets receive only an exact-Manifest release plus a 0600 `.devpilot/runtime.env`; SSH SFTP creates the incoming plaintext file at 0600, the activation script independently verifies that mode, and failure cleanup removes incoming archive/runtime files. Root verification passed all 8 CodeGraph-affected suites/33 tests under real MySQL, isolated SSH and authenticated JWT/Authz/MySQL/artifact-storage/SSH HTTP composition, including a deliberately unreachable global SSH configuration overridden by the active environment binding, foreign binding rejection before any decrypt, three concurrent drift classes and DB/API/provider/process-log Secret sentinel scans. API type-check/build, changed-file lint 0/0, Prisma validation, Prettier, diff check and source ceilings passed; full API passed 260 suites/1,542 tests with only the independently reproduced unrelated seven-test `repository-analysis-run.service.spec.ts` stale harness failure. Independent final review reported READY, P0/P1/P2/P3=0/0/0/0 and AC-STG-007～013 all PASS; review selfHash is `95c9d2b49e4207a1fcbaa460eb843c17be9da0d3745834c2af9769e5f747e586`. Evidence manifest `/tmp/codex-tool-runs/svton/f432/evidence-manifest.json` has SHA-256 `deb1ae1eacdd0becfdcfb9f8b9113a0b5662bda37a01650dc04b535d472702d4`. F432 is `done`; F433 workload startup and service-level health is next. F437 Production frozen-input consumption, SSH host-key pinning, F428's separate AC-BUILD-019～025 Browser matrix, push/PR/merge and external production enablement remain outside this slice.

- F433 completion evidence: Staging freezes every active service against its exact Manifest component and permits only artifact-local executables, explicit script runtimes, `java -jar`, bounded file tests and no-op cleanup; source, download, package-install, shell interpolation, external-path and execution-control environment overrides fail before Provider execution. Filesystem and SSH adapters materialize protected runtime control files, run one shared managed-process/managed-command lifecycle, expose process/status evidence, probe only target-loopback HTTP endpoints and restore the previous workload after a failed activation. Terminal compare-and-set makes lost completion acknowledgements idempotent while conflicting terminal outcomes fail closed; only a successful provider result writes the Staging EnvironmentVersion/current pointer, and failed health retains sanitized diagnostics without advancing it. Root verification passed 61 release-delivery suites/266 tests, real MySQL staging/health/completion 10/10, real SSH exact-Manifest lifecycle/restoration 2/2, authenticated JWT/Authz/MySQL/build-artifact/SSH HTTP composition 1/1, API type/build, changed lint, Prisma, diff, source ceilings and all 13 CodeGraph-affected tests. Full API passed 265 suites/1,579 tests with only the independently reproduced unrelated seven-test `repository-analysis-run.service.spec.ts` stale harness failure; independent final review reported READY, P0/P1/P2/P3=0/0/0/0 and AC-STG-014～020 all PASS with selfHash `b00cbaf5e633426fc473e01f009591b78e312ef504691d9e92362400fb2f8b57`. Evidence manifest `/tmp/codex-tool-runs/svton/f433/evidence-manifest.json` has SHA-256 `63508a677f61675daffa041985029e39dbaaa92db4e2cc3d89c49df63eabaae1`. F433 is `done`; F434 Staging Web deployment action and per-run evidence/log drawer is next. F437 Production execution, SSH host-key pinning, F428's separate AC-BUILD-019～025 Browser matrix, push/PR/merge and external production enablement remain outside this slice.

- F434 completion evidence: Staging Web now renders one selected BuildRun/revision/Manifest summary, a cumulative DeploymentRun total and a Production-prerequisite summary card, plus a six-column Demo-aligned history table (部署运行 / 构建·Manifest / 执行结果 / 验证结论 / 耗时·时间 / 操作) with the canonical 部署 row action posting the exact persisted artifactManifestId. Each run opens a right-side per-run evidence/log Drawer (structured metadata, separate technical-deployment vs non-blocking business-validation conclusions, failure diagnostics, sanitized logs) preserved as a `focusedDeploymentRunId` deep-link; row actions carry run/Manifest-specific localized accessible names and the first cell is a scoped `<th scope="row">`. The Staging hook polls while runs are active, exposes loaded/error ownership, and never invokes a Build action. Browser evidence v2 (`/tmp/codex-tool-runs/svton/f434/f434-browser-evidence-v2.json`) proves the full five-state matrix (blocked/running/completed/failed plus empty no-Manifest order), 390x844 responsive containment, zero console errors/warnings, and a frozen 1484x1324 Demo same-state comparison (`f434-demo-visual-comparison.md`) with exact six-header/summary alignment; DB evidence proves a row redeploy grew DeploymentRun count 3->4 while BuildRun count stayed 1. Root gates passed focused Web tests 5 files/20 tests, full Web build/type-check, API type-check, i18n parity, diff check and source ceilings. Independent v2 review reported READY with P0/P1/P2=0 (four P3 notes: historical v1 seed hash, unpaginated history, no narrow-table scroll cue, no live screen-reader run) and AC-STG-021～027 all PASS; report `/tmp/codex-tool-runs/svton/long-goals/devpilot-v13-demo-parity/workers/f434-independent-v2-review-report.md`. F434 is `done`; F435 Production snapshot-confirmation Dialog is next. F437 Production execution, F428's separate AC-BUILD-019～025 Browser matrix, push/PR/merge and external production enablement remain outside this slice.

- F435 completion evidence: the Production step replaces its inline checkbox + snapshot `<dl>` with an always-open confirmation Dialog (`release-production-confirm-dialog.tsx`) triggered by the `requestProductionApproval` button; the button exists only in the Production step, stays disabled until a proven Manifest snapshot is available (web-disable still driven by `provenManifestIds` from completed Staging DeploymentRuns), and clicking it always opens the Dialog. The Dialog shows a warning callout, target environment (localized name + key/role), release version, reused artifact (BuildRun id + `R{revision}`, Manifest id + digest) with the non-blocking staging-proven note ("该制品已在 Staging 成功部署；业务验证结果不作为当前生产阻断条件。"), Build/commit (revision · sourceBranch · sourceCommitSha), production config snapshot (revision · snapshotHash plus bounded resource/route counts), and release policy (revision/synthetic · 标准发布 · snapshotHash); Cancel/close creates nothing, and the primary confirm calls `production.confirm()` once, stays open on failure showing the error, and closes on success relying on `onChanged`. Root gates passed focused Web tests (release-order-production-step.spec.tsx 6 + release-order-candidate-sources.spec.tsx 3 = 9), full Web suite 57 files/222 tests, Web type-check, Web build, i18n parity (3107 messages), Prettier and diff check; both changed source files stay under 200 lines (step 151, dialog 116). AC-PROD-001..003 are checked in the acceptance file; AC-PROD-004..009 are now checked with authenticated 1484x1324 CDP Browser evidence (`/tmp/codex-tool-runs/svton/f435/f435-browser-evidence.json`, artifacts `/tmp/codex-tool-runs/svton/f435/browser/`) on commit `bfbfc805`: dialog opens with role=dialog/aria-modal + all six frozen-input fields (environment, 4.34.0, BuildRun+Manifest/digest, R1·main·commit, config revision + 1 resource/0 routes, standard policy), Cancel leaves ReleaseRun/OperationApproval at 0/0, Confirm creates exactly one ReleaseRun (awaiting_approval) + one pending approval bound by the same immutable inputHash, and the 390x844 viewport shows no horizontal overflow. Console had 0 unexpected errors (single 404 per phase is /favicon.ico); DB proof in `f435-db-evidence.log`. A staging-only F434 fixture gap required seeding one active Production baseline environment + config revision as fixture data. F435 is `done`; F436 in-project production approval is next.

- F436 completion evidence: the Production step now renders a project-context approval card (`release-production-approval-card.tsx` + `use-production-approval.ts`) for the latest/focused ReleaseRun: pending shows the frozen approval summary (生产发布 4.34.0 / Build #1), 高风险 tag and 批准/拒绝 actions with a required-comment reject modal (RejectReasonModal); approved shows reviewer/reviewedAt/reviewComment and the primary 执行生产发布 action that POSTs `/projects/:projectId/delivery/environment-versions/:environmentId/actions` `{kind:'upgrade', manifestId, releaseRunId}` with an in-flight double-submit guard, then reloads evidence via onChanged. The evidence read model now carries the approval reviewer/reviewerId/risk/summary/reviewComment/consumedAt/expiresAt (`release-order-evidence-production.repository.ts` + presenter + web type). The global `/operation-approvals` module is preserved, localizes the release category/action (生产发布 · 执行生产发布), renders the release 执行已批准 button disabled with the explicit 请在项目发布上下文执行 reason, and supports `?id=<approvalId>` deep link that auto-switches to the all-status view and focuses the matching card (`ring-2 ring-primary` + `aria-current`). Server invariants are proven by focused tests: `environment-version-execution-policy.integration.spec.ts` (real MySQL) proves reject cannot execute, rejected/expired/consumed/input-drift approvals throw on execute, and concurrent execute converges to exactly one Production DeploymentRun (the pre-existing broken executor test double was fixed via `release-staging-executor.spec-utils.ts`); `operation-approval.service.spec.ts` proves review approve/reject, non-pending rejection, and `resolveApproved` fail-closed negatives; `operation-approval.controller.spec.ts` proves the review route is guarded `@Roles('team_admin')` (non-admin approve = 403). Focused API tests 23/23, focused Web tests 4 files/19 tests (hook double-submit guard, card render/approve/reject-comment-required/execute, production-step card wiring), full Web suite 59 files, API+Web type-check, API+Web build, i18n parity (3120 messages), Prettier and diff check all pass. AC-PROD-010..016 are checked with authenticated 1484x1324 CDP Browser evidence (`/tmp/codex-tool-runs/svton/f436/f436-browser-evidence.json`, artifacts `/tmp/codex-tool-runs/svton/f436/browser/`) on the F436 tree: pending card -> 批准 -> approved (审批人/审批时间) -> project-context 执行生产发布 click hits the real server and the production deploy-gate denial (422 production 门禁未满足) surfaces in the card -> global /operation-approvals shows the same approved state with localized release labels and the disabled global execute -> `?id=cmshlhmo4001jw80j2wjsshbu` auto-selects 全部 and focuses the card. The execute does not create a DeploymentRun because the real production deploy-gate service denies admission (D06/D09 capability providers return hardcoded `unavailable` and the fixture has no production deploy evidence); those production-execution gate semantics are the F437 slice (AC-PROD-017~024) and were NOT changed by F436, which proves the execute-after-approval path at the integration-test level instead. F436 is `done`; F437 Production execution is next.

## Next

Begin F442 EnvironmentVersion candidate filtering: per-environment upgrade candidates (same project, succeeded, traceable Manifest — AC-ENVVER-007), Production only lists candidates with same-Manifest Staging proof (AC-ENVVER-008), rollback lists only that environment's historical successful versions with the last successful version as the default recommendation (AC-ENVVER-009/010), and the web select accepts no arbitrary text version or unknown image. Keep the F441 Demo-aligned env-version cards, the exact-current re-validation (fail closed) and the F439 recovery contract intact. AC-PROD-029 (browser loads the FINAL site URL page) remains open from F438 because the demo environment cannot serve the fixture final domain; it is re-checkable when a resolvable/serving final domain is available. Retry the separate current-tree F428 authenticated Browser matrix before checking AC-BUILD-019～025 or marking F428 done. F461-F463 remain registered post-parity hardening/migration work only. Push, PR creation, merge and external production deployment remain outside the current authorization.
