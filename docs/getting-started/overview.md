# 项目概览

> Svton 是一个基于 TypeScript 全栈技术的社区平台项目，采用 Monorepo 架构统一管理前后端代码。

---

## 🎯 项目定位

**社区助手** 是一个面向社区居民的生活互助平台，包含：

- **移动端小程序** - 居民使用入口
- **管理后台** - 运营管理入口
- **后端 API** - 统一数据服务

---

## 🏗️ 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户层                                    │
│  ┌─────────────────┐           ┌─────────────────┐              │
│  │   微信小程序     │           │   管理后台       │              │
│  │   (Taro React)  │           │   (Next.js)     │              │
│  └────────┬────────┘           └────────┬────────┘              │
└───────────┼─────────────────────────────┼───────────────────────┘
            │         HTTP/REST           │
            └──────────────┬──────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        服务层                                    │
│  ┌─────────────────────────────────────────────────────┐        │
│  │               NestJS Backend API                     │        │
│  │  ┌──────────┬──────────┬──────────┬──────────┐      │        │
│  │  │  Auth    │  Content │  User    │  Upload  │ ...  │        │
│  │  │  Module  │  Module  │  Module  │  Module  │      │        │
│  │  └──────────┴──────────┴──────────┴──────────┘      │        │
│  └─────────────────────────────────────────────────────┘        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────┼─────────────────────────────────────┐
│                        数据层                                    │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐          │
│  │   MySQL     │    │   Redis     │    │ 腾讯云 COS   │          │
│  │   (Prisma)  │    │   (Cache)   │    │ (文件存储)   │          │
│  └─────────────┘    └─────────────┘    └─────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 项目结构

```
community-next/
├── apps/                           # 应用程序
│   ├── backend/                    # 🔧 NestJS 后端 API
│   │   ├── src/
│   │   │   ├── modules/            # 业务模块
│   │   │   ├── common/             # 公共模块
│   │   │   └── prisma/             # 数据库服务
│   │   └── prisma/                 # Prisma Schema 和迁移
│   │
│   ├── admin/                      # 💻 Next.js 管理后台
│   │   └── src/
│   │       ├── app/                # App Router 页面
│   │       ├── components/         # UI 组件
│   │       └── lib/                # 工具库
│   │
│   └── mobile/                     # 📱 Taro 移动端
│       └── src/
│           ├── pages/              # 小程序页面
│           ├── components/         # 业务组件
│           └── store/              # 状态管理
│
├── packages/                       # 共享包
│   ├── types/                      # 📝 类型定义
│   ├── api-client/                 # 🔌 API 客户端
│   ├── hooks/                      # 🎣 React Hooks
│   └── taro-ui/                    # 🎨 Taro UI 组件
│
├── docs/                           # 📚 项目文档
├── scripts/                        # 🛠️ 脚本工具
└── sql/                            # 💾 SQL 脚本
```

---

## 🛠️ 技术栈详情

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **NestJS** | ^10.3.0 | Node.js 企业级框架 |
| **Prisma** | ^5.7.0 | 类型安全的 ORM |
| **MySQL** | 8.0 | 关系型数据库 |
| **Redis** | 7 | 缓存和会话存储 |
| **JWT** | ^10.2.0 | 身份认证 |
| **Swagger** | ^7.1.17 | API 文档 |

### 前端技术栈 (Admin)

| 技术 | 版本 | 用途 |
|------|------|------|
| **Next.js** | ^15.5.0 | React 全栈框架 |
| **React** | ^19.0.0 | UI 库 |
| **TailwindCSS** | ^3.4.0 | 原子化 CSS |
| **Radix UI** | latest | 无障碍组件库 |
| **SWR** | ^2.2.5 | 数据请求 |
| **Zustand** | ^5.0.2 | 状态管理 |

### 移动端技术栈 (Mobile)

| 技术 | 版本 | 用途 |
|------|------|------|
| **Taro** | 3.6.23 | 跨端框架 |
| **React** | ^18.2.0 | UI 库 |
| **Zustand** | ^4.4.7 | 状态管理 |
| **SCSS** | - | 样式预处理 |

### 工程化工具

| 技术 | 版本 | 用途 |
|------|------|------|
| **pnpm** | ^8.12.0 | 包管理器 |
| **Turborepo** | ^1.11.0 | Monorepo 构建 |
| **TypeScript** | ^5.3.0 | 类型系统 |
| **ESLint** | ^8.55.0 | 代码检查 |
| **Prettier** | ^3.1.0 | 代码格式化 |

---

## 🔄 数据流

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│    Client    │────▶│   API Call   │────▶│   Backend    │
│  (Mobile/    │     │ (@svton/     │     │  (NestJS)    │
│   Admin)     │     │  api-client) │     │              │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                  │
       ┌──────────────────────────────────────────┤
       │                                          │
       ▼                                          ▼
┌──────────────┐                          ┌──────────────┐
│    Redis     │                          │    MySQL     │
│   (Cache)    │                          │  (Prisma)    │
└──────────────┘                          └──────────────┘
```

---

## 🎨 共享包设计

所有共享代码统一使用 `@svton` 命名空间：

```typescript
// 类型定义
import type { ContentVo, UserVo } from '@svton/types';

// API 调用
import { apiAsync, defineApi } from '@svton/api-client';

// React Hooks
import { usePersistFn, useDebounce } from '@svton/hooks';

// Taro UI 组件
import { NavBar, StatusBar, Button } from '@svton/taro-ui';
```

---

## 🚀 快速了解

| 我想要... | 查看文档 |
|----------|---------|
| 快速运行项目 | [快速开始](./quick-start.md) |
| 了解如何开发后端模块 | [模块开发](../backend/modules.md) |
| 了解如何开发 Admin 页面 | [页面开发](../admin/pages.md) |
| 了解如何开发小程序页面 | [页面开发](../mobile/pages.md) |
| 创建新的共享包 | [共享包](../packages/types.md) |
| 部署到生产环境 | [生产部署](../deployment/production.md) |

---

**下一步**: [快速开始](./quick-start.md)
