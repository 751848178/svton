# 2026-07-22 Agent Auto-Reviewer Continuation

Scope: non-Devpilot AI agent stack; current focus ai/agent-core auto-reviewer child-process safety.

Carry-in: A900-A907 are in docs/todos/2026-07-13-agent-audit-repair.md; A907 is the last old-doc slice.

Rules: one A-item per slice; every row must include runtime evidence, pre-fix/current-dist mismatch, implementation summary, verification logs, and residual guard behavior.

| ID | Status | Area | Requirement | Evidence / implementation / verification |
| --- | --- | --- | --- | --- |
| A908 | done | Core E2E | Detect Python static string `+` expressions used in shell strings, direct argv arrays, executable overrides, and `os.spawn*` / `os.exec*` process APIs, so `"rm" + " -rf /"` and `["r" + "m", "-r" + "f", "/"]` deny instead of slipping through as user-reviewable. | Runtime probe `python-static-plus-runtime-a908-20260722-160042.log` confirmed real Python evaluates static `+` into `os.system("rm -rf /")`, `subprocess.run(..., shell=True)`, direct argv arrays, `executable="rm"`, `os.spawnlp`, and `os.execvp`, while `rm --version` remains non-dangerous. Pre-fix current-dist probe `python-static-plus-dist-before-a908-20260722-160042.log` reproduced 6 mismatches where dangerous plus-built forms returned `ask_user`; safe `--version`, future-reference, and dynamic-plus guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-static-plus-a908-focused-red-2-20260722-160247.log` (2 failing tests), then passed after `python-string-literal.utils.ts` was extended to parse static Python string `+` chains while rejecting unresolved operands and mixed bytes/text chains. Tests now cover dangerous shell/direct/executable/spawn plus forms and residual guards for `--version`, future references, and dynamic values. Verification passed in `agent-core-python-static-plus-a908-focused-rerun-20260722-160336.log` (2 files / 4 tests), `agent-core-python-static-plus-a908-related-20260722-160403.log` (5 files / 10 tests), `agent-core-build-a908-20260722-160403.log`, `python-static-plus-dist-fixed-a908-20260722-160430.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a908-rerun-20260722-160452.log` (230 files / 706 tests), and `agent-core-full-test-a908-20260722-160508.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-string-literal.utils.ts` 100, `python-static-string.utils.ts` 65, `python-static-argv.utils.ts` 110, `python-direct-command.utils.ts` 145, `python-shell-command.utils.ts` 136. |
| A909 | done | Core E2E | Detect Ruby/Perl static string concatenation in interpreter shell and direct process APIs, so Ruby `system("rm" + " -rf /")`, `Process.spawn("r" + "m", "-r" + "f", "/")`, Ruby command-array pairs, and Perl `system("rm" . " -rf /")` / `system("r" . "m", "-r" . "f", "/")` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-static-concat-runtime-a909-rerun-20260722-160945.log` confirmed real Ruby evaluates `+` into shell strings, direct argv, `Process.spawn`, and command-array pairs, and real Perl evaluates `.` into shell strings and direct argv; safe `rm --version` concat remains non-dangerous. Pre-fix current-dist probe `ruby-perl-static-concat-dist-before-a909-20260722-160910.log` reproduced 8 mismatches where dangerous Ruby/Perl concat forms returned `ask_user`; safe Ruby/Perl `--version` and dynamic concat guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-static-concat-a909-focused-red-20260722-161044.log` (2 failing tests), then passed after adding `interpreter-static-string.utils.ts` and wiring language-specific operators into Ruby `+` and Perl `.` shell/direct parsers without broadening Python or Node parsing. Tests now cover dangerous Ruby shell/direct/spawn/array-pair concat, dangerous Perl shell/direct concat, and residual guards for safe `--version` and dynamic values; Perl indirect executable concat was not claimed in this runtime-proven slice. Verification passed in `agent-core-ruby-perl-static-concat-a909-focused-final-20260722-161508.log` (2 files / 4 tests), `agent-core-ruby-perl-static-concat-a909-related-final-20260722-161508.log` (5 files / 10 tests), `agent-core-build-a909-final-20260722-161508.log`, `ruby-perl-static-concat-dist-fixed-a909-final-20260722-161525.log` (12 cases / 0 mismatches), `agent-core-auto-reviewer-a909-final-20260722-161525.log` (230 files / 706 tests), and `agent-core-full-test-a909-final-20260722-161525.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 35, `interpreter-literal-list.utils.ts` 71, `interpreter-direct-command.utils.ts` 72, `ruby-command-array-pair.utils.ts` 65, `interpreter-shell-command.utils.ts` 139, and `python-string-literal.utils.ts` 100. |
| A910 | done | Core E2E | Detect Perl indirect executable static concat in direct process APIs, so `system {"r" . "m"} "custom-rm", "-r" . "f", "/"` and parenthesized `exec {("r" . "m")} ...` deny instead of slipping through as user-reviewable. | Runtime probe `perl-indirect-static-concat-runtime-a910-20260722-162007.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Perl launches the concat-built indirect executable with `argc=2 args=-rf /`; literal `{"rm"} ... "--version"` remains non-dangerous. Pre-fix current-dist probe `perl-indirect-static-concat-dist-before-a910-20260722-162007.log` reproduced 2 mismatches where dangerous indirect executable concat forms returned `ask_user`; literal safe and dynamic executable guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-indirect-static-concat-a910-focused-red-20260722-162050.log` (1 failing file / 1 failing test), then passed after `perl-indirect-executable.utils.ts` started reading Perl `.` static strings in the indirect executable block, including one parenthesized static expression, and reused the same static list reader for concat-built args. Tests now cover dangerous indirect executable concat, safe `--version`, and dynamic `$tail` guards. Verification passed in `agent-core-perl-indirect-static-concat-a910-focused-rerun-20260722-162129.log` (1 file / 2 tests), `agent-core-perl-indirect-static-concat-a910-related-20260722-162138.log` (5 files / 10 tests), `agent-core-build-a910-20260722-162138.log`, `perl-indirect-static-concat-dist-fixed-a910-20260722-162204.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a910-20260722-162204.log` (230 files / 706 tests), and `agent-core-full-test-a910-20260722-162204.log` (327 files / 1979 tests). Production source line counts remain under 200: `perl-indirect-executable.utils.ts` 49, `interpreter-static-string.utils.ts` 35, `interpreter-literal-list.utils.ts` 71, `interpreter-direct-command.utils.ts` 72, and `interpreter-shell-command.utils.ts` 139. |
| A911 | done | Core E2E | Detect Ruby `spawn` / `Process.spawn` command-array pairs, so `spawn(["rm", "custom-rm"], "-rf", "/")`, `Process.spawn(["rm", "custom-rm"], "-rf", "/")`, and static-concat `Process.spawn(["r" + "m", "custom-rm"], "-r" + "f", "/")` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-spawn-array-pair-runtime-a911-20260722-162438.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Ruby executes `spawn` and `Process.spawn` array-pair commands with `argc=2 args=-rf /`; `--version` remains non-dangerous. Pre-fix current-dist probe `ruby-spawn-array-pair-dist-before-a911-20260722-162457.log` reproduced 3 mismatches where dangerous array-pair `spawn` forms returned `ask_user`; safe `--version` and dynamic command guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-spawn-array-pair-a911-focused-red-20260722-162714.log` (1 failing file / 1 failing test), then passed after `interpreter-direct-command.utils.ts` wired `DIRECT_RUBY_SPAWN_FUNCTIONS` into the existing Ruby command-array-pair parser with `+` static concat support. Tests now cover dangerous `spawn` / `Process.spawn` array pairs, static concat in the executable and flag, safe `--version`, and dynamic command guards. Verification passed in `agent-core-ruby-spawn-array-pair-a911-focused-green-20260722-162733.log` (1 file / 2 tests), `agent-core-ruby-spawn-array-pair-a911-related-20260722-162808.log` (5 files / 10 tests), `agent-core-build-a911-20260722-162808.log`, `ruby-spawn-array-pair-dist-fixed-a911-20260722-162817.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a911-20260722-162828.log` (230 files / 706 tests), and `agent-core-full-test-a911-20260722-162828.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-direct-command.utils.ts` 73, `ruby-command-array-pair.utils.ts` 65, `interpreter-static-string.utils.ts` 35, `perl-indirect-executable.utils.ts` 49, `python-string-literal.utils.ts` 100, `interpreter-literal-list.utils.ts` 71, and `interpreter-shell-command.utils.ts` 139. |
| A912 | done | Core E2E | Detect Ruby process calls with a leading static environment hash, so `system({"SVTON_A912"=>"system"}, "rm", "-rf", "/")`, `spawn({"SVTON_A912"=>"spawn"}, "rm", "-rf", "/")`, and `Process.spawn({"SVTON_A912"=>"pair"}, ["r" + "m", "custom-rm"], "-r" + "f", "/")` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-env-prefix-runtime-a912-20260722-163217.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Ruby executes `system`, `spawn`, and `Process.spawn` after a leading env hash with `argc=2 args=-rf /`, while `spawn(..., "rm", "--version")` remains non-dangerous. Pre-fix current-dist probe `ruby-env-prefix-dist-before-a912-20260722-163217.log` reproduced 3 mismatches where dangerous leading-env direct/array-pair forms returned `ask_user`; safe `--version` and dynamic `env=get_env()` guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-env-prefix-a912-focused-red-20260722-163318.log` (1 failing file / 1 failing test), then passed after adding `ruby-env-prefix.utils.ts` and wiring its balanced `{...},` skipper into Ruby literal multi-arg and Ruby command-array-pair parsing. Tests now cover dangerous leading static env hash forms, static concat in the array-pair executable and flag, safe `--version`, and dynamic env guard behavior. Verification passed in `agent-core-ruby-env-prefix-a912-focused-green-20260722-163407.log` (1 file / 2 tests), `agent-core-ruby-env-prefix-a912-related-20260722-163432.log` (5 files / 10 tests), `agent-core-build-a912-20260722-163432.log`, `ruby-env-prefix-dist-fixed-a912-20260722-163439.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a912-20260722-163448.log` (230 files / 706 tests), and `agent-core-full-test-a912-20260722-163448.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-direct-command.utils.ts` 81, `ruby-command-array-pair.utils.ts` 66, `ruby-env-prefix.utils.ts` 37, `interpreter-static-string.utils.ts` 35, and `interpreter-literal-list.utils.ts` 71. |
| A913 | done | Core E2E | Detect Python `os.posix_spawn` / `os.posix_spawnp` direct process APIs, so `os.posix_spawn("/bin/rm", ["rm", "-rf", "/"], os.environ)`, `os.posix_spawnp("rm", ["rm", "-rf", "/"], os.environ)`, and static-plus `os.posix_spawnp("r" + "m", ["r" + "m", "-r" + "f", "/"], os.environ)` deny instead of slipping through as user-reviewable. | Runtime probe `python-posix-spawn-runtime-a913-20260722-163828.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Python executes both `posix_spawn` and `posix_spawnp` with `argc=2 args=-rf /`, while `posix_spawnp("rm", ["rm", "--version"], ...)` remains non-dangerous. Pre-fix current-dist probe `python-posix-spawn-dist-before-a913-20260722-163828.log` reproduced 3 mismatches where dangerous `posix_spawn*` forms returned `ask_user`; safe `--version` and dynamic `argv=get_argv()` guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-posix-spawn-a913-focused-red-20260722-163902.log` (1 failing file / 1 failing test), then passed after `python-os-process-command.utils.ts` added `POSIX_SPAWN_FUNCTIONS` and reused the existing command-plus-argv-list parser with static string and static argv support. Tests now cover dangerous absolute/path-search/static-plus forms, safe `--version`, and dynamic argv guard behavior. Verification passed in `agent-core-python-posix-spawn-a913-focused-green-20260722-163923.log` (1 file / 2 tests), `agent-core-python-posix-spawn-a913-related-20260722-163950.log` (5 files / 10 tests), `agent-core-build-a913-20260722-163950.log`, `python-posix-spawn-dist-fixed-a913-20260722-164000.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a913-20260722-164013.log` (230 files / 706 tests), and `agent-core-full-test-a913-20260722-164013.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-os-process-command.utils.ts` 145, `python-direct-command.utils.ts` 145, `python-string-literal.utils.ts` 100, `python-static-argv.utils.ts` 110, and `python-static-string.utils.ts` 65. |
| A914 | done | Core E2E | Detect Node child_process direct/shell commands built from static JS template literal interpolations, so ``spawnSync(`r${"m"}`, [`-r${"f"}`, `/`])``, ``execSync(`rm ${"-rf"} /`)``, and shell-option ``spawnSync(`rm ${"-rf"} /`, {shell:`/bin/${"sh"}`})`` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-template-runtime-a914-20260722-192525.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node evaluates direct static template interpolation into direct argv, `execSync` shell command strings, and shell-option commands with `argc=2 args=-rf /`; static `--version` interpolation remains non-dangerous. Pre-fix current-dist probe `node-static-template-dist-before-a914-20260722-192525.log` reproduced 3 mismatches where dangerous static-template forms returned `ask_user`; safe `--version` and existing identifier interpolation guard `${flag}` stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-template-a914-focused-red-20260722-192612.log` (3 failing files / 3 failing tests), then passed after adding `javascript-template-literal.utils.ts` and wiring it into `javascript-static-string.utils.ts`; the new reader accepts only directly static `${...}` string expressions with no identifier resolution, preserving variable interpolation guard behavior. Tests now cover dangerous direct argv, `execSync`, and shell-option static-template forms plus safe `--version` and identifier interpolation guards. Verification passed in `agent-core-node-static-template-a914-focused-green-20260722-192702.log` (3 files / 6 tests), `agent-core-node-static-template-a914-related-20260722-192729.log` (3 files / 6 tests), `agent-core-build-a914-20260722-192729.log`, `node-static-template-dist-fixed-a914-20260722-192738.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a914-20260722-192747.log` (230 files / 706 tests), and `agent-core-full-test-a914-20260722-192747.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-string.utils.ts` 147, `javascript-template-literal.utils.ts` 55, `javascript-static-value.utils.ts` 164, `javascript-static-concat-method.utils.ts` 76, and `javascript-string-raw-template.utils.ts` 40. |
| A915 | done | Core E2E | Detect Node child_process direct/shell commands built from `String.raw` static template interpolations, so ``spawnSync(String.raw`r${"m"}`, [String.raw`-r${"f"}`, String.raw`/`])``, ``execSync(String.raw`rm ${"-rf"} /`)``, and shell-option ``spawnSync(String.raw`rm ${"-rf"} /`, {shell:String.raw`/bin/${"sh"}`})`` deny instead of slipping through as user-reviewable. | Runtime probe `node-string-raw-static-template-runtime-a915-20260722-193131.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node evaluates `String.raw` static interpolation into direct argv, `execSync` shell command strings, and shell-option commands with `argc=2 args=-rf /`; static `--version` interpolation remains non-dangerous. Pre-fix current-dist probe `node-string-raw-static-template-dist-before-a915-20260722-193131.log` reproduced 3 mismatches where dangerous `String.raw` static-template forms returned `ask_user`; safe `--version` and identifier interpolation guard `${flag}` stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-raw-static-template-a915-focused-red-20260722-193241.log` (3 failing files / 3 failing tests), then passed after `javascript-string-raw-template.utils.ts` reused the shared template reader in raw-escape mode and `javascript-template-literal.utils.ts` preserved the no-identifier static interpolation boundary. Tests now cover dangerous direct argv, `execSync`, and shell-option `String.raw` static-template forms plus safe `--version` and identifier interpolation guards. Verification passed in `agent-core-node-string-raw-static-template-a915-focused-green-20260722-193323.log` (3 files / 6 tests), `agent-core-node-string-raw-static-template-a915-related-20260722-193347.log` (3 files / 6 tests), `agent-core-build-a915-20260722-193347.log`, `node-string-raw-static-template-dist-fixed-a915-20260722-193357.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a915-20260722-193405.log` (230 files / 706 tests), and `agent-core-full-test-a915-20260722-193405.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-string-raw-template.utils.ts` 32, `javascript-template-literal.utils.ts` 56, `javascript-static-string.utils.ts` 147, `javascript-static-value.utils.ts` 164, and `javascript-static-concat-method.utils.ts` 76. |
| A916 | done | Core E2E | Detect Node child_process direct/shell commands invoked through `Function.prototype.call`, so `spawnSync.call(null, "rm", ["-rf", "/"])`, alias `s.call(null, "rm", ["-rf", "/"])`, `execSync.call(null, "rm -rf /")`, and shell-option `spawnSync.call(null, "rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-call-runtime-a916-20260722-193920.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `.call(null, ...)` for direct argv, alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; `spawnSync.call(null, "rm", ["--version"])` remains non-dangerous. Pre-fix current-dist probe `node-child-process-call-dist-before-a916-20260722-193941.log` reproduced 4 mismatches where dangerous `.call` forms returned `ask_user`; safe `--version` and dynamic command guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-call-a916-focused-red-20260722-194023.log` (3 failing files / 3 failing tests), then passed after adding `node-child-process-call-wrapper.utils.ts` to skip the `.call` receiver argument and feed the real command-argument start into the existing Node direct, shell exec, and shell-option parsers. Tests now cover dangerous member and alias direct argv `.call`, dangerous `execSync.call`, dangerous shell-option `.call`, plus safe `--version` and dynamic command guards. Verification passed in `agent-core-node-child-process-call-a916-focused-green-20260722-194107.log` (3 files / 6 tests), `agent-core-node-child-process-call-a916-related-20260722-194130.log` (3 files / 6 tests), `agent-core-build-a916-20260722-194130.log`, `node-child-process-call-dist-fixed-a916-20260722-194137.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a916-20260722-194143.log` (230 files / 706 tests), and `agent-core-full-test-a916-20260722-194157.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-call-wrapper.utils.ts` 22, `node-direct-command.utils.ts` 49, `interpreter-shell-command.utils.ts` 150, `node-child-process-call-name.utils.ts` 144, and `javascript-static-string.utils.ts` 147. |
| A917 | done | Core E2E | Detect Node child_process shell commands invoked through `Function.prototype.apply` with a static argument array, so `execSync.apply(null, ["rm -rf /"])`, alias `e.apply(null, ["rm -rf /"])`, and shell-option `spawnSync.apply(null, ["rm -rf /", [], {shell:true}])` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-apply-shell-runtime-a917-20260722-194500.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `.apply(null, [...])` for `execSync` shell strings, alias `execSync`, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; `execSync.apply(null, ["rm --version"])` remains non-dangerous. Pre-fix current-dist probe `node-child-process-apply-shell-dist-before-a917-20260722-194519.log` reproduced 3 mismatches where dangerous `.apply` shell forms returned `ask_user`; safe `--version` and dynamic `args=getArgs()` guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-apply-shell-a917-focused-red-20260722-194548.log` (2 failing files / 2 failing tests), then passed after adding `node-child-process-apply-shell-command.utils.ts` to read the static `.apply` argument-array command string and reuse existing shell-option detection for array contents. Tests now cover dangerous member and alias `execSync.apply`, dangerous shell-option `spawnSync.apply`, plus safe `--version` and dynamic argument-array guards; direct nested argv `.apply` remains outside this shell-command slice. Verification passed in `agent-core-node-child-process-apply-shell-a917-focused-green-20260722-194704.log` (2 files / 4 tests), `agent-core-node-child-process-apply-shell-a917-related-20260722-194713.log` (2 files / 4 tests), `agent-core-build-a917-20260722-194713.log`, `node-child-process-apply-shell-dist-fixed-a917-20260722-194719.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a917-20260722-194725.log` (230 files / 706 tests), and `agent-core-full-test-a917-20260722-194738.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-apply-shell-command.utils.ts` 55, `interpreter-shell-command.utils.ts` 156, `node-child-process-call-wrapper.utils.ts` 22, `node-direct-command.utils.ts` 49, and `javascript-static-value.utils.ts` 164. |
| A918 | done | Core E2E | Detect Node child_process direct argv commands invoked through `Function.prototype.apply` with a static nested argv array, so `spawnSync.apply(null, ["rm", ["-rf", "/"]])`, alias `s.apply(null, ["rm", ["-rf", "/"]])`, and `execFileSync.apply(null, ["rm", ["-rf", "/"]])` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-apply-direct-runtime-a918-20260722-195002.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `.apply(null, ["rm", ["-rf", "/"], ...])` for `spawnSync`, alias `spawnSync`, and `execFileSync` direct argv with `argc=2 args=-rf /`; `spawnSync.apply(null, ["rm", ["--version"], ...])` remains non-dangerous. Pre-fix current-dist probe `node-child-process-apply-direct-dist-before-a918-20260722-195021.log` reproduced 3 mismatches where dangerous direct argv `.apply` forms returned `ask_user`; safe `--version` and dynamic `args=getArgs()` guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-apply-direct-a918-focused-red-20260722-195043.log` (1 failing file / 1 failing test), then passed after adding `node-child-process-apply-direct-command.utils.ts` for static nested argv extraction, extracting shared `.apply` second-argument-array parsing into `node-child-process-apply-wrapper.utils.ts`, and extending `javascript-static-value.utils.ts` with a boundary-aware static array reader for nested apply-array operands. Tests now cover dangerous member and alias `spawnSync.apply`, dangerous `execFileSync.apply`, plus safe `--version` and dynamic argument-array guards. Verification passed in `agent-core-node-child-process-apply-direct-a918-focused-green-20260722-195208.log` (1 file / 2 tests), `agent-core-node-child-process-apply-direct-a918-related-20260722-195224.log` (3 files / 6 tests), `agent-core-build-a918-20260722-195224.log`, `node-child-process-apply-direct-dist-fixed-a918-20260722-195231.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a918-20260722-195236.log` (230 files / 706 tests), and `agent-core-full-test-a918-20260722-195250.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-apply-direct-command.utils.ts` 36, `node-child-process-apply-wrapper.utils.ts` 48, `node-child-process-apply-shell-command.utils.ts` 19, `node-direct-command.utils.ts` 51, `javascript-static-value.utils.ts` 173, and `interpreter-shell-command.utils.ts` 156. |
| A919 | done | Core E2E | Detect Node child_process direct/shell commands invoked through immediately executed `Function.prototype.bind`, so `spawnSync.bind(null, "rm", ["-rf", "/"])()`, alias `s.bind(null, "rm", ["-rf", "/"])()`, `execSync.bind(null, "rm -rf /")()`, and shell-option `spawnSync.bind(null, "rm -rf /", [], {shell:true})()` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-bind-immediate-runtime-a919-20260722-195514.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes immediate `.bind(... )()` forms for direct argv, alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; `spawnSync.bind(null, "rm", ["--version"])()` remains non-dangerous. Pre-fix current-dist probe `node-child-process-bind-immediate-dist-before-a919-20260722-195535.log` reproduced 4 mismatches where dangerous immediate bind forms returned `ask_user`; safe `--version` and a bound-but-not-invoked guard stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-bind-immediate-a919-focused-red-20260722-195618.log` (3 failing files / 3 failing tests), then passed after adding `node-child-process-bind-wrapper.utils.ts`, which only returns bound argument starts when the `.bind(...)` call is immediately invoked with `()`, and wiring those starts into the existing Node direct, shell exec, and shell-option parsers. Tests now cover dangerous member and alias direct argv bind, dangerous `execSync.bind`, dangerous shell-option bind, safe `--version`, and bound-but-not-invoked guard behavior. Verification passed in `agent-core-node-child-process-bind-immediate-a919-focused-green-20260722-195701.log` (3 files / 6 tests), `agent-core-node-child-process-bind-immediate-a919-related-20260722-195710.log` (3 files / 6 tests), `agent-core-build-a919-20260722-195710.log`, `node-child-process-bind-immediate-dist-fixed-a919-20260722-195719.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a919-20260722-195724.log` (230 files / 706 tests), and `agent-core-full-test-a919-20260722-195738.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-bind-wrapper.utils.ts` 33, `node-direct-command.utils.ts` 54, `interpreter-shell-command.utils.ts` 165, `node-child-process-apply-wrapper.utils.ts` 48, and `javascript-static-value.utils.ts` 173. |
| A920 | done | Core E2E | Detect Node child_process direct/shell commands invoked through receiver-only `Function.prototype.bind` followed by call arguments, so `spawnSync.bind(null)("rm", ["-rf", "/"])`, alias `s.bind(null)("rm", ["-rf", "/"])`, `execSync.bind(null)("rm -rf /")`, and shell-option `spawnSync.bind(null)("rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-bind-callargs-runtime-a920-20260722-200012.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes receiver-only `.bind(null)(...)` forms for direct argv, alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; `spawnSync.bind(null)("rm", ["--version"])` remains non-dangerous. Pre-fix current-dist probe `node-child-process-bind-callargs-dist-before-a920-20260722-200035.log` reproduced 4 mismatches where dangerous receiver-only bind call-argument forms returned `ask_user`; safe `--version` and a bound-but-not-invoked guard stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-bind-callargs-a920-focused-red-20260722-200114.log` (3 failing files / 3 failing tests), then passed after extending `node-child-process-bind-wrapper.utils.ts` with `nodeChildProcessReceiverBindCallArgumentStartIndexes`, which only returns the immediate invocation argument start when the bind call has no bound command arguments, and wiring those starts into the existing Node direct, shell exec, and shell-option parsers. Tests now cover dangerous member and alias direct argv receiver-only bind calls, dangerous `execSync.bind(null)(...)`, dangerous shell-option bind calls, safe `--version`, and bound-but-not-invoked guard behavior. Verification passed in `agent-core-node-child-process-bind-callargs-a920-focused-green-20260722-200207.log` (3 files / 6 tests), `agent-core-node-child-process-bind-callargs-a920-related-20260722-200220.log` (3 files / 6 tests), `agent-core-build-a920-20260722-200220.log`, `node-child-process-bind-callargs-dist-fixed-a920-20260722-200227.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a920-20260722-200232.log` (230 files / 706 tests), and `agent-core-full-test-a920-20260722-200246.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-bind-wrapper.utils.ts` 57, `node-direct-command.utils.ts` 59, `interpreter-shell-command.utils.ts` 176, and `javascript-static-value.utils.ts` 173. |
| A921 | done | Core E2E | Detect Node child_process partial-bind forms where the command is bound and argv/options are supplied by the immediate invocation, so `spawnSync.bind(null, "rm")(["-rf", "/"])`, alias `s.bind(null, "rm")(["-rf", "/"])`, `execFileSync.bind(null, "rm")(["-rf", "/"])`, and shell-option `spawnSync.bind(null, "rm -rf /")([], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-bind-partial-runtime-a921-20260722-200525.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node combines bound command arguments with invocation argv/options for direct argv, alias direct argv, `execFileSync`, and shell-option forms with `argc=2 args=-rf /`; `spawnSync.bind(null, "rm")(["--version"])` remains non-dangerous. Pre-fix current-dist probe `node-child-process-bind-partial-dist-before-a921-20260722-200546.log` reproduced 4 mismatches where dangerous partial-bind forms returned `ask_user`; safe `--version` and dynamic invocation argv guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-bind-partial-a921-focused-red-20260722-200618.log` (2 failing files / 2 failing tests), then passed after extending `node-child-process-bind-wrapper.utils.ts` with partial bind metadata and adding `node-child-process-bind-direct-command.utils.ts` / `node-child-process-bind-shell-option.utils.ts` to combine bound command strings with invocation argv or shell-option objects. Tests now cover dangerous member and alias direct argv partial bind, dangerous `execFileSync` partial bind, dangerous shell-option partial bind, plus safe `--version` and dynamic argv guards. Verification passed in `agent-core-node-child-process-bind-partial-a921-focused-green-20260722-200714.log` (2 files / 4 tests), `agent-core-node-child-process-bind-partial-a921-related-20260722-200725.log` (3 files / 6 tests), `agent-core-build-a921-20260722-200725.log`, `node-child-process-bind-partial-dist-fixed-a921-20260722-200732.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a921-20260722-200738.log` (230 files / 706 tests), and `agent-core-full-test-a921-20260722-200752.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-bind-wrapper.utils.ts` 82, `node-child-process-bind-direct-command.utils.ts` 22, `node-child-process-bind-shell-option.utils.ts` 20, `node-direct-command.utils.ts` 61, `interpreter-shell-command.utils.ts` 178, and `javascript-static-value.utils.ts` 173. |
| A922 | done | Core E2E | Detect Node child_process optional-call forms, so `spawnSync?.("rm", ["-rf", "/"])`, alias `s?.("rm", ["-rf", "/"])`, `execSync?.("rm -rf /")`, and shell-option `spawnSync?.("rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-optional-call-runtime-a922-20260722-201255.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional-call forms for direct argv, alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `spawnSync?.("rm", ["--version"])` returns `argc=1 args=--version`, and an undefined optional-call receiver is not executed. Pre-fix current-dist probe `node-child-process-optional-call-dist-before-a922-20260722-201255.log` reproduced 4 mismatches where dangerous optional-call forms returned `ask_user`; safe `--version` and undefined receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-optional-call-a922-focused-red-rerun-20260722-201353.log` (3 failing files / 3 failing tests), then passed after adding `node-child-process-optional-call.utils.ts` to return `?.(` argument starts for known child_process call names and wiring those starts into existing Node direct argv, shell exec, and shell-option parsers. Tests now cover dangerous member and alias direct argv optional calls, dangerous `execSync?.`, dangerous shell-option optional call, plus safe `--version` and undefined-receiver guards; bracket optional calls remain outside this minimal slice. Verification passed in `agent-core-node-child-process-optional-call-a922-focused-green-20260722-201428.log` (3 files / 6 tests), `agent-core-node-child-process-optional-call-a922-related-20260722-201446.log` (3 files / 6 tests), `agent-core-build-a922-20260722-201446.log`, `node-child-process-optional-call-dist-fixed-a922-20260722-201457.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a922-20260722-201457.log` (230 files / 706 tests), and `agent-core-full-test-a922-20260722-201457.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-optional-call.utils.ts` 12, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `javascript-static-value.utils.ts` 173. |
| A923 | done | Core E2E | Detect Node child_process bracket optional-call forms, so `require("node:child_process")["spawnSync"]?.("rm", ["-rf", "/"])`, module alias `cp["spawnSync"]?.("rm", ["-rf", "/"])`, `require("child_process")["execSync"]?.("rm -rf /")`, and shell-option `cp["spawnSync"]?.("rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-bracket-optional-runtime-a923-20260722-201838.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes bracket optional-call forms for direct argv, module alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe bracket optional `spawnSync` with `--version` remains non-dangerous, and an undefined bracket optional receiver is not executed. Pre-fix current-dist probe `node-child-process-bracket-optional-dist-before-a923-20260722-201838.log` reproduced 4 mismatches where dangerous bracket optional-call forms returned `ask_user`; safe `--version` and non-child object receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-bracket-optional-a923-focused-red-20260722-201923.log` (3 failing files / 3 failing tests), then passed after `node-child-process-call-name.utils.ts` allowed `]?.(` in the shared direct-require and module-alias bracket child_process call start regexes, preserving the existing normal `](` path. Tests now cover dangerous direct-require and module-alias bracket optional direct argv, dangerous `execSync` bracket optional shell command, dangerous shell-option bracket optional call, plus safe `--version` and non-child receiver guards. Verification passed in `agent-core-node-child-process-bracket-optional-a923-focused-green-20260722-201951.log` (3 files / 6 tests), `agent-core-node-child-process-bracket-optional-a923-related-20260722-202004.log` (3 files / 6 tests), `agent-core-build-a923-20260722-202004.log`, `node-child-process-bracket-optional-dist-fixed-a923-20260722-202015.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a923-20260722-202015.log` (230 files / 706 tests), and `agent-core-full-test-a923-20260722-202015.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-call-name.utils.ts` 144, `node-child-process-optional-call.utils.ts` 12, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `javascript-static-value.utils.ts` 173. |
| A924 | done | Core E2E | Detect Node child_process `Function.prototype.call` wrapper optional-call forms, so `spawnSync.call?.(null, "rm", ["-rf", "/"])`, alias `s.call?.(null, "rm", ["-rf", "/"])`, `execSync.call?.(null, "rm -rf /")`, and shell-option `spawnSync.call?.(null, "rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-call-wrapper-optional-runtime-a924-20260722-202327.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `.call?.(...)` wrapper forms for direct argv, alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `.call?.(null, "rm", ["--version"])` remains non-dangerous, and the dynamic command guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-child-process-call-wrapper-optional-dist-before-a924-20260722-202327.log` reproduced 4 mismatches where dangerous call-wrapper optional-call forms returned `ask_user`; safe `--version` and dynamic command guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-call-wrapper-optional-a924-focused-red-20260722-202406.log` (3 failing files / 3 failing tests), then passed after `node-child-process-call-wrapper.utils.ts` allowed `.call?.(` in the shared child_process call-wrapper argument-start regex while preserving the existing `.call(` path and receiver-argument skip behavior. Tests now cover dangerous member and alias direct argv call-wrapper optional calls, dangerous `execSync.call?.`, dangerous shell-option `spawnSync.call?.`, plus safe `--version` and dynamic command guards. Verification passed in `agent-core-node-child-process-call-wrapper-optional-a924-focused-green-20260722-202424.log` (3 files / 6 tests), `agent-core-node-child-process-call-wrapper-optional-a924-related-20260722-202437.log` (3 files / 6 tests), `agent-core-build-a924-20260722-202437.log`, `node-child-process-call-wrapper-optional-dist-fixed-a924-20260722-202448.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a924-20260722-202448.log` (230 files / 706 tests), and `agent-core-full-test-a924-20260722-202448.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-call-wrapper.utils.ts` 22, `node-child-process-call-name.utils.ts` 144, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `javascript-static-value.utils.ts` 173. |
| A925 | done | Core E2E | Detect Node child_process `Function.prototype.apply` wrapper optional-call forms, so `spawnSync.apply?.(null, ["rm", ["-rf", "/"]])`, alias `s.apply?.(null, ["rm", ["-rf", "/"]])`, `execSync.apply?.(null, ["rm -rf /"])`, and shell-option `spawnSync.apply?.(null, ["rm -rf /", [], {shell:true}])` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-apply-wrapper-optional-runtime-a925-20260722-202732.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `.apply?.(...)` wrapper forms for direct argv, alias direct argv, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `.apply?.(null, ["rm", ["--version"]])` remains non-dangerous, and the dynamic args guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-child-process-apply-wrapper-optional-dist-before-a925-20260722-202732.log` reproduced 4 mismatches where dangerous apply-wrapper optional-call forms returned `ask_user`; safe `--version` and dynamic args guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-apply-wrapper-optional-a925-focused-red-20260722-202817.log` (3 failing files / 3 failing tests), then passed after `node-child-process-apply-wrapper.utils.ts` allowed `.apply?.(` in the shared child_process apply-wrapper argument-array start regex while preserving the existing `.apply(` path and second-argument array parsing. Tests now cover dangerous member and alias direct argv apply-wrapper optional calls, dangerous `execSync.apply?.`, dangerous shell-option `spawnSync.apply?.`, plus safe `--version` and dynamic args guards. Verification passed in `agent-core-node-child-process-apply-wrapper-optional-a925-focused-green-20260722-202835.log` (3 files / 6 tests), `agent-core-node-child-process-apply-wrapper-optional-a925-related-20260722-202848.log` (3 files / 6 tests), `agent-core-build-a925-20260722-202848.log`, `node-child-process-apply-wrapper-optional-dist-fixed-a925-20260722-202858.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a925-20260722-202858.log` (230 files / 706 tests), and `agent-core-full-test-a925-20260722-202858.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-apply-wrapper.utils.ts` 48, `node-child-process-call-wrapper.utils.ts` 22, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `javascript-static-value.utils.ts` 173. |
| A926 | done | Core E2E | Detect Node child_process `Function.prototype.bind` wrapper optional-call forms, so `spawnSync.bind?.(null, "rm", ["-rf", "/"])()`, alias receiver-only `s.bind?.(null)("rm", ["-rf", "/"])`, `execSync.bind?.(null, "rm -rf /")()`, and partial shell-option `spawnSync.bind?.(null, "rm -rf /")([], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-child-process-bind-wrapper-optional-runtime-a926-20260722-203156.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `.bind?.(...)` wrapper forms for immediate direct argv, alias receiver-only direct argv, `execSync` shell strings, and partial shell-option strings with `argc=2 args=-rf /`; safe `.bind?.(null, "rm", ["--version"])()` remains non-dangerous, and the dynamic invocation args guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-child-process-bind-wrapper-optional-dist-before-a926-20260722-203156.log` reproduced 4 mismatches where dangerous bind-wrapper optional-call forms returned `ask_user`; safe `--version` and dynamic invocation args guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-child-process-bind-wrapper-optional-a926-focused-red-20260722-203237.log` (3 failing files / 3 failing tests), then passed after `node-child-process-bind-wrapper.utils.ts` allowed `.bind?.(` in all three shared child_process bind-wrapper regexes while preserving existing `.bind(` behavior for immediate, receiver-only, and partial bind parsing. Tests now cover dangerous immediate direct argv, dangerous alias receiver-only direct argv, dangerous `execSync.bind?.`, dangerous partial shell-option `spawnSync.bind?.`, plus safe `--version` and dynamic invocation args guards. Verification passed in `agent-core-node-child-process-bind-wrapper-optional-a926-focused-green-20260722-203300.log` (3 files / 6 tests), `agent-core-node-child-process-bind-wrapper-optional-a926-related-20260722-203315.log` (3 files / 6 tests), `agent-core-build-a926-20260722-203315.log`, `node-child-process-bind-wrapper-optional-dist-fixed-a926-20260722-203327.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a926-20260722-203327.log` (230 files / 706 tests), and `agent-core-full-test-a926-20260722-203327.log` (327 files / 1979 tests). Production source line counts remain under 200: `node-child-process-bind-wrapper.utils.ts` 82, `node-child-process-bind-direct-command.utils.ts` 22, `node-child-process-bind-shell-option.utils.ts` 20, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `javascript-static-value.utils.ts` 173. |
| A927 | done | Core E2E | Detect Node child_process static JS boolean conditional string expressions, so `spawnSync(true ? "rm" : "noop", [true ? "-rf" : "--version", "/"])`, static assignments from `true ? "r" + "m" : "noop"`, `execSync(true ? "rm -rf /" : "echo safe")`, and shell-option `spawnSync(true ? "rm -rf /" : "echo safe", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-conditional-runtime-a927-20260722-203941.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes the true branch for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; the false-branch safe shell command returned `safe`, and the dynamic condition guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-conditional-dist-before-a927-20260722-203941.log` reproduced 4 mismatches where dangerous conditional direct/shell/shell-option forms returned `ask_user`; false-branch safe and dynamic-condition guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-conditional-a927-focused-red-20260722-204035.log` (3 failing files / 3 failing tests), then passed after new pure helper `javascript-static-conditional.utils.ts` folded only boolean-literal `true`/`false` conditions, required both string branches to parse statically, consumed the full selected expression boundary, and left dynamic conditions unresolved. `javascript-static-string.utils.ts` now delegates conditional primary operands to that helper while preserving literal, template, `String.raw`, concat, parenthesized, comma, and reference behavior. Tests now cover dangerous direct argv conditionals, conditional static assignments, dangerous `execSync` conditionals, dangerous shell-option conditionals, plus false-branch safe and dynamic-condition guards. Verification passed in `agent-core-node-static-conditional-a927-focused-green-20260722-204148.log` (3 files / 6 tests), `agent-core-node-static-conditional-a927-related-20260722-204205.log` (3 files / 6 tests), `agent-core-build-a927-20260722-204205.log`, `node-static-conditional-dist-fixed-a927-20260722-204212.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a927-rerun-20260722-204252.log` (230 files / 706 tests), and `agent-core-full-test-a927-20260722-204222.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-conditional.utils.ts` 66, `javascript-static-string.utils.ts` 159, `javascript-static-value.utils.ts` 173, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `node-shell-option.utils.ts` 70. |
| A928 | done | Core E2E | Detect Node child_process static JS boolean logical string expressions, so `spawnSync(true && "rm", [true && "-rf", "/"])`, static assignments from `false || "r" + "m"`, `execSync(false || "rm -rf /")`, and shell-option `spawnSync(true && "rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-logical-runtime-a928-20260722-204610.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes selected logical-string branches for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; `false && "rm -rf /"` did not execute the fake `rm`, and the dynamic logical guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-logical-dist-before-a928-20260722-204610.log` reproduced 4 mismatches where dangerous logical direct/shell/shell-option forms returned `ask_user`; false-and non-executed and dynamic-condition guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-logical-a928-focused-red-20260722-204641.log` (3 failing files / 3 failing tests), then passed after new pure helper `javascript-static-logical.utils.ts` folded only boolean-literal `true && <static string>` and `false || <static string>`, required the selected right side to parse through the same static JS string reader, and left `false &&`, `true ||`, and dynamic left operands unresolved. `javascript-static-string.utils.ts` now delegates logical primary operands to that helper after conditional parsing while preserving literal, template, `String.raw`, concat, parenthesized, comma, reference, and A927 conditional behavior. Tests now cover dangerous direct argv logical expressions, logical static assignments, dangerous `execSync` logical expressions, dangerous shell-option logical expressions, plus false-and and dynamic logical guards. Verification passed in `agent-core-node-static-logical-a928-focused-green-20260722-204721.log` (3 files / 6 tests), `agent-core-node-static-logical-a928-related-20260722-204729.log` (3 files / 6 tests), `agent-core-build-a928-20260722-204729.log`, `node-static-logical-dist-fixed-a928-20260722-204736.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a928-20260722-204743.log` (230 files / 706 tests), and `agent-core-full-test-a928-20260722-204743.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-logical.utils.ts` 59, `javascript-static-conditional.utils.ts` 66, `javascript-static-string.utils.ts` 169, `javascript-static-value.utils.ts` 173, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `node-shell-option.utils.ts` 70. |
| A929 | done | Core E2E | Detect Node child_process static JS `null ?? <static string>` expressions, so `spawnSync(null ?? "rm", [null ?? "-rf", "/"])`, static assignments from `null ?? "r" + "m"`, `execSync(null ?? "rm -rf /")`, and shell-option `spawnSync(null ?? "rm -rf /", [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-nullish-runtime-a929-rerun-20260722-205250.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes the nullish right side for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; the non-nullish-left guard returned `safe`, and the dynamic nullish guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-nullish-dist-before-a929-rerun-20260722-205250.log` reproduced 4 mismatches where dangerous nullish direct/shell/shell-option forms returned `ask_user`; non-nullish-left and dynamic-nullish guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-nullish-a929-focused-red-rerun-20260722-205303.log` (3 failing files / 3 failing tests), then passed after new pure helper `javascript-static-nullish.utils.ts` folded only unshadowable `null ?? <static string>` and required the selected right side to parse through the same static JS string reader. It intentionally does not fold `undefined ??` because `undefined` can be locally shadowed, and it leaves non-nullish left operands plus dynamic left operands unresolved. `javascript-static-string.utils.ts` now delegates nullish primary operands to that helper after conditional and logical parsing while preserving literal, template, `String.raw`, concat, parenthesized, comma, reference, conditional, and logical behavior. Tests now cover dangerous direct argv nullish expressions, nullish static assignments, dangerous `execSync` nullish expressions, dangerous shell-option nullish expressions, plus non-nullish-left and dynamic-nullish guards. Verification passed in `agent-core-node-static-nullish-a929-focused-green-20260722-205336.log` (3 files / 6 tests), `agent-core-node-static-nullish-a929-related-20260722-205345.log` (3 files / 6 tests), `agent-core-build-a929-20260722-205345.log`, `node-static-nullish-dist-fixed-a929-20260722-205356.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a929-20260722-205404.log` (230 files / 706 tests), and `agent-core-full-test-a929-20260722-205404.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-nullish.utils.ts` 49, `javascript-static-logical.utils.ts` 59, `javascript-static-conditional.utils.ts` 66, `javascript-static-string.utils.ts` 179, `javascript-static-value.utils.ts` 173, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `node-shell-option.utils.ts` 70. |
| A930 | done | Core E2E | Detect Node child_process static JS array `join` string expressions, so `spawnSync(["r", "m"].join(""), [["-r", "f"].join(""), "/"])`, static assignments from `["r", "m"].join("")`, `execSync(["rm", " -rf /"].join(""))`, and shell-option `spawnSync(["rm", " -rf /"].join(""), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-array-join-runtime-a930-20260722-205736.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes static array `.join("")` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `["--", "version"].join("")` produced `--version`, and the dynamic separator guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-array-join-dist-before-a930-20260722-205737.log` reproduced 4 mismatches where dangerous array-join direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic separator guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-array-join-a930-focused-red-20260722-205809.log` (3 failing files / 3 failing tests), then passed after new pure helper `javascript-static-array-join.utils.ts` parsed only array literals whose elements and join separator resolve through the same static JS string reader, and left dynamic elements or separators unresolved. `javascript-static-string.utils.ts` now delegates array-join primary operands to that helper after conditional, logical, and nullish parsing while preserving literal, template, `String.raw`, concat, parenthesized, comma, reference, conditional, logical, and nullish behavior. Tests now cover dangerous direct argv array join expressions, array-join static assignments, dangerous `execSync` array join expressions, dangerous shell-option array join expressions, plus safe `--version` and dynamic separator guards. Verification passed in `agent-core-node-static-array-join-a930-focused-green-20260722-205848.log` (3 files / 6 tests), `agent-core-node-static-array-join-a930-related-20260722-205900.log` (3 files / 6 tests), `agent-core-build-a930-20260722-205900.log`, `node-static-array-join-dist-fixed-a930-20260722-205907.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a930-20260722-205914.log` (230 files / 706 tests), and `agent-core-full-test-a930-20260722-205914.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-array-join.utils.ts` 88, `javascript-static-nullish.utils.ts` 49, `javascript-static-logical.utils.ts` 59, `javascript-static-conditional.utils.ts` 66, `javascript-static-string.utils.ts` 189, `javascript-static-value.utils.ts` 173, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 187, and `node-shell-option.utils.ts` 70. |
| A931 | done | Core E2E | Detect Node child_process static JS `String.fromCharCode(...)` string expressions, so `spawnSync(String.fromCharCode(114, 109), [String.fromCharCode(45, 114, 102), "/"])`, static assignments from `String.fromCharCode(114, 109)`, `execSync(String.fromCharCode(114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode(114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-runtime-a931-20260722-210431.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes static `String.fromCharCode` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `String.fromCharCode(45, 45, 118, 101, 114, 115, 105, 111, 110)` produced `--version`, and the dynamic spread guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-from-char-code-dist-before-a931-20260722-210431.log` reproduced 4 mismatches where dangerous fromCharCode direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic spread guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-a931-focused-red-20260722-210529.log` (3 failing files / 3 failing tests), then passed after new pure helper `javascript-static-from-char-code.utils.ts` parsed only global `String.fromCharCode(...)` calls whose arguments are static decimal integers and left spread, variable, and expression arguments unresolved. `javascript-static-string.utils.ts` now delegates fromCharCode primary operands to that helper after array-join parsing while preserving literal, template, `String.raw`, concat, parenthesized, comma, reference, conditional, logical, nullish, and array-join behavior. Tests now cover dangerous direct argv fromCharCode expressions, fromCharCode static assignments, dangerous `execSync` fromCharCode expressions, dangerous shell-option fromCharCode expressions, plus safe `--version` and dynamic spread guards. Verification passed in `agent-core-node-static-from-char-code-a931-focused-green-20260722-210633.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-a931-related-20260722-210649.log` (3 files / 6 tests), `agent-core-build-a931-20260722-210649.log`, `node-static-from-char-code-dist-fixed-a931-20260722-210701.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a931-rerun-20260722-210722.log` (230 files / 706 tests), and `agent-core-full-test-a931-20260722-210701.log` (327 files / 1979 tests). The first auto-reviewer command `agent-core-auto-reviewer-a931-20260722-210701.log` failed only because the quoted glob was not expanded; the rerun above is the suite result. Production source line counts remain under 200: `javascript-static-string.utils.ts` 193, `javascript-static-from-char-code.utils.ts` 65, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A932 | done | Core E2E | Detect Node child_process static JS `String.fromCodePoint(...)` string expressions, so `spawnSync(String.fromCodePoint(114, 109), [String.fromCodePoint(45, 114, 102), "/"])`, static assignments from `String.fromCodePoint(114, 109)`, `execSync(String.fromCodePoint(114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCodePoint(114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-code-point-runtime-a932-20260722-211044.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes static `String.fromCodePoint` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `String.fromCodePoint(45, 45, 118, 101, 114, 115, 105, 111, 110)` produced `--version`, and the dynamic spread guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-from-code-point-dist-before-a932-20260722-211044.log` reproduced 4 mismatches where dangerous fromCodePoint direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic spread guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-code-point-a932-focused-red-20260722-211136.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` reused the same static decimal-integer argument reader for global `String.fromCodePoint(...)` calls, added code-point range guarding, and continued to leave spread, variable, and expression arguments unresolved. `javascript-static-string.utils.ts` still delegates primary operands to this static code-call helper after array-join parsing while preserving literal, template, `String.raw`, concat, parenthesized, comma, reference, conditional, logical, nullish, array-join, and fromCharCode behavior. Tests now cover dangerous direct argv fromCodePoint expressions, fromCodePoint static assignments, dangerous `execSync` fromCodePoint expressions, dangerous shell-option fromCodePoint expressions, plus safe `--version` and dynamic spread guards. Verification passed in `agent-core-node-static-from-code-point-a932-focused-green-20260722-211211.log` (3 files / 6 tests), `agent-core-node-static-from-code-point-a932-related-20260722-211225.log` (3 files / 6 tests), `agent-core-build-a932-20260722-211225.log`, `node-static-from-code-point-dist-fixed-a932-20260722-211236.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a932-20260722-211236.log` (230 files / 706 tests), and `agent-core-full-test-a932-20260722-211236.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 92, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A933 | done | Core E2E | Detect Node child_process static JS `String.fromCharCode.call(null, ...)` string expressions, so `spawnSync(String.fromCharCode.call(null, 114, 109), [String.fromCharCode.call(null, 45, 114, 102), "/"])`, static assignments from `String.fromCharCode.call(null, 114, 109)`, `execSync(String.fromCharCode.call(null, 114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode.call(null, 114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-call-runtime-a933-20260722-211516.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes static `String.fromCharCode.call(null, ...)` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `String.fromCharCode.call(null, 45, 45, 118, 101, 114, 115, 105, 111, 110)` produced `--version`, and the dynamic receiver guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-from-char-code-call-dist-before-a933-20260722-211516.log` reproduced 4 mismatches where dangerous fromCharCode call-wrapper direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-call-a933-focused-red-20260722-211619.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` added a narrow argument-start parser for direct `String.fromCharCode(...)` and `String.fromCharCode.call(null, ...)` forms, requiring the call-wrapper receiver to be literal `null` before folding the same static decimal-integer arguments. Dynamic receivers, spread, variables, and expression arguments remain unresolved. Tests now cover dangerous direct argv fromCharCode call-wrapper expressions, call-wrapper static assignments, dangerous `execSync` call-wrapper expressions, dangerous shell-option call-wrapper expressions, plus safe `--version` and dynamic receiver guards. Verification passed in `agent-core-node-static-from-char-code-call-a933-focused-green-20260722-211658.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-call-a933-related-20260722-211718.log` (3 files / 6 tests), `agent-core-build-a933-20260722-211718.log`, `node-static-from-char-code-call-dist-fixed-a933-20260722-211735.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a933-20260722-211735.log` (230 files / 706 tests), and `agent-core-full-test-a933-20260722-211735.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 117, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A934 | done | Core E2E | Detect Node child_process static JS optional `String.fromCharCode.call?.(null, ...)` string expressions, so `spawnSync(String.fromCharCode.call?.(null, 114, 109), [String.fromCharCode.call?.(null, 45, 114, 102), "/"])`, static assignments from `String.fromCharCode.call?.(null, 114, 109)`, `execSync(String.fromCharCode.call?.(null, 114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode.call?.(null, 114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-call-optional-runtime-a934-20260722-212026.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional `String.fromCharCode.call?.(null, ...)` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `String.fromCharCode.call?.(null, 45, 45, 118, 101, 114, 115, 105, 111, 110)` produced `--version`, and a missing optional method guard returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-call-optional-dist-before-a934-20260722-212026.log` reproduced 4 mismatches where dangerous optional fromCharCode call-wrapper direct/shell/shell-option forms returned `ask_user`; safe `--version` and missing optional method guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-call-optional-a934-focused-red-20260722-212115.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` allowed the existing literal-null call-wrapper parser to start arguments from either `.call(` or `.call?.(` while keeping receiver and static decimal-integer argument restrictions unchanged. Dynamic receivers, missing optional methods, spread, variables, and expression arguments remain unresolved. Tests now cover dangerous direct argv optional fromCharCode call-wrapper expressions, optional call-wrapper static assignments, dangerous `execSync` optional call-wrapper expressions, dangerous shell-option optional call-wrapper expressions, plus safe `--version` and missing optional method guards. Verification passed in `agent-core-node-static-from-char-code-call-optional-a934-focused-green-20260722-212139.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-call-optional-a934-related-20260722-212155.log` (3 files / 6 tests), `agent-core-build-a934-20260722-212155.log`, `node-static-from-char-code-call-optional-dist-fixed-a934-20260722-212209.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a934-20260722-212209.log` (230 files / 706 tests), and `agent-core-full-test-a934-20260722-212209.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 123, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A935 | done | Core E2E | Detect Node child_process static JS `String.fromCharCode.apply(null, [...])` string expressions, so `spawnSync(String.fromCharCode.apply(null, [114, 109]), [String.fromCharCode.apply(null, [45, 114, 102]), "/"])`, static assignments from `String.fromCharCode.apply(null, [114, 109])`, `execSync(String.fromCharCode.apply(null, [114, 109, 32, 45, 114, 102, 32, 47]))`, and shell-option `spawnSync(String.fromCharCode.apply(null, [114, 109, 32, 45, 114, 102, 32, 47]), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-apply-runtime-a935-20260722-212509.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes `String.fromCharCode.apply(null, [...])` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `String.fromCharCode.apply(null, [45, 45, 118, 101, 114, 115, 105, 111, 110])` produced `--version`, and the dynamic array guard was kept as non-executed runtime control. Pre-fix current-dist probe `node-static-from-char-code-apply-dist-before-a935-20260722-212509.log` reproduced 4 mismatches where dangerous fromCharCode apply-wrapper direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic array guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-apply-a935-focused-red-20260722-212600.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` added a narrow apply-wrapper parser that only folds `.apply(null, [static decimal integers])`, reuses the same code-string value validation, and leaves variable arrays, spread, non-array second args, and expression arguments unresolved. Tests now cover dangerous direct argv fromCharCode apply-wrapper expressions, apply-wrapper static assignments, dangerous `execSync` apply-wrapper expressions, dangerous shell-option apply-wrapper expressions, plus safe `--version` and dynamic array guards. Verification passed in `agent-core-node-static-from-char-code-apply-a935-focused-green-20260722-212646.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-apply-a935-related-20260722-212702.log` (3 files / 6 tests), `agent-core-build-a935-20260722-212702.log`, `node-static-from-char-code-apply-dist-fixed-a935-20260722-212714.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a935-20260722-212714.log` (230 files / 706 tests), and `agent-core-full-test-a935-20260722-212714.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 181, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70; future similar string-code wrapper expansions should split the helper before adding more responsibility. |
| A936 | done | Core E2E | Detect Node child_process static JS optional `String.fromCharCode.apply?.(null, [...])` string expressions, so `spawnSync(String.fromCharCode.apply?.(null, [114, 109]), [String.fromCharCode.apply?.(null, [45, 114, 102]), "/"])`, static assignments from `String.fromCharCode.apply?.(null, [114, 109])`, `execSync(String.fromCharCode.apply?.(null, [114, 109, 32, 45, 114, 102, 32, 47]))`, and shell-option `spawnSync(String.fromCharCode.apply?.(null, [114, 109, 32, 45, 114, 102, 32, 47]), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-apply-optional-runtime-a936-20260722-213010.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional `String.fromCharCode.apply?.(null, [...])` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe `String.fromCharCode.apply?.(null, [45, 45, 118, 101, 114, 115, 105, 111, 110])` produced `--version`, and a missing optional apply method guard returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-apply-optional-dist-before-a936-20260722-213010.log` reproduced 4 mismatches where dangerous optional fromCharCode apply-wrapper direct/shell/shell-option forms returned `ask_user`; safe `--version` and missing optional method guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-apply-optional-a936-focused-red-20260722-213101.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` reused the existing apply-wrapper parser for either `.apply(` or `.apply?.(` and `javascript-static-decimal-code.utils.ts` took over static decimal-integer and decimal-array parsing as a pure utility split. The parser still requires literal `null` receiver and static decimal array values; dynamic receivers, missing optional methods, variable arrays, spread, non-array second args, and expression arguments remain unresolved. Tests now cover dangerous direct argv optional fromCharCode apply-wrapper expressions, optional apply-wrapper static assignments, dangerous `execSync` optional apply-wrapper expressions, dangerous shell-option optional apply-wrapper expressions, plus safe `--version` and missing optional method guards. Verification passed in `agent-core-node-static-from-char-code-apply-optional-a936-focused-green-20260722-213147.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-apply-optional-a936-related-20260722-213204.log` (3 files / 6 tests), `agent-core-build-a936-20260722-213204.log`, `node-static-from-char-code-apply-optional-dist-fixed-a936-20260722-213217.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a936-20260722-213217.log` (230 files / 706 tests), and `agent-core-full-test-a936-20260722-213217.log` (327 files / 1979 tests). Production source line counts remain under 200 after the split: `javascript-static-from-char-code.utils.ts` 138, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A937 | done | Core E2E | Detect Node child_process static JS `String.fromCharCode.bind(null, ...)()` string expressions, so `spawnSync(String.fromCharCode.bind(null, 114, 109)(), [String.fromCharCode.bind(null, 45, 114, 102)(), "/"])`, static assignments from immediate bind-wrapper calls, `execSync(String.fromCharCode.bind(null, 114, 109, 32, 45, 114, 102, 32, 47)())`, and shell-option `spawnSync(String.fromCharCode.bind(null, 114, 109, 32, 45, 114, 102, 32, 47)(), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-bind-runtime-a937-20260722-213647.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes immediate `String.fromCharCode.bind(null, ...)()` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe bind-wrapper `--version` produced `--version`, and uncalled bind remained a function rather than a command string. Pre-fix current-dist probe `node-static-from-char-code-bind-dist-before-a937-20260722-213647.log` reproduced 4 mismatches where dangerous bind-wrapper direct/shell/shell-option forms returned `ask_user`; safe `--version` and uncalled bind guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-bind-a937-focused-red-rerun-20260722-213809.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` added a narrow bind-wrapper parser that only folds `.bind(null, static decimal integers...)()` and requires an empty immediate invocation. The parser still requires literal `null` receiver and static decimal integer bound arguments; uncalled bind, dynamic receivers, spread, variables, expression arguments, optional bind calls, and invocation arguments remain unresolved. Tests now cover dangerous direct argv bind-wrapper expressions, bind-wrapper static assignments, dangerous `execSync` bind-wrapper expressions, dangerous shell-option bind-wrapper expressions, plus safe `--version` and uncalled bind guards. Verification passed in `agent-core-node-static-from-char-code-bind-a937-focused-green-20260722-213857.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-bind-a937-related-20260722-213914.log` (3 files / 6 tests), `agent-core-build-a937-20260722-213914.log`, `node-static-from-char-code-bind-dist-fixed-a937-20260722-213926.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a937-rerun-20260722-214019.log` (230 files / 706 tests), and `agent-core-full-test-a937-20260722-213945.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 197, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A938 | done | Core E2E | Detect Node child_process static JS optional `String.fromCharCode.bind?.(null, ...)()` string expressions, so `spawnSync(String.fromCharCode.bind?.(null, 114, 109)(), [String.fromCharCode.bind?.(null, 45, 114, 102)(), "/"])`, static assignments from optional bind-wrapper calls, `execSync(String.fromCharCode.bind?.(null, 114, 109, 32, 45, 114, 102, 32, 47)())`, and shell-option `spawnSync(String.fromCharCode.bind?.(null, 114, 109, 32, 45, 114, 102, 32, 47)(), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-bind-optional-runtime-a938-20260722-214346.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional `String.fromCharCode.bind?.(null, ...)()` strings for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe optional bind-wrapper `--version` produced `--version`, and a missing optional bind method returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-bind-optional-dist-before-a938-20260722-214346.log` reproduced 4 mismatches where dangerous optional bind-wrapper direct/shell/shell-option forms returned `ask_user`; safe `--version` and missing optional bind method guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-bind-optional-a938-focused-red-20260722-214450.log` (3 failing files / 3 failing tests), then passed after splitting wrapper parsing from `javascript-static-from-char-code.utils.ts` into `javascript-static-code-wrapper.utils.ts` and allowing the bind-wrapper parser to start from either `.bind(` or `.bind?.(` while still requiring literal `null`, static decimal integer bound arguments, and an empty immediate invocation. Tests now cover dangerous direct argv optional bind-wrapper expressions, optional bind-wrapper static assignments, dangerous `execSync` optional bind-wrapper expressions, dangerous shell-option optional bind-wrapper expressions, plus safe `--version` and missing optional method guards. Uncalled bind, dynamic receivers, spread, variables, expression arguments, and invocation arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-bind-optional-a938-focused-green-20260722-214618.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-bind-optional-a938-related-20260722-214628.log` (3 files / 6 tests), `agent-core-build-a938-20260722-214628.log`, `node-static-from-char-code-bind-optional-dist-fixed-a938-20260722-214639.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a938-20260722-214639.log` (230 files / 706 tests), and `agent-core-full-test-a938-20260722-214640.log` (327 files / 1979 tests). Production source line counts remain under 200 after the split: `javascript-static-from-char-code.utils.ts` 89, `javascript-static-code-wrapper.utils.ts` 121, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A939 | done | Core E2E | Detect Node child_process static JS partial `String.fromCharCode.bind(null, ...)(...)` string expressions, so `spawnSync(String.fromCharCode.bind(null, 114)(109), [String.fromCharCode.bind(null, 45)(114, 102), "/"])`, static assignments from partial bind invocation calls, `execSync(String.fromCharCode.bind(null, 114, 109, 32)(45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode.bind(null, 114, 109, 32)(45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-bind-callargs-runtime-a939-20260722-214923.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node combines bound and invocation arguments for `String.fromCharCode.bind(null, ...)(...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe partial bind `--version` produced `--version`, and the dynamic invocation control demonstrated the runtime can produce `rm` while remaining unresolved to static analysis when the invocation argument is non-literal. Pre-fix current-dist probe `node-static-from-char-code-bind-callargs-dist-before-a939-20260722-214923.log` reproduced 4 mismatches where dangerous partial bind callarg direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic invocation argument guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-bind-callargs-a939-focused-red-20260722-215026.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` combined static decimal integers bound through `.bind(null, ...)` with static decimal integers supplied to the immediate invocation. The parser still rejects dynamic invocation arguments, spread, variables, expression arguments, dynamic receivers, and non-empty invocation forms with unresolved pieces. Tests now cover dangerous direct argv partial bind callarg expressions, partial bind callarg static assignments, dangerous `execSync` partial bind callarg expressions, dangerous shell-option partial bind callarg expressions, plus safe `--version` and dynamic invocation argument guards. Verification passed in `agent-core-node-static-from-char-code-bind-callargs-a939-focused-green-20260722-215054.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-bind-callargs-a939-related-20260722-215104.log` (3 files / 6 tests), `agent-core-build-a939-20260722-215104.log`, `node-static-from-char-code-bind-callargs-dist-fixed-a939-20260722-215116.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a939-20260722-215116.log` (230 files / 706 tests), and `agent-core-full-test-a939-20260722-215116.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 126, `javascript-static-from-char-code.utils.ts` 89, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A940 | done | Core E2E | Detect Node child_process static JS receiver-only `String.fromCharCode.bind(null)(...)` string expressions, so `spawnSync(String.fromCharCode.bind(null)(114, 109), [String.fromCharCode.bind(null)(45, 114, 102), "/"])`, static assignments from receiver-only bind invocation calls, `execSync(String.fromCharCode.bind(null)(114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode.bind(null)(114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-bind-receiver-callargs-runtime-a940-20260722-215350.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes receiver-only `String.fromCharCode.bind(null)(...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe receiver-only bind `--version` produced `--version`, and empty invocation returned an empty string as a guard control. Pre-fix current-dist probe `node-static-from-char-code-bind-receiver-callargs-dist-before-a940-20260722-215350.log` reproduced 4 mismatches where dangerous receiver-only bind callarg direct/shell/shell-option forms returned `ask_user`; safe `--version` and empty invocation guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-bind-receiver-callargs-a940-focused-red-20260722-215443.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` allowed `.bind(null)` followed by an immediate invocation with static decimal integer arguments while still rejecting empty invocation and unresolved pieces. Tests now cover dangerous direct argv receiver-only bind callarg expressions, receiver-only bind callarg static assignments, dangerous `execSync` receiver-only bind callarg expressions, dangerous shell-option receiver-only bind callarg expressions, plus safe `--version` and empty invocation guards. Dynamic invocation arguments, spread, variables, expression arguments, dynamic receivers, and optional receiver-only bind remain unresolved. Verification passed in `agent-core-node-static-from-char-code-bind-receiver-callargs-a940-focused-green-20260722-215513.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-bind-receiver-callargs-a940-related-20260722-215523.log` (3 files / 6 tests), `agent-core-build-a940-20260722-215523.log`, `node-static-from-char-code-bind-receiver-callargs-dist-fixed-a940-20260722-215535.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a940-20260722-215535.log` (230 files / 706 tests), and `agent-core-full-test-a940-20260722-215535.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 138, `javascript-static-from-char-code.utils.ts` 89, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A941 | done | Core E2E | Detect Node child_process static JS bracket `String["fromCharCode"](...)` method expressions, so `spawnSync(String["fromCharCode"](114, 109), [String["fromCharCode"](45, 114, 102), "/"])`, static assignments from bracket method calls, `execSync(String["fromCharCode"](114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String["fromCharCode"](114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-bracket-runtime-a941-20260722-215830.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes static bracket `String["fromCharCode"](...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe bracket method `--version` produced `--version`, and a dynamic method key control demonstrated runtime can produce `rm` while remaining unresolved when the key is not a static literal. Pre-fix current-dist probe `node-static-from-char-code-bracket-dist-before-a941-20260722-215830.log` reproduced 4 mismatches where dangerous bracket method direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic method key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-bracket-a941-focused-red-20260722-215926.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` accepted static bracket member keys matching exactly `"fromCharCode"` or `'fromCharCode'` before reusing the existing static decimal argument parser. Tests now cover dangerous direct argv bracket method expressions, bracket method static assignments, dangerous `execSync` bracket method expressions, dangerous shell-option bracket method expressions, plus safe `--version` and dynamic key guards. Dynamic bracket keys, escaped string keys, computed keys, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-bracket-a941-focused-green-20260722-215957.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-bracket-a941-related-20260722-220010.log` (3 files / 6 tests), `agent-core-build-a941-20260722-220010.log`, `node-static-from-char-code-bracket-dist-fixed-a941-20260722-220021.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a941-20260722-220021.log` (230 files / 706 tests), and `agent-core-full-test-a941-20260722-220022.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 116, `javascript-static-code-wrapper.utils.ts` 138, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A942 | done | Core E2E | Detect Node child_process static JS computed bracket method keys such as `String["from" + "CharCode"](...)`, so `spawnSync(String["from" + "CharCode"](114, 109), [String["from" + "CharCode"](45, 114, 102), "/"])`, static assignments from computed bracket key calls, `execSync(String["from" + "CharCode"](114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String["from" + "CharCode"](114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-computed-key-runtime-a942-20260722-220259.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes static computed bracket key `String["from" + "CharCode"](...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe computed key `--version` produced `--version`, and a dynamic suffix control demonstrated runtime can produce `rm` while remaining unresolved when the key suffix is not static. Pre-fix current-dist probe `node-static-from-char-code-computed-key-dist-before-a942-20260722-220259.log` reproduced 4 mismatches where dangerous computed bracket key direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic suffix guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-computed-key-a942-focused-red-20260722-220357.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` folded bracket member keys made from one or more quoted string literal pieces joined by `+`, accepting only final keys matching `fromCharCode` or `fromCodePoint`. Tests now cover dangerous direct argv computed key expressions, computed key static assignments, dangerous `execSync` computed key expressions, dangerous shell-option computed key expressions, plus safe `--version` and dynamic suffix guards. Dynamic suffixes, non-literal computed keys, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-computed-key-a942-focused-green-20260722-220459.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-computed-key-a942-related-20260722-220510.log` (3 files / 6 tests), `agent-core-build-a942-20260722-220510.log`, `node-static-from-char-code-computed-key-dist-fixed-a942-20260722-220524.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a942-20260722-220524.log` (230 files / 706 tests), and `agent-core-full-test-a942-20260722-220524.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 131, `javascript-static-code-wrapper.utils.ts` 138, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A943 | done | Core E2E | Detect Node child_process static JS optional direct `String.fromCharCode?.(...)` method calls, so `spawnSync(String.fromCharCode?.(114, 109), [String.fromCharCode?.(45, 114, 102), "/"])`, static assignments from optional direct calls, `execSync(String.fromCharCode?.(114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode?.(114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-optional-direct-runtime-a943-20260722-220800.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional direct `String.fromCharCode?.(...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe optional direct `--version` produced `--version`, and a missing optional receiver returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-optional-direct-dist-before-a943-20260722-220800.log` reproduced 4 mismatches where dangerous optional direct call direct/shell/shell-option forms returned `ask_user`; safe `--version` and missing optional receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-optional-direct-a943-focused-red-20260722-220903.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` allowed the direct argument-start parser to accept `?.(` as an immediate invocation start while preserving the existing literal `String.fromCharCode` receiver requirement. Tests now cover dangerous direct argv optional direct expressions, optional direct static assignments, dangerous `execSync` optional direct expressions, dangerous shell-option optional direct expressions, plus safe `--version` and missing optional receiver guards. Missing optional receivers, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-optional-direct-a943-focused-green-20260722-220939.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-optional-direct-a943-related-20260722-220950.log` (3 files / 6 tests), `agent-core-build-a943-20260722-220950.log`, `node-static-from-char-code-optional-direct-dist-fixed-a943-20260722-221011.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a943-20260722-221011.log` (230 files / 706 tests), and `agent-core-full-test-a943-20260722-221012.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 139, `javascript-static-from-char-code.utils.ts` 131, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A944 | done | Core E2E | Detect Node child_process static JS optional receiver member `String?.fromCharCode(...)` calls, so `spawnSync(String?.fromCharCode(114, 109), [String?.fromCharCode(45, 114, 102), "/"])`, static assignments from optional receiver calls, `execSync(String?.fromCharCode(114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String?.fromCharCode(114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-optional-receiver-runtime-a944-20260722-221318.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional receiver `String?.fromCharCode(...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe optional receiver `--version` produced `--version`, and a null dynamic receiver returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-optional-receiver-dist-before-a944-20260722-221319.log` reproduced 4 mismatches where dangerous optional receiver call direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-optional-receiver-a944-focused-red-20260722-221420.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` allowed the literal `String` receiver member parser to accept `?.` before the static method name. Tests now cover dangerous direct argv optional receiver expressions, optional receiver static assignments, dangerous `execSync` optional receiver expressions, dangerous shell-option optional receiver expressions, plus safe `--version` and dynamic/null receiver guards. Dynamic receivers, missing non-String receivers, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-optional-receiver-a944-focused-green-20260722-221453.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-optional-receiver-a944-related-20260722-221504.log` (3 files / 6 tests), `agent-core-build-a944-20260722-221505.log`, `node-static-from-char-code-optional-receiver-dist-fixed-a944-20260722-221521.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a944-20260722-221521.log` (230 files / 706 tests), and `agent-core-full-test-a944-20260722-221522.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 132, `javascript-static-code-wrapper.utils.ts` 139, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A945 | done | Core E2E | Detect Node child_process static JS optional receiver bracket member `String?.["fromCharCode"](...)` calls, so `spawnSync(String?.["fromCharCode"](114, 109), [String?.["fromCharCode"](45, 114, 102), "/"])`, static assignments from optional receiver bracket calls, `execSync(String?.["fromCharCode"](114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String?.["fromCharCode"](114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-optional-receiver-bracket-runtime-a945-20260722-221815.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional receiver bracket `String?.["fromCharCode"](...)` strings in direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe optional receiver bracket `--version` produced `--version`, and a null dynamic receiver returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-optional-receiver-bracket-dist-before-a945-20260722-221816.log` reproduced 4 mismatches where dangerous optional receiver bracket direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-optional-receiver-bracket-a945-focused-red-20260722-222104.log` (3 failing files / 3 failing tests), then passed after `javascript-static-from-char-code.utils.ts` allowed the literal `String` receiver member parser to accept `?.[` and reuse the existing bracket static key parser for `fromCharCode` / `fromCodePoint` before static decimal argument parsing. Tests now cover dangerous direct argv optional receiver bracket expressions, optional receiver bracket static assignments, dangerous `execSync` optional receiver bracket expressions, dangerous shell-option optional receiver bracket expressions, plus safe `--version` and dynamic/null receiver guards. Dynamic receivers, missing non-String receivers, spread, variables, expression arguments, and non-static bracket keys remain unresolved. Verification passed in `agent-core-node-static-from-char-code-optional-receiver-bracket-a945-focused-green-20260722-222137.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-optional-receiver-bracket-a945-related-20260722-222147.log` (3 files / 6 tests), `agent-core-build-a945-20260722-222153.log`, `node-static-from-char-code-optional-receiver-bracket-dist-fixed-a945-20260722-222201.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a945-20260722-222210.log` (230 files / 706 tests), and `agent-core-full-test-a945-20260722-222226.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-from-char-code.utils.ts` 133, `javascript-static-code-wrapper.utils.ts` 139, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A946 | done | Core E2E | Detect Node child_process static JS bracket `Function.prototype.call` wrappers on `String.fromCharCode`, so `spawnSync(String.fromCharCode["call"](null, 114, 109), [String.fromCharCode["call"](null, 45, 114, 102), "/"])`, static assignments from bracket-call wrappers, `execSync(String.fromCharCode["call"](null, 114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode["call"](null, 114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-bracket-call-runtime-a946-20260722-222612.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes bracket `["call"]` wrappers for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe bracket-call `--version` produced `--version`, and a non-null receiver still produced `rm` at runtime while remaining a guard input for static analysis. Pre-fix current-dist probe `node-static-from-char-code-bracket-call-dist-before-a946-20260722-222617.log` reproduced 4 mismatches where dangerous bracket-call direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-bracket-call-a946-focused-red-20260722-222724.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` replaced duplicated dot-only wrapper checks with a shared wrapper member reader that accepts `.call` and static bracket `["call"]` keys before reusing the existing null-receiver and decimal argument parser. Tests now cover dangerous direct argv bracket-call expressions, bracket-call static assignments, dangerous `execSync` bracket-call expressions, dangerous shell-option bracket-call expressions, plus safe `--version` and dynamic receiver guards. Non-null receivers, dynamic wrapper keys, computed wrapper keys, optional bracket-call invocation, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-bracket-call-a946-focused-green-20260722-222807.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-bracket-call-a946-related-20260722-222813.log` (3 files / 6 tests), `agent-core-build-a946-20260722-222819.log`, `node-static-from-char-code-bracket-call-dist-fixed-a946-20260722-222829.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a946-20260722-222834.log` (230 files / 706 tests), and `agent-core-full-test-a946-20260722-222848.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 165, `javascript-static-from-char-code.utils.ts` 133, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A947 | done | Core E2E | Detect Node child_process static JS computed bracket `Function.prototype.call` wrappers on `String.fromCharCode`, so `spawnSync(String.fromCharCode["ca" + "ll"](null, 114, 109), [String.fromCharCode["ca" + "ll"](null, 45, 114, 102), "/"])`, static assignments from computed bracket-call wrappers, `execSync(String.fromCharCode["ca" + "ll"](null, 114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode["ca" + "ll"](null, 114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-computed-wrapper-call-runtime-a947-20260722-223622.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes computed bracket `["ca" + "ll"]` wrappers for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe computed bracket-call `--version` produced `--version`, and a dynamic suffix control still produced `rm` at runtime while remaining unresolved to static analysis. Pre-fix current-dist probe `node-static-from-char-code-computed-wrapper-call-dist-before-a947-20260722-223627.log` reproduced 4 mismatches where dangerous computed bracket-call direct/shell/shell-option forms returned `ask_user`; safe `--version` and dynamic wrapper-key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-computed-wrapper-call-a947-focused-red-20260722-223721.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` folded bracket wrapper member keys made from quoted string literal pieces joined by `+`, accepting only final keys matching `call`, `apply`, or `bind` before reusing the existing null-receiver and decimal argument parser. Tests now cover dangerous direct argv computed bracket-call expressions, computed bracket-call static assignments, dangerous `execSync` computed bracket-call expressions, dangerous shell-option computed bracket-call expressions, plus safe `--version` and dynamic suffix guards. Dynamic wrapper keys, non-literal computed keys, non-null receivers, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-computed-wrapper-call-a947-focused-green-20260722-223744.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-computed-wrapper-call-a947-related-20260722-223754.log` (3 files / 6 tests), `agent-core-build-a947-20260722-223800.log`, `node-static-from-char-code-computed-wrapper-call-dist-fixed-a947-20260722-223808.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a947-20260722-223812.log` (230 files / 706 tests), and `agent-core-full-test-a947-20260722-223828.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 177, `javascript-static-from-char-code.utils.ts` 133, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A948 | done | Core E2E | Detect Node child_process static JS optional bracket `Function.prototype.call` wrappers on `String.fromCharCode`, so `spawnSync(String.fromCharCode?.["call"](null, 114, 109), [String.fromCharCode?.["call"](null, 45, 114, 102), "/"])`, static assignments from optional bracket-call wrappers, `execSync(String.fromCharCode?.["call"](null, 114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode?.["call"](null, 114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-optional-wrapper-bracket-call-runtime-a948-20260722-224122.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional bracket `?.["call"]` wrappers for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe optional bracket-call `--version` produced `--version`, and a missing optional receiver returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-optional-wrapper-bracket-call-dist-before-a948-20260722-224127.log` reproduced 4 mismatches where dangerous optional bracket-call direct/shell/shell-option forms returned `ask_user`; safe `--version` and missing optional receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-optional-wrapper-bracket-call-a948-focused-red-20260722-224222.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` allowed the wrapper member reader to accept `?.[` before reusing the existing static bracket wrapper key parser, null-receiver check, and decimal argument parser. Tests now cover dangerous direct argv optional bracket-call expressions, optional bracket-call static assignments, dangerous `execSync` optional bracket-call expressions, dangerous shell-option optional bracket-call expressions, plus safe `--version` and missing optional receiver guards. Missing optional receivers, dynamic wrapper keys, non-literal computed keys, non-null receivers, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-optional-wrapper-bracket-call-a948-focused-green-20260722-224247.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-optional-wrapper-bracket-call-a948-related-20260722-224255.log` (3 files / 6 tests), `agent-core-build-a948-20260722-224301.log`, `node-static-from-char-code-optional-wrapper-bracket-call-dist-fixed-a948-20260722-224309.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a948-20260722-224314.log` (230 files / 706 tests), and `agent-core-full-test-a948-20260722-224331.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 180, `javascript-static-from-char-code.utils.ts` 133, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A949 | done | Core E2E | Detect Node child_process static JS optional dot `Function.prototype.call` wrappers on `String.fromCharCode`, so `spawnSync(String.fromCharCode?.call(null, 114, 109), [String.fromCharCode?.call(null, 45, 114, 102), "/"])`, static assignments from optional dot-call wrappers, `execSync(String.fromCharCode?.call(null, 114, 109, 32, 45, 114, 102, 32, 47))`, and shell-option `spawnSync(String.fromCharCode?.call(null, 114, 109, 32, 45, 114, 102, 32, 47), [], {shell:true})` deny instead of slipping through as user-reviewable. | Runtime probe `node-static-from-char-code-optional-wrapper-dot-call-runtime-a949-20260722-224617.log` used a temporary fake `rm` earlier on `PATH` and confirmed real Node executes optional dot `?.call` wrappers for direct argv command/args, static string and argv assignments, `execSync` shell strings, and `spawnSync` shell-option strings with `argc=2 args=-rf /`; safe optional dot-call `--version` produced `--version`, and a missing optional receiver returned `not-executed`. Pre-fix current-dist probe `node-static-from-char-code-optional-wrapper-dot-call-dist-before-a949-20260722-224625.log` reproduced 4 mismatches where dangerous optional dot-call direct/shell/shell-option forms returned `ask_user`; safe `--version` and missing optional receiver guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-from-char-code-optional-wrapper-dot-call-a949-focused-red-20260722-224718.log` (3 failing files / 3 failing tests), then passed after `javascript-static-code-wrapper.utils.ts` extracted a dot-wrapper member reader and allowed `?.call` / `?.apply` / `?.bind` to reuse the same static member-name, null-receiver, and decimal argument parser as the existing dot wrapper path. Tests now cover dangerous direct argv optional dot-call expressions, optional dot-call static assignments, dangerous `execSync` optional dot-call expressions, dangerous shell-option optional dot-call expressions, plus safe `--version` and missing optional receiver guards. Missing optional receivers, dynamic wrapper keys, non-literal computed keys, non-null receivers, spread, variables, and expression arguments remain unresolved. Verification passed in `agent-core-node-static-from-char-code-optional-wrapper-dot-call-a949-focused-green-20260722-224750.log` (3 files / 6 tests), `agent-core-node-static-from-char-code-optional-wrapper-dot-call-a949-related-20260722-224800.log` (3 files / 6 tests), `agent-core-build-a949-20260722-224806.log`, `node-static-from-char-code-optional-wrapper-dot-call-dist-fixed-a949-20260722-224814.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a949-20260722-224820.log` (230 files / 706 tests), and `agent-core-full-test-a949-20260722-224839.log` (327 files / 1979 tests). Production source line counts remain under 200: `javascript-static-code-wrapper.utils.ts` 191, `javascript-static-from-char-code.utils.ts` 133, `javascript-static-decimal-code.utils.ts` 49, `javascript-static-string.utils.ts` 193, `javascript-static-array-join.utils.ts` 88, `javascript-static-value.utils.ts` 173, `interpreter-shell-command.utils.ts` 187, `node-direct-command.utils.ts` 64, and `node-shell-option.utils.ts` 70. |
| A950 | done | Core E2E | Detect Python `subprocess` shell execution when the `shell` truthy flag is wrapped in simple parentheses, so `subprocess.run("rm -rf /", shell=(True))`, `subprocess.Popen("rm -rf /", shell=(1))`, `subprocess.run("rm -rf /", shell=("yes"))`, and `flag=(True); subprocess.run("rm -rf /", shell=flag)` deny instead of slipping through as user-reviewable. | Runtime probe `python-parenthesized-shell-truthy-runtime-a950-rerun-20260722-225232.log` confirmed real Python treats simple parenthesized `True`, `1`, `"yes"`, and static `flag=(True)` as shell-enabled and reaches a fake `rm` with `argc=2 args=-rf /`, while parenthesized `False` and `0` stay non-shell guarded. Pre-fix current-dist probe `python-parenthesized-shell-truthy-dist-before-a950-20260722-225238.log` reproduced 4 mismatches where dangerous parenthesized shell flags returned `ask_user`; safe `False` / `0` guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-parenthesized-shell-truthy-a950-focused-red-20260722-225311.log` (1 failing file / 1 failing test), then passed after `python-truthy-value.utils.ts` refactored static truthy parsing into an end-index-aware reader that accepts simple parenthesized string literals, static string references, static truthy references, booleans, `None`, and integers, with static truthy assignment parsing reusing the same reader. Tests now cover dangerous parenthesized `True`, `1`, `"yes"`, and static truthy flag inputs plus safe parenthesized `False` and `0` guards. Composite tuple/list/dict truthiness, computed expressions, future assignments, and dynamic shell flags remain unresolved/guarded. Verification passed in `agent-core-python-parenthesized-shell-truthy-a950-focused-green-20260722-225557.log` (1 file / 2 tests), `agent-core-python-parenthesized-shell-truthy-a950-related-20260722-225609.log` (2 files / 4 tests), `agent-core-build-a950-20260722-225617.log`, `python-parenthesized-shell-truthy-dist-fixed-a950-20260722-225624.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a950-expanded-20260722-225654.log` (230 files / 706 tests), and `agent-core-full-test-a950-20260722-225708.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-truthy-value.utils.ts` 136, `python-shell-command.utils.ts` 136, `python-static-string.utils.ts` 65, `python-string-literal.utils.ts` 100, `interpreter-shell-command.utils.ts` 187, `interpreter-direct-command.utils.ts` 81, and `javascript-static-code-wrapper.utils.ts` 191. |
| A951 | done | Core E2E | Detect Python `subprocess` shell execution when the `shell` truthy flag is built through static `bool(...)` calls, so `subprocess.run("rm -rf /", shell=bool("yes"))`, `subprocess.Popen("rm -rf /", shell=bool(1))`, `subprocess.run("rm -rf /", shell=(bool(1)))`, and `flag=bool("yes"); subprocess.run("rm -rf /", shell=flag)` deny instead of slipping through as user-reviewable. | Runtime probe `python-bool-shell-truthy-runtime-a951-20260722-230059.log` confirmed real Python treats static `bool("yes")`, `bool(1)`, parenthesized `bool(1)`, and static `flag=bool("yes")` as shell-enabled and reaches a fake `rm` with `argc=2 args=-rf /`, while `bool(False)` and `bool("")` stay non-shell guarded. Pre-fix current-dist probe `python-bool-shell-truthy-dist-before-a951-20260722-230059.log` reproduced 4 mismatches where dangerous static bool shell flags returned `ask_user`; safe bool false/empty-string guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-bool-shell-truthy-a951-focused-red-20260722-230136.log` (1 failing file / 1 failing test), then passed after `python-truthy-value.utils.ts` added a narrow `bool(...)` static reader that reuses the same end-index-aware truthy parser for the single argument, requires the call to close cleanly, and skips builtin `bool` handling when the current scanner has seen a prior `bool` assignment. Tests now cover dangerous static bool string/integer/parenthesized/static-flag inputs plus safe `bool(False)` and `bool("")` guards. Dynamic bool arguments, multiple bool arguments, untracked `def bool(...)` shadowing, and other computed truthiness remain unresolved/guarded. Verification passed in `agent-core-python-bool-shell-truthy-a951-focused-green-20260722-230229.log` (1 file / 2 tests), `agent-core-python-bool-shell-truthy-a951-related-20260722-230241.log` (2 files / 4 tests), `agent-core-build-a951-20260722-230247.log`, `python-bool-shell-truthy-dist-fixed-a951-20260722-230256.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a951-20260722-230303.log` (230 files / 706 tests), and `agent-core-full-test-a951-20260722-230318.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-truthy-value.utils.ts` 169, `python-shell-command.utils.ts` 136, `python-static-string.utils.ts` 65, `python-string-literal.utils.ts` 100, `interpreter-shell-command.utils.ts` 187, `interpreter-direct-command.utils.ts` 81, and `javascript-static-code-wrapper.utils.ts` 191. |
| A952 | done | Core E2E | Avoid false-positive denial for Python `subprocess` shell flags when local code shadows builtin `bool`, so `def bool(...): return False; subprocess.run("rm -rf /", shell=bool("yes"))` and `class bool: ... __bool__ -> False; subprocess.run("rm -rf /", shell=bool("yes"))` remain user-reviewable instead of being treated as builtin `bool("yes")`. | Runtime probe `python-bool-shadow-shell-runtime-a952-20260722-230655.log` confirmed real Python executes the builtin `bool("yes")` control through shell, while local `def bool`, local `class bool`, and assignment shadowing guards with false truthiness do not enter shell and raise the non-shell `FileNotFoundError` path. Pre-fix current-dist probe `python-bool-shadow-shell-dist-before-a952-20260722-230655.log` reproduced 2 false-positive mismatches where `def bool` and `class bool` shadowing returned `deny`; builtin `bool("yes")` stayed `deny`, and assignment shadowing already stayed `ask_user`. Focused E2E first failed in `agent-core-python-bool-shadow-shell-a952-focused-red-20260722-230720.log` (1 failing file / 1 failing test), then passed after `python-truthy-value.utils.ts` expanded the builtin-bool shadow guard to treat prior `def bool` or `class bool` declarations as local name shadowing before accepting static `bool(...)` as builtin truthiness. Tests now cover safe `def bool` and `class bool` shadowing while preserving the A951 builtin bool-deny behavior. Shadowing via imports, nested scopes, late definitions, and user-defined `bool` functions that return truthy values remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-bool-shadow-shell-a952-focused-green-20260722-230745.log` (1 file / 2 tests), `agent-core-python-bool-shadow-shell-a952-related-20260722-230756.log` (2 files / 4 tests), `agent-core-build-a952-20260722-230805.log`, `python-bool-shadow-shell-dist-fixed-a952-20260722-230812.log` (4 cases / 0 mismatches), `agent-core-auto-reviewer-a952-20260722-230817.log` (230 files / 706 tests), and `agent-core-full-test-a952-20260722-230832.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-truthy-value.utils.ts` 177, `python-shell-command.utils.ts` 136, `python-static-string.utils.ts` 65, `python-string-literal.utils.ts` 100, `interpreter-shell-command.utils.ts` 187, `interpreter-direct-command.utils.ts` 81, and `javascript-static-code-wrapper.utils.ts` 191. |
| A953 | done | Core E2E | Detect Python `subprocess` shell execution when the `shell` truthy flag is built with static unary `not`, so `subprocess.run("rm -rf /", shell=not False)`, `subprocess.Popen("rm -rf /", shell=not 0)`, `flag=not False; subprocess.run("rm -rf /", shell=flag)`, and `shell=(not False)` deny instead of slipping through as user-reviewable. | Runtime probe `python-not-shell-truthy-runtime-a953-20260722-231116.log` confirmed real Python treats static `not False`, `not 0`, static `flag=not False`, and parenthesized `(not False)` as shell-enabled and reaches a fake `rm` with `argc=2 args=-rf /`, while `not True` and `not "yes"` stay non-shell guarded. Pre-fix current-dist probe `python-not-shell-truthy-dist-before-a953-20260722-231116.log` reproduced 4 mismatches where dangerous static `not` shell flags returned `ask_user`; safe `not True` and `not "yes"` guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-not-shell-truthy-a953-focused-red-20260722-231149.log` (1 failing file / 1 failing test), then passed after `python-truthy-value.utils.ts` added a narrow `not` prefix reader that requires keyword boundary, delegates the operand to the same static truthy parser, and flips only fully resolved static operand values. Tests now cover dangerous direct, `Popen`, static flag, and parenthesized `not` shell flags plus safe `not True` and `not "yes"` guards. Dynamic operands, compound boolean expressions, and side-effecting truthiness remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-not-shell-truthy-a953-focused-green-20260722-231221.log` (1 file / 2 tests), `agent-core-python-not-shell-truthy-a953-related-20260722-231232.log` (2 files / 4 tests), `agent-core-build-a953-20260722-231238.log`, `python-not-shell-truthy-dist-fixed-a953-20260722-231244.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a953-20260722-231250.log` (230 files / 706 tests), and `agent-core-full-test-a953-20260722-231303.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-truthy-value.utils.ts` 194, `python-shell-command.utils.ts` 136, `python-static-string.utils.ts` 65, `python-string-literal.utils.ts` 100, `interpreter-shell-command.utils.ts` 187, `interpreter-direct-command.utils.ts` 81, and `javascript-static-code-wrapper.utils.ts` 191; further truthy parsing additions should split `python-truthy-value.utils.ts` before adding more logic. |
| A954 | done | Core E2E | Detect Python direct process calls that use annotated static argv assignments, so `argv: list[str] = ["rm", "-rf", "/"]; subprocess.run(argv)`, `argv: tuple[str, str, str] = ("rm", "-rf", "/"); subprocess.Popen(argv)`, and `subprocess.run(args=argv)` deny instead of slipping through as user-reviewable. | Runtime probe `python-annotated-argv-runtime-a954-20260722-231640.log` confirmed real Python executes annotated list, annotated tuple, and annotated `args=argv` direct argv forms through a fake `rm` with `argc=2 args=-rf /`, while annotated `["rm", "--version"]` remains a non-dangerous direct call. Pre-fix current-dist probe `python-annotated-argv-dist-before-a954-20260722-231640.log` reproduced 3 false-negative mismatches where dangerous annotated argv references returned `ask_user`; the annotated `--version` guard stayed `ask_user`. Focused E2E first failed in `agent-core-python-annotated-argv-a954-focused-red-20260722-231712.log` (1 failing file / 1 failing test), then passed after `python-static-argv.utils.ts` allowed `pythonStaticArgvAssignments` to recognize simple annotated assignments before reusing the existing list/tuple argv literal reader and static reference safeguards. Tests now cover annotated dangerous list, tuple, and `args` keyword argv references plus an annotated safe `--version` guard. Dynamic annotations are ignored as type text only; non-literal values, future assignments, and overridden argv values remain unresolved/user-reviewable. Verification passed in `agent-core-python-annotated-argv-a954-focused-green-20260722-231752.log` (1 file / 2 tests), `agent-core-python-annotated-argv-a954-related-20260722-231811.log` (2 files / 4 tests), `agent-core-build-a954-20260722-231824.log`, `python-annotated-argv-dist-fixed-a954-20260722-231832.log` (4 cases / 0 mismatches), `agent-core-auto-reviewer-a954-20260722-231838.log` (230 files / 706 tests), and `agent-core-full-test-a954-20260722-231858.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-static-argv.utils.ts` 110, `python-direct-command.utils.ts` 145, `python-os-process-command.utils.ts` 145, `python-truthy-value.utils.ts` 194, `interpreter-direct-command.utils.ts` 81, `interpreter-shell-command.utils.ts` 187, and `javascript-static-code-wrapper.utils.ts` 191. |
| A955 | done | Core E2E | Detect Python static string references assigned with type annotations across shell and direct process paths, so `cmd: str = "rm -rf /"; subprocess.run(cmd, shell=True)`, `exe: str = "rm"; subprocess.run(["not-rm", "-rf", "/"], executable=exe)`, and `flag: str = "-rf"; subprocess.run(["rm", flag, "/"])` deny instead of slipping through as user-reviewable. | Runtime probe `python-annotated-static-string-runtime-a955-20260722-232131.log` confirmed real Python executes annotated shell command strings, annotated executable strings, and annotated argv element strings through a fake `rm` with `argc=2 args=-rf /`, while annotated `flag: str = "--version"` remains a non-dangerous direct call. Pre-fix current-dist probe `python-annotated-static-string-dist-before-a955-20260722-232131.log` reproduced 3 false-negative mismatches where dangerous annotated static string references returned `ask_user`; the annotated `--version` guard stayed `ask_user`. Focused E2E first failed in `agent-core-python-annotated-static-string-a955-focused-red-20260722-232206.log` (2 failing files / 2 failing tests), then passed after `python-static-string.utils.ts` allowed `pythonStaticStringAssignments` to recognize simple annotated assignments before reusing the existing literal parser, assignment-boundary check, future-assignment guard, and override safeguards. Tests now cover annotated shell command strings, direct executable strings, argv element strings, and annotated safe `--version` flags. Dynamic annotations are ignored as type text only; non-literal values, future assignments, and overridden values remain unresolved/user-reviewable. Verification passed in `agent-core-python-annotated-static-string-a955-focused-green-20260722-232235.log` (2 files / 4 tests), `agent-core-python-annotated-static-string-a955-related-20260722-232247.log` (2 files / 4 tests), `agent-core-build-a955-20260722-232256.log`, `python-annotated-static-string-dist-fixed-a955-20260722-232306.log` (4 cases / 0 mismatches), `agent-core-auto-reviewer-a955-20260722-232312.log` (230 files / 706 tests), and `agent-core-full-test-a955-20260722-232854.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-static-string.utils.ts` 65, `python-static-argv.utils.ts` 110, `python-direct-command.utils.ts` 145, `python-shell-command.utils.ts` 136, `python-truthy-value.utils.ts` 194, `interpreter-direct-command.utils.ts` 81, `interpreter-shell-command.utils.ts` 187, and `javascript-static-code-wrapper.utils.ts` 191. |
| A956 | done | Core E2E | Detect Python `subprocess` shell execution when the `shell` flag is a static truthy assignment with a type annotation, so `flag: bool = True; subprocess.run("rm -rf /", shell=flag)` and `flag: int = 1; subprocess.Popen("rm -rf /", shell=flag)` deny instead of slipping through as user-reviewable. | Runtime probe `python-annotated-truthy-runtime-a956-20260722-233410.log` confirmed real Python treats annotated `bool=True` and `int=1` shell flags as shell-enabled and reaches a fake `rm` with `argc=2 args=-rf /`, while annotated `False` and `0` stay non-shell guarded. Pre-fix current-dist probe `python-annotated-truthy-dist-before-a956-20260722-233410.log` reproduced 2 false-negative mismatches where dangerous annotated truthy shell flags returned `ask_user`; safe annotated false/zero guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-annotated-truthy-a956-focused-red-20260722-233449.log` (1 failing file / 1 failing test), then passed after `python-truthy-value.utils.ts` switched static truthy assignment scanning to the shared annotated assignment pattern. New `python-assignment-pattern.utils.ts` now owns the common Python `name[: annotation] =` regex used by static string, argv, and truthy assignment scanners, reducing duplication while keeping production source files under 200 lines. Tests now cover dangerous annotated `bool=True` and `int=1` shell flags plus safe annotated `False` and `0` guards. Dynamic annotations are ignored as type text only; non-literal values, future assignments, overridden values, compound annotations with unsupported syntax, and dynamic truthiness remain unresolved/user-reviewable. Verification passed in `agent-core-python-annotated-truthy-a956-focused-green-20260722-233521.log` (1 file / 2 tests), `agent-core-python-annotated-truthy-a956-related-20260722-233544.log` (2 files / 4 tests), `agent-core-build-a956-20260722-233544.log`, `python-annotated-truthy-dist-fixed-a956-20260722-233559.log` (4 cases / 0 mismatches), `agent-core-auto-reviewer-a956-rerun-20260722-233634.log` (230 files / 706 tests), and `agent-core-full-test-a956-20260722-233559.log` (327 files / 1979 tests). Production source line counts remain under 200: `python-assignment-pattern.utils.ts` 5, `python-truthy-value.utils.ts` 193, `python-static-string.utils.ts` 64, `python-static-argv.utils.ts` 109, `python-shell-command.utils.ts` 136, `interpreter-shell-command.utils.ts` 187, and `javascript-static-code-wrapper.utils.ts` 191. |
| A957 | done | Core E2E | Detect Python `subprocess` shell execution when the `shell` flag is a static non-empty collection literal or a static collection flag, so `subprocess.run("rm -rf /", shell=[1])`, `subprocess.Popen("rm -rf /", shell=(1,))`, `subprocess.run(args="rm -rf /", shell={"ok": 1})`, and `flag=[1]; subprocess.run("rm -rf /", shell=flag)` deny instead of slipping through as user-reviewable. | Runtime probe `python-collection-truthy-runtime-a957-rerun-20260722-234237.log` confirmed real Python treats non-empty list, tuple, dict, and static list flags as shell-enabled and reaches a fake `rm` with `argc=2 args=-rf /`, while empty list, tuple, dict, and static empty-list flags stay non-shell guarded. Pre-fix current-dist probe `python-collection-truthy-dist-before-a957-rerun-20260722-234237.log` reproduced 4 false-negative mismatches where dangerous static collection truthy shell flags returned `ask_user`; all four empty-collection guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-collection-truthy-a957-focused-red-20260722-234148.log` (1 failing file / 1 failing test), then passed after `python-truthy-value.utils.ts` delegated static collection-literal truthiness to the new `python-collection-truthy.utils.ts`. The new utils owns balanced `[]`, `{}`, and tuple `()` literal scanning, skips Python string literals while finding collection boundaries, treats empty containers as false, requires a top-level comma for tuple truthiness, and preserves grouped-expression handling such as `(True)`. Tests now cover dangerous non-empty list, tuple, dict, and static list flags plus safe empty list, tuple, dict, and static empty-list guards. Dynamic collection expressions, `set(...)`, comprehensions, malformed tuples, comments inside literals, and side-effect analysis remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-collection-truthy-a957-focused-green-20260722-234357.log` (1 file / 2 tests), `agent-core-python-collection-truthy-a957-related-20260722-234415.log` (2 files / 4 tests), `agent-core-build-a957-20260722-234415.log`, `python-collection-truthy-dist-fixed-a957-20260722-234427.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a957-20260722-234427.log` (230 files / 706 tests), and `agent-core-full-test-a957-20260722-234427.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `python-collection-truthy.utils.ts` 94, `python-truthy-value.utils.ts` 200, `python-static-string.utils.ts` 64, `python-static-argv.utils.ts` 109, `python-shell-command.utils.ts` 136, `interpreter-shell-command.utils.ts` 187, and `javascript-static-code-wrapper.utils.ts` 191; future truthy parser additions should split `python-truthy-value.utils.ts` before adding more logic. |
| A958 | done | Core E2E | Detect Ruby/Perl shell command strings written with static quote-like literals, so `ruby -e 'system(%q{rm -rf /})'` and `perl -e 'system(qq{rm -rf /})'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-quote-like-shell-runtime-a958-20260722-234903.log` confirmed real Ruby `%q{rm -rf /}` and Perl `qq{rm -rf /}` reach a fake `rm` with `argc=2 args=-rf /`, while Ruby `%q{rm --version}` and Perl `q{rm --version}` remain non-dangerous command strings. Pre-fix current-dist probe `ruby-perl-quote-like-shell-dist-before-a958-20260722-234903.log` reproduced 2 false-negative mismatches where dangerous Ruby `%q` and Perl `qq` shell strings returned `ask_user`; safe `--version` and interpolation guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-quote-like-shell-a958-focused-red-20260722-234933.log` (1 failing file / 1 failing test), then passed after Ruby/Perl shell extraction moved into `interpreter-ruby-perl-shell-command.utils.ts`, with quote-like literal parsing isolated in `interpreter-quote-like-string.utils.ts` and `readStaticInterpreterString` accepting language-specific literal readers. The parser now accepts Ruby non-interpolating `%q` bodies and Perl static `q`/`qq` bodies without interpolation markers, while preserving existing quote and concat behavior. Tests now cover dangerous Ruby `%q`, dangerous Perl `qq`, and safe Ruby `%Q{...#{...}}` / Perl `qq{...$...}` interpolation guards. Dynamic interpolation, heredocs, command output literals, Ruby percent arrays, Perl here-docs, and direct argv quote-like forms remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-quote-like-shell-a958-focused-green-20260722-235150.log` (1 file / 2 tests), `agent-core-ruby-perl-quote-like-shell-a958-related-20260722-235215.log` (2 files / 4 tests), `agent-core-build-a958-20260722-235215.log`, `ruby-perl-quote-like-shell-dist-fixed-a958-20260722-235228.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a958-20260722-235228.log` (230 files / 706 tests), and `agent-core-full-test-a958-20260722-235228.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-quote-like-string.utils.ts` 79, `interpreter-ruby-perl-shell-command.utils.ts` 50, `interpreter-static-string.utils.ts` 55, `interpreter-shell-command.utils.ts` 143, `python-truthy-value.utils.ts` 200, and `javascript-static-code-wrapper.utils.ts` 191. |
| A959 | done | Core E2E | Detect Ruby/Perl command-output literals that execute shell commands, so Ruby backticks, Ruby `%x{rm -rf /}`, Perl backticks, and Perl `qx{rm -rf /}` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-command-output-runtime-a959-20260722-235545.log` confirmed real Ruby backticks, Ruby `%x`, Perl backticks, and Perl `qx` execute through shell and reach a fake `rm` with `argc=2 args=-rf /`, while Ruby `%x{rm --version}` and Perl `qx{rm --version}` remain non-dangerous command strings. Pre-fix current-dist probe `ruby-perl-command-output-dist-before-a959-20260722-235546.log` reproduced 4 false-negative mismatches where dangerous Ruby/Perl command-output literals returned `ask_user`; safe `--version` and interpolation guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-command-output-a959-focused-red-20260722-235620.log` (1 failing file / 1 failing test), then passed after `interpreter-quote-like-string.utils.ts` added command-output literal readers for Ruby/Perl backticks, Ruby `%x`, and Perl `qx`, and `interpreter-ruby-perl-shell-command.utils.ts` now scans inline Ruby/Perl code for those executable literals in addition to `system` / `exec` call arguments. The scan skips normal quoted strings and static quote-like strings before accepting command-output literals, and rejects Ruby `#{...}` / `#$` / `#@` plus Perl `$` / `@` interpolation as dynamic. Tests now cover dangerous Ruby backticks, Ruby `%x`, Perl backticks, Perl `qx`, safe `--version`, and Ruby/Perl interpolation guards. Dynamic interpolation, heredocs, nested parser edge cases, command output hidden inside unsupported quote syntaxes, and direct argv quote-like forms remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-command-output-a959-focused-green-20260722-235716.log` (1 file / 2 tests), `agent-core-ruby-perl-command-output-a959-related-20260722-235733.log` (2 files / 4 tests), `agent-core-build-a959-20260722-235734.log`, `ruby-perl-command-output-dist-fixed-a959-20260722-235748.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a959-20260722-235749.log` (230 files / 706 tests), and `agent-core-full-test-a959-20260722-235749.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-quote-like-string.utils.ts` 122, `interpreter-ruby-perl-shell-command.utils.ts` 90, `interpreter-static-string.utils.ts` 55, `interpreter-shell-command.utils.ts` 143, `python-truthy-value.utils.ts` 200, and `javascript-static-code-wrapper.utils.ts` 191. |
| A960 | done | Core E2E | Detect Ruby/Perl direct process calls that use static quote-like literals for executable names, so `ruby -e 'system(%q{rm}, "-rf", "/")'`, Ruby command array pair `system([%q{rm}, "custom-rm"], "-rf", "/")`, `perl -e 'system(q{rm}, "-rf", "/")'`, and Perl indirect executable `system { q{rm} } "custom-rm", "-rf", "/"` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-quote-like-direct-runtime-a960-20260723-000135.log` confirmed real Ruby `%q` direct command names, Ruby `%q` command array pairs, Perl `q` direct command names, and Perl `q` indirect executable blocks reach a fake `rm` with `argc=2 args=-rf /`, while Ruby `%q{rm} --version` and Perl `q{rm} --version` remain non-dangerous direct calls. Pre-fix current-dist probe `ruby-perl-quote-like-direct-dist-before-a960-20260723-000135.log` reproduced 4 false-negative mismatches where dangerous direct quote-like forms returned `ask_user`; safe `--version` and Ruby/Perl interpolation guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-quote-like-direct-a960-focused-red-20260723-000219.log` (1 failing file / 1 failing test), then passed after `readLiteralList` accepted optional language-specific literal readers and Ruby/Perl direct paths passed the existing quote-like readers into multi-arg calls, Ruby command array pair parsing, and Perl indirect executable parsing. Tests now cover dangerous Ruby `%q` direct command, Ruby `%q` array-pair command, Perl `q` direct command, Perl indirect `q` executable, and safe Ruby `%Q{#{...}}` / Perl `qq{$...}` interpolation guards. Dynamic interpolation, Ruby percent arrays, Perl heredocs, command-output direct argv forms, and unsupported quote-like delimiters remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-quote-like-direct-a960-focused-green-20260723-000331.log` (1 file / 2 tests), `agent-core-ruby-perl-quote-like-direct-a960-related-20260723-000353.log` (2 files / 4 tests), `agent-core-build-a960-20260723-000353.log`, `ruby-perl-quote-like-direct-dist-fixed-a960-20260723-000405.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a960-20260723-000405.log` (230 files / 706 tests), and `agent-core-full-test-a960-20260723-000405.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-literal-list.utils.ts` 82, `interpreter-direct-command.utils.ts` 92, `ruby-command-array-pair.utils.ts` 78, `perl-indirect-executable.utils.ts` 65, `interpreter-static-string.utils.ts` 55, `interpreter-quote-like-string.utils.ts` 122, and `python-truthy-value.utils.ts` 200. |
| A961 | done | Core E2E | Detect Ruby static `%Q{...}` quote-like strings without interpolation across shell and direct process paths, so `ruby -e 'system(%Q{rm -rf /})'` and `ruby -e 'system(%Q{rm}, "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-percent-q-static-runtime-a961-20260723-000738.log` confirmed real Ruby executes `%Q{rm -rf /}` as a shell command and `%Q{rm}` as a direct executable name, both reaching a fake `rm` with `argc=2 args=-rf /`, while `%Q{rm --version}` and `%Q{rm}, "--version"` remain non-dangerous command strings. Pre-fix current-dist probe `ruby-percent-q-static-dist-before-a961-20260723-000738.log` reproduced 2 false-negative mismatches where dangerous static `%Q` shell/direct forms returned `ask_user`; safe `--version` and `%Q` interpolation guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-percent-q-static-a961-focused-red-20260723-000805.log` (2 failing files / 2 failing tests), then passed after `readRubyQuoteLikeStringLiteral` accepted both `%q` and `%Q` bodies while preserving the existing Ruby interpolation guard for `#{...}`, `#$`, and `#@`. Tests now cover dangerous static `%Q` shell strings, dangerous static `%Q` direct executable names, safe static `%Q` version commands, and existing `%Q` interpolation guards. Dynamic interpolation, percent arrays, command-output forms beyond A959, unsupported delimiters, and side-effecting Ruby expressions remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-percent-q-static-a961-focused-green-20260723-000823.log` (2 files / 4 tests), `agent-core-ruby-percent-q-static-a961-related-20260723-000840.log` (2 files / 4 tests), `agent-core-build-a961-20260723-000840.log`, `ruby-percent-q-static-dist-fixed-a961-20260723-000854.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a961-20260723-000854.log` (230 files / 706 tests), and `agent-core-full-test-a961-20260723-000854.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-quote-like-string.utils.ts` 122, `interpreter-ruby-perl-shell-command.utils.ts` 90, `interpreter-direct-command.utils.ts` 92, `interpreter-shell-command.utils.ts` 143, `python-truthy-value.utils.ts` 200, and `javascript-static-code-wrapper.utils.ts` 191. |
| A962 | done | Core E2E | Detect Ruby/Perl static word-list direct process argv forms, so `ruby -e 'system(*%w[rm -rf /])'`, `ruby -e 'system(*%W[rm -rf /])'`, `ruby -e 'spawn(*%w[rm -rf /])'`, `perl -e 'system(qw{rm -rf /})'`, and `perl -e 'exec(qw{rm -rf /})'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-word-list-direct-runtime-a962-20260723-001800.log` confirmed real Ruby `%w/%W` splats and Perl `qw` lists expand into direct argv and reach a fake `rm` with `argc=2 args=-rf /`, while `--version` forms remain non-dangerous direct calls and Ruby `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-perl-word-list-direct-dist-before-a962-20260723-001805.log` reproduced 5 false-negative mismatches where dangerous Ruby `%w`, Ruby `%W`, Ruby `spawn`, Perl `system(qw...)`, and Perl `exec(qw...)` returned `ask_user`; safe Ruby/Perl `--version` and Ruby `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-word-list-direct-a962-focused-red-20260723-001452.log` (1 failing file / 1 failing test), then passed after static word-list parsing moved into `interpreter-word-list-literal.utils.ts` and direct invocation extraction moved into `interpreter-word-list-direct-command.utils.ts`, with `interpreter-direct-command.utils.ts` wiring Ruby `*%w/%W` splats and Perl `qw` calls into the existing direct argv token flow. Tests now cover dangerous Ruby `%w/%W` system splats, Ruby `spawn` splat, Perl `system`/`exec` `qw` lists, safe Ruby/Perl `--version` lists, and Ruby `%W` interpolation guard. Dynamic Ruby `%W` interpolation, Ruby word lists without splat, splat-plus-trailing-literal composition, Perl indirect `qw` executable blocks, heredocs, and other side-effecting expressions remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-word-list-direct-a962-focused-green-20260723-001621.log` (1 file / 2 tests), `agent-core-ruby-perl-word-list-direct-a962-related-20260723-001636.log` (2 files / 4 tests), `agent-core-build-a962-20260723-001636.log`, `ruby-perl-word-list-direct-dist-fixed-a962-20260723-001640.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a962-20260723-001806.log` (230 files / 706 tests), and `agent-core-full-test-a962-20260723-001708.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-word-list-literal.utils.ts` 72, `interpreter-word-list-direct-command.utils.ts` 45, `interpreter-direct-command.utils.ts` 99, `interpreter-quote-like-string.utils.ts` 122, `interpreter-ruby-perl-shell-command.utils.ts` 90, and `python-truthy-value.utils.ts` 200. |
| A963 | done | Core E2E | Detect Ruby/Perl direct process argv where static word-list literals are mixed with adjacent static literal arguments, so `ruby -e 'system(*%w[rm -rf], "/")'`, `ruby -e 'system("rm", *%w[-rf /])'`, `ruby -e 'spawn("rm", *%w[-rf /])'`, `perl -e 'system(qw{rm -rf}, "/")'`, `perl -e 'system("rm", qw{-rf /})'`, and `perl -e 'exec("rm", qw{-rf /})'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-word-list-mixed-direct-runtime-a963-20260723-002200.log` confirmed real Ruby splats and Perl `qw` lists compose with neighboring literal argv and reach a fake `rm` with `argc=2 args=-rf /`, while mixed `--version /` forms remain non-dangerous direct calls. Pre-fix current-dist probe `ruby-perl-word-list-mixed-direct-dist-before-a963-20260723-002205.log` reproduced 6 false-negative mismatches where dangerous Ruby splat-plus-literal, Ruby literal-plus-splat, Ruby spawn literal-plus-splat, Perl qw-plus-literal, Perl literal-plus-qw, and Perl exec literal-plus-qw forms returned `ask_user`; safe Ruby/Perl `--version` mixes and Ruby `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-word-list-mixed-direct-a963-focused-red-20260723-002204.log` (1 failing file / 1 failing test), then passed after `interpreter-word-list-direct-command.utils.ts` learned to read mixed static direct argv arguments, expanding Ruby `*%w/%W` and Perl `qw` lists alongside static quoted or quote-like literals while returning only when a word-list is present so the existing plain literal-list path remains responsible for ordinary argv. Tests now cover dangerous Ruby word-list then literal, Ruby literal then word-list, Ruby spawn literal then word-list, Perl word-list then literal, Perl literal then word-list, Perl exec literal then word-list, safe Ruby/Perl `--version` mixed argv, and Ruby `%W` interpolation guard. Dynamic Ruby `%W` interpolation, dynamic splats/lists, Ruby word lists without splat, Perl indirect `qw` executable blocks, splat/list elements followed by dynamic operands, and other side-effecting expressions remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-word-list-mixed-direct-a963-focused-green-20260723-002300.log` (1 file / 2 tests), `agent-core-ruby-perl-word-list-mixed-direct-a963-related-20260723-002311.log` (2 files / 4 tests), `agent-core-build-a963-20260723-002311.log`, `ruby-perl-word-list-mixed-direct-dist-fixed-a963-20260723-002320.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a963-20260723-002338.log` (230 files / 706 tests), and `agent-core-full-test-a963-20260723-002338.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-word-list-direct-command.utils.ts` 123, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-quote-like-string.utils.ts` 122, `interpreter-ruby-perl-shell-command.utils.ts` 90, and `python-truthy-value.utils.ts` 200. |
| A964 | done | Core E2E | Detect Perl indirect executable blocks that use a static single-word `qw` literal, so `perl -e 'system { qw{rm} } "custom-rm", "-rf", "/"'`, `perl -e 'system { (qw{rm}) } "custom-rm", "-rf", "/"'`, and `perl -e 'exec { qw{rm} } "custom-rm", "-rf", "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-indirect-qw-executable-runtime-a964-20260723-003000.log` confirmed real Perl treats single-word `qw{rm}` in direct and parenthesized indirect executable blocks as the executable path and reaches a fake `rm` with `argc=2 args=-rf /`, while the `--version` form remains non-dangerous. Pre-fix current-dist probe `perl-indirect-qw-executable-dist-before-a964-20260723-003005.log` reproduced 3 false-negative mismatches where dangerous `system { qw{rm} }`, `system { (qw{rm}) }`, and `exec { qw{rm} }` returned `ask_user`; safe `--version` and multi-word `qw{not-rm rm}` guard stayed `ask_user`. Focused E2E first failed in `agent-core-perl-indirect-qw-executable-a964-focused-red-20260723-002659.log` (1 failing file / 1 failing test), then passed after `perl-indirect-executable.utils.ts` added a narrow indirect executable value reader that preserves existing static string / quote-like parsing and accepts only single-word `readPerlQwWordListLiteral` results. Tests now cover dangerous direct and parenthesized `qw{rm}` system blocks, dangerous `exec` `qw{rm}` blocks, safe `--version`, and multi-word `qw` guard behavior. Multi-word `qw` scalar-context details, dynamic list contents, variables, imports/shadowing, Perl indirect word-list argv beyond A963, heredocs, and side-effecting expressions remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-indirect-qw-executable-a964-focused-green-20260723-002726.log` (1 file / 2 tests), `agent-core-perl-indirect-qw-executable-a964-related-20260723-002736.log` (2 files / 4 tests), `agent-core-build-a964-20260723-002736.log`, `perl-indirect-qw-executable-dist-fixed-a964-20260723-002745.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a964-20260723-002802.log` (230 files / 706 tests), and `agent-core-full-test-a964-20260723-002802.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `perl-indirect-executable.utils.ts` 80, `interpreter-word-list-literal.utils.ts` 72, `interpreter-word-list-direct-command.utils.ts` 123, `interpreter-direct-command.utils.ts` 99, `interpreter-quote-like-string.utils.ts` 122, and `python-truthy-value.utils.ts` 200. |
| A965 | done | Core E2E | Detect Perl indirect process argv where static `qw` word-list arguments appear after an indirect executable block, so `perl -e 'system { "rm" } "custom-rm", qw{-rf /}'`, `perl -e 'system { qw{rm} } "custom-rm", qw{-rf /}'`, `perl -e 'system { "rm" } "custom-rm", "-rf", qw{/}'`, and `perl -e 'exec { "rm" } "custom-rm", qw{-rf /}'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-indirect-qw-argv-runtime-a965-20260723-003500.log` confirmed real Perl composes indirect executable blocks with following `qw` argv lists and reaches a fake `rm` with `argc=2 args=-rf /`, while `qw{--version /}` remains a non-dangerous direct call. Pre-fix current-dist probe `perl-indirect-qw-argv-dist-before-a965-20260723-003505.log` reproduced 4 false-negative mismatches where dangerous static executable plus `qw` argv, `qw` executable plus `qw` argv, mixed literal plus `qw` argv, and `exec` plus `qw` argv returned `ask_user`; safe `--version` and multi-word executable guard stayed `ask_user`. Focused E2E first failed in `agent-core-perl-indirect-qw-argv-a965-focused-red-20260723-003101.log` (1 failing file / 1 failing test), then passed after `perl-indirect-executable.utils.ts` replaced the post-block argv reader with a static indirect argument list reader that expands Perl `qw` lists alongside quoted or quote-like static literals while still stopping on dynamic expressions. Tests now cover dangerous indirect static executable plus `qw` argv, dangerous mixed literal plus `qw` argv, dangerous `qw` executable plus `qw` argv, dangerous `exec` plus `qw` argv, safe `--version` `qw` argv, and multi-word executable guard behavior. Dynamic argv expressions, list variables, non-`qw` list forms, side-effecting Perl expressions, and unresolved multi-word executable scalar-context details remain user-reviewable rather than guessed. Verification passed in `agent-core-perl-indirect-qw-argv-a965-focused-green-20260723-003139.log` (1 file / 2 tests), `agent-core-perl-indirect-qw-argv-a965-related-20260723-003147.log` (2 files / 4 tests), `agent-core-build-a965-20260723-003147.log`, `perl-indirect-qw-argv-dist-fixed-a965-20260723-003155.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a965-20260723-003217.log` (230 files / 706 tests), and `agent-core-full-test-a965-20260723-003217.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `perl-indirect-executable.utils.ts` 118, `interpreter-word-list-literal.utils.ts` 72, `interpreter-word-list-direct-command.utils.ts` 123, `interpreter-direct-command.utils.ts` 99, `interpreter-quote-like-string.utils.ts` 122, and `python-truthy-value.utils.ts` 200. |
| A966 | done | Core E2E | Detect Ruby command array pairs whose argv0 value is a static quote-like literal or static string expression, so `ruby -e 'system(["rm", %q{custom-rm}], "-rf", "/")'`, `ruby -e 'spawn(["rm", %Q{custom-rm}], "-rf", "/")'`, and `ruby -e 'Process.spawn(["rm", "custom-" + "rm"], "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-static-argv0-runtime-a966-20260723-004010.log` confirmed real Ruby executes command array pairs with quote-like or statically concatenated argv0 values and reaches a fake `rm` with `argc=2 args=-rf /`, while the `--version` form remains non-dangerous. Pre-fix current-dist probe `ruby-array-pair-static-argv0-dist-before-a966-20260723-004015.log` reproduced 3 false-negative mismatches where dangerous `system`, `spawn`, and `Process.spawn` array-pair calls with static non-plain argv0 returned `ask_user`; safe `--version` and dynamic argv0 guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-static-argv0-a966-focused-red-20260723-003553.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` changed argv0 validation from plain quoted literal only to `readStaticInterpreterString`, reusing the same Ruby quote-like and `+` static-expression readers already used for command names. Tests now cover dangerous `%q` argv0, `%Q` argv0, static-concat argv0, safe `%q` argv0 with `--version`, and dynamic argv0 guard behavior. Dynamic argv0 expressions, variables, array splats inside command pairs, non-static argv0 coercions, and side-effecting Ruby expressions remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-static-argv0-a966-focused-green-20260723-003614.log` (1 file / 2 tests), `agent-core-ruby-array-pair-static-argv0-a966-related-20260723-003624.log` (2 files / 4 tests), `agent-core-build-a966-20260723-003624.log`, `ruby-array-pair-static-argv0-dist-fixed-a966-20260723-003635.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a966-20260723-003649.log` (230 files / 706 tests), and `agent-core-full-test-a966-20260723-003649.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 78, `interpreter-direct-command.utils.ts` 99, `interpreter-quote-like-string.utils.ts` 122, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200. |
| A967 | done | Core E2E | Detect Ruby command array pairs whose following argv are static `*%w/%W` word-list splats, so `ruby -e 'system(["rm", "custom-rm"], *%w[-rf /])'`, `ruby -e 'spawn(["rm", "custom-rm"], "-rf", *%w[/])'`, and `ruby -e 'Process.spawn(["rm", "custom-rm"], *%W[-rf /])'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-word-list-argv-runtime-a967-20260723-004500.log` confirmed real Ruby expands `*%w/%W` argv after command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `*%w[--version /]` remains non-dangerous and `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-array-pair-word-list-argv-dist-before-a967-20260723-004505.log` reproduced 3 false-negative mismatches where dangerous `system`, `spawn`, and `Process.spawn` array-pair calls with word-list argv returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-word-list-argv-a967-focused-red-20260723-004005.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` replaced the post-pair literal-list reader with a static Ruby pair argument reader that accepts quoted/quote-like/static string args and expands Ruby `*%w/%W` lists while preserving interpolation guards and stopping on dynamic expressions. Tests now cover dangerous array-pair `*%w`, mixed literal plus `*%w`, `Process.spawn` `*%W`, safe `*%w[--version /]`, and `%W` interpolation guard behavior. Dynamic splats, variables, non-percent list forms, side-effecting Ruby expressions, and interpolated `%W` argv remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-word-list-argv-a967-focused-green-20260723-004047.log` (1 file / 2 tests), `agent-core-ruby-array-pair-word-list-argv-a967-related-20260723-004056.log` (2 files / 4 tests), `agent-core-build-a967-20260723-004056.log`, `ruby-array-pair-word-list-argv-dist-fixed-a967-20260723-004105.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a967-20260723-004121.log` (230 files / 706 tests), and `agent-core-full-test-a967-20260723-004121.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 126, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200. |
| A968 | done | Core E2E | Detect Ruby command array pairs whose pair array itself is produced from a static `*%w/%W` word-list splat, so `ruby -e 'system([*%w[rm custom-rm]], "-rf", "/")'`, `ruby -e 'spawn([*%w[rm custom-rm]], "-rf", "/")'`, and `ruby -e 'Process.spawn([*%W[rm custom-rm]], "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-splat-pair-runtime-a968-rerun-20260723-004902.log` confirmed real Ruby expands `[*%w/%W[rm custom-rm]]` into a command array pair and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-array-pair-splat-pair-dist-before-a968-20260723-004523.log` reproduced 3 false-negative mismatches where dangerous `system`, `spawn`, and `Process.spawn` splat-built array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-splat-pair-a968-focused-red-20260723-004553.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` added a narrow command-pair reader for `[*%w/%W[command argv0]]` that accepts only exactly two static word-list values and requires the array to close immediately after the splat. Tests now cover dangerous `system`, `spawn`, and `Process.spawn` splat-built array pairs plus safe `--version` and `%W` interpolation guard behavior. Dynamic splats, interpolated `%W`, non-word-list array contents, word lists with other arities, side-effecting Ruby expressions, and mixed array-pair splats remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-splat-pair-a968-focused-green-20260723-004622.log` (1 file / 2 tests), `agent-core-ruby-array-pair-splat-pair-a968-related-20260723-004632.log` (2 files / 4 tests), `agent-core-build-a968-20260723-004632.log`, `ruby-array-pair-splat-pair-dist-fixed-a968-20260723-004649.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a968-20260723-004656.log` (230 files / 706 tests), and `agent-core-full-test-a968-20260723-004656.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 140, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A969 | done | Core E2E | Detect Ruby command array pairs whose two-element pair is composed from mixed static `*%w/%W` splats and static string literals, so `ruby -e 'system([*%w[rm], "custom-rm"], "-rf", "/")'`, `ruby -e 'spawn(["rm", *%w[custom-rm]], "-rf", "/")'`, and `ruby -e 'Process.spawn([*%W[rm], %q{custom-rm}], "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-mixed-splat-runtime-a969-20260723-005113.log` confirmed real Ruby composes mixed splat/literal command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-array-pair-mixed-splat-dist-before-a969-20260723-005130.log` reproduced 3 false-negative mismatches where dangerous `system`, `spawn`, and `Process.spawn` mixed pair forms returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-mixed-splat-a969-focused-red-20260723-005201.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` replaced the special-case full-splat pair reader with a static pair-value list reader that expands `*%w/%W` alongside static string values and accepts the pair only when the array closes after exactly two expanded values. Tests now cover dangerous splat-command plus literal argv0, literal command plus splat argv0, `%W` command plus quote-like argv0, safe `--version`, and `%W` interpolation guard behavior. Dynamic splats, interpolated `%W`, non-word-list splats, arrays that expand to arities other than two, side-effecting Ruby expressions, and nested/complex array composition remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-mixed-splat-a969-focused-green-20260723-005245.log` (1 file / 2 tests), `agent-core-ruby-array-pair-mixed-splat-a969-related-20260723-005300.log` (2 files / 4 tests), `agent-core-build-a969-20260723-005300.log`, `ruby-array-pair-mixed-splat-dist-fixed-a969-20260723-005316.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a969-20260723-005324.log` (230 files / 706 tests), and `agent-core-full-test-a969-20260723-005324.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 161, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A970 | done | Core E2E | Detect Ruby command array pairs that use a trailing comma after the two-element pair, so `ruby -e 'system(["rm", "custom-rm",], "-rf", "/")'`, `ruby -e 'spawn([*%w[rm], "custom-rm",], "-rf", "/")'`, and `ruby -e 'Process.spawn([*%W[rm custom-rm],], "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-trailing-comma-runtime-a970-20260723-005609.log` confirmed real Ruby accepts trailing commas in command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-array-pair-trailing-comma-dist-before-a970-20260723-005627.log` reproduced 3 false-negative mismatches where dangerous literal, mixed splat, and full-splat trailing-comma pair forms returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-trailing-comma-a970-focused-red-20260723-005659.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` allowed one optional trailing comma after a pair has already expanded to exactly two static values and still requires the array to close immediately afterwards. Tests now cover dangerous literal pair trailing comma, mixed splat pair trailing comma, full-splat pair trailing comma, safe `--version`, and `%W` interpolation guard behavior. Third values, double commas, dynamic splats, interpolated `%W`, side-effecting Ruby expressions, and nested/complex array composition remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-trailing-comma-a970-focused-green-20260723-005719.log` (1 file / 2 tests), `agent-core-ruby-array-pair-trailing-comma-a970-related-20260723-005733.log` (2 files / 4 tests), `agent-core-build-a970-20260723-005733.log`, `ruby-array-pair-trailing-comma-dist-fixed-a970-20260723-005750.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a970-20260723-005757.log` (230 files / 706 tests), and `agent-core-full-test-a970-20260723-005757.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 162, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A971 | done | Core E2E | Detect Ruby command array pairs wrapped in static parentheses, so `ruby -e 'system((["rm", "custom-rm"]), "-rf", "/")'`, `ruby -e 'spawn(([*%w[rm], "custom-rm"]), "-rf", "/")'`, and `ruby -e 'Process.spawn(([*%W[rm custom-rm]]), "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-parenthesized-runtime-a971-20260723-010039.log` confirmed real Ruby accepts parenthesized command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-array-pair-parenthesized-dist-before-a971-20260723-010055.log` reproduced 3 false-negative mismatches where dangerous literal, mixed splat, and full-splat parenthesized pair forms returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-parenthesized-a971-focused-red-20260723-010134.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` added a narrow parenthesized pair reader that recursively parses a static array pair after `(` and accepts it only when the pair is immediately followed by `)`. Tests now cover dangerous parenthesized literal pair, parenthesized mixed splat pair, parenthesized full-splat pair, safe `--version`, and `%W` interpolation guard behavior. Dynamic expressions inside parentheses, interpolated `%W`, non-array parenthesized arguments, side-effecting Ruby expressions, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-parenthesized-a971-focused-green-20260723-010158.log` (1 file / 2 tests), `agent-core-ruby-array-pair-parenthesized-a971-related-20260723-010212.log` (2 files / 4 tests), `agent-core-build-a971-20260723-010212.log`, `ruby-array-pair-parenthesized-dist-fixed-a971-20260723-010229.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a971-20260723-010239.log` (230 files / 706 tests), and `agent-core-full-test-a971-20260723-010239.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 176, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A972 | done | Core E2E | Detect Ruby `%w/%W` word-list literals used directly as command array pairs, so `ruby -e 'system(%w[rm custom-rm], "-rf", "/")'`, `ruby -e 'spawn(%w[rm custom-rm], "-rf", "/")'`, and `ruby -e 'Process.spawn(%W[rm custom-rm], "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-word-list-array-pair-runtime-a972-20260723-010529.log` confirmed real Ruby treats `%w/%W[rm custom-rm]` as the two-element command array pair and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous. Pre-fix current-dist probe `ruby-word-list-array-pair-dist-before-a972-20260723-010544.log` reproduced 3 false-negative mismatches where dangerous `system`, `spawn`, and `Process.spawn` word-list array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-word-list-array-pair-a972-focused-red-20260723-010619.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` added a narrow direct word-list pair reader that accepts only exactly two static `%w/%W` values and requires the next token to be a comma or call close, avoiding broader Ruby expression evaluation. Tests now cover dangerous `system`, `spawn`, and `Process.spawn` direct word-list pairs plus safe `--version` and `%W` interpolation guard behavior. Interpolated `%W`, word lists with arities other than two, word-list method chains, variables, side-effecting Ruby expressions, and broader array expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-word-list-array-pair-a972-focused-green-20260723-010651.log` (1 file / 2 tests), `agent-core-ruby-word-list-array-pair-a972-related-20260723-010707.log` (2 files / 4 tests), `agent-core-build-a972-20260723-010707.log`, `ruby-word-list-array-pair-dist-fixed-a972-20260723-010723.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a972-20260723-010730.log` (230 files / 706 tests), and `agent-core-full-test-a972-20260723-010730.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 186, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 99, `interpreter-word-list-direct-command.utils.ts` 123, `perl-indirect-executable.utils.ts` 118, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A973 | done | Core E2E | Detect Ruby command array pairs stored in prior static local variables, so `ruby -e 'pair=["rm", "custom-rm"]; system(pair, "-rf", "/")'`, `ruby -e 'pair=%w[rm custom-rm]; pid=spawn(pair, "-rf", "/"); Process.wait(pid)'`, and `ruby -e 'pair=[*%w[rm], "custom-rm"]; pid=Process.spawn(pair, "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-assigned-array-pair-runtime-a973-20260723-011313.log` confirmed real Ruby resolves static local variables containing command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-assigned-array-pair-dist-before-a973-20260723-011313.log` reproduced 3 false-negative mismatches where dangerous assigned literal, word-list, and mixed-splat array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-assigned-array-pair-a973-focused-red-20260723-011353.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` was slimmed to the call/argv entry point, shared static pair-expression parsing moved to `ruby-command-array-literal.utils.ts`, and `ruby-command-array-assignment.utils.ts` added latest-prior simple local assignment resolution for static pair expressions. Tests now cover dangerous assigned literal, assigned `%w`, and assigned mixed-splat pairs plus safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated pair assignments, method chains, side-effecting assignment values, control-flow-sensitive assignments, and non-simple Ruby locals remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-assigned-array-pair-a973-focused-green-20260723-011612.log` (1 file / 2 tests), `agent-core-ruby-assigned-array-pair-a973-related-20260723-011624.log` (2 files / 4 tests), `agent-core-build-a973-20260723-011624.log`, `ruby-assigned-array-pair-dist-fixed-a973-20260723-011633.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a973-20260723-011640.log` (230 files / 706 tests), and `agent-core-full-test-a973-20260723-011640.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-pair.utils.ts` 91, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-assignment.utils.ts` 76, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A974 | done | Core E2E | Detect Ruby command array pairs stored in prior static constants, so `ruby -e 'PAIR=["rm", "custom-rm"]; system(PAIR, "-rf", "/")'`, `ruby -e 'PAIR=%w[rm custom-rm]; pid=spawn(PAIR, "-rf", "/"); Process.wait(pid)'`, and `ruby -e 'COMMAND_PAIR=[*%w[rm], "custom-rm"]; pid=Process.spawn(COMMAND_PAIR, "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-constant-array-pair-runtime-a974-20260723-011950.log` confirmed real Ruby resolves static constants containing command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-constant-array-pair-dist-before-a974-20260723-011950.log` reproduced 3 false-negative mismatches where dangerous assigned constant literal, word-list, and mixed-splat array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-constant-array-pair-a974-focused-red-20260723-012020.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-assignment.utils.ts` expanded simple Ruby static reference parsing from local variables to local-or-constant names while preserving latest-prior static assignment resolution. Tests now cover dangerous constant literal, constant `%w`, and constant mixed-splat pairs plus safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated constant assignments, constant method chains, namespaced constants, side-effecting assignment values, control-flow-sensitive constants, and non-simple Ruby references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-constant-array-pair-a974-focused-green-20260723-012046.log` (1 file / 2 tests), `agent-core-ruby-constant-array-pair-a974-related-20260723-012056.log` (2 files / 4 tests), `agent-core-build-a974-20260723-012056.log`, `ruby-constant-array-pair-dist-fixed-a974-20260723-012105.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a974-20260723-012113.log` (230 files / 706 tests), and `agent-core-full-test-a974-20260723-012113.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-assignment.utils.ts` 76, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-pair.utils.ts` 91, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A975 | done | Core E2E | Detect Ruby command array pairs stored in prior static sigiled variables, so `ruby -e '@pair=["rm", "custom-rm"]; system(@pair, "-rf", "/")'`, `ruby -e '@@pair=%w[rm custom-rm]; pid=spawn(@@pair, "-rf", "/"); Process.wait(pid)'`, and `ruby -e '$pair=[*%w[rm], "custom-rm"]; pid=Process.spawn($pair, "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-sigiled-array-pair-runtime-a975-20260723-012345.log` confirmed real Ruby resolves instance, class, and global variables containing command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-sigiled-array-pair-dist-before-a975-20260723-012345.log` reproduced 3 false-negative mismatches where dangerous `@pair`, `@@pair`, and `$pair` array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-sigiled-array-pair-a975-focused-red-20260723-012413.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-assignment.utils.ts` expanded simple Ruby static reference parsing to include `@name`, `@@name`, and `$name` while preserving latest-prior static assignment resolution and argument boundary checks. Tests now cover dangerous instance-variable literal, class-variable `%w`, and global-variable mixed-splat pairs plus safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated sigiled assignments, special Ruby globals, method/index access, side-effecting assignment values, control-flow-sensitive variables, and non-simple Ruby references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-sigiled-array-pair-a975-focused-green-20260723-012438.log` (1 file / 2 tests), `agent-core-ruby-sigiled-array-pair-a975-related-20260723-012447.log` (2 files / 4 tests), `agent-core-build-a975-20260723-012447.log`, `ruby-sigiled-array-pair-dist-fixed-a975-20260723-012455.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a975-20260723-012502.log` (230 files / 706 tests), and `agent-core-full-test-a975-20260723-012502.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-assignment.utils.ts` 76, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-pair.utils.ts` 91, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A976 | done | Core E2E | Detect Ruby command array pairs stored in prior static namespaced constants, so `ruby -e 'M=Module.new; M::PAIR=["rm", "custom-rm"]; system(M::PAIR, "-rf", "/")'`, `ruby -e 'M=Module.new; M::PAIR=%w[rm custom-rm]; pid=spawn(M::PAIR, "-rf", "/"); Process.wait(pid)'`, and `ruby -e 'Outer=Module.new; Outer::Inner=Module.new; Outer::Inner::PAIR=[*%w[rm], "custom-rm"]; pid=Process.spawn(Outer::Inner::PAIR, "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-namespaced-constant-array-pair-runtime-a976-20260723-012749.log` confirmed real Ruby resolves simple and nested namespaced constants containing command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-namespaced-constant-array-pair-dist-before-a976-20260723-012749.log` reproduced 3 false-negative mismatches where dangerous `M::PAIR` and `Outer::Inner::PAIR` array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-namespaced-constant-array-pair-a976-focused-red-20260723-012820.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-assignment.utils.ts` split the Ruby static-name regex into named pieces and added `A::B::CONST` constant-path support while preserving latest-prior static assignment resolution and argument boundary checks. Tests now cover dangerous namespaced literal, namespaced `%w`, and nested mixed-splat pairs plus safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated namespaced constant assignments, module existence/control-flow modeling, side-effecting assignment values, method/index access, and non-simple Ruby references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-namespaced-constant-array-pair-a976-focused-green-20260723-012844.log` (1 file / 2 tests), `agent-core-ruby-namespaced-constant-array-pair-a976-related-20260723-012853.log` (2 files / 4 tests), `agent-core-build-a976-20260723-012853.log`, `ruby-namespaced-constant-array-pair-dist-fixed-a976-20260723-012900.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a976-20260723-012907.log` (230 files / 706 tests), and `agent-core-full-test-a976-20260723-012907.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-assignment.utils.ts` 79, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-pair.utils.ts` 91, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A977 | done | Core E2E | Detect Ruby command array pairs referenced through absolute constant paths, so `ruby -e 'PAIR=["rm", "custom-rm"]; system(::PAIR, "-rf", "/")'`, `ruby -e 'M=Module.new; M::PAIR=%w[rm custom-rm]; pid=spawn(::M::PAIR, "-rf", "/"); Process.wait(pid)'`, and `ruby -e 'Outer=Module.new; Outer::Inner=Module.new; Outer::Inner::PAIR=[*%w[rm], "custom-rm"]; pid=Process.spawn(::Outer::Inner::PAIR, "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-absolute-constant-array-pair-runtime-a977-20260723-013152.log` confirmed real Ruby resolves top-level and namespaced absolute constant paths containing command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-absolute-constant-array-pair-dist-before-a977-20260723-013152.log` reproduced 3 false-negative mismatches where dangerous `::PAIR`, `::M::PAIR`, and `::Outer::Inner::PAIR` array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-absolute-constant-array-pair-a977-focused-red-20260723-013223.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-assignment.utils.ts` allowed an optional leading `::` on static constant paths and canonicalized comparison keys by stripping that leading absolute marker while preserving latest-prior assignment checks. Tests now cover dangerous absolute top-level literal, absolute namespaced `%w`, and absolute nested mixed-splat pairs plus safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated absolute constant assignments, constant lookup side effects, autoload/module existence modeling, method/index access, and non-simple Ruby references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-absolute-constant-array-pair-a977-focused-green-rerun-20260723-013307.log` (1 file / 2 tests), `agent-core-ruby-absolute-constant-array-pair-a977-related-20260723-013318.log` (2 files / 4 tests), `agent-core-build-a977-20260723-013319.log`, `ruby-absolute-constant-array-pair-dist-fixed-a977-20260723-013329.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a977-20260723-013337.log` (230 files / 706 tests), and `agent-core-full-test-a977-20260723-013337.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-assignment.utils.ts` 84, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-pair.utils.ts` 91, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A978 | done | Core E2E | Detect Ruby command array pair references wrapped in static parentheses, so `ruby -e 'pair=["rm", "custom-rm"]; system((pair), "-rf", "/")'`, `ruby -e 'M=Module.new; M::PAIR=%w[rm custom-rm]; pid=spawn((M::PAIR), "-rf", "/"); Process.wait(pid)'`, and `ruby -e 'PAIR=[*%w[rm], "custom-rm"]; pid=Process.spawn((::PAIR), "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-parenthesized-reference-array-pair-runtime-a978-20260723-013608.log` confirmed real Ruby resolves parenthesized local, namespaced-constant, and absolute-constant command array pair references and reaches a fake `rm` with `argc=2 args=-rf /`, while `--version` remains non-dangerous and `%W` interpolation can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-parenthesized-reference-array-pair-dist-before-a978-20260723-013608.log` reproduced 3 false-negative mismatches where dangerous `(pair)`, `(M::PAIR)`, and `(::PAIR)` array pairs returned `ask_user`; safe `--version` and `%W` interpolation guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-parenthesized-reference-array-pair-a978-focused-red-20260723-013643.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-assignment.utils.ts` added parenthesized static-reference expression parsing that requires a matching close paren and outer argument boundary while preserving latest-prior assignment checks and existing static name canonicalization. Tests now cover dangerous parenthesized local literal, parenthesized namespaced `%w`, and parenthesized absolute mixed-splat pairs plus safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated assignments, side-effecting references, parenthesized method/index access, non-static Ruby expressions, unmatched parentheses, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-parenthesized-reference-array-pair-a978-focused-green-20260723-013840.log` (1 file / 2 tests), `agent-core-ruby-parenthesized-reference-array-pair-a978-related-20260723-013850.log` (2 files / 4 tests), `agent-core-build-a978-20260723-013850.log`, `ruby-parenthesized-reference-array-pair-dist-fixed-a978-20260723-013857.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a978-20260723-013942.log` (230 files / 706 tests), and `agent-core-full-test-a978-20260723-013905.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-assignment.utils.ts` 106, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-pair.utils.ts` 91, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A979 | done | Core E2E | Detect Ruby command array pair argv operands wrapped in static parentheses, so `ruby -e 'system(["rm", "custom-rm"], ("-rf"), ("/"))'`, `ruby -e 'pid=spawn(%w[rm custom-rm], ("-rf"), ("/")); Process.wait(pid)'`, and `ruby -e 'pid=Process.spawn([*%w[rm], "custom-rm"], ("-rf"), "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-parenthesized-pair-argv-runtime-a979-20260723-014230.log` confirmed real Ruby evaluates parenthesized literal argv operands after command array pairs and reaches a fake `rm` with `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a parenthesized dynamic variable can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-parenthesized-pair-argv-dist-before-a979-20260723-014230.log` reproduced 3 false-negative mismatches where dangerous parenthesized `"-rf"` and `"/"` argv operands returned `ask_user`; safe `("--version")` and dynamic variable guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-parenthesized-pair-argv-a979-focused-red-20260723-014304.log` (1 failing file / 1 failing test), then passed after `ruby-command-array-pair.utils.ts` added parenthesized static argv argument parsing that requires a matching close paren and comma/call-close boundary, while preserving non-static variables and method-call operands as user-reviewable. Tests now cover dangerous parenthesized literal argv with literal, `%w`, and mixed-splat command pairs plus safe `("--version")`, dynamic variable, and `("-rf").upcase` method-boundary guard behavior. Dynamic/interpolated argv, side-effecting argument expressions, parenthesized method/index access, unmatched parentheses, broader Ruby expression evaluation, and control-flow-sensitive arguments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-parenthesized-pair-argv-a979-focused-green-20260723-014328.log` (1 file / 2 tests), `agent-core-ruby-parenthesized-pair-argv-a979-related-20260723-014339.log` (2 files / 4 tests), `agent-core-build-a979-20260723-014339.log`, `ruby-parenthesized-pair-argv-dist-fixed-a979-20260723-014346.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a979-20260723-014354.log` (230 files / 706 tests), and `agent-core-full-test-a979-20260723-014354.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-command-array-assignment.utils.ts` 106, `ruby-command-array-literal.utils.ts` 121, `ruby-command-array-pair.utils.ts` 114, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A980 | done | Core E2E | Detect Ruby direct-process argv operands wrapped in static parentheses, so `ruby -e 'pid=spawn("rm", ("-rf"), ("/")); Process.wait(pid)'`, `ruby -e 'pid=Process.spawn("r" + "m", ("-r" + "f"), "/"); Process.wait(pid)'`, and `ruby -e 'system(%q{rm}, (%q{-rf}), ("/"))'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-parenthesized-direct-argv-runtime-a980-20260723-014714.log` confirmed real Ruby evaluates parenthesized literal, concatenated, and `%q` argv operands in direct process calls and reaches a fake `rm` with `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a parenthesized dynamic variable can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-parenthesized-direct-argv-dist-before-a980-20260723-014714.log` reproduced 3 false-negative mismatches where dangerous parenthesized direct argv operands returned `ask_user`; safe `("--version")` and dynamic variable guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-parenthesized-direct-argv-a980-focused-red-20260723-014751.log` (1 failing file / 1 failing test), then passed after `interpreter-literal-list.utils.ts` added parenthesized static literal-list value parsing that reuses the existing static string readers and requires a matching close paren plus comma/call-close boundary. Tests now cover dangerous parenthesized literal argv, parenthesized concatenated argv, and parenthesized `%q` argv plus safe `("--version")`, dynamic variable, and `("-rf").upcase` method-boundary guard behavior. Dynamic/interpolated argv, side-effecting argument expressions, parenthesized method/index access, unmatched parentheses, broader Ruby expression evaluation, and control-flow-sensitive arguments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-parenthesized-direct-argv-a980-focused-green-20260723-014823.log` (1 file / 2 tests), `agent-core-ruby-parenthesized-direct-argv-a980-related-20260723-014831.log` (2 files / 4 tests), `agent-core-build-a980-20260723-014831.log`, `ruby-parenthesized-direct-argv-dist-fixed-a980-20260723-014838.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a980-20260723-014846.log` (230 files / 706 tests), and `agent-core-full-test-a980-20260723-014846.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-literal-list.utils.ts` 118, `ruby-command-array-assignment.utils.ts` 106, `ruby-command-array-pair.utils.ts` 114, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A981 | done | Core E2E | Detect Perl indirect-executable argv operands wrapped in static parentheses, so `perl -e 'system {"rm"} "custom-rm", ("-rf"), ("/")'`, `perl -e 'system {"r" . "m"} "custom-rm", ("-r" . "f"), "/"'`, and `perl -e 'system { qw{rm} } "custom-rm", (qw{-rf /})'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-indirect-parenthesized-argv-runtime-a981-20260723-015105.log` confirmed real Perl evaluates parenthesized literal, concatenated, and `qw` argv operands after indirect executable blocks and reaches a fake `rm` with `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a parenthesized dynamic variable can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `perl-indirect-parenthesized-argv-dist-before-a981-20260723-015105.log` reproduced 3 false-negative mismatches where dangerous parenthesized indirect argv operands returned `ask_user`; safe `("--version")` and dynamic variable guard stayed `ask_user`. Focused E2E first failed in `agent-core-perl-indirect-parenthesized-argv-a981-focused-red-20260723-015141.log` (1 failing file / 1 failing test), then passed after `perl-indirect-executable.utils.ts` added parenthesized indirect-argument parsing for existing static string and `qw` readers, requiring a matching close paren plus comma/call-close/statement/end boundary. Tests now cover dangerous parenthesized literal argv, parenthesized concatenated argv, and parenthesized `qw` argv plus safe `("--version")`, dynamic variable, and `("-rf") x 1` expression-boundary guard behavior. Dynamic/interpolated argv, side-effecting argument expressions, parenthesized operators beyond static concatenation/`qw`, method/index-like expressions, unmatched parentheses, broader Perl expression evaluation, and control-flow-sensitive arguments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-indirect-parenthesized-argv-a981-focused-green-20260723-015210.log` (1 file / 2 tests), `agent-core-perl-indirect-parenthesized-argv-a981-related-20260723-015219.log` (2 files / 4 tests), `agent-core-build-a981-20260723-015219.log`, `perl-indirect-parenthesized-argv-dist-fixed-a981-20260723-015226.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a981-20260723-015236.log` (230 files / 706 tests), and `agent-core-full-test-a981-20260723-015236.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `perl-indirect-executable.utils.ts` 140, `interpreter-literal-list.utils.ts` 118, `ruby-command-array-pair.utils.ts` 114, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A982 | done | Core E2E | Detect Perl indirect-executable expressions wrapped in nested static parentheses, so `perl -e 'system { (("rm")) } "custom-rm", "-rf", "/"'`, `perl -e 'system { (("r" . "m")) } "custom-rm", "-rf", "/"'`, and `perl -e 'exec { ((qw{rm})) } "custom-rm", "-rf", "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-indirect-parenthesized-executable-runtime-a982-20260723-015540.log` confirmed real Perl resolves nested parenthesized literal, concatenated, and single-word `qw` executable expressions inside indirect executable blocks and reaches a fake `rm` with `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a nested parenthesized dynamic executable can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `perl-indirect-parenthesized-executable-dist-before-a982-20260723-015540.log` reproduced 3 false-negative mismatches where dangerous nested parenthesized indirect executables returned `ask_user`; safe `--version` and dynamic executable guard stayed `ask_user`. Focused E2E first failed in `agent-core-perl-indirect-parenthesized-executable-a982-focused-red-20260723-015610.log` (1 failing file / 1 failing test), then passed after `perl-indirect-executable.utils.ts` made indirect executable parenthesis parsing recurse through the same static executable expression reader rather than only accepting one parenthesized value. Tests now cover dangerous nested literal executable, nested concatenated executable, and nested single-word `qw` executable plus safe `--version`, dynamic executable, and `("rm") x 1` expression-boundary guard behavior. Dynamic/interpolated executable expressions, side-effecting executable expressions, Perl operators beyond static concatenation/single-word `qw`, unmatched parentheses, broader Perl expression evaluation, and control-flow-sensitive executable selection remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-indirect-parenthesized-executable-a982-focused-green-20260723-015636.log` (1 file / 2 tests), `agent-core-perl-indirect-parenthesized-executable-a982-related-20260723-015645.log` (2 files / 4 tests), `agent-core-build-a982-20260723-015645.log`, `perl-indirect-parenthesized-executable-dist-fixed-a982-20260723-015652.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a982-20260723-015700.log` (230 files / 706 tests), and `agent-core-full-test-a982-20260723-015700.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `perl-indirect-executable.utils.ts` 138, `interpreter-literal-list.utils.ts` 118, `ruby-command-array-pair.utils.ts` 114, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A983 | done | Core E2E | Detect Perl direct-process `qw` argv operands wrapped in static parentheses, so `perl -e 'system((qw{rm -rf /}))'`, `perl -e 'exec((qw{rm -rf /}))'`, and `perl -e 'system("rm", (qw{-rf /}))'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-direct-parenthesized-qw-runtime-a983-20260723-015906.log` confirmed real Perl expands parenthesized `qw` lists in direct `system/exec` calls and reaches a fake `rm` with `argc=2 args=-rf /`, while `qw{rm --version}` remains non-dangerous and a parenthesized dynamic scalar argument stays outside static list expansion (5 cases / 0 failures). Pre-fix current-dist probe `perl-direct-parenthesized-qw-dist-before-a983-20260723-015906.log` reproduced 3 false-negative mismatches where dangerous parenthesized direct `qw` argv operands returned `ask_user`; safe `qw{rm --version}` and dynamic scalar guard stayed `ask_user`. Focused E2E first failed in `agent-core-perl-direct-parenthesized-qw-a983-focused-red-20260723-015930.log` (1 failing file / 1 failing test), then passed after `interpreter-word-list-direct-command.utils.ts` added parenthesized direct-argument parsing that preserves the existing `literal` vs `word-list` kind and requires a matching close paren plus comma/call-close boundary. Tests now cover dangerous parenthesized full-argv `system` `qw`, full-argv `exec` `qw`, and tail-argv `qw` plus safe `qw{rm --version}`, dynamic scalar, and `(qw{-rf /})[0]` expression-boundary guard behavior. Dynamic/interpolated scalar/list construction, side-effecting arguments, indexed/sliced `qw` expressions, Perl operators beyond static string concatenation and direct `qw`, unmatched parentheses, broader Perl expression evaluation, and control-flow-sensitive argument selection remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-direct-parenthesized-qw-a983-focused-green-20260723-020001.log` (1 file / 2 tests), `agent-core-perl-direct-parenthesized-qw-a983-related-20260723-020009.log` (2 files / 4 tests), `agent-core-build-a983-20260723-020009.log`, `perl-direct-parenthesized-qw-dist-fixed-a983-20260723-020016.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a983-20260723-020023.log` (230 files / 706 tests), and `agent-core-full-test-a983-20260723-020023.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `interpreter-word-list-direct-command.utils.ts` 143, `perl-indirect-executable.utils.ts` 138, `interpreter-literal-list.utils.ts` 118, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A984 | done | Core E2E | Detect Python subprocess argv list/tuple elements wrapped in static parentheses, so `python3 -c 'import subprocess; subprocess.run(["rm", ("-rf"), ("/")])'`, `python3 -c 'import subprocess; flag="-rf"; subprocess.call(["rm", (flag), "/"])'`, and `python3 -c 'import subprocess; p=subprocess.Popen(("rm", ("-rf"), ("/"))); p.wait()'` deny instead of slipping through as user-reviewable. | Runtime probe `python-parenthesized-argv-element-runtime-a984-20260723-020331.log` confirmed real Python evaluates parenthesized literal and static-reference argv elements in subprocess list/tuple calls and reaches a fake `rm` with `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a dynamic `get_flag()` overwrite can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `python-parenthesized-argv-element-dist-before-a984-20260723-020331.log` reproduced 3 false-negative mismatches where dangerous parenthesized argv elements returned `ask_user`; safe `("--version")` and dynamic overwrite guard stayed `ask_user`. Focused E2E first failed in `agent-core-python-parenthesized-argv-element-a984-focused-red-20260723-020432.log` (1 failing file / 1 failing test), then passed after `python-static-argv.utils.ts` added parenthesized argv-element parsing for existing static string literals and static string references, requiring a matching close paren plus list/tuple element boundary. Tests now cover dangerous parenthesized literal argv, parenthesized static-reference argv, and parenthesized tuple argv plus safe `("--version")`, dynamic overwrite, and `("-rf").upper()` expression-boundary guard behavior. Dynamic/interpolated argv, side-effecting argument expressions, method/index access after parentheses, unmatched parentheses, broader Python expression evaluation, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-parenthesized-argv-element-a984-focused-green-rerun2-20260723-020607.log` (1 file / 2 tests), `agent-core-python-parenthesized-argv-element-a984-related-20260723-020618.log` (2 files / 4 tests), `agent-core-build-a984-20260723-020618.log`, `python-parenthesized-argv-element-dist-fixed-a984-20260723-020627.log` (5 cases / 0 mismatches), `agent-core-auto-reviewer-a984-20260723-020637.log` (230 files / 706 tests), and `agent-core-full-test-a984-20260723-020637.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `python-static-argv.utils.ts` 128, `interpreter-word-list-direct-command.utils.ts` 143, `perl-indirect-executable.utils.ts` 138, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A985 | done | Core E2E | Detect Python `os.spawnlp`/`os.execlp` multi-argument process command and argv operands wrapped in static parentheses, so `python3 -c 'import os; os.spawnlp(os.P_WAIT, "rm", ("rm"), ("-rf"), ("/"))'`, `python3 -c 'import os; os.execlp("rm", ("rm"), ("-rf"), "/")'`, and `python3 -c 'import os; exe="rm"; os.spawnlp(os.P_WAIT, (exe), ("rm"), "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `python-os-parenthesized-multiarg-runtime-a985-20260723-021154.log` confirmed real Python evaluates parenthesized literal and static-reference command/argv operands in `os.spawnlp`/`os.execlp` and reaches a fake `rm` with `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a dynamic `get_flag()` overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `python-os-parenthesized-multiarg-dist-before-a985-20260723-021155.log` reproduced 4 false-negative mismatches where dangerous parenthesized multi-argument command/argv operands returned `ask_user`; safe `("--version")` and dynamic overwrite guard stayed `ask_user`. Focused E2E first failed in `agent-core-python-os-parenthesized-multiarg-a985-focused-red-20260723-021235.log` (1 failing file / 1 failing test), then passed after `python-static-string.utils.ts` introduced shared parenthesized static string argument parsing for literals and latest-prior static string references, `python-static-argv.utils.ts` reused that parser for list/tuple argv elements, and `python-os-process-command.utils.ts` used it for `os.*` command and multi-argument operands. Tests now cover dangerous parenthesized literal multi-args, parenthesized `exe` command references, and parenthesized `flag` argv references plus safe `("--version")`, dynamic overwrite, and `("-rf").upper()` expression-boundary guard behavior. Dynamic/interpolated command or argv operands, side-effecting argument expressions, method/index access after parentheses, unmatched parentheses, broader Python expression evaluation, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-os-parenthesized-multiarg-a985-focused-green-20260723-021357.log` (1 file / 2 tests), `agent-core-python-os-parenthesized-multiarg-a985-related-20260723-021411.log` (2 files / 4 tests), `agent-core-build-a985-20260723-021411.log`, `python-os-parenthesized-multiarg-dist-fixed-a985-20260723-021420.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a985-20260723-021428.log` (230 files / 706 tests), and `agent-core-full-test-a985-20260723-021428.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `python-static-string.utils.ts` 96, `python-static-argv.utils.ts` 94, `python-os-process-command.utils.ts` 143, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A986 | done | Core E2E | Detect Python subprocess `executable=` keyword values wrapped in static parentheses, so `python3 -c 'import subprocess; subprocess.run(["not-rm", "-rf", "/"], executable=("rm"))'`, `python3 -c 'import subprocess; exe="rm"; subprocess.run(["not-rm", "-rf", "/"], executable=(exe))'`, and `python3 -c 'import subprocess; exe="rm"; argv=["not-rm", "-rf", "/"]; p=subprocess.Popen(args=argv, executable=(exe)); p.wait()'` deny instead of slipping through as user-reviewable. | Runtime probe `python-subprocess-parenthesized-executable-runtime-a986-20260723-021742.log` confirmed real Python evaluates parenthesized literal and static-reference `executable=` operands in `subprocess.run`/`subprocess.Popen`, replaces the actual program with fake `rm`, and reaches `argc=2 args=-rf /`, while `("--version")` remains non-dangerous and a dynamic `get_exe()` overwrite can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `python-subprocess-parenthesized-executable-dist-before-a986-20260723-021742.log` reproduced 3 false-negative mismatches where dangerous parenthesized `executable=` operands returned `ask_user`; safe `("--version")`, dynamic overwrite, and `("rm").upper()` method-boundary guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-subprocess-parenthesized-executable-a986-focused-red-20260723-021816.log` (1 failing file / 1 failing test), then passed after `python-direct-command.utils.ts` reused `readPythonStaticStringArgument` for `executable=` values, preserving literal/latest-prior static-reference support and requiring the shared close-paren plus argument-boundary guard. Tests now cover dangerous parenthesized literal executable, parenthesized static executable reference, and parenthesized executable with `args=` reference plus safe `("--version")`, dynamic overwrite, and `("rm").upper()` guard behavior. Dynamic/interpolated executable operands, side-effecting argument expressions, method/index access after parentheses, unmatched parentheses, broader Python expression evaluation, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-subprocess-parenthesized-executable-a986-focused-green-20260723-021837.log` (1 file / 2 tests), `agent-core-python-subprocess-parenthesized-executable-a986-related-20260723-021846.log` (2 files / 4 tests), `agent-core-build-a986-20260723-021846.log`, `python-subprocess-parenthesized-executable-dist-fixed-a986-20260723-021853.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a986-20260723-021901.log` (230 files / 706 tests), and `agent-core-full-test-a986-20260723-021901.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `python-direct-command.utils.ts` 143, `python-static-string.utils.ts` 96, `python-static-argv.utils.ts` 94, `python-os-process-command.utils.ts` 143, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A987 | done | Core E2E | Detect Python shell-command scalar values wrapped in static parentheses and stop over-reading method expressions, so `python3 -c 'import os; os.system(("rm -rf /"))'` and `python3 -c 'import os; cmd="rm -rf /"; os.popen((cmd)).read()'` deny, while `python3 -c 'import subprocess; subprocess.run(("rm -rf /").upper(), shell=True)'` stays user-reviewable instead of being incorrectly denied. | Runtime probe `python-parenthesized-shell-command-runtime-a987-20260723-022219.log` confirmed real Python evaluates parenthesized shell-command literals/static references for `os.system`, `os.popen`, and `subprocess(..., shell=True)`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `("rm --version")` remains non-dangerous and a dynamic `get_cmd()` overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `python-parenthesized-shell-command-dist-before-a987-20260723-022219.log` reproduced 3 mismatches: 2 false negatives where dangerous `os.system((...))` and `os.popen((cmd))` returned `ask_user`, plus 1 false positive where `("rm -rf /").upper()` returned `deny`; safe and dynamic guards stayed `ask_user`, and existing `subprocess(..., shell=True)` parenthesized scalars were already detected. Focused E2E first failed in `agent-core-python-parenthesized-shell-command-a987-focused-red-20260723-022309.log` (1 file / 2 failing tests), then passed after `python-shell-command.utils.ts` reused `readPythonStaticStringArgument` for scalar Python shell-command arguments, blocked generic quote fallback for rejected parenthesized scalar expressions, and preserved tuple/list shell command handling such as `("rm -rf /",)`. Tests now cover dangerous parenthesized `os.system` literals and `os.popen` static references, safe parenthesized `rm --version`, dynamic overwrite, and method-boundary guard behavior; the regression test `agent-core-python-parenthesized-shell-command-a987-sequence-20260723-022527.log` keeps parenthesized one-element tuple shell sequences denied. Dynamic/interpolated shell command operands, side-effecting argument expressions, method/index access after parentheses, unmatched parentheses, broader Python expression evaluation, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-parenthesized-shell-command-a987-focused-green-rerun2-20260723-022527.log` (1 file / 2 tests), `agent-core-python-parenthesized-shell-command-a987-related-rerun-20260723-022536.log` (3 files / 6 tests), `agent-core-build-a987-rerun-20260723-022536.log`, `python-parenthesized-shell-command-dist-fixed-a987-rerun-20260723-022543.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a987-rerun-20260723-022554.log` (230 files / 706 tests), and `agent-core-full-test-a987-rerun-20260723-022554.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `python-shell-command.utils.ts` 160, `python-static-string.utils.ts` 96, `python-direct-command.utils.ts` 143, and `python-truthy-value.utils.ts` 200; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A988 | done | Core E2E | Detect Ruby/Perl shell-command strings wrapped in static parentheses, so `ruby -e 'system(("rm -rf /"))'`, `ruby -e 'system(("rm" + " -rf /"))'`, `perl -e 'system(("rm -rf /"))'`, and `perl -e 'exec(("rm" . " -rf /"))'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-parenthesized-shell-command-runtime-a988-20260723-022920.log` confirmed real Ruby and Perl evaluate parenthesized literal and concatenated shell-command strings in `system`/`exec`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `("rm --version")` remains non-dangerous and Ruby `("rm -rf /").upcase` does not execute the fake lowercase `rm` command (6 cases / 0 failures). Pre-fix current-dist probe `ruby-perl-parenthesized-shell-command-dist-before-a988-20260723-022920.log` reproduced 4 false-negative mismatches where dangerous parenthesized Ruby literal/concat and Perl literal/concat shell commands returned `ask_user`; safe `("rm --version")` and `.upcase` guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-parenthesized-shell-command-a988-focused-red-20260723-022954.log` (1 failing file / 1 failing test), then passed after `interpreter-static-string.utils.ts` added parenthesized static interpreter string parsing for existing literal readers and concat operators, requiring a matching close paren plus argument boundary so method/index expressions remain unresolved. Tests now cover dangerous Ruby literal, Ruby concat, Perl literal, and Perl concat parenthesized shell commands plus safe `("rm --version")` and Ruby `.upcase` guard behavior. Dynamic/interpolated shell command operands, side-effecting argument expressions, method/index access after parentheses, unmatched parentheses, concat outside the parentheses such as `("rm") + " -rf /"`, broader Ruby/Perl expression evaluation, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-parenthesized-shell-command-a988-focused-green-20260723-023038.log` (1 file / 2 tests), `agent-core-ruby-perl-parenthesized-shell-command-a988-related-20260723-023048.log` (3 files / 6 tests), `agent-core-build-a988-20260723-023048.log`, `ruby-perl-parenthesized-shell-command-dist-fixed-a988-20260723-023055.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a988-20260723-023102.log` (230 files / 706 tests), and `agent-core-full-test-a988-20260723-023102.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 86, `interpreter-ruby-perl-shell-command.utils.ts` 90, `interpreter-shell-command.utils.ts` 143, and `python-shell-command.utils.ts` 160; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A989 | done | Core E2E | Detect Ruby/Perl shell-command concatenation where the first static string operand is parenthesized and the concat operator is outside that parenthesis, so `ruby -e 'system(("rm") + " -rf /")'`, `ruby -e 'exec(("rm") + " -rf /")'`, `perl -e 'system(("rm") . " -rf /")'`, and `perl -e 'exec(("rm") . " -rf /")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-parenthesized-leading-concat-shell-runtime-a989-20260723-023525.log` confirmed real Ruby and Perl evaluate parenthesized leading string operands before shell-command concat and reach a fake `rm` with `argc=2 args=-rf /`; it also confirmed `("rm") + " --version"` remains non-dangerous and Ruby `("rm").reverse + " -rf /"` does not execute fake `rm` (6 cases / 0 failures). Pre-fix current-dist probe `ruby-perl-parenthesized-leading-concat-shell-dist-before-a989-20260723-023525.log` reproduced 4 false-negative mismatches where dangerous Ruby/Perl parenthesized-leading concat commands returned `ask_user`; safe `--version` and Ruby `.reverse` method guard stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-parenthesized-leading-concat-shell-a989-focused-red-20260723-023553.log` (1 failing file / 1 failing test), then passed after `interpreter-static-string.utils.ts` allowed a parenthesized static interpreter string to end before the current language's configured concat operator as well as before normal argument boundaries, letting the existing outer concat loop assemble the full command. Tests now cover dangerous Ruby `system`/`exec` leading-parenthesized `+` concat and Perl `system`/`exec` leading-parenthesized `.` concat plus safe `("rm") + " --version"` and Ruby `.reverse` guard behavior. Dynamic/interpolated shell command operands, side-effecting argument expressions, method/index access after parentheses, unmatched parentheses, Ruby operators other than configured `+`, Perl operators other than configured `.`, broader Ruby/Perl expression evaluation, and control-flow-sensitive assignments remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-parenthesized-leading-concat-shell-a989-focused-green-20260723-023617.log` (1 file / 2 tests), `agent-core-ruby-perl-parenthesized-leading-concat-shell-a989-related-20260723-023617.log` (3 files / 6 tests), `agent-core-build-a989-20260723-023617.log`, `ruby-perl-parenthesized-leading-concat-shell-dist-fixed-a989-20260723-023630.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a989-20260723-023703.log` (230 files / 706 tests), and `agent-core-full-test-a989-20260723-023630.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 99, `interpreter-ruby-perl-shell-command.utils.ts` 90, `interpreter-shell-command.utils.ts` 143, and `python-shell-command.utils.ts` 160; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A990 | done | Core E2E | Detect Ruby/Perl shell-command strings passed through latest-prior simple static variables, so `ruby -e 'cmd = "rm -rf /"; system(cmd)'`, `ruby -e 'cmd = "rm" + " -rf /"; exec((cmd))'`, `perl -e 'my $cmd = "rm -rf /"; system($cmd)'`, and `perl -e 'my $cmd = "rm" . " -rf /"; exec(($cmd))'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-static-shell-reference-runtime-a990-20260723-024034.log` confirmed real Ruby and Perl evaluate simple static shell-command variables before `system`/`exec` and reach a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd = "rm --version"` remains non-dangerous and a dynamic Ruby overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-perl-static-shell-reference-dist-before-a990-20260723-024034.log` reproduced 4 false-negative mismatches where dangerous Ruby/Perl static shell variables returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-static-shell-reference-a990-focused-red-20260723-024101.log` (1 failing file / 1 failing test), then passed after `interpreter-ruby-perl-shell-command.utils.ts` added Ruby/Perl simple static shell assignment collection and a reference literal reader for `readStaticInterpreterString`, using the latest prior assignment and treating dynamic/latest-null assignments as unresolved. Tests now cover dangerous Ruby literal and concat variable shell commands, dangerous Perl literal and concat variable shell commands, plus safe `rm --version`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, method/index access after references, non-simple or control-flow-sensitive assignments, broader Ruby/Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-static-shell-reference-a990-focused-green-20260723-024156.log` (1 file / 2 tests), `agent-core-ruby-perl-static-shell-reference-a990-related-20260723-024212.log` (3 files / 6 tests), `agent-core-build-a990-20260723-024212.log`, `ruby-perl-static-shell-reference-dist-fixed-a990-20260723-024226.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a990-20260723-024226.log` (230 files / 706 tests), and `agent-core-full-test-a990-20260723-024226.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-ruby-perl-shell-command.utils.ts` 192, `interpreter-static-string.utils.ts` 99, `interpreter-shell-command.utils.ts` 143, and `python-shell-command.utils.ts` 160; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A991 | done | Core E2E | Detect Ruby/Perl direct-process argv operands passed through latest-prior simple static variables, so `ruby -e 'cmd = "rm"; flag = "-rf"; system(cmd, flag, "/")'`, `ruby -e 'cmd = "r" + "m"; flag = "-r" + "f"; exec((cmd), (flag), "/")'`, `perl -e 'my $cmd = "rm"; my $flag = "-rf"; system($cmd, $flag, "/")'`, and `perl -e 'my $cmd = "r" . "m"; my $flag = "-r" . "f"; exec(($cmd), ($flag), "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-perl-static-direct-reference-runtime-a991-20260723-103157.log` confirmed real Ruby and Perl evaluate simple static direct-process command/argv variables before `system`/`exec` and reach a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd = "rm"; flag = "--version"` remains non-dangerous and a dynamic Ruby overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-perl-static-direct-reference-dist-before-a991-20260723-103157.log` reproduced 4 false-negative mismatches where dangerous Ruby/Perl static direct variables returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-perl-static-direct-reference-a991-focused-red-20260723-103426.log` (1 failing file / 1 failing test), then passed after `ruby-perl-static-reference.utils.ts` extracted A990's Ruby/Perl simple static assignment and reference reader, `interpreter-ruby-perl-shell-command.utils.ts` reused that utility, and `interpreter-direct-command.utils.ts` passed the same reference reader into direct literal-list parsing for Ruby/Perl `system`/`exec`. Tests now cover dangerous Ruby literal and concat variable direct argv, dangerous Perl literal and concat variable direct argv, plus safe `rm --version`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, method/index access after references, non-simple or control-flow-sensitive assignments, Ruby `spawn` static variables, Perl indirect executable static variables, broader Ruby/Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-perl-static-direct-reference-a991-focused-green-20260723-103620.log` (1 file / 2 tests), `agent-core-ruby-perl-static-direct-reference-a991-related-20260723-103640.log` (3 files / 6 tests), `agent-core-build-a991-20260723-103640.log`, `ruby-perl-static-direct-reference-dist-fixed-a991-20260723-103703.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a991-20260723-103703.log` (230 files / 706 tests), and `agent-core-full-test-a991-20260723-103703.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-perl-static-reference.utils.ts` 108, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 108, and `interpreter-literal-list.utils.ts` 118; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A992 | done | Core E2E | Detect Ruby `spawn`/`Process.spawn` direct-process argv operands passed through latest-prior simple static variables, so `ruby -e 'cmd = "rm"; flag = "-rf"; pid = spawn(cmd, flag, "/"); Process.wait(pid)'`, `ruby -e 'cmd = "r" + "m"; flag = "-r" + "f"; pid = Process.spawn((cmd), (flag), "/"); Process.wait(pid)'`, and `ruby -e 'cmd = "rm"; flag = "-rf"; pid = spawn({"SVTON_A992"=>"ok"}, cmd, flag, "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-spawn-static-direct-reference-runtime-a992-20260723-104144.log` confirmed real Ruby evaluates simple static command/argv variables in `spawn`/`Process.spawn`, including after an env hash, and reaches a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd = "rm"; flag = "--version"` remains non-dangerous and a dynamic Ruby overwrite can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-spawn-static-direct-reference-dist-before-a992-20260723-104144.log` reproduced 3 false-negative mismatches where dangerous Ruby `spawn` static variables returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-spawn-static-direct-reference-a992-focused-red-20260723-104501.log` (1 failing file / 1 failing test), then passed after `interpreter-direct-command.utils.ts` built Ruby `spawn`/`Process.spawn` literal-list readers from the shared `rubyPerlStaticReferenceReader`, while keeping Ruby array-pair variable parsing outside this slice. Tests now cover dangerous `spawn` literal variables, `Process.spawn` concat variables, env-hash `spawn` variables, and an existing dangerous static flag reference, plus safe `rm --version`, dynamic overwrite, and future-assignment guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, method/index access after references, non-simple or control-flow-sensitive assignments, Ruby array-pair static variables such as `spawn([cmd, "custom-rm"], "-rf", "/")`, Perl indirect executable static variables, broader Ruby expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-spawn-static-direct-reference-a992-focused-green-rerun-20260723-104740.log` (1 file / 2 tests), `agent-core-ruby-spawn-static-direct-reference-a992-related-20260723-104758.log` (3 files / 6 tests), `agent-core-build-a992-20260723-104759.log`, `ruby-spawn-static-direct-reference-dist-fixed-a992-20260723-104821.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a992-20260723-104821.log` (230 files / 706 tests), and `agent-core-full-test-a992-20260723-104821.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-direct-command.utils.ts` 114, `ruby-perl-static-reference.utils.ts` 108, and `ruby-command-array-pair.utils.ts` 114; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A993 | done | Core E2E | Detect Ruby array-pair direct-process command operands passed through latest-prior simple static variables, so `ruby -e 'cmd = "rm"; pid = spawn([cmd, "custom-rm"], "-rf", "/"); Process.wait(pid)'`, `ruby -e 'cmd = "r" + "m"; pid = Process.spawn([(cmd), "custom-rm"], "-rf", "/"); Process.wait(pid)'`, `ruby -e 'cmd = "rm"; pair = [cmd, "custom-rm"]; pid = spawn(pair, "-rf", "/"); Process.wait(pid)'`, and `ruby -e 'cmd = "rm"; PAIR = [cmd, "custom-rm"]; pid = spawn(PAIR, "-rf", "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-pair-static-reference-runtime-a993-20260723-105412.log` confirmed real Ruby resolves simple static variables inside inline, local assigned, and constant assigned array-pair command operands before `spawn`/`Process.spawn`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd = "rm"; spawn([cmd, "custom-rm"], "--version")` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-array-pair-static-reference-dist-before-a993-20260723-105413.log` reproduced 4 false-negative mismatches where dangerous Ruby array-pair static references returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-pair-static-reference-a993-focused-red-20260723-105601.log` (1 failing file / 1 failing test), then passed after `interpreter-direct-command.utils.ts` passed the shared `rubyPerlStaticReferenceReader` into `rubyCommandArrayPairArguments` for Ruby `system`/`exec` and `spawn`/`Process.spawn`, allowing the existing inline and assigned pair parsers to resolve latest-prior simple static command values. Tests now cover dangerous inline pair static command variables, inline concat variables, local assigned pair variables, constant assigned pair variables, and an existing dangerous static flag pair argument, plus safe `rm --version`, dynamic overwrite, future-assignment, interpolated pair, dynamic pair, method, and non-rm pair guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, method/index access after references, non-simple or control-flow-sensitive assignments, broader Ruby expression evaluation, command-output/backtick variable references, and Perl indirect executable static variables remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-pair-static-reference-a993-focused-green-rerun-20260723-105848.log` (1 file / 2 tests), `agent-core-ruby-array-pair-static-reference-a993-related-20260723-105903.log` (3 files / 6 tests), `agent-core-build-a993-20260723-105903.log`, `ruby-array-pair-static-reference-dist-fixed-a993-20260723-105919.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a993-20260723-105919.log` (230 files / 706 tests), and `agent-core-full-test-a993-20260723-105919.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-direct-command.utils.ts` 114, `ruby-command-array-pair.utils.ts` 114, `ruby-command-array-literal.utils.ts` 121, and `ruby-perl-static-reference.utils.ts` 108; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A994 | done | Core E2E | Detect Perl indirect-executable and indirect-argv operands passed through latest-prior simple static variables, so `perl -e 'my $cmd = "rm"; system {$cmd} "custom-rm", "-rf", "/"'`, `perl -e 'my $cmd = "r" . "m"; exec {($cmd)} "custom-rm", "-rf", "/"'`, `perl -e 'my $flag = "-rf"; system {"rm"} "custom-rm", ($flag), "/"'`, and `perl -e 'my $cmd = "rm"; my $flag = "-r" . "f"; system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-indirect-static-reference-runtime-a994-20260723-110752.log` confirmed real Perl resolves simple static variables inside indirect executable braces and indirect argv operands before `system`/`exec`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd = "rm"; system {$cmd} "custom-rm", "--version"` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `perl-indirect-static-reference-dist-before-a994-rerun-20260723-110938.log` reproduced 4 false-negative mismatches where dangerous Perl indirect static references returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-indirect-static-reference-a994-focused-red-20260723-111115.log` (1 failing file / 1 failing test), then passed after `interpreter-direct-command.utils.ts` passed the shared Perl `rubyPerlStaticReferenceReader` into `perlIndirectExecutableArguments`, and `ruby-perl-static-reference.utils.ts` added an opt-in `}` reference boundary used only by Perl indirect executable parsing. Tests now cover dangerous static executable variables, concat executable variables, nested parenthesized executable variables, static argv flag variables, and executable+argv concat variables, plus safe `rm --version`, dynamic overwrite, future-assignment, dynamic concat tail, expression operator, and non-rm guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, method/index/operator access after references, non-simple or control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-indirect-static-reference-a994-focused-green-rerun-20260723-111325.log` (1 file / 2 tests), `agent-core-perl-indirect-static-reference-a994-related-20260723-111346.log` (3 files / 6 tests), `agent-core-build-a994-20260723-111347.log`, `perl-indirect-static-reference-dist-fixed-a994-20260723-111419.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a994-shellglob-20260723-111554.log` (230 files / 706 tests), and `agent-core-full-test-a994-20260723-111445.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-direct-command.utils.ts` 120, `ruby-perl-static-reference.utils.ts` 116, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A995 | done | Core E2E | Detect Perl `our`/`local` declaration variables in shell, direct-process, and indirect-executable paths, so `perl -e 'our $cmd = "rm -rf /"; system($cmd)'`, `perl -e 'local $cmd = "rm -rf /"; system($cmd)'`, `perl -e 'our $cmd = "rm"; our $flag = "-rf"; system($cmd, $flag, "/")'`, `perl -e 'local $cmd = "r" . "m"; local $flag = "-r" . "f"; exec(($cmd), ($flag), "/")'`, and `perl -e 'our $cmd = "rm"; local $flag = "-rf"; system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-declaration-static-reference-runtime-a995-20260723-112205.log` confirmed real Perl resolves `our` and `local` declaration variables before shell scalar `system`, direct argv `system`/`exec`, and indirect executable/argv `system {...}`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `our $cmd = "rm"; system($cmd, "--version")` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (7 cases / 0 failures). Pre-fix current-dist probe `perl-declaration-static-reference-dist-before-a995-20260723-112206.log` reproduced 5 false-negative mismatches where dangerous `our`/`local` declaration variables returned `ask_user`; safe `--version`, dynamic overwrite, and future-declaration guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-declaration-static-reference-a995-focused-red-20260723-112337.log` (2 failing files / 2 failing tests), then passed after `ruby-perl-static-reference.utils.ts` expanded the Perl static assignment declaration prefix from optional `my` to optional `my`/`our`/`local`, preserving latest-prior ordering and dynamic-null/future-assignment guard behavior for all existing shell, direct, and indirect consumers. Tests now cover dangerous `our` and `local` shell scalars, direct argv variables, concat argv variables, and mixed indirect executable/argv variables, plus safe `rm --version`, dynamic overwrite, and future declaration guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, method/index/operator access after references, declarations with attributes or complex lvalues, non-simple or control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-declaration-static-reference-a995-focused-green-20260723-112456.log` (2 files / 4 tests), `agent-core-perl-declaration-static-reference-a995-related-20260723-112510.log` (3 files / 6 tests), `agent-core-build-a995-20260723-112510.log`, `perl-declaration-static-reference-dist-fixed-a995-20260723-112545.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a995-20260723-112615.log` (230 files / 706 tests), and `agent-core-full-test-a995-20260723-112615.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-perl-static-reference.utils.ts` 117, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A996 | done | Core E2E | Detect Perl single-scalar parenthesized lvalue assignments in shell, direct-process, and indirect-executable paths, so `perl -e 'my ($cmd) = "rm -rf /"; system($cmd)'`, `perl -e 'my $cmd = "echo"; ($cmd) = "rm -rf /"; system($cmd)'`, `perl -e 'my ($cmd) = "rm"; my ($flag) = "-rf"; system($cmd, $flag, "/")'`, `perl -e 'my ($cmd) = "r" . "m"; my ($flag) = "-r" . "f"; exec(($cmd), ($flag), "/")'`, and `perl -e 'my ($cmd) = "rm"; my ($flag) = "-rf"; system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-parenthesized-lvalue-static-reference-runtime-a996-20260723-113238.log` confirmed real Perl assigns single-scalar parenthesized lvalues before shell scalar `system`, direct argv `system`/`exec`, and indirect executable/argv `system {...}`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `my ($cmd) = "rm"; system($cmd, "--version")` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (7 cases / 0 failures). Pre-fix current-dist probe `perl-parenthesized-lvalue-static-reference-dist-before-a996-20260723-113238.log` reproduced 5 false-negative mismatches where dangerous parenthesized lvalue assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-parenthesized-lvalue-static-reference-a996-focused-red-20260723-113403.log` (2 failing files / 2 failing tests), then passed after `ruby-perl-static-reference.utils.ts` expanded the Perl static assignment target from a direct scalar to either a direct scalar or one parenthesized scalar target, while keeping multi-variable list assignment unresolved. Tests now cover dangerous parenthesized shell scalar assignments, bare parenthesized reassignment, direct argv variables, concat argv variables, and indirect executable/argv variables, plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Dynamic/interpolated assignments, side-effecting assignment expressions, multi-variable list assignment, parenthesized RHS list unpacking, method/index/operator access after references, declarations with attributes or complex lvalues, non-simple or control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-parenthesized-lvalue-static-reference-a996-focused-green-20260723-113450.log` (2 files / 4 tests), `agent-core-perl-parenthesized-lvalue-static-reference-a996-related-20260723-113503.log` (3 files / 6 tests), `agent-core-build-a996-20260723-113503.log`, `perl-parenthesized-lvalue-static-reference-dist-fixed-a996-20260723-113521.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a996-20260723-113534.log` (230 files / 706 tests), and `agent-core-full-test-a996-20260723-113534.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-perl-static-reference.utils.ts` 122, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A997 | done | Core E2E | Detect Perl multi-scalar static list assignments in shell, direct-process, and indirect-executable paths, so `perl -e 'my ($cmd, $unused) = ("rm -rf /", "safe"); system($cmd)'`, `perl -e 'my $cmd = "echo"; my $unused = "safe"; ($cmd, $unused) = ("rm -rf /", "safe"); system($cmd)'`, `perl -e 'my ($cmd, $flag) = ("rm", "-rf"); system($cmd, $flag, "/")'`, `perl -e 'my ($cmd, $flag) = ("r" . "m", "-r" . "f"); exec(($cmd), ($flag), "/")'`, and `perl -e 'my ($cmd, $flag) = ("rm", "-rf"); system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-list-assignment-static-reference-runtime-a997-20260723-114206.log` confirmed real Perl distributes static RHS list values into multi-scalar lvalue lists before shell scalar `system`, direct argv `system`/`exec`, and indirect executable/argv `system {...}`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `my ($cmd, $flag) = ("rm", "--version"); system($cmd, $flag)` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (7 cases / 0 failures). Pre-fix current-dist probe `perl-list-assignment-static-reference-dist-before-a997-20260723-114207.log` reproduced 5 false-negative mismatches where dangerous static list assignments returned `ask_user`; safe `--version`, dynamic RHS overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-list-assignment-static-reference-a997-focused-red-20260723-114335.log` (2 failing files / 2 failing tests), then passed after `ruby-perl-static-reference.utils.ts` added a narrow Perl list-assignment collector that accepts only scalar target lists and static RHS string lists, records each target with the corresponding latest-prior value, and writes null assignments for unresolved RHS lists so dynamic overwrite guards still win. Tests now cover dangerous shell scalar list assignment, bare list reassignment, direct argv variables, concat argv variables, and indirect executable/argv variables, plus safe `rm --version`, dynamic RHS overwrite, and future assignment guard behavior. Dynamic/interpolated RHS lists, side-effecting list expressions, mismatched or sparse list assignment, complex lvalues, parenthesized RHS list unpacking beyond flat static strings, declarations with attributes, non-simple/control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-list-assignment-static-reference-a997-focused-green-20260723-114503.log` (2 files / 4 tests), `agent-core-perl-list-assignment-static-reference-a997-related-20260723-114522.log` (3 files / 6 tests), `agent-core-build-a997-20260723-114523.log`, `perl-list-assignment-static-reference-dist-fixed-a997-20260723-114553.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a997-20260723-114609.log` (230 files / 706 tests), and `agent-core-full-test-a997-20260723-114609.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-perl-static-reference.utils.ts` 184, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A998 | done | Core E2E | Detect Perl bare-RHS multi-scalar assignment first-target values in shell, direct-process, and indirect-executable paths, so `perl -e 'my ($cmd, $unused) = "rm -rf /", "safe"; system($cmd)'`, `perl -e 'my $cmd = "echo"; my $unused = "safe"; ($cmd, $unused) = "rm -rf /", "safe"; system($cmd)'`, `perl -e 'my ($cmd, $unused) = "rm", "safe"; system($cmd, "-rf", "/")'`, `perl -e 'my ($cmd, $unused) = "r" . "m", "safe"; exec(($cmd), "-rf", "/")'`, and `perl -e 'my ($cmd, $unused) = "rm", "safe"; system {$cmd} "custom-rm", "-rf", "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-bare-list-assignment-static-reference-runtime-a998-rerun-20260723-115202.log` confirmed real Perl assigns only the first bare RHS expression to the first lvalue target in `($cmd, $unused) = "rm", "safe"` style code, while later targets remain empty; shell scalar `system`, direct argv `system`/`exec`, and indirect executable `system {...}` still reach a fake `rm` with `argc=2 args=-rf /` when the static first target is used as the command, and `my ($cmd, $unused) = "rm", "safe"; system($cmd, "--version")` remains non-dangerous (7 cases / 0 failures). Pre-fix current-dist probe `perl-bare-list-assignment-static-reference-dist-before-a998-rerun-20260723-115205.log` reproduced 5 false-negative mismatches where dangerous bare-RHS first-target assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-bare-list-assignment-static-reference-a998-focused-red-20260723-115300.log` (2 failing files / 2 failing tests), then passed after A997's Perl list-assignment collector moved into `perl-static-list-assignment.utils.ts` and added bare-RHS parsing that records only the first target's static value when a comma follows the first static RHS expression, leaving later targets unresolved so it matches Perl runtime behavior rather than inventing list distribution. Tests now cover dangerous shell scalar bare-RHS assignment, bare list reassignment, direct command variables, concat command variables, and indirect executable variables, plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Dynamic/interpolated bare RHS expressions, second-or-later bare RHS values, complex lvalues, parenthesized RHS list unpacking beyond flat static strings, declarations with attributes, non-simple/control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-bare-list-assignment-static-reference-a998-focused-green-20260723-115447.log` (2 files / 4 tests), `agent-core-perl-bare-list-assignment-static-reference-a998-related-20260723-115516.log` (3 files / 6 tests), `agent-core-build-a998-20260723-115516.log`, `perl-bare-list-assignment-static-reference-dist-fixed-a998-20260723-115537.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a998-20260723-115548.log` (230 files / 706 tests), and `agent-core-full-test-a998-20260723-115549.log` (327 files / 1979 tests). Production source line counts remain under 200 after the split: `ruby-perl-static-reference.utils.ts` 129, `perl-static-list-assignment.utils.ts` 102, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A999 | done | Core E2E | Detect Ruby multi-variable parallel static assignments in shell, direct-process, spawn, and command-array-pair paths, so `ruby -e 'cmd, unused = "rm -rf /", "safe"; system(cmd)'`, `ruby -e 'cmd = "echo"; unused = "safe"; cmd, unused = "rm -rf /", "safe"; system(cmd)'`, `ruby -e 'cmd, flag = "rm", "-rf"; system(cmd, flag, "/")'`, `ruby -e 'cmd, flag = "r" + "m", "-r" + "f"; exec((cmd), (flag), "/")'`, `ruby -e 'cmd, flag = "rm", "-rf"; pid = spawn(cmd, flag, "/"); Process.wait(pid)'`, and `ruby -e 'cmd, flag = "rm", "-rf"; pid = Process.spawn([cmd, "custom-rm"], flag, "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-list-assignment-static-reference-runtime-a999-20260723-120138.log` confirmed real Ruby distributes static RHS values into parallel-assignment targets before shell scalar `system`, direct argv `system`/`exec`, `spawn`, and `Process.spawn` array-pair calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd, flag = "rm", "--version"; system(cmd, flag)` remains non-dangerous and a dynamic parallel overwrite can be runtime-dangerous (8 cases / 0 failures). Pre-fix current-dist probe `ruby-list-assignment-static-reference-dist-before-a999-20260723-120138.log` reproduced 6 false-negative mismatches where dangerous Ruby parallel static assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-list-assignment-static-reference-a999-focused-red-20260723-120321.log` (2 failing files / 2 failing tests), then passed after adding `ruby-static-list-assignment.utils.ts` and wiring Ruby parallel static assignments into `rubyPerlStaticStringAssignments` alongside the existing simple Ruby and Perl assignment collectors. Tests now cover dangerous shell scalar parallel assignment and reassignment, direct argv variables, concat argv variables, spawn variables, and array-pair command/flag variables, plus safe `rm --version`, dynamic parallel overwrite, and future parallel assignment guard behavior. Dynamic/interpolated RHS expressions, side-effecting assignment expressions, nested destructuring, sparse/rest parallel assignment, complex lvalues, non-simple/control-flow-sensitive assignments, broader Ruby expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-list-assignment-static-reference-a999-focused-green-20260723-120443.log` (2 files / 4 tests), `agent-core-ruby-list-assignment-static-reference-a999-related-20260723-120504.log` (3 files / 6 tests), `agent-core-build-a999-20260723-120504.log`, `ruby-list-assignment-static-reference-dist-fixed-a999-20260723-120522.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a999-20260723-120613.log` (229 files / 689 tests), and `agent-core-full-test-a999-20260723-120535.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-list-assignment.utils.ts` 78, `ruby-perl-static-reference.utils.ts` 134, `perl-static-list-assignment.utils.ts` 102, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 120, and `ruby-command-array-pair.utils.ts` 114; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1000 | done | Core E2E | Detect Ruby parallel assignments whose RHS is one static bracket array, so `ruby -e 'cmd, unused = ["rm -rf /", "safe"]; system(cmd)'`, `ruby -e 'cmd = "echo"; unused = "safe"; cmd, unused = ["rm -rf /", "safe"]; system(cmd)'`, `ruby -e 'cmd, flag = ["rm", "-rf"]; system(cmd, flag, "/")'`, `ruby -e 'cmd, flag = ["r" + "m", "-r" + "f"]; exec((cmd), (flag), "/")'`, `ruby -e 'cmd, flag = ["rm", "-rf"]; pid = spawn(cmd, flag, "/"); Process.wait(pid)'`, and `ruby -e 'cmd, flag = ["rm", "-rf"]; pid = Process.spawn([cmd, "custom-rm"], flag, "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-array-rhs-static-reference-runtime-a1000-20260723-123719.log` confirmed real Ruby expands a single static bracket array RHS across parallel-assignment targets before shell scalar `system`, direct argv `system`/`exec`, `spawn`, and `Process.spawn` array-pair calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd, flag = ["rm", "--version"]; system(cmd, flag)` remains non-dangerous and a dynamic array overwrite can be runtime-dangerous (8 cases / 0 failures). Pre-fix current-dist probe `ruby-array-rhs-static-reference-dist-before-a1000-20260723-123719.log` reproduced 6 false-negative mismatches where dangerous Ruby bracket-array RHS assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-array-rhs-static-reference-a1000-focused-red-20260723-123818.log` (2 failing files / 2 failing tests), then passed after `ruby-static-list-assignment.utils.ts` added bracket-array RHS parsing that accepts only static interpreter strings and existing Ruby `+` / quote-like readers, while unresolved array values still write null assignments so dynamic overwrite guards win. Tests now cover dangerous shell scalar array RHS assignment and reassignment, direct argv variables, concat argv variables, spawn variables, and array-pair command/flag variables, plus safe `rm --version`, dynamic array overwrite, and future array assignment guard behavior. Dynamic/interpolated array elements, side-effecting array expressions, `%w/%W` array RHS, splats inside RHS arrays, nested destructuring, sparse/rest parallel assignment, complex lvalues, non-simple/control-flow-sensitive assignments, broader Ruby expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-array-rhs-static-reference-a1000-focused-green-20260723-123939.log` (2 files / 4 tests), `agent-core-ruby-array-rhs-static-reference-a1000-related-20260723-124009.log` (3 files / 6 tests), `agent-core-build-a1000-20260723-124009.log`, `ruby-array-rhs-static-reference-dist-fixed-a1000-20260723-124023.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1000-20260723-124036.log` (229 files / 689 tests), and `agent-core-full-test-a1000-20260723-124036.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-list-assignment.utils.ts` 108, `ruby-perl-static-reference.utils.ts` 134, `perl-static-list-assignment.utils.ts` 102, `interpreter-ruby-perl-shell-command.utils.ts` 106, `interpreter-direct-command.utils.ts` 120, and `ruby-command-array-pair.utils.ts` 114; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1001 | done | Core E2E | Detect Ruby parallel assignments whose RHS is a static `%w/%W` word-list array in direct-process paths, so `ruby -e 'cmd, flag = %w[rm -rf]; system(cmd, flag, "/")'`, `ruby -e 'cmd, flag = %W[rm -rf]; system(cmd, flag, "/")'`, `ruby -e 'cmd = "echo"; flag = "--version"; cmd, flag = %w[rm -rf]; system(cmd, flag, "/")'`, `ruby -e 'cmd, flag = %w[rm -rf]; pid = spawn(cmd, flag, "/"); Process.wait(pid)'`, and `ruby -e 'cmd, flag = %w[rm -rf]; pid = Process.spawn([cmd, "custom-rm"], flag, "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-word-list-rhs-static-reference-runtime-a1001-20260723-124458.log` confirmed real Ruby expands `%w/%W` word-list RHS values across parallel-assignment targets before direct argv `system`, `spawn`, and `Process.spawn` array-pair calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `cmd, flag = %w[rm --version]; system(cmd, flag)` remains non-dangerous, interpolated `%W` can be runtime-dangerous, and a dynamic overwrite can be runtime-dangerous (8 cases / 0 failures). Pre-fix current-dist probe `ruby-word-list-rhs-static-reference-dist-before-a1001-20260723-124458.log` reproduced 5 false-negative mismatches where dangerous Ruby `%w/%W` RHS assignments returned `ask_user`; safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-word-list-rhs-static-reference-a1001-focused-red-20260723-124551.log` (1 failing file / 1 failing test), then passed after `ruby-static-list-assignment.utils.ts` reused `readRubyPercentWordListLiteral` for direct `%w/%W` RHS parsing, preserving its `%W` interpolation guard and existing latest-prior/null-overwrite behavior. Tests now cover dangerous direct `%w`, static `%W`, reassignment, spawn variables, and array-pair command/flag variables, plus safe `rm --version`, interpolated `%W`, dynamic overwrite, and future assignment guard behavior. Escaped-space shell scalar word-list commands, splats inside RHS arrays, nested destructuring, sparse/rest parallel assignment, complex lvalues, side-effecting RHS expressions, non-simple/control-flow-sensitive assignments, broader Ruby expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-word-list-rhs-static-reference-a1001-focused-green-20260723-124641.log` (1 file / 2 tests), `agent-core-ruby-word-list-rhs-static-reference-a1001-related-20260723-124706.log` (3 files / 6 tests), `agent-core-build-a1001-20260723-124706.log`, `ruby-word-list-rhs-static-reference-dist-fixed-a1001-20260723-124726.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1001-20260723-124734.log` (229 files / 689 tests), and `agent-core-full-test-a1001-20260723-124735.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-list-assignment.utils.ts` 112, `ruby-perl-static-reference.utils.ts` 134, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 120, and `ruby-command-array-pair.utils.ts` 114; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1002 | done | Core E2E | Detect Ruby parallel assignments whose RHS is a latest-prior static list variable, so `ruby -e 'argv = ["rm", "-rf"]; cmd, flag = argv; system(cmd, flag, "/")'`, `ruby -e 'argv = %w[rm -rf]; cmd, flag = argv; system(cmd, flag, "/")'`, `ruby -e 'argv = ["r" + "m", "-r" + "f"]; cmd, flag = argv; exec((cmd), (flag), "/")'`, `ruby -e 'argv = ["echo", "--version"]; argv = ["rm", "-rf"]; cmd, flag = argv; system(cmd, flag, "/")'`, `ruby -e 'argv = %w[rm -rf]; cmd, flag = argv; pid = spawn(cmd, flag, "/"); Process.wait(pid)'`, and `ruby -e 'argv = ["rm", "-rf"]; cmd, flag = argv; pid = Process.spawn([cmd, "custom-rm"], flag, "/"); Process.wait(pid)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-list-reference-static-reference-runtime-a1002-20260723-125208.log` confirmed real Ruby expands prior static bracket-array and `%w` list variables into parallel-assignment targets before direct argv `system`, `exec`, `spawn`, and `Process.spawn` array-pair calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `argv = %w[rm --version]; cmd, flag = argv; system(cmd, flag)` remains non-dangerous, interpolated `%W` list variables can be runtime-dangerous, and dynamic list overwrites can be runtime-dangerous (9 cases / 0 failures). Pre-fix current-dist probe `ruby-list-reference-static-reference-dist-before-a1002-20260723-125209.log` reproduced 6 false-negative mismatches where dangerous Ruby latest-prior static list references returned `ask_user`; safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-list-reference-static-reference-a1002-focused-red-20260723-125256.log` (1 failing file / 1 failing test), then passed after `ruby-static-list-assignment.utils.ts` added a narrow single-variable static-list assignment collector for bracket arrays and `%w/%W` lists, and `cmd, flag = argv` now resolves the latest prior non-null list assignment while dynamic/latest-null assignments and future assignments remain unresolved. Tests now cover dangerous bracket list references, `%w` list references, concat list references, reassignment, spawn variables, and array-pair command/flag variables, plus safe `rm --version`, interpolated `%W`, dynamic overwrite, and future assignment guard behavior. Reference chains, interpolated list values, side-effecting RHS expressions, escaped-space shell scalar word-list commands, splats inside RHS arrays, nested destructuring, sparse/rest parallel assignment, complex lvalues, non-simple/control-flow-sensitive assignments, broader Ruby expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-list-reference-static-reference-a1002-focused-green-20260723-125410.log` (1 file / 2 tests), `agent-core-ruby-list-reference-static-reference-a1002-related-20260723-125440.log` (3 files / 6 tests), `agent-core-build-a1002-20260723-125440.log`, `ruby-list-reference-static-reference-dist-fixed-a1002-20260723-125458.log` (10 cases / 0 mismatches), `agent-core-auto-reviewer-a1002-20260723-125513.log` (229 files / 689 tests), and `agent-core-full-test-a1002-20260723-125514.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-list-assignment.utils.ts` 178, `ruby-perl-static-reference.utils.ts` 134, `interpreter-word-list-literal.utils.ts` 72, `interpreter-direct-command.utils.ts` 120, and `ruby-command-array-pair.utils.ts` 114; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1003 | done | Core E2E | Detect Ruby `%w/%W` word-list values with escaped spaces when they form shell scalar commands, so `ruby -e 'cmd, unused = %w[rm\ -rf\ / safe]; system(cmd)'`, `ruby -e 'cmd, unused = %W[rm\ -rf\ / safe]; system(cmd)'`, and `ruby -e 'argv = %w[rm\ -rf\ / safe]; cmd, unused = argv; system(cmd)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-word-list-escaped-shell-runtime-a1003-20260723-130045.log` confirmed real Ruby preserves escaped spaces inside `%w/%W` words, so `rm\ -rf\ /` becomes one shell scalar command that reaches a fake `rm` with `argc=2 args=-rf /`; it also confirmed escaped safe `rm\ --version` remains non-dangerous, interpolated `%W` can be runtime-dangerous, and a dynamic overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-word-list-escaped-shell-dist-before-a1003-20260723-130045.log` reproduced 3 false-negative mismatches where dangerous escaped `%w/%W` shell scalar assignments and latest-prior list references returned `ask_user`; escaped safe `--version`, interpolated `%W`, dynamic overwrite, and future-assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-word-list-escaped-shell-a1003-focused-red-20260723-130131.log` (1 failing file / 1 failing test), then passed after `interpreter-word-list-literal.utils.ts` changed `%w/%W` and Perl `qw` tokenization to split only on unescaped whitespace while preserving escaped whitespace in the current word. Tests now cover dangerous escaped `%w`, escaped `%W`, and latest-prior escaped `%w` list-reference shell scalar commands, plus escaped safe `rm --version`, interpolated `%W`, dynamic overwrite, and future assignment guard behavior. Interpolated `%W`, dynamic overwrites, future assignments, nested/destructuring/splat list forms, complex lvalues, non-simple or control-flow-sensitive assignments, broader Ruby expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-word-list-escaped-shell-a1003-focused-green-20260723-130208.log` (1 file / 2 tests), `agent-core-ruby-word-list-escaped-shell-a1003-related-20260723-130229.log` (2 files / 4 tests), `agent-core-build-a1003-20260723-130229.log`, `ruby-word-list-escaped-shell-dist-fixed-a1003-20260723-130239.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1003-20260723-130330.log` (229 files / 689 tests), and `agent-core-full-test-a1003-20260723-130253.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-word-list-literal.utils.ts` 82, `ruby-static-list-assignment.utils.ts` 178, `ruby-perl-static-reference.utils.ts` 134, and `interpreter-ruby-perl-shell-command.utils.ts` 106; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1004 | done | Core E2E | Detect Perl multi-scalar assignments whose RHS is a static `qw[...]` word list, so `perl -e 'my ($cmd, $flag) = qw[rm -rf]; system($cmd, $flag, "/")'`, `perl -e 'my ($cmd, $flag) = qw[rm -rf]; exec($cmd, $flag, "/")'`, and `perl -e 'my ($cmd, $flag) = qw[rm -rf]; system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-qw-list-assignment-runtime-a1004-rerun-20260723-130823.log` confirmed real Perl distributes `qw[rm -rf]` RHS values into multi-scalar lvalue lists before direct argv `system`, `exec`, and indirect executable `system {...}` calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `qw[rm --version]` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `perl-qw-list-assignment-dist-before-a1004-rerun-20260723-130823.log` reproduced 3 false-negative mismatches where dangerous Perl `qw` list assignments returned `ask_user`; safe `--version`, dynamic overwrite, future assignment, and escaped-operand boundary guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-qw-list-assignment-a1004-focused-red-20260723-130918.log` (2 failing files / 2 failing tests), then passed after `perl-static-list-assignment.utils.ts` added `readPerlQwWordListLiteral` as a static RHS list reader for Perl multi-scalar assignments while keeping unresolved RHS expressions as null assignments so dynamic/latest and future guards still win. Tests now cover dangerous `qw` shell/direct/indirect variable flows, plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Parenthesized `qw` RHS lists, dynamic/interpolated RHS expressions, side-effecting list expressions, mismatched or sparse list assignment, complex lvalues, declarations with attributes, non-simple/control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-qw-list-assignment-a1004-focused-green-20260723-130954.log` (2 files / 4 tests), `agent-core-perl-qw-list-assignment-a1004-related-20260723-131012.log` (2 files / 4 tests), `agent-core-build-a1004-20260723-131013.log`, `perl-qw-list-assignment-dist-fixed-a1004-20260723-131021.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1004-20260723-131036.log` (229 files / 689 tests), and `agent-core-full-test-a1004-20260723-131036.log` (327 files / 1979 tests). Production source line counts remain under 200: `perl-static-list-assignment.utils.ts` 106, `ruby-perl-static-reference.utils.ts` 134, `interpreter-word-list-literal.utils.ts` 82, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1005 | done | Core E2E | Detect Perl multi-scalar assignments whose parenthesized RHS contains a static `qw[...]` word list, so `perl -e 'my ($cmd, $flag) = (qw[rm -rf]); system($cmd, $flag, "/")'`, `perl -e 'my ($cmd, $flag) = (qw[rm -rf]); exec($cmd, $flag, "/")'`, `perl -e 'my ($cmd, $flag) = (qw[rm -rf]); system {$cmd} "custom-rm", $flag, "/"'`, and `perl -e 'my ($cmd, $flag) = (qw[rm], "-rf"); system($cmd, $flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-parenthesized-qw-list-assignment-runtime-a1005-20260723-131423.log` confirmed real Perl flattens parenthesized `qw` RHS values into multi-scalar lvalue lists before direct argv `system`, `exec`, indirect executable `system {...}`, and mixed `qw` plus literal list calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `(qw[rm --version])` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `perl-parenthesized-qw-list-assignment-dist-before-a1005-20260723-131424.log` reproduced 4 false-negative mismatches where dangerous parenthesized `qw` RHS list assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-parenthesized-qw-list-assignment-a1005-focused-red-20260723-131516.log` (2 failing files / 2 failing tests), then passed after `perl-static-list-assignment.utils.ts` expanded the parenthesized static list reader to flatten `readPerlQwWordListLiteral` results alongside existing static string values. Tests now cover dangerous parenthesized `qw` shell/direct/indirect variable flows and mixed `qw` plus literal RHS values, plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Nested parentheses around RHS values, dynamic/interpolated RHS expressions, side-effecting list expressions, mismatched or sparse list assignment, complex lvalues, declarations with attributes, non-simple/control-flow-sensitive assignments, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-parenthesized-qw-list-assignment-a1005-focused-green-20260723-131543.log` (2 files / 4 tests), `agent-core-perl-parenthesized-qw-list-assignment-a1005-related-20260723-131602.log` (2 files / 4 tests), `agent-core-build-a1005-20260723-131602.log`, `perl-parenthesized-qw-list-assignment-dist-fixed-a1005-20260723-131612.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1005-20260723-131623.log` (229 files / 689 tests), and `agent-core-full-test-a1005-20260723-131623.log` (327 files / 1979 tests). Production source line counts remain under 200: `perl-static-list-assignment.utils.ts` 119, `ruby-perl-static-reference.utils.ts` 134, `interpreter-word-list-literal.utils.ts` 82, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1006 | done | Core E2E | Detect Perl multi-scalar assignments whose RHS contains nested parenthesized static `qw[...]` word lists, so `perl -e 'my ($cmd, $flag) = ((qw[rm -rf])); system($cmd, $flag, "/")'`, `perl -e 'my ($cmd, $flag) = (((qw[rm -rf]))); exec($cmd, $flag, "/")'`, `perl -e 'my ($cmd, $flag) = ((qw[rm -rf])); system {$cmd} "custom-rm", $flag, "/"'`, and `perl -e 'my ($cmd, $flag) = ((qw[rm]), "-rf"); system($cmd, $flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-nested-qw-list-assignment-runtime-a1006-20260723-131946.log` confirmed real Perl flattens nested parenthesized `qw` RHS values into multi-scalar lvalue lists before direct argv `system`, `exec`, indirect executable `system {...}`, and nested mixed `qw` plus literal list calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed nested `qw[rm --version]` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `perl-nested-qw-list-assignment-dist-before-a1006-20260723-131946.log` reproduced 4 false-negative mismatches where dangerous nested `qw` RHS list assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-nested-qw-list-assignment-a1006-focused-red-20260723-132040.log` (2 failing files / 2 failing tests), then passed after `perl-static-list-assignment.utils.ts` made the parenthesized static list value reader recurse on nested `(...)` values and flatten their static list results before assigning variables. Tests now cover dangerous nested `qw` shell/direct/indirect variable flows and nested mixed `qw` plus literal RHS values, plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Deep or control-flow-sensitive expression evaluation, dynamic/interpolated RHS expressions, side-effecting list expressions, mismatched or sparse list assignment, complex lvalues, declarations with attributes, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-nested-qw-list-assignment-a1006-focused-green-20260723-132109.log` (2 files / 4 tests), `agent-core-perl-nested-qw-list-assignment-a1006-related-20260723-132137.log` (2 files / 4 tests), `agent-core-build-a1006-20260723-132137.log`, `perl-nested-qw-list-assignment-dist-fixed-a1006-20260723-132146.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1006-20260723-132203.log` (229 files / 689 tests), and `agent-core-full-test-a1006-20260723-132203.log` (327 files / 1979 tests). Production source line counts remain under 200: `perl-static-list-assignment.utils.ts` 125, `ruby-perl-static-reference.utils.ts` 134, `interpreter-word-list-literal.utils.ts` 82, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1007 | done | Core E2E | Detect Perl multi-scalar assignments that use `undef` as a sparse target placeholder, so `perl -e 'my ($cmd, undef, $flag) = ("rm", "ignored", "-rf"); system($cmd, $flag, "/")'`, `perl -e 'my ($cmd, undef, $flag) = qw[rm ignored -rf]; exec($cmd, $flag, "/")'`, and `perl -e 'my ($cmd, undef, $flag) = ((qw[rm ignored -rf])); system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-undef-target-list-assignment-runtime-a1007-20260723-132521.log` confirmed real Perl skips `undef` targets while assigning neighboring static RHS values into scalar variables before direct argv `system`, `exec`, and indirect executable `system {...}` calls, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `undef` placeholder `rm --version` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `perl-undef-target-list-assignment-dist-before-a1007-20260723-132522.log` reproduced 3 false-negative mismatches where dangerous `undef` placeholder assignments returned `ask_user`; safe `--version`, dynamic overwrite, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-undef-target-list-assignment-a1007-focused-red-20260723-132641.log` (2 failing files / 2 failing tests), then passed after `perl-static-list-assignment.utils.ts` accepted `undef` as a positional placeholder target and skipped emitting an assignment for it while preserving indexes for real scalar targets. Tests now cover dangerous `undef` placeholder shell/direct/indirect variable flows, plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Other placeholder forms, complex lvalues, mismatched or sparse assignment beyond `undef`, declarations with attributes, dynamic/interpolated RHS expressions, side-effecting list expressions, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-undef-target-list-assignment-a1007-focused-green-20260723-132712.log` (2 files / 4 tests), `agent-core-perl-undef-target-list-assignment-a1007-related-20260723-132739.log` (2 files / 4 tests), `agent-core-build-a1007-20260723-132739.log`, `perl-undef-target-list-assignment-dist-fixed-a1007-20260723-132758.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1007-20260723-132809.log` (229 files / 689 tests), and `agent-core-full-test-a1007-20260723-132809.log` (327 files / 1979 tests). Production source line counts remain under 200: `perl-static-list-assignment.utils.ts` 134, `ruby-perl-static-reference.utils.ts` 134, `interpreter-word-list-literal.utils.ts` 82, `interpreter-direct-command.utils.ts` 120, and `perl-indirect-executable.utils.ts` 138; `git diff --check` is clean for the touched source, test, and continuation doc paths. |
| A1008 | done | Core E2E | Detect Perl `state $scalar = ...` static assignments before shell/direct/indirect process execution, so `perl -e 'use feature "state"; state $cmd = "rm -rf /"; system($cmd)'`, `perl -e 'use feature "state"; state $cmd = "rm"; state $flag = "-rf"; exec($cmd, $flag, "/")'`, and `perl -e 'use feature "state"; state $cmd = "rm"; state $flag = "-rf"; system {$cmd} "custom-rm", $flag, "/"'` deny instead of slipping through as user-reviewable. | Runtime probe `perl-state-static-assignment-runtime-a1008-20260723-133606.log` confirmed real Perl `state $scalar` assignments, with `use feature "state"`, initialize scalar variables before single-string shell `system`, direct argv `exec`, and indirect executable `system {...}`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `state` safe `rm --version` remains non-dangerous and a dynamic overwrite can be runtime-dangerous (5 cases / 0 failures). A discarded probe run `perl-state-static-assignment-runtime-a1008-20260723-133449.log` proved Perl rejects `state (...) = ...` list initialization, so this slice intentionally excludes state list assignments. Pre-fix current-dist probe `perl-state-static-assignment-dist-before-a1008-20260723-133606.log` reproduced 3 false-negative mismatches where dangerous state scalar shell/direct/indirect variable flows returned `ask_user`; safe `--version`, dynamic overwrite, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-perl-state-static-assignment-a1008-focused-red-20260723-133731.log` (2 failing files / 2 failing tests), then passed after `PERL_STATIC_TARGET_PATTERN` split scalar declarations from parenthesized/list declarations so only scalar targets accept `state` while list assignment declarations remain `my|our|local`. Tests now cover dangerous state scalar shell/direct/indirect flows plus safe `rm --version`, dynamic overwrite, and future assignment guard behavior. Perl `state (...)` list initialization, feature-version validation, complex lvalues, declarations with attributes, dynamic/interpolated RHS expressions, side-effecting expressions, broader Perl expression evaluation, and command-output/backtick variable references remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-perl-state-static-assignment-a1008-focused-green-20260723-133816.log` (2 files / 4 tests), `agent-core-perl-state-static-assignment-a1008-related-20260723-133839.log` (2 files / 4 tests), `agent-core-build-a1008-20260723-133839.log`, `perl-state-static-assignment-dist-fixed-a1008-20260723-133858.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1008-20260723-133913.log` (230 files / 706 tests), and `agent-core-full-test-a1008-20260723-133913.log` (327 files / 1979 tests). Production source line counts remain under 200: `perl-static-list-assignment.utils.ts` 135, `ruby-perl-static-reference.utils.ts` 134, `interpreter-direct-command.utils.ts` 120, and `interpreter-ruby-perl-shell-command.utils.ts` 106; `git diff --check` is clean for touched source/test paths. |
| A1009 | done | Core E2E | Detect Ruby static `String#concat` method chains before shell/direct process execution, so `ruby -e 'cmd = "rm".concat(" -rf /"); system(cmd)'`, `ruby -e 'cmd = "r".concat("m"); flag = "-r".concat("f"); exec(cmd, flag, "/")'`, and `ruby -e 'system("r".concat("m"), "-r".concat("f"), "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-string-concat-method-runtime-a1009-20260723-134711.log` confirmed real Ruby evaluates `String#concat` receiver/argument chains before single-string shell `system`, direct argv `exec`, and direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed `concat`-built safe `rm --version` remains non-dangerous and a dynamic concat argument can be runtime-dangerous (5 cases / 0 failures). A discarded candidate probe `perl-parenthesized-state-static-assignment-runtime-a1009-20260723-134540.log` proved Perl rejects `state($cmd = ...)`, so that invalid syntax was not implemented. Pre-fix current-dist probe `ruby-string-concat-method-dist-before-a1009-20260723-134712.log` reproduced 3 false-negative mismatches where dangerous Ruby concat shell/direct/literal-argv flows returned `ask_user`; safe `--version`, dynamic concat argument, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-string-concat-method-a1009-focused-red-20260723-134938.log` (2 failing files / 2 failing tests), then passed after `readStaticInterpreterString` gained an opt-in static method-chain reader and Ruby paths passed `methodNames: ["concat"]` through assignment, shell-call, and literal-list direct-call parsing; Perl paths still pass no method names. Tests now cover dangerous Ruby concat shell variable, direct exec variable, direct literal argv, reassignment shell variable, plus safe `rm --version`, dynamic concat argument, and future assignment guard behavior. Multi-argument `concat`, receiver variable mutation, `<<` string append, interpolated/dynamic concat arguments, list-assignment RHS concat, Ruby method dispatch beyond literal receivers, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-string-concat-method-a1009-focused-green-20260723-135146.log` (2 files / 4 tests), `agent-core-ruby-string-concat-method-a1009-related-20260723-135206.log` (2 files / 4 tests), `agent-core-build-a1009-20260723-135206.log`, `ruby-string-concat-method-dist-fixed-a1009-20260723-135226.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1009-20260723-135239.log` (230 files / 706 tests), and `agent-core-full-test-a1009-20260723-135239.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 132, `interpreter-literal-list.utils.ts` 120, `ruby-perl-static-reference.utils.ts` 135, `interpreter-direct-command.utils.ts` 123, and `interpreter-ruby-perl-shell-command.utils.ts` 110; `git diff --check` is clean for touched source/test paths. |
| A1010 | done | Core E2E | Detect Ruby multi-argument static `String#concat` method chains before shell/direct process execution, so `ruby -e 'cmd = "rm".concat(" -", "rf", " /"); system(cmd)'`, `ruby -e 'cmd = "r".concat("m"); flag = "-".concat("r", "f"); exec(cmd, flag, "/")'`, and `ruby -e 'system("r".concat("m"), "-".concat("r", "f"), "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-multi-arg-concat-method-runtime-a1010-20260723-135757.log` confirmed real Ruby evaluates multi-argument `String#concat` calls before single-string shell `system`, direct argv `exec`, and direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed multi-arg concat-built safe `rm --version` remains non-dangerous and a dynamic second concat argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-multi-arg-concat-method-dist-before-a1010-20260723-135758.log` reproduced 3 false-negative mismatches where dangerous Ruby multi-arg concat shell/direct/literal-argv flows returned `ask_user`; safe `--version`, dynamic second concat argument, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-multi-arg-concat-method-a1010-focused-red-20260723-135913.log` (2 failing files / 2 failing tests), then passed after the opt-in interpreter static method-call reader accumulated comma-separated static arguments and returned `null` if any argument could not be statically resolved. Tests now cover dangerous Ruby multi-arg concat shell variable, direct exec variable, direct literal argv, plus safe `rm --version`, dynamic second concat argument, and future assignment guard behavior. Receiver variable mutation, `<<` string append, keyword/block forms, interpolated/dynamic concat arguments, list-assignment RHS concat, Ruby method dispatch beyond literal receivers, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-multi-arg-concat-method-a1010-focused-green-20260723-140033.log` (2 files / 4 tests), `agent-core-ruby-multi-arg-concat-method-a1010-related-20260723-140051.log` (2 files / 4 tests), `agent-core-build-a1010-20260723-140051.log`, `ruby-multi-arg-concat-method-dist-fixed-a1010-20260723-140102.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1010-20260723-140114.log` (230 files / 706 tests), and `agent-core-full-test-a1010-20260723-140114.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 142, `interpreter-literal-list.utils.ts` 120, `ruby-perl-static-reference.utils.ts` 135, `interpreter-direct-command.utils.ts` 123, and `interpreter-ruby-perl-shell-command.utils.ts` 110; `git diff --check` is clean for touched source/test paths. |
| A1011 | done | Core E2E | Detect Ruby static `<<` string append assignments before shell/direct process execution, so `ruby -e 'cmd = "rm"; cmd << " -rf /"; system(cmd)'`, `ruby -e 'cmd = "r"; cmd << "m"; flag = "-r"; flag << "f"; exec(cmd, flag, "/")'`, and `ruby -e 'cmd = "r"; cmd << "m"; system(cmd, "-rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-static-append-assignment-runtime-a1011-20260723-140519.log` confirmed real Ruby mutates existing string variables with `<<` before single-string shell `system`, direct argv `exec`, and mixed direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed append-built safe `rm --version` remains non-dangerous and a dynamic appended value can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-static-append-assignment-dist-before-a1011-20260723-140520.log` reproduced 3 false-negative mismatches where dangerous Ruby append shell/direct/mixed direct flows returned `ask_user`; safe `--version`, dynamic appended value, and future append guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-static-append-assignment-a1011-focused-red-20260723-140646.log` (2 failing files / 2 failing tests), then passed after `ruby-static-append-assignment.utils.ts` scanned statement-boundary `name << <static-string>` mutations, combined them with the latest prior static assignment, and emitted `null` when the prior value or appended value was not statically known. Tests now cover dangerous Ruby append shell variable, direct exec variable, mixed direct literal argv, plus safe `rm --version`, dynamic appended value, and future append guard behavior. Chained `<<` in one expression, append on non-variable receivers, append with interpolated/dynamic values, destructive mutation through aliases, list-assignment RHS append, Ruby method dispatch beyond simple variable receivers, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-static-append-assignment-a1011-focused-green-20260723-140804.log` (2 files / 4 tests), `agent-core-ruby-static-append-assignment-a1011-related-20260723-140829.log` (2 files / 4 tests), `agent-core-build-a1011-20260723-140829.log`, `ruby-static-append-assignment-dist-fixed-a1011-20260723-140849.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1011-20260723-140940.log` (230 files / 706 tests), and `agent-core-full-test-a1011-20260723-140940.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-append-assignment.utils.ts` 54, `interpreter-static-string.utils.ts` 142, `ruby-perl-static-reference.utils.ts` 137, `interpreter-direct-command.utils.ts` 123, and `interpreter-ruby-perl-shell-command.utils.ts` 110; `git diff --check` is clean for touched source/test paths. |
| A1012 | done | Core E2E | Detect Ruby chained static `<<` string append expressions before shell/direct process execution, so `ruby -e 'cmd = "rm"; cmd << " -" << "rf /"; system(cmd)'`, `ruby -e 'cmd = "r"; cmd << "m"; flag = "-"; flag << "r" << "f"; exec(cmd, flag, "/")'`, and `ruby -e 'cmd = "r"; cmd << "m"; system(cmd, "-" << "rf", "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-chained-append-assignment-runtime-a1012-20260723-141618.log` confirmed real Ruby evaluates chained `<<` mutations and literal `"-" << "rf"` expressions before single-string shell `system`, direct argv `exec`, and mixed direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed chained append-built safe `rm --version` remains non-dangerous and a dynamic chained appended value can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-chained-append-assignment-dist-before-a1012-20260723-141618.log` reproduced 3 false-negative mismatches where dangerous chained append shell/direct/literal-argv flows returned `ask_user`; safe `--version`, dynamic chained append, and future append guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-chained-append-assignment-a1012-focused-red-20260723-141838.log` (2 files / 4 tests; 1 failing test at the direct literal `"-" << "rf"` argv path), then passed after `ruby-static-append-assignment.utils.ts` accumulated consecutive static `<<` append segments, `readStaticInterpreterString` gained an opt-in multi-character concat operator reader, and Ruby shell/direct paths passed `['+', '<<']` while Perl paths kept `['.']`. Tests now cover dangerous Ruby chained append shell variable, direct exec variable, direct literal argv, plus safe `rm --version`, dynamic appended value, and future append guard behavior. Append on non-variable receivers beyond static literals, interpolated/dynamic append values, destructive mutation through aliases, list-assignment RHS append, Ruby method dispatch beyond statically readable literals/references, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-chained-append-assignment-a1012-focused-green-20260723-141952.log` (2 files / 4 tests), `agent-core-ruby-chained-append-assignment-a1012-related-20260723-142008.log` (2 files / 4 tests), `agent-core-build-a1012-20260723-142008.log`, `ruby-chained-append-assignment-dist-fixed-a1012-20260723-142303.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1012-20260723-142340.log` (230 files / 706 tests), and `agent-core-full-test-a1012-20260723-142340.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 150, `ruby-static-append-assignment.utils.ts` 82, `interpreter-direct-command.utils.ts` 123, and `interpreter-ruby-perl-shell-command.utils.ts` 110. |
| A1013 | done | Core E2E | Detect Ruby static `String#prepend` method calls before shell/direct process execution, so `ruby -e 'cmd = " -rf /"; cmd.prepend("rm"); system(cmd)'`, `ruby -e 'cmd = "m"; cmd.prepend("r"); flag = "f"; flag.prepend("-r"); exec(cmd, flag, "/")'`, and `ruby -e 'system("m".prepend("r"), "f".prepend("-r"), "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-string-prepend-method-runtime-a1013-20260723-143106.log` confirmed real Ruby prepends static method arguments before single-string shell `system`, direct argv `exec`, and direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed prepend-built safe `rm --version` remains non-dangerous and a dynamic prepended value can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-string-prepend-method-dist-before-a1013-20260723-143106.log` reproduced 3 false-negative mismatches where dangerous Ruby prepend shell/direct/literal-argv flows returned `ask_user`; safe `--version`, dynamic prepend argument, and future prepend guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-string-prepend-method-a1013-focused-red-20260723-143345.log` (2 failing files / 2 failing tests), then passed after `readStaticInterpreterString` gained opt-in prepend method semantics, `ruby-static-prepend-assignment.utils.ts` scanned statement-boundary `name.prepend(<static-string>)` mutations against the latest prior static assignment, and Ruby shell/direct literal-list paths passed `prependMethodNames: ["prepend"]` while Perl paths kept no prepend methods. Tests now cover dangerous Ruby prepend shell variable, direct exec variable, direct literal argv, plus safe `rm --version`, dynamic prepended value, and future prepend guard behavior. Prepend on aliases, method dispatch beyond simple variables and static literal receivers, interpolated/dynamic prepend arguments, list-assignment RHS prepend, block/keyword forms, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-string-prepend-method-a1013-focused-green-20260723-144051.log` (2 files / 4 tests), `agent-core-ruby-string-prepend-method-a1013-related-20260723-144145.log` (2 files / 4 tests), `agent-core-build-a1013-20260723-144145.log`, `ruby-string-prepend-method-dist-fixed-a1013-20260723-144201.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1013-20260723-144238.log` (230 files / 706 tests), and `agent-core-full-test-a1013-20260723-144238.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 166, `interpreter-literal-list.utils.ts` 122, `ruby-static-prepend-assignment.utils.ts` 60, `ruby-perl-static-reference.utils.ts` 151, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1014 | done | Core E2E | Detect Ruby multi-argument static `String#prepend` variable mutations before shell/direct process execution, so `ruby -e 'cmd = " /"; cmd.prepend("rm", " -rf"); system(cmd)'` and `ruby -e 'cmd = "m"; cmd.prepend("r"); flag = "f"; flag.prepend("-", "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-multi-arg-prepend-method-runtime-a1014-20260723-145040.log` confirmed real Ruby prepends multiple method arguments in order before single-string shell `system`, direct argv `exec`, and direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed multi-arg prepend-built safe `rm --version` remains non-dangerous and a dynamic prepended value can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-multi-arg-prepend-method-dist-before-a1014-20260723-145040.log` reproduced 2 false-negative mismatches where dangerous Ruby multi-arg prepend shell/direct variable mutations returned `ask_user`; direct literal argv was already denied by A1013 static method-call semantics, while safe `--version`, dynamic prepend argument, and future prepend guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-multi-arg-prepend-method-a1014-focused-red-20260723-145310.log` (2 failing files / 2 failing tests), then passed after `ruby-static-prepend-assignment.utils.ts` read comma-separated static prepend arguments through `readStaticInterpreterString`, concatenated them in Ruby runtime order, and kept the assignment unknown if any argument or boundary was not statically readable. Tests now cover dangerous Ruby multi-arg prepend shell variable and direct exec variable flows, plus already-covered direct literal argv behavior, safe `rm --version`, dynamic prepended value, and future prepend guard behavior. Prepend on aliases, method dispatch beyond simple variables/static literals, interpolated/dynamic prepend arguments, list-assignment RHS prepend, block/keyword forms, side-effecting argument expressions, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-multi-arg-prepend-method-a1014-focused-green-20260723-145419.log` (2 files / 4 tests), `agent-core-ruby-multi-arg-prepend-method-a1014-related-20260723-145515.log` (2 files / 4 tests), `agent-core-build-a1014-20260723-145515.log`, `ruby-multi-arg-prepend-method-dist-fixed-a1014-20260723-145550.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1014-20260723-145704.log` (230 files / 706 tests), and `agent-core-full-test-a1014-20260723-145705.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-prepend-assignment.utils.ts` 88, `interpreter-static-string.utils.ts` 166, `ruby-perl-static-reference.utils.ts` 151, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1015 | done | Core E2E | Detect Ruby static `String#replace` method calls and variable mutations before shell/direct process execution, so `ruby -e 'cmd = "echo safe"; cmd.replace("rm -rf /"); system(cmd)'`, `ruby -e 'cmd = "echo"; cmd.replace("rm"); flag = "--version"; flag.replace("-rf"); exec(cmd, flag, "/")'`, and `ruby -e 'system("echo".replace("rm"), "--version".replace("-rf"), "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-string-replace-method-runtime-a1015-20260723-150907.log` confirmed real Ruby replaces the receiver string before single-string shell `system`, direct argv `exec`, and direct literal argv `system`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed replace-built safe `rm --version` remains non-dangerous and a dynamic replacement value can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-string-replace-method-dist-before-a1015-20260723-150907.log` reproduced 3 false-negative mismatches where dangerous Ruby replace shell/direct/literal-argv flows returned `ask_user`; safe `--version`, dynamic replace argument, and future replace guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-string-replace-method-a1015-focused-red-20260723-151039.log` (2 failing files / 2 failing tests), then passed after `readStaticInterpreterString` treated opt-in Ruby `replace` method calls as returning the static argument value instead of appending to the receiver, `ruby-static-replace-assignment.utils.ts` scanned statement-boundary `name.replace(<static-string>)` mutations, and Ruby shell/direct paths passed `replace` through methodNames while Perl paths stayed unchanged. Tests now cover dangerous Ruby replace shell variable, direct exec variables, direct literal argv, plus safe `rm --version`, dynamic replacement value, and future replace guard behavior. Replace on aliases, method dispatch beyond simple variables/static literals, interpolated/dynamic replace arguments, list-assignment RHS replace, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-string-replace-method-a1015-focused-green-20260723-151700.log` (2 files / 4 tests), `agent-core-ruby-string-replace-method-a1015-related-20260723-151834.log` (2 files / 4 tests), `agent-core-build-a1015-20260723-151834.log`, `ruby-string-replace-method-dist-fixed-a1015-20260723-151850.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1015-20260723-152039.log` (230 files / 706 tests), and `agent-core-full-test-a1015-20260723-152040.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-replace-assignment.utils.ts` 48, `interpreter-static-string.utils.ts` 174, `ruby-perl-static-reference.utils.ts` 159, `interpreter-direct-command.utils.ts` 160, `interpreter-ruby-perl-shell-command.utils.ts` 139, and `interpreter-literal-list.utils.ts` 122. |
| A1016 | done | Core E2E | Detect Ruby static `String#concat` variable mutations before shell/direct process execution, so `ruby -e 'cmd = "rm"; cmd.concat(" -rf /"); system(cmd)'`, `ruby -e 'cmd = "rm"; cmd.concat(" -", "rf", " /"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.concat("m"); flag = "-"; flag.concat("r", "f"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-concat-mutation-runtime-a1016-20260723-153132.log` confirmed real Ruby mutates existing string variables with single- and multi-argument `concat` before single-string shell `system` and direct argv `exec`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed concat-built safe `rm --version` remains non-dangerous and a dynamic concat value can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-concat-mutation-dist-before-a1016-20260723-153132.log` reproduced 3 false-negative mismatches where dangerous Ruby concat shell/direct variable mutations returned `ask_user`; safe `--version`, dynamic concat argument, and future concat guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-concat-mutation-a1016-focused-red-20260723-153456.log` (2 failing files / 2 failing tests), then passed after `ruby-static-concat-assignment.utils.ts` scanned statement-boundary `name.concat(<static...>)` mutations, read comma-separated static concat arguments through `readStaticInterpreterString`, appended them to the latest prior static assignment, and kept the assignment unknown if any argument or boundary was not statically readable. Tests now cover dangerous Ruby concat shell variable, multi-arg shell variable, direct exec variable flows, plus safe `rm --version`, dynamic concat value, and future concat guard behavior. Concat on aliases, method dispatch beyond simple variables/static literals, interpolated/dynamic concat arguments, list-assignment RHS mutation, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-concat-mutation-a1016-focused-green-20260723-153620.log` (2 files / 4 tests), `agent-core-ruby-concat-mutation-a1016-related-20260723-153657.log` (2 files / 4 tests), `agent-core-build-a1016-20260723-153657.log`, `ruby-concat-mutation-dist-fixed-a1016-20260723-153710.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1016-20260723-153854.log` (230 files / 706 tests), and `agent-core-full-test-a1016-20260723-153854.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-concat-assignment.utils.ts` 88, `ruby-perl-static-reference.utils.ts` 168, `interpreter-static-string.utils.ts` 174, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1017 | done | Core E2E | Detect Ruby static `String#replace` followed by later static `concat` / `<<` / `prepend` variable mutations before shell/direct process execution, so `ruby -e 'cmd = "echo safe"; cmd.replace("rm"); cmd.concat(" -rf /"); system(cmd)'`, `ruby -e 'cmd = "echo safe"; cmd.replace("rm"); cmd << " -rf /"; system(cmd)'`, `ruby -e 'cmd = "echo safe"; cmd.replace(" -rf /"); cmd.prepend("rm"); system(cmd)'`, and `ruby -e 'cmd = "echo"; cmd.replace("r"); cmd.concat("m"); flag = "--version"; flag.replace("-"); flag.concat("r", "f"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-replace-followup-mutation-runtime-a1017-20260723-155755.log` confirmed real Ruby applies `replace` before later `concat`, `<<`, and `prepend` mutations for shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed the safe follow-up `--version` path remains non-dangerous and a dynamic replacement value can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-replace-followup-mutation-dist-before-a1017-20260723-155756.log` reproduced 4 false-negative mismatches where dangerous replace-followed-by-mutation shell/direct flows returned `ask_user`, while safe `--version`, dynamic replacement, and future mutation guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-replace-followup-mutation-a1017-focused-red-20260723-155909.log` (2 failing files / 2 failing tests), then passed after `rubyPerlStaticStringAssignments` registered Ruby static `replace` mutation assignments before `<<`, `concat`, and `prepend` follow-up mutation scanners, allowing each scanner to see only prior source-order replace values through its existing `endIndex <= cursor` guard. Tests now cover dangerous shell concat/append/prepend after replace, dangerous direct exec variables after replace and concat, plus safe `rm --version`, dynamic replacement, and future mutation guard behavior. Replace-follow-up mutation through aliases, dynamic/interpolated replacement or follow-up arguments, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-replace-followup-mutation-a1017-focused-green-20260723-160111.log` (2 files / 4 tests), `agent-core-ruby-replace-followup-mutation-a1017-related-20260723-160207.log` (2 files / 4 tests), `agent-core-build-a1017-20260723-160208.log`, `ruby-replace-followup-mutation-dist-fixed-a1017-20260723-160337.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1017-20260723-160511.log` (230 files / 706 tests), and `agent-core-full-test-a1017-20260723-160511.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-perl-static-reference.utils.ts` 168, `ruby-static-replace-assignment.utils.ts` 48, `ruby-static-concat-assignment.utils.ts` 88, `ruby-static-append-assignment.utils.ts` 82, `ruby-static-prepend-assignment.utils.ts` 88, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1018 | done | Core E2E | Detect Ruby static cross-mutation chains where later `concat`, `<<`, or `prepend` scanners previously could not see earlier mutations from a different scanner batch, so `ruby -e 'cmd = "r"; cmd.concat("m"); cmd << " -rf /"; system(cmd)'`, `ruby -e 'cmd = "m"; cmd.prepend("r"); cmd.concat(" -rf /"); system(cmd)'`, `ruby -e 'cmd = "m"; cmd.prepend("r"); cmd << " -rf /"; system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.concat("m"); flag = "-"; flag.concat("r"); flag << "f"; exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-cross-mutation-order-runtime-a1018-20260723-162505.log` confirmed real Ruby applies cross-method string mutations in source order before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed cross-mutation `rm --version` remains non-dangerous and a dynamic append after static concat can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-cross-mutation-order-dist-before-a1018-20260723-162506.log` reproduced 4 false-negative mismatches where dangerous concat-then-append, prepend-then-concat, prepend-then-append, and direct concat-then-append flows returned `ask_user`, while safe `--version`, dynamic append, and future mutation guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-cross-mutation-order-a1018-focused-red-20260723-162737.log` (2 failing files / 2 failing tests), then passed after `ruby-static-mutation-assignment.utils.ts` collected Ruby `replace`, `<<`, `concat`, and `prepend` mutation assignments to a stable fixpoint keyed by variable/endIndex, so each scanner can see prior source-order mutations produced by other scanner families without using future assignments. `ruby-perl-static-reference.utils.ts` now delegates Ruby mutation aggregation to that utility, reducing the entry file while leaving Perl unchanged. Tests now cover dangerous shell concat-append, prepend-concat, prepend-append, dangerous direct concat-append variables, plus safe `rm --version`, dynamic append, and future mutation guard behavior. Mutation through aliases, dynamic/interpolated mutation arguments, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-cross-mutation-order-a1018-focused-green-20260723-162936.log` (2 files / 4 tests), `agent-core-ruby-cross-mutation-order-a1018-related-20260723-163057.log` (2 files / 4 tests), `agent-core-build-a1018-20260723-163057.log`, `ruby-cross-mutation-order-dist-fixed-a1018-20260723-163112.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1018-20260723-163147.log` (230 files / 706 tests), and `agent-core-full-test-a1018-20260723-163147.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-perl-static-reference.utils.ts` 149, `ruby-static-mutation-assignment.utils.ts` 87, `ruby-static-append-assignment.utils.ts` 82, `ruby-static-concat-assignment.utils.ts` 88, `ruby-static-prepend-assignment.utils.ts` 88, `ruby-static-replace-assignment.utils.ts` 48, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1019 | done | Core E2E | Detect Ruby `<<` variable mutations whose appended static RHS uses `String#prepend`, so `ruby -e 'cmd = "rm"; cmd << " /".prepend(" -rf"); system(cmd)'`, `ruby -e 'cmd = "r"; cmd << "m"; cmd << " /".prepend(" -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd << "m"; flag = "-"; flag << "f".prepend("r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-append-prepend-argument-runtime-a1019-20260723-164726.log` confirmed real Ruby evaluates `String#prepend` on the appended RHS before applying `<<` mutation for shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed the safe RHS-prepend `rm --version` path remains non-dangerous and a dynamic prepend argument can become runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-append-prepend-argument-dist-before-a1019-20260723-164726.log` reproduced 3 false-negative mismatches where dangerous append-RHS-prepend shell/direct flows returned `ask_user`, while safe `--version`, dynamic prepend argument, and future append guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-append-prepend-argument-a1019-focused-red-20260723-164957.log` (2 failing files / 2 failing tests), then passed after `rubyStaticAppendAssignments` accepted `prependMethodNames` and passed them through `readStaticAppendValue` into `readStaticInterpreterString`, so append RHS expressions can reuse the same opt-in static Ruby prepend semantics as concat/prepend scanners. `ruby-static-mutation-assignment.utils.ts` now forwards `prependMethodNames` to append aggregation; Perl and non-Ruby paths stay unchanged. Tests now cover dangerous shell append-RHS-prepend, chained shell append-RHS-prepend, dangerous direct exec append-RHS-prepend, plus safe `rm --version`, dynamic prepend argument, and future append guard behavior. Append RHS prepend through aliases, interpolated/dynamic prepend arguments, parenthesized inner method expressions not already covered by the shared reader, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-append-prepend-argument-a1019-focused-green-20260723-165217.log` (2 files / 4 tests), `agent-core-ruby-append-prepend-argument-a1019-related-20260723-165345.log` (2 files / 4 tests), `agent-core-build-a1019-20260723-165346.log`, `ruby-append-prepend-argument-dist-fixed-a1019-20260723-165700.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1019-20260723-165737.log` (230 files / 706 tests), and `agent-core-full-test-a1019-20260723-165738.log` (327 files / 1979 tests). Production source line counts remain under 200: `ruby-static-append-assignment.utils.ts` 92, `ruby-static-mutation-assignment.utils.ts` 94, `ruby-perl-static-reference.utils.ts` 149, `interpreter-static-string.utils.ts` 174, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1020 | done | Core E2E | Detect parenthesized Ruby static method-chain strings in shell/direct process paths and append RHS expressions, so `ruby -e 'system(("rm".concat(" -rf /")))'`, `ruby -e 'cmd = "rm"; cmd << (" /".prepend(" -rf")); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd << "m"; flag = "-"; flag << ("f".prepend("r")); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-parenthesized-method-chain-runtime-a1020-20260723-171047.log` confirmed real Ruby evaluates parenthesized `concat` / `prepend` method-chain strings before shell strings, direct literal argv, append RHS mutations, and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed parenthesized `rm --version` remains non-dangerous and a dynamic parenthesized concat argument can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-parenthesized-method-chain-dist-before-a1020-20260723-171047.log` reproduced 3 false-negative mismatches where parenthesized concat shell and append-RHS-prepend shell/direct flows returned `ask_user`; already-covered parenthesized direct literal prepend stayed `deny`, while safe `--version`, dynamic concat argument, and future method-chain guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-parenthesized-method-chain-a1020-focused-red-20260723-171613.log` (2 failing files / 2 failing tests), then passed after `readParenthesizedInterpreterString` preserved enabled static method-chain readers through recursive parsing while keeping parenthesized method arguments literal-only to avoid turning variable/dynamic method arguments into automatic denies, and after parenthesized static strings accepted statement boundaries needed by append RHS expressions. Tests now cover dangerous parenthesized concat shell, append RHS parenthesized prepend shell/direct variables, plus safe `rm --version`, dynamic parenthesized concat, and future method-chain guard behavior. Parenthesized method-chain arguments through variable references, aliases, dynamic/interpolated arguments, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-parenthesized-method-chain-a1020-focused-green-20260723-172437.log` (2 files / 4 tests), `agent-core-ruby-parenthesized-method-chain-a1020-related-20260723-172505.log` (2 files / 4 tests), `agent-core-build-a1020-20260723-172505.log`, `ruby-parenthesized-method-chain-dist-fixed-a1020-20260723-172558.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1020-20260723-172646.log` (230 files / 706 tests), and `agent-core-full-test-a1020-20260723-172646.log` (327 files / 1979 tests). Production source line counts remain under 200: `interpreter-static-string.utils.ts` 199, `ruby-static-append-assignment.utils.ts` 92, `ruby-static-mutation-assignment.utils.ts` 94, `ruby-perl-static-reference.utils.ts` 149, `interpreter-direct-command.utils.ts` 160, and `interpreter-ruby-perl-shell-command.utils.ts` 139. |
| A1021 | done | Core E2E | Detect Ruby static `String#insert` variable mutations before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "r"; cmd.concat("m /"); cmd.insert(2, " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(1, "m"); flag = "-f"; flag.insert(1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-mutation-runtime-a1021-20260723-173157.log` confirmed real Ruby mutates existing string variables with static non-negative integer `insert` indexes before single-string shell `system` and direct argv `exec`, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed insert-built safe `rm --version` remains non-dangerous and a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-mutation-dist-before-a1021-20260723-173157.log` reproduced 3 false-negative mismatches where dangerous Ruby insert shell/direct variable mutations returned `ask_user`; safe `--version`, dynamic insert argument, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-mutation-a1021-focused-red-20260723-173330.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` scanned statement-boundary `name.insert(<static non-negative integer>, <static string>)` mutations, inserted the static value into the latest prior static assignment when the index is within the current string length, and fed those mutations into the shared Ruby stable mutation fixpoint through `ruby-static-mutation-assignment.utils.ts`. Tests now cover dangerous shell insert, insert after concat, direct exec insert variables, plus safe `rm --version`, dynamic insert value, and future insert guard behavior. Negative indexes, out-of-range indexes, variable or computed indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-mutation-a1021-focused-green-20260723-173435.log` (2 files / 4 tests), `agent-core-ruby-insert-mutation-a1021-related-20260723-174113.log` (2 files / 4 tests), `agent-core-build-a1021-20260723-174113.log`, `ruby-insert-mutation-dist-fixed-a1021-20260723-174251.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1021-20260723-174251.log` (230 files / 706 tests), and `agent-core-full-test-a1021-20260723-174251.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 93, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1022 | done | Core E2E | Detect Ruby static `String#insert` variable mutations that use valid negative indexes before shell/direct process execution, so `ruby -e 'cmd = "rm/"; cmd.insert(-2, " -rf "); system(cmd)'`, `ruby -e 'cmd = "r"; cmd.concat("m/"); cmd.insert(-2, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(-1, "m"); flag = "-f"; flag.insert(-2, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-negative-index-runtime-a1022-rerun-20260723-175240.log` confirmed real Ruby resolves negative `String#insert` indexes relative to the current string length plus one before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed negative-index insert can build safe `rm --version` and that a dynamic negative-index insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-negative-index-dist-before-a1022-rerun-20260723-175240.log` reproduced 3 false-negative mismatches where dangerous Ruby negative-index insert shell/direct variable mutations returned `ask_user`; safe `--version`, dynamic insert argument, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-negative-index-a1022-focused-red-20260723-175428.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` accepted signed static integer arguments and normalized negative indexes with Ruby's `source.length + index + 1` insertion rule while preserving the existing out-of-range guard as unresolved. Tests now cover dangerous negative-index shell insert, negative-index insert after concat, dangerous direct exec negative-index variables, plus safe negative-index `rm --version`, dynamic insert value, and future negative insert guard behavior. Out-of-range negative indexes, variable or computed indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-negative-index-a1022-focused-green-20260723-175503.log` (2 files / 4 tests), `agent-core-ruby-insert-negative-index-a1022-related-20260723-175610.log` (2 files / 4 tests), `agent-core-build-a1022-20260723-175610.log`, `ruby-insert-negative-index-dist-fixed-a1022-20260723-175629.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1022-20260723-175629.log` (230 files / 706 tests), and `agent-core-full-test-a1022-20260723-175630.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 99, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1023 | done | Core E2E | Detect Ruby static `String#insert` variable mutations that use explicit plus-signed indexes before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(+2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "r"; cmd.concat("m /"); cmd.insert(+2, " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(+1, "m"); flag = "-f"; flag.insert(+1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-plus-index-runtime-a1023-20260723-180521.log` confirmed real Ruby accepts explicit plus-signed `String#insert` indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed plus-index insert can build safe `rm --version` and that a dynamic plus-index insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-plus-index-dist-before-a1023-20260723-180521.log` reproduced 3 false-negative mismatches where dangerous Ruby plus-index insert shell/direct variable mutations returned `ask_user`; safe `--version`, dynamic insert argument, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-plus-index-a1023-focused-red-20260723-180731.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` accepted an optional `+` sign in static integer insert arguments while preserving A1022 negative-index normalization and the existing out-of-range guard. Tests now cover dangerous plus-index shell insert, plus-index insert after concat, dangerous direct exec plus-index variables, plus safe plus-index `rm --version`, dynamic insert value, and future plus insert guard behavior. Out-of-range indexes, variable or computed indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-plus-index-a1023-focused-green-20260723-180808.log` (2 files / 4 tests), `agent-core-ruby-insert-plus-index-a1023-related-20260723-180836.log` (2 files / 4 tests), `agent-core-build-a1023-20260723-180837.log`, `ruby-insert-plus-index-dist-fixed-a1023-20260723-180854.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1023-20260723-180854.log` (230 files / 706 tests), and `agent-core-full-test-a1023-20260723-180854.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 99, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1024 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose static integer index is parenthesized before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert((2), " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert((-2), " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert((+1), "m"); flag = "-f"; flag.insert((+1), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-parenthesized-index-runtime-a1024-20260723-193313.log` confirmed real Ruby evaluates parenthesized static `String#insert` indexes, including negative and explicit plus-signed integers, before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed parenthesized-index insert can build safe `rm --version` and that a dynamic parenthesized-index insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-parenthesized-index-dist-before-a1024-20260723-193314.log` reproduced 3 false-negative mismatches where dangerous Ruby parenthesized-index insert shell/direct variable mutations returned `ask_user`; safe `--version`, dynamic insert argument, future insert, and arithmetic-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-parenthesized-index-a1024-focused-red-20260723-193352.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` added parenthesized static integer parsing for insert indexes, requiring the inner expression to be a single static signed integer and requiring the closing parenthesis before the argument comma so expressions such as `(1 + 1)` remain unresolved. Tests now cover dangerous parenthesized positive, negative, and plus-signed index inserts, plus safe parenthesized-index `rm --version`, dynamic insert value, future insert, and arithmetic-index guard behavior. Arithmetic or computed indexes, nested parentheses, out-of-range indexes, variable indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-parenthesized-index-a1024-focused-green-20260723-193418.log` (2 files / 4 tests), `agent-core-ruby-insert-parenthesized-index-a1024-related-20260723-193436.log` (2 files / 4 tests), `agent-core-build-a1024-20260723-193436.log`, `ruby-insert-parenthesized-index-dist-fixed-a1024-20260723-193449.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1024-20260723-193449.log` (230 files / 706 tests), and `agent-core-full-test-a1024-20260723-193450.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 115, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1025 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose static integer index is nested-parenthesized before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(((2)), " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(((-2)), " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(((+1)), "m"); flag = "-f"; flag.insert(((+1)), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-nested-parenthesized-index-runtime-a1025-20260723-193825.log` confirmed real Ruby evaluates nested-parenthesized static `String#insert` indexes, including negative and explicit plus-signed integers, before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed nested-parenthesized index insert can build safe `rm --version` and that a dynamic nested-parenthesized insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-nested-parenthesized-index-dist-before-a1025-20260723-193826.log` reproduced 3 false-negative mismatches where dangerous Ruby nested-parenthesized index insert shell/direct variable mutations returned `ask_user`; safe `--version`, dynamic insert argument, future insert, and nested arithmetic-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-nested-parenthesized-index-a1025-focused-red-20260723-193908.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` routed insert indexes through a recursive static-integer expression reader that unwraps balanced parentheses around a single signed integer while still requiring each closing parenthesis before the argument comma, so `((1 + 1))` remains unresolved. Tests now cover dangerous nested-parenthesized positive, negative, and plus-signed index inserts, plus safe nested-parenthesized `rm --version`, dynamic insert value, future insert, and nested arithmetic-index guard behavior. Arithmetic or computed indexes, variable indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-nested-parenthesized-index-a1025-focused-green-20260723-193930.log` (2 files / 4 tests), `agent-core-ruby-insert-nested-parenthesized-index-a1025-related-20260723-193949.log` (2 files / 4 tests), `agent-core-build-a1025-20260723-193949.log`, `ruby-insert-nested-parenthesized-index-dist-fixed-a1025-20260723-194002.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1025-20260723-194002.log` (230 files / 706 tests), and `agent-core-full-test-a1025-20260723-194002.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 120, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1026 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index is a simple static `+` / `-` arithmetic expression before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(1 + 1, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(0 - 2, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(0 + 1, "m"); flag = "-f"; flag.insert(2 - 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-static-arithmetic-index-runtime-a1026-20260723-194356.log` confirmed real Ruby evaluates simple static addition/subtraction insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed arithmetic-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-static-arithmetic-index-dist-before-a1026-20260723-194357.log` reproduced 4 false-negative mismatches where dangerous Ruby static arithmetic index insert shell/direct/parenthesized flows returned `ask_user`; safe `--version`, dynamic insert argument, future insert, multiplication-index, and dynamic-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-static-arithmetic-index-a1026-focused-red-20260723-194449.log` (2 failing files / 3 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` split integer parsing into static integer terms and a simple expression reader that supports exactly one static `+` or `-` between integer terms while preserving comma-boundary checks, parenthesis unwrapping, and unresolved behavior for multiplication or variable indexes. Tests now cover dangerous addition, subtraction, parenthesized addition, and direct exec arithmetic inserts, plus safe arithmetic-index `rm --version`, dynamic insert value, dynamic index, multiplication index, and future insert guard behavior. Multiplication/division/modulo, chained arithmetic, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-static-arithmetic-index-a1026-focused-green-20260723-194521.log` (2 files / 4 tests), `agent-core-ruby-insert-static-arithmetic-index-a1026-related-20260723-194539.log` (2 files / 4 tests), `agent-core-build-a1026-20260723-194540.log`, `ruby-insert-static-arithmetic-index-dist-fixed-a1026-20260723-194556.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1026-20260723-194556.log` (230 files / 706 tests), and `agent-core-full-test-a1026-20260723-194556.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 137, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1027 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index is a chained static `+` / `-` arithmetic expression before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(1 + 1 + 0, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(0 - 1 - 1, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(0 + 1 + 0, "m"); flag = "-f"; flag.insert(3 - 1 - 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-chained-arithmetic-index-runtime-a1027-20260723-195112.log` confirmed real Ruby evaluates chained static addition/subtraction insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed chained arithmetic-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-chained-arithmetic-index-dist-before-a1027-20260723-195112.log` reproduced 4 false-negative mismatches where dangerous Ruby chained static arithmetic index insert shell/direct/parenthesized flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, multiplication-index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-chained-arithmetic-index-a1027-focused-red-20260723-195203.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` changed the static integer expression reader from a single optional `+` / `-` into a loop over static integer terms while preserving comma-boundary checks, parenthesis unwrapping, and unresolved behavior for multiplication or variable indexes. Tests now cover dangerous chained addition, chained subtraction, parenthesized chained arithmetic, and direct exec chained arithmetic inserts, plus safe chained arithmetic-index `rm --version`, dynamic insert value, dynamic index, multiplication index, and future insert guard behavior. Multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-chained-arithmetic-index-a1027-focused-green-20260723-195245.log` (2 files / 4 tests), `agent-core-ruby-insert-chained-arithmetic-index-a1027-related-20260723-195259.log` (2 files / 4 tests), `agent-core-build-a1027-20260723-195259.log`, `ruby-insert-chained-arithmetic-index-dist-fixed-a1027-20260723-195308.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1027-20260723-195316.log` (230 files / 706 tests), and `agent-core-full-test-a1027-20260723-195317.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 138, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1028 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index is a signed parenthesized static expression before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(+(1 + 1), " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(-(1 + 1), " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(+(0 + 1), "m"); flag = "-f"; flag.insert(+(2 - 1), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-signed-parenthesized-index-runtime-a1028-20260723-195716.log` confirmed real Ruby evaluates unary plus/minus applied to parenthesized static insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed signed-parenthesized insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-signed-parenthesized-index-dist-before-a1028-20260723-195716.log` reproduced 3 false-negative mismatches where dangerous Ruby signed-parenthesized index insert shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, multiplication-index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-signed-parenthesized-index-a1028-focused-red-20260723-195806.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` added a small signed-parenthesized integer term reader that applies unary `+` / `-` only when the following expression is parenthesized and statically resolved, preserving existing comma-boundary checks, recursive expression parsing, and unresolved behavior for variables or multiplication. Tests now cover dangerous unary-plus parenthesized shell insert, unary-minus parenthesized shell insert, and direct exec signed-parenthesized arithmetic inserts, plus safe signed-parenthesized `rm --version`, dynamic insert value, dynamic index, multiplication index, and future insert guard behavior. Multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-signed-parenthesized-index-a1028-focused-green-20260723-195901.log` (2 files / 4 tests), `agent-core-ruby-insert-signed-parenthesized-index-a1028-related-20260723-195912.log` (2 files / 4 tests), `agent-core-build-a1028-20260723-195913.log`, `ruby-insert-signed-parenthesized-index-dist-fixed-a1028-20260723-195923.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1028-20260723-195930.log` (230 files / 706 tests), and `agent-core-full-test-a1028-20260723-195930.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 156, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1029 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose signed integer index contains whitespace between unary sign and digits before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(+ 2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(- 2, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(+ 1, "m"); flag = "-f"; flag.insert(+ 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-spaced-unary-index-runtime-a1029-20260723-200314.log` confirmed real Ruby evaluates whitespace-separated unary plus/minus integer insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed spaced-unary insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-spaced-unary-index-dist-before-a1029-20260723-200314.log` reproduced 3 false-negative mismatches where dangerous Ruby spaced-unary index insert shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-spaced-unary-index-a1029-focused-red-20260723-200421.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` changed the static integer reader to normalize an optional unary `+` / `-` followed by whitespace and digits while preserving existing immediate-sign support and unresolved behavior when the signed operand is a variable or non-integer expression. Tests now cover dangerous spaced-unary plus shell insert, spaced-unary minus shell insert, and direct exec spaced-unary inserts, plus safe spaced-unary `rm --version`, dynamic insert value, dynamic index, and future insert guard behavior. Multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-spaced-unary-index-a1029-focused-green-20260723-200507.log` (2 files / 4 tests), `agent-core-ruby-insert-spaced-unary-index-a1029-related-20260723-200516.log` (2 files / 4 tests), `agent-core-build-a1029-20260723-200516.log`, `ruby-insert-spaced-unary-index-dist-fixed-a1029-20260723-200528.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1029-20260723-200541.log` (230 files / 706 tests), and `agent-core-full-test-a1029-20260723-200543.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 164, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1030 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose integer index uses repeated unary signs before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(--2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(+-2, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(++1, "m"); flag = "-f"; flag.insert(- -1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-repeated-unary-index-runtime-a1030-20260723-200847.log` confirmed real Ruby evaluates repeated unary plus/minus integer insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed repeated-unary insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-repeated-unary-index-dist-before-a1030-20260723-200847.log` reproduced 3 false-negative mismatches where dangerous Ruby repeated-unary index insert shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-repeated-unary-index-a1030-focused-red-20260723-200947.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` changed the static integer reader to fold a sequence of unary `+` / `-` signs, allowing whitespace between signs and digits while still requiring the final operand to be a static integer. Tests now cover dangerous double-minus positive shell insert, plus-minus negative shell insert, and direct exec repeated-unary inserts, plus safe repeated-unary `rm --version`, dynamic insert value, dynamic index, and future insert guard behavior. Multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-repeated-unary-index-a1030-focused-green-20260723-201058.log` (2 files / 4 tests), `agent-core-ruby-insert-repeated-unary-index-a1030-related-20260723-201112.log` (2 files / 4 tests), `agent-core-build-a1030-20260723-201112.log`, `ruby-insert-repeated-unary-index-dist-fixed-a1030-20260723-201121.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1030-20260723-201131.log` (230 files / 706 tests), and `agent-core-full-test-a1030-20260723-201131.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 168, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1031 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose integer index uses underscore digit separators before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(0_2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(-0_2, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(+0_1, "m"); flag = "-f"; flag.insert(0_1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-underscore-integer-index-runtime-a1031-20260723-201440.log` confirmed real Ruby evaluates underscore-separated integer insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed underscore-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-underscore-integer-index-dist-before-a1031-20260723-201440.log` reproduced 3 false-negative mismatches where dangerous Ruby underscore integer index insert shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-underscore-integer-index-a1031-focused-red-20260723-201552.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` changed the static integer reader to accept Ruby-style single underscore digit separators and strip underscores before computing the signed value, while still requiring a digit before and after each underscore. Tests now cover dangerous positive underscore shell insert, negative underscore shell insert, and direct exec underscore inserts, plus safe underscore-index `rm --version`, dynamic insert value, dynamic index, and future insert guard behavior. Consecutive/trailing/leading underscores, multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-underscore-integer-index-a1031-focused-green-20260723-201648.log` (2 files / 4 tests), `agent-core-ruby-insert-underscore-integer-index-a1031-related-20260723-201658.log` (2 files / 4 tests), `agent-core-build-a1031-20260723-201658.log`, `ruby-insert-underscore-integer-index-dist-fixed-a1031-20260723-201709.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1031-20260723-201723.log` (230 files / 706 tests), and `agent-core-full-test-a1031-20260723-201724.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 168, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1032 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose integer index uses base-prefixed literals before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(0x2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(-0b10, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(+0o1, "m"); flag = "-f"; flag.insert(0b1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-base-prefixed-integer-index-runtime-a1032-20260723-202210.log` confirmed real Ruby evaluates hexadecimal, binary, octal, signed, and negative base-prefixed insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed base-prefixed insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-base-prefixed-integer-index-dist-before-a1032-20260723-202233.log` reproduced 3 false-negative mismatches where dangerous Ruby base-prefixed index insert shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-base-prefixed-integer-index-a1032-focused-red-20260723-202423.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` changed the static integer reader to recognize Ruby `0b`/`0o`/`0x` base-prefixed integer literals with single underscore digit separators, parse them with the proper radix, and preserve the existing comma-boundary check. Tests now cover dangerous hex shell insert, negative binary shell insert, direct exec octal/binary inserts, plus safe hex `rm --version`, dynamic insert value, dynamic index, and future insert guard behavior. Legacy bare `02` octal/decimal ambiguity, invalid base literals, consecutive/trailing/leading underscores, multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-base-prefixed-integer-index-a1032-focused-green-20260723-202508.log` (2 files / 4 tests), `agent-core-ruby-insert-base-prefixed-integer-index-a1032-related-20260723-202531.log` (2 files / 4 tests), `agent-core-build-a1032-20260723-202531.log`, `ruby-insert-base-prefixed-integer-index-dist-fixed-a1032-20260723-202545.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1032-20260723-202545.log` (230 files / 706 tests), and `agent-core-full-test-a1032-20260723-202545.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 179, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1033 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose integer index uses decimal base-prefixed literals before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(0d2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(-0D2, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(+0d1, "m"); flag = "-f"; flag.insert(0D1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-decimal-prefixed-integer-index-runtime-a1033-20260723-203013.log` confirmed real Ruby evaluates lowercase/uppercase decimal-prefixed, signed, negative, and underscore-capable decimal insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed decimal-prefixed insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-decimal-prefixed-integer-index-dist-before-a1033-20260723-203013.log` reproduced 3 false-negative mismatches where dangerous Ruby decimal-prefixed index insert shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-decimal-prefixed-integer-index-a1033-focused-red-20260723-203106.log` (2 failing files / 2 failing tests), then passed after `ruby-static-insert-assignment.utils.ts` extended the base-prefixed static integer reader to recognize Ruby `0d`/`0D` decimal literals with single underscore digit separators and parse them with radix 10 while preserving the existing comma-boundary check. Tests now cover dangerous lowercase decimal shell insert, uppercase negative decimal shell insert, direct exec signed decimal inserts, plus safe decimal `rm --version`, dynamic insert value, dynamic index, and future insert guard behavior. Legacy bare `02` octal/decimal ambiguity, invalid base literals, consecutive/trailing/leading underscores, multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-decimal-prefixed-integer-index-a1033-focused-green-20260723-203142.log` (2 files / 4 tests), `agent-core-ruby-insert-decimal-prefixed-integer-index-a1033-related-20260723-203159.log` (2 files / 4 tests), `agent-core-build-a1033-20260723-203159.log`, `ruby-insert-decimal-prefixed-integer-index-dist-fixed-a1033-20260723-203212.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1033-20260723-203212.log` (230 files / 706 tests), and `agent-core-full-test-a1033-20260723-203213.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 181, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1034 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose shell command index uses legacy leading-zero octal integer literals before shell execution, so `ruby -e 'cmd = "rm -rf X"; cmd.insert(010, " /"); system(cmd)'`, `ruby -e 'cmd = "rm -rf X"; cmd.insert(0_10, " /"); system(cmd)'`, and `ruby -e 'cmd = "rm -rf X"; cmd.insert(+010, " /"); system(cmd)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-legacy-octal-integer-index-runtime-a1034-20260723-203613.log` confirmed real Ruby evaluates `010`, `0_10`, and signed `+010` insert indexes as octal 8 before shell execution, reaching a fake `rm` with `argc=3 args=-rf X /`; it also confirmed legacy-octal insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-legacy-octal-integer-index-dist-before-a1034-20260723-203613.log` reproduced 3 false-negative mismatches where dangerous Ruby legacy-octal index shell insert flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic index, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-legacy-octal-integer-index-a1034-focused-red-20260723-203713.log` (1 failing file / 1 failing test), then passed after `ruby-static-insert-assignment.utils.ts` added a legacy leading-zero octal integer reader before the decimal fallback, parsing single-underscore-separated `0...` octal forms with radix 8 while preserving the existing comma-boundary check. Tests now cover dangerous plain legacy octal shell insert, underscore legacy octal shell insert, signed legacy octal shell insert, plus safe legacy-octal `rm --version`, dynamic insert value, dynamic index, and future insert guard behavior. Invalid octal literals such as `08`/`018`, consecutive/trailing/leading underscores, multiplication/division/modulo, variable or method-call indexes, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, direct argv argument-splitting assumptions, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-legacy-octal-integer-index-a1034-focused-green-20260723-203758.log` (1 file / 2 tests), `agent-core-ruby-insert-legacy-octal-integer-index-a1034-related-20260723-203827.log` (2 files / 4 tests), `agent-core-build-a1034-20260723-203827.log`, `ruby-insert-legacy-octal-integer-index-dist-fixed-a1034-20260723-203840.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1034-20260723-203840.log` (230 files / 706 tests), and `agent-core-full-test-a1034-20260723-203840.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 189, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1035 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static multiplication before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(1 * 2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(+(1 * 2), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(1 * 1, "m"); flag = "-f"; flag.insert(1 * 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-multiplication-index-runtime-a1035-20260723-204236.log` confirmed real Ruby evaluates static multiplication insert indexes, including signed-parenthesized multiplication and direct argv variable mutations, before execution, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed multiplication-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-multiplication-index-dist-before-a1035-20260723-204237.log` reproduced 3 false-negative mismatches where dangerous Ruby static multiplication index shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic factor, division, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-multiplication-index-a1035-focused-red-20260723-204325.log` (2 failing files / 2 failing tests), then passed after static integer expression parsing gained `*` term evaluation while preserving `+`/`-` precedence and unresolved behavior for non-static factors. To keep production source files below the 200-line ceiling and isolate responsibility, Ruby static integer expression parsing moved from `ruby-static-insert-assignment.utils.ts` into new pure utility `ruby-static-integer-expression.utils.ts`; the insert assignment module now owns insert mutation scanning and argument boundaries only. Tests now cover dangerous shell multiplication insert, signed-parenthesized multiplication insert, direct exec multiplication inserts, plus safe multiplication `rm --version`, dynamic insert value, dynamic factor, division, and future insert guard behavior. Division/modulo, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-multiplication-index-a1035-focused-green-rerun-20260723-204543.log` (2 files / 4 tests), `agent-core-ruby-insert-multiplication-index-a1035-related-20260723-204602.log` (2 files / 4 tests), `agent-core-build-a1035-20260723-204603.log`, `ruby-insert-multiplication-index-dist-fixed-a1035-20260723-204618.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1035-20260723-204619.log` (230 files / 706 tests), and `agent-core-full-test-a1035-20260723-204619.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 114, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1036 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static integer division before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(4 / 2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(+(4 / 2), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(2 / 2, "m"); flag = "-f"; flag.insert(2 / 2, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-division-index-runtime-a1036-20260723-204939.log` confirmed real Ruby evaluates static integer division insert indexes, including signed-parenthesized division and direct argv variable mutations, before execution, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed division-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-division-index-dist-before-a1036-20260723-204939.log` reproduced 3 false-negative mismatches where dangerous Ruby static division index shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic divisor, division-by-zero, modulo, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-division-index-a1036-focused-red-20260723-205023.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` extended same-precedence static term parsing from `*` to `*`/`/`, using Ruby-compatible floored integer division and returning unresolved for division by zero. Tests now cover dangerous shell division insert, signed-parenthesized division insert, direct exec division inserts, plus safe division `rm --version`, dynamic insert value, dynamic divisor, division-by-zero, modulo, and future insert guard behavior. Modulo, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-division-index-a1036-focused-green-20260723-205103.log` (2 files / 4 tests), `agent-core-ruby-insert-division-index-a1036-related-20260723-205125.log` (2 files / 4 tests), `agent-core-build-a1036-20260723-205126.log`, `ruby-insert-division-index-dist-fixed-a1036-20260723-205139.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1036-20260723-205139.log` (230 files / 706 tests), and `agent-core-full-test-a1036-20260723-205139.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 122, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1037 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static modulo before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(5 % 3, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(+(5 % 3), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(5 % 2, "m"); flag = "-f"; flag.insert(4 % 3, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-modulo-index-runtime-a1037-20260723-205706.log` confirmed real Ruby evaluates static modulo insert indexes, including signed-parenthesized modulo and direct argv variable mutations, before execution, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed modulo-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-modulo-index-dist-before-a1037-20260723-205706.log` reproduced 3 false-negative mismatches where dangerous Ruby static modulo index shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic divisor, modulo-by-zero, exponentiation, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-modulo-index-a1037-focused-red-rerun-20260723-205807.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` extended same-precedence static term parsing from `*`/`/` to `*`/`/`/`%`, using Ruby-compatible modulo computed from floored integer division and returning unresolved for modulo by zero. Tests now cover dangerous shell modulo insert, signed-parenthesized modulo insert, direct exec modulo inserts, plus safe modulo `rm --version`, dynamic insert value, dynamic divisor, modulo-by-zero, exponentiation, and future insert guard behavior. Exponentiation, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-modulo-index-a1037-focused-green-20260723-205842.log` (2 files / 4 tests), `agent-core-ruby-insert-modulo-index-a1037-related-20260723-205856.log` (2 files / 4 tests), `agent-core-build-a1037-20260723-205856.log`, `ruby-insert-modulo-index-dist-fixed-a1037-20260723-205915.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1037-rerun-20260723-205945.log` (230 files / 706 tests), and `agent-core-full-test-a1037-20260723-205915.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 123, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1038 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses non-negative static integer exponentiation before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(2 ** 1, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(+(2 ** 1), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(2 ** 0, "m"); flag = "-f"; flag.insert(3 ** 0, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-exponent-index-runtime-a1038-20260723-210325.log` confirmed real Ruby evaluates static exponent insert indexes, including signed-parenthesized exponent and direct argv variable mutations, before execution, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed exponent-index insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-exponent-index-dist-before-a1038-20260723-210325.log` reproduced 3 false-negative mismatches where dangerous Ruby static exponent index shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic exponent, dynamic base, negative exponent, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-exponent-index-a1038-focused-red-20260723-210359.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added a right-recursive static power layer above factor parsing and below `*`/`/`/`%`, accepting only safe integer results with non-negative static integer exponents. Tests now cover dangerous shell exponent insert, signed-parenthesized exponent insert, direct exec exponent inserts, plus safe exponent `rm --version`, dynamic insert value, dynamic exponent, dynamic base, negative exponent, and future insert guard behavior. Negative exponents, dynamic base or exponent expressions, unsafe integer results, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-exponent-index-a1038-focused-green-20260723-210445.log` (2 files / 4 tests), `agent-core-ruby-insert-exponent-index-a1038-related-20260723-210455.log` (2 files / 4 tests), `agent-core-build-a1038-20260723-210455.log`, `ruby-insert-exponent-index-dist-fixed-a1038-20260723-210508.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1038-20260723-210508.log` (230 files / 706 tests), and `agent-core-full-test-a1038-20260723-210508.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 137, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1039 | done | Core E2E | Respect Ruby unary-sign precedence around static exponentiation in `String#insert` indexes before shell/direct process execution, so `ruby -e 'cmd = "rm -rf X"; cmd.insert(-2 ** 2 + 1, " /"); system(cmd)'`, `ruby -e 'cmd = "rm -rf X"; cmd.insert(+-2 ** 2 + 1, " /"); system(cmd)'`, and `ruby -e 'cmd = "m"; cmd.insert(-2 ** 2 + 2, "r"); flag = "-f"; flag.insert(-2 ** 2 + 2, "r"); exec(cmd, flag, "/")'` deny instead of being mis-evaluated as parenthesized negative-base exponentiation and slipping through as user-reviewable. | Runtime probe `ruby-insert-signed-exponent-precedence-runtime-a1039-20260723-211011.log` confirmed real Ruby evaluates leading unary signs outside `**`, so `-2 ** 2 + 1` becomes `-3` and can insert ` /` into `rm -rf X` to reach a fake `rm` with `argc=3 args=-rf / X`; it also confirmed compound signs, direct argv mutations using `-2 ** 2 + 2`, parenthesized negative base `(-2) ** 2 + 1`, and dynamic insert values (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-signed-exponent-precedence-dist-before-a1039-20260723-211011.log` reproduced 3 false-negative mismatches where dangerous signed-exponent shell/direct flows returned `ask_user`; parenthesized negative base, dynamic insert value, dynamic exponent, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-signed-exponent-precedence-a1039-focused-red-20260723-211051.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` split unary-sign handling from the static power base: signs are now accumulated before a power expression and applied after `**`, while parenthesized negative bases keep their own value. Tests now cover dangerous shell signed-exponent insert, compound-sign signed-exponent insert, direct exec signed-exponent inserts, plus parenthesized negative base, dynamic insert value, dynamic exponent, dynamic base, negative exponent, safe exponent `rm --version`, and future insert guard behavior. Dynamic base or exponent expressions, negative exponent results, unsafe integer results, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-signed-exponent-precedence-a1039-focused-green-20260723-211202.log` (2 files / 4 tests), `agent-core-ruby-insert-signed-exponent-precedence-a1039-related-20260723-211216.log` (2 files / 4 tests), `agent-core-build-a1039-20260723-211217.log`, `ruby-insert-signed-exponent-precedence-dist-fixed-a1039-20260723-211231.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1039-20260723-211231.log` (230 files / 706 tests), and `agent-core-full-test-a1039-20260723-211231.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 132, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1040 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses non-negative static integer bit shifts before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(1 << 1, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(8 >> 2, " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(1 << 0, "m"); flag = "-f"; flag.insert(4 >> 2, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitshift-index-runtime-a1040-20260723-211635.log` confirmed real Ruby evaluates static left/right shift insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed bit-shift insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitshift-index-dist-before-a1040-20260723-211636.log` reproduced 3 false-negative mismatches where dangerous Ruby static bit-shift index shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic shift count, negative shift count, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitshift-index-a1040-focused-red-20260723-211722.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added a shift-expression layer above additive parsing, evaluating `<<` and `>>` only when the shift count is a non-negative static integer and the result remains a safe integer. Tests now cover dangerous shell left-shift insert, shell right-shift insert, direct exec bit-shift inserts, plus safe bit-shift `rm --version`, dynamic insert value, dynamic shift count, negative shift count, and future insert guard behavior. Negative shift counts, dynamic shift counts, unsafe integer results, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitshift-index-a1040-focused-green-20260723-211806.log` (2 files / 4 tests), `agent-core-ruby-insert-bitshift-index-a1040-related-20260723-211817.log` (2 files / 4 tests), `agent-core-build-a1040-20260723-211818.log`, `ruby-insert-bitshift-index-dist-fixed-a1040-20260723-211830.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1040-20260723-211830.log` (230 files / 706 tests), and `agent-core-full-test-a1040-20260723-211831.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 165, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1041 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses Ruby-defined negative static bit-shift counts before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(4 << -1, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(1 >> -1, " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(2 << -1, "m"); flag = "-f"; flag.insert(2 << -1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-negative-bitshift-index-runtime-a1041-20260723-212145.log` confirmed real Ruby evaluates negative shift counts by shifting in the opposite direction before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed negative-shift insert can build safe `rm --version` and that a dynamic insert argument can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-negative-bitshift-index-dist-before-a1041-20260723-212146.log` reproduced 3 false-negative mismatches where dangerous Ruby negative bit-shift index shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic negative shift count, unsafe negative shift result, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-negative-bitshift-index-a1041-focused-red-20260723-212222.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` changed static shift evaluation to route negative counts through the opposite shift direction while preserving the safe-integer result guard. Tests now cover dangerous shell negative left-shift insert, shell negative right-shift insert, direct exec negative-shift inserts, plus safe negative-shift `rm --version`, dynamic insert value, dynamic negative shift count, unsafe negative-shift result, and future insert guard behavior. Dynamic shift counts, unsafe integer results, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-negative-bitshift-index-a1041-focused-green-20260723-212259.log` (2 files / 4 tests), `agent-core-ruby-insert-negative-bitshift-index-a1041-related-20260723-212309.log` (2 files / 4 tests), `agent-core-build-a1041-20260723-212309.log`, `ruby-insert-negative-bitshift-index-dist-fixed-a1041-20260723-212325.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1041-20260723-212325.log` (230 files / 706 tests), and `agent-core-full-test-a1041-20260723-212325.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 167, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1042 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static bitwise AND before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(6 & 2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert((7 & 2), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(3 & 1, "m"); flag = "-f"; flag.insert(5 & 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-and-index-runtime-a1042-20260723-212910.log` confirmed real Ruby evaluates static bitwise-AND insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed bitwise-AND insert can build safe `rm --version`, and that dynamic insert values and logical `&&` index expressions can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-and-index-dist-before-a1042-20260723-212911.log` reproduced 3 false-negative mismatches where dangerous Ruby static bitwise-AND shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic bitwise operand, logical `&&` operand, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-and-index-a1042-focused-red-20260723-213230.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added a static bitwise-AND layer above shift parsing, explicitly preserving the `&&` boundary and evaluating only 32-bit-safe static operands. Tests now cover dangerous shell bitwise-AND insert, parenthesized shell bitwise-AND insert, direct exec bitwise-AND inserts, plus safe bitwise-AND `rm --version`, dynamic insert value, dynamic bitwise operand, logical `&&` operand, and future insert guard behavior. Dynamic bitwise operands, `&&`, unsafe/non-32-bit integer operands, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-and-index-a1042-focused-green-20260723-213339.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-and-index-a1042-related-20260723-213421.log` (2 files / 4 tests), `agent-core-build-a1042-20260723-213421.log`, `ruby-insert-bitwise-and-index-dist-fixed-a1042-20260723-213439.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1042-20260723-213439.log` (230 files / 706 tests), and `agent-core-full-test-a1042-20260723-213440.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-integer-expression.utils.ts` 196, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1043 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static bitwise XOR before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(6 ^ 4, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert((7 ^ 5), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(3 ^ 2, "m"); flag = "-f"; flag.insert(5 ^ 4, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-xor-index-runtime-a1043-20260723-213852.log` confirmed real Ruby evaluates static bitwise-XOR insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed bitwise-XOR insert can build safe `rm --version` and that dynamic insert values can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-xor-index-dist-before-a1043-20260723-213853.log` reproduced 3 false-negative mismatches where dangerous Ruby static bitwise-XOR shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic XOR operand, unsafe XOR operand, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-xor-index-a1043-focused-red-20260723-214034.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added a static bitwise-XOR layer above bitwise-AND parsing. Because the previous expression file was already near the 200-line ceiling, pure responsibilities were split into `ruby-static-syntax.utils.ts` for whitespace, `ruby-static-integer-literal.utils.ts` for integer literals, and `ruby-static-integer-operator.utils.ts` for static operator evaluation; XOR/AND still evaluate only 32-bit-safe operands. Tests now cover dangerous shell bitwise-XOR insert, parenthesized shell bitwise-XOR insert, direct exec bitwise-XOR inserts, plus safe bitwise-XOR `rm --version`, dynamic insert value, dynamic XOR operand, unsafe XOR operand, and future insert guard behavior. Dynamic XOR operands, unsafe/non-32-bit integer operands, bitwise OR, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-xor-index-a1043-focused-green-20260723-214213.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-xor-index-a1043-related-20260723-214238.log` (2 files / 4 tests), `agent-core-build-a1043-20260723-214238.log`, `ruby-insert-bitwise-xor-index-dist-fixed-a1043-20260723-214258.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1043-20260723-214258.log` (230 files / 706 tests), and `agent-core-full-test-a1043-20260723-214259.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 170, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 20, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1044 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static bitwise OR before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(0 \| 2, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert((2 \| 0), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(1 \| 0, "m"); flag = "-f"; flag.insert(1 \| 0, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-or-index-runtime-a1044-20260723-214628.log` confirmed real Ruby evaluates static bitwise-OR insert indexes before shell strings and direct argv variables, reaching a fake `rm` with `argc=2 args=-rf /`; it also confirmed bitwise-OR insert can build safe `rm --version` and that dynamic insert values can be runtime-dangerous (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-or-index-dist-before-a1044-20260723-214628.log` reproduced 3 false-negative mismatches where dangerous Ruby static bitwise-OR shell/direct flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic OR operand, unsafe OR operand, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-or-index-a1044-focused-red-20260723-214821.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` folded `\|` into the same left-associative bitwise layer as `^`, above bitwise-AND parsing, preserving Ruby's `^`/`\|` precedence relationship while continuing to evaluate only 32-bit-safe operands through `ruby-static-integer-operator.utils.ts`. Tests now cover dangerous shell bitwise-OR insert, parenthesized shell bitwise-OR insert, direct exec bitwise-OR inserts, plus safe bitwise-OR `rm --version`, dynamic insert value, dynamic OR operand, unsafe OR operand, and future insert guard behavior. Dynamic OR operands, unsafe/non-32-bit integer operands, mixed broader Ruby expressions outside the static parser, variable or method-call factors, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-or-index-a1044-focused-green-20260723-214918.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-or-index-a1044-related-20260723-214944.log` (2 files / 4 tests), `agent-core-build-a1044-20260723-214944.log`, `ruby-insert-bitwise-or-index-dist-fixed-a1044-20260723-215009.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1044-20260723-215009.log` (230 files / 706 tests), and `agent-core-full-test-a1044-20260723-215009.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 171, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 21, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1045 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses unary bitwise NOT before shell/direct process execution, so `ruby -e 'cmd = "rm/"; cmd.insert(~1, " -rf "); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(~(-3), " -rf"); system(cmd)'`, `ruby -e 'cmd = "r"; cmd.insert(~(-2), "m"); flag = "-f"; flag.insert(~(-2), "r"); exec(cmd, flag, "/")'`, and the Ruby precedence case `ruby -e 'cmd = "rm -rf Xx"; cmd.insert(~2 ** 2, " /"); system(cmd)'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-index-runtime-a1045-rerun-20260723-215459.log` confirmed real Ruby evaluates unary bitwise-NOT insert indexes before shell strings and direct argv variables, supports both negative and parenthesized positive results, and treats `~2 ** 2` as `(~2) ** 2`, reaching fake `rm` commands for all dangerous cases (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-index-dist-before-a1045-rerun-20260723-215459.log` reproduced 4 false-negative mismatches where dangerous Ruby unary bitwise-NOT shell/direct/precedence flows returned `ask_user`; safe `--version`, dynamic insert argument, dynamic bitwise-NOT operand, unsafe bitwise-NOT operand, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-index-a1045-focused-red-20260723-215648.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added `~` as a static power-base prefix, preserving the existing Ruby `+`/`-` exponent handling while letting `~2 ** 2` bind as Ruby does. `ruby-static-integer-operator.utils.ts` now owns `evaluatedBitwiseNotValue`, using the same 32-bit-safe operand guard as `&`/`^`/`\|`. Tests now cover dangerous negative shell insert, parenthesized positive shell insert, direct exec bitwise-NOT inserts, `~2 ** 2` precedence, plus safe bitwise-NOT `rm --version`, dynamic insert value, dynamic bitwise-NOT operand, unsafe bitwise-NOT operand, and future insert guard behavior. Dynamic bitwise-NOT operands, unsafe/non-32-bit integer operands, bare `~-literal` without parentheses, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-index-a1045-focused-green-20260723-215757.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-index-a1045-related-20260723-215832.log` (2 files / 4 tests), `agent-core-build-a1045-20260723-215832.log`, `ruby-insert-bitwise-not-index-dist-fixed-a1045-20260723-215904.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1045-20260723-215904.log` (230 files / 706 tests), and `agent-core-full-test-a1045-20260723-215904.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 182, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1046 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses signed unary bitwise NOT operands before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(~-3, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm/"; cmd.insert(~+1, " -rf "); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(~-2, "m"); flag = "-f"; flag.insert(~-2, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-signed-index-runtime-a1046-20260723-220516.log` confirmed real Ruby evaluates signed bitwise-NOT insert indexes before shell strings and direct argv variables, can build safe `rm --version`, can be dangerous when the insert value is dynamic, and treats `~-2 ** 2` as Ruby precedence distinct from `(~-2) ** 2` (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-signed-index-dist-before-a1046-20260723-220516.log` reproduced 3 false-negative mismatches where dangerous signed `~-3`, `~+1`, and direct `~-2` flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic signed operand, unsafe signed operand, signed power-precedence, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-signed-index-a1046-focused-red-20260723-220621.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` taught unary `~` to read optional static `+`/`-` operands and explicitly reject signed operands followed by `**` so the reviewer does not guess Ruby's `~-2 ** 2` precedence. Tests now cover dangerous shell signed bitwise-NOT inserts, direct exec signed bitwise-NOT inserts, safe signed bitwise-NOT `rm --version`, dynamic insert values, dynamic signed operands, unsafe signed operands, signed power-precedence residual behavior, and future insert guard behavior. Dynamic signed operands, unsafe/non-32-bit signed operands, signed bitwise-NOT exponent combinations such as `~-2 ** 2`, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-signed-index-a1046-focused-green-rerun-20260723-220732.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-signed-index-a1046-related-20260723-220745.log` (2 files / 4 tests), `agent-core-build-a1046-20260723-220745.log`, `ruby-insert-bitwise-not-signed-index-dist-fixed-a1046-20260723-220758.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1046-20260723-220758.log` (230 files / 706 tests), and `agent-core-full-test-a1046-20260723-220758.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 199, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1047 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses a negative signed unary bitwise-NOT power expression before shell/direct process execution, so `ruby -e 'cmd = "rm rf /"; cmd.insert(~-2 ** 2, "-"); system(cmd)'` and `ruby -e 'cmd = "r"; cmd.insert(~-2 ** 1, "m"); flag = "-f"; flag.insert(~-2 ** 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-negative-power-index-runtime-a1047-20260723-221217.log` confirmed real Ruby treats negative signed bitwise-NOT power indexes like `~-2 ** 2` as `~(-2 ** 2)`, evaluates them before `String#insert`, reaches fake `rm -rf /` for shell and direct argv flows, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (4 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-negative-power-index-dist-before-a1047-20260723-221218.log` reproduced 2 false-negative mismatches where dangerous shell/direct `~-2 ** n` flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic exponent, unsafe negative power, multi-sign power, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-negative-power-index-a1047-focused-red-20260723-221335.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added a narrow negative signed power branch for unary `~`: a single `-` followed by a static power expression is evaluated as the signed power value before applying bitwise NOT, while `+`, multi-sign, dynamic exponent, and unsafe values continue to fall back to user review. Tests now cover dangerous shell negative signed power inserts, direct exec negative signed power inserts, safe negative signed power `rm --version`, dynamic insert values, dynamic exponents, unsafe negative powers, multi-sign powers, and future insert guard behavior. Dynamic exponents, unsafe/non-32-bit negative power results, multi-sign expressions such as `~--2 ** 2`, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-negative-power-index-a1047-focused-green-20260723-221436.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-negative-power-index-a1047-related-20260723-221448.log` (2 files / 4 tests), `agent-core-build-a1047-20260723-221448.log`, `ruby-insert-bitwise-not-negative-power-index-dist-fixed-a1047-20260723-221502.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1047-20260723-221502.log` (230 files / 706 tests), and `agent-core-full-test-a1047-20260723-221502.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 184, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1048 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses a positive signed unary bitwise-NOT power expression before shell/direct process execution, so `ruby -e 'cmd = "rm -rf Xx"; cmd.insert(~+2 ** 2, " /"); system(cmd)'` and `ruby -e 'cmd = "r"; cmd.insert(~+0 ** 1, "m"); flag = "-f"; flag.insert(~+1 ** 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-positive-power-index-runtime-a1048-20260723-221931.log` confirmed real Ruby treats positive signed bitwise-NOT power indexes like `~+2 ** 2` as `(~(+2)) ** 2`, evaluates them before `String#insert`, reaches fake `rm` for shell and direct argv flows, can preserve safe `rm --version`, and can be dangerous when the insert value is dynamic (4 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-positive-power-index-dist-before-a1048-20260723-221931.log` reproduced 2 false-negative mismatches where dangerous shell/direct `~+... ** ...` flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic exponent, unsafe positive power, multi-plus power, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-positive-power-index-a1048-focused-red-20260723-222030.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` allowed a single `+` after unary `~` to remain a static power base so the outer power parser evaluates Ruby's `(~+2) ** 2` form, while retaining A1047's negative signed power branch and leaving multi-sign, dynamic exponent, and unsafe values user-reviewable. Tests now cover dangerous shell positive signed power inserts, direct exec positive signed power inserts, safe positive signed power `rm --version`, dynamic insert values, dynamic exponents, unsafe positive powers, multi-plus powers, and future insert guard behavior. Dynamic exponents, unsafe/non-32-bit positive power results, multi-sign expressions such as `~++2 ** 2`, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-positive-power-index-a1048-focused-green-20260723-222111.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-positive-power-index-a1048-related-20260723-222122.log` (2 files / 4 tests), `agent-core-build-a1048-20260723-222122.log`, `ruby-insert-bitwise-not-positive-power-index-dist-fixed-a1048-20260723-222140.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1048-20260723-222140.log` (230 files / 706 tests), and `agent-core-full-test-a1048-20260723-222140.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 184, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1049 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses an all-plus multi-sign unary bitwise-NOT power expression before shell/direct process execution, so `ruby -e 'cmd = "rm -rf Xx"; cmd.insert(~++2 ** 2, " /"); system(cmd)'` and `ruby -e 'cmd = "r"; cmd.insert(~++0 ** 1, "m"); flag = "-f"; flag.insert(~++1 ** 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-multi-plus-power-index-runtime-a1049-20260723-222539.log` confirmed real Ruby evaluates all-plus multi-sign bitwise-NOT power indexes like `~++2 ** 2` before `String#insert`, reaches fake `rm` for shell and direct argv flows, can preserve safe `rm --version`, and can be dangerous when the insert value is dynamic (4 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-multi-plus-power-index-dist-before-a1049-20260723-222539.log` reproduced 2 false-negative mismatches where dangerous shell/direct `~++... ** ...` flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic exponent, unsafe multi-plus power, mixed-sign power, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-multi-plus-power-index-a1049-focused-red-20260723-222716.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` tracked whether a unary `~` signed operand contained `-` and allowed all-plus multi-sign operands to remain static power bases for the outer Ruby power parser, while mixed-sign combinations continue to fall back to user review. Tests now cover dangerous shell all-plus multi-sign power inserts, direct exec all-plus multi-sign power inserts, safe all-plus multi-sign power `rm --version`, dynamic insert values, dynamic exponents, unsafe multi-plus powers, mixed-sign powers, and future insert guard behavior. Dynamic exponents, unsafe/non-32-bit multi-plus power results, mixed-sign expressions such as `~+-2 ** 2`, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-multi-plus-power-index-a1049-focused-green-20260723-222807.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-multi-plus-power-index-a1049-related-20260723-222818.log` (2 files / 4 tests), `agent-core-build-a1049-20260723-222818.log`, `ruby-insert-bitwise-not-multi-plus-power-index-dist-fixed-a1049-20260723-222833.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1049-20260723-222833.log` (230 files / 706 tests), and `agent-core-full-test-a1049-20260723-222833.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 188, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1050 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses an effective-negative mixed-sign unary bitwise-NOT power expression before shell/direct process execution, so `ruby -e 'cmd = "rm rf /"; cmd.insert(~+-2 ** 2, "-"); system(cmd)'` and `ruby -e 'cmd = "r"; cmd.insert(~+-2 ** 1, "m"); flag = "-f"; flag.insert(~+-2 ** 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-mixed-negative-power-index-runtime-a1050-20260723-223245.log` confirmed real Ruby evaluates effective-negative mixed-sign bitwise-NOT power indexes like `~+-2 ** 2` before `String#insert`, reaches fake `rm -rf /` for shell and direct argv flows, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (4 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-mixed-negative-power-index-dist-before-a1050-20260723-223245.log` reproduced 2 false-negative mismatches where dangerous shell/direct `~+-... ** ...` flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic exponent, unsafe mixed-negative power, mixed-positive power, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-mixed-negative-power-index-a1050-focused-red-20260723-223400.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` generalized the unary `~` signed-power branch from single `-` to any static signed operand whose effective sign is negative and contains `-`; effective-positive mixed-sign combinations continue to fall back to user review. Tests now cover dangerous shell effective-negative mixed-sign power inserts, direct exec effective-negative mixed-sign power inserts, safe mixed-negative power `rm --version`, dynamic insert values, dynamic exponents, unsafe mixed-negative powers, mixed-positive powers, and future insert guard behavior. Dynamic exponents, unsafe/non-32-bit mixed-negative power results, effective-positive mixed-sign expressions such as `~--2 ** 2`, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-mixed-negative-power-index-a1050-focused-green-20260723-223441.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-mixed-negative-power-index-a1050-related-20260723-223454.log` (2 files / 4 tests), `agent-core-build-a1050-20260723-223455.log`, `ruby-insert-bitwise-not-mixed-negative-power-index-dist-fixed-a1050-20260723-223510.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1050-20260723-223510.log` (230 files / 706 tests), and `agent-core-full-test-a1050-20260723-223511.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 188, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1051 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses an effective-positive mixed-sign unary bitwise-NOT power expression before shell/direct process execution, so `ruby -e 'cmd = "rmrf /"; cmd.insert(~--2 ** 2, " -"); system(cmd)'` and `ruby -e 'cmd = "r"; cmd.insert(~--0 ** 1, "m"); flag = "-f"; flag.insert(~--1 ** 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-bitwise-not-mixed-positive-power-index-runtime-a1051-20260723-223942.log` confirmed real Ruby evaluates effective-positive mixed-sign bitwise-NOT power indexes like `~--2 ** 2` into negative `String#insert` indexes before shell strings and direct argv variables, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (4 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-bitwise-not-mixed-positive-power-index-dist-before-a1051-20260723-223943.log` reproduced 2 false-negative mismatches where dangerous shell/direct `~--... ** ...` flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic exponent, unsafe mixed-positive power, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-bitwise-not-mixed-positive-power-index-a1051-focused-red-20260723-224132.log` (2 failing files / 2 failing tests), then passed after `ruby-static-integer-expression.utils.ts` added the complementary effective-positive mixed-sign signed-power branch: a static signed operand whose effective sign is positive and contains `-` now evaluates the power before unary bitwise NOT, while dynamic exponents and unsafe values continue to fall back to user review. Tests now cover dangerous shell effective-positive mixed-sign power inserts, direct exec effective-positive mixed-sign power inserts, safe mixed-positive power `rm --version`, dynamic insert values, dynamic exponents, unsafe mixed-positive powers, and future insert guard behavior. Dynamic exponents, unsafe/non-32-bit mixed-positive power results, broader unary combinations outside this static parser, out-of-range indexes, dynamic/interpolated insert arguments, aliases, list-assignment RHS mutation chains, block/keyword forms, side-effecting argument expressions, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-bitwise-not-mixed-positive-power-index-a1051-focused-green-20260723-224218.log` (2 files / 4 tests), `agent-core-ruby-insert-bitwise-not-mixed-positive-power-index-a1051-related-20260723-224234.log` (2 files / 4 tests), `agent-core-build-a1051-20260723-224234.log`, `ruby-insert-bitwise-not-mixed-positive-power-index-dist-fixed-a1051-20260723-224251.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1051-20260723-224251.log` (230 files / 706 tests), and `agent-core-full-test-a1051-20260723-224252.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 194, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1052 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses static string length methods before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("xx".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("xx".size, " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("x".length, "m"); flag = "-f"; flag.insert("x".bytesize, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-string-length-index-runtime-a1052-20260723-225529.log` confirmed real Ruby evaluates static `String#length`, `String#size`, and `String#bytesize` insert indexes before shell strings and direct argv variables, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-string-length-index-dist-before-a1052-20260723-225530.log` reproduced 3 false-negative mismatches where dangerous shell/direct string length index flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic length receiver, interpolated string receiver, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-string-length-index-a1052-focused-red-20260723-225658.log` (2 failing files / 2 failing tests), then passed after new `ruby-static-string-length-integer.utils.ts` added a conservative Ruby plain-string length integer reader for `.length`, `.size`, and `.bytesize`, and `ruby-static-integer-expression.utils.ts` delegated power-base parsing to it before unsigned integer literals. Tests now cover dangerous shell `.length` and `.size` inserts, direct exec `.length`/`.bytesize` inserts, safe `.bytesize` `rm --version`, dynamic insert values, dynamic receivers, interpolated string receivers, and future insert guard behavior. Escaped string literals, interpolated/dynamic receivers, variable-derived lengths, non-plain quote-like literals, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-string-length-index-a1052-focused-green-20260723-225749.log` (2 files / 4 tests), `agent-core-ruby-insert-string-length-index-a1052-related-20260723-225808.log` (2 files / 4 tests), `agent-core-build-a1052-20260723-225808.log`, `ruby-insert-string-length-index-dist-fixed-a1052-20260723-225817.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1052-rerun-20260723-225924.log` (229 files / 689 tests), and `agent-core-full-test-a1052-20260723-225832.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 196, `ruby-static-string-length-integer.utils.ts` 45, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1053 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses empty-call static string length methods before shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("xx".length(), " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("xx".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("x".length(), "m"); flag = "-f"; flag.insert("x".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-string-length-call-index-runtime-a1053-20260723-230421.log` confirmed real Ruby evaluates empty-call `String#length()`, `String#size()`, and `String#bytesize()` insert indexes before shell strings and direct argv variables, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-string-length-call-index-dist-before-a1053-20260723-230421.log` reproduced 3 false-negative mismatches where dangerous shell/direct empty-call string length index flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic length-call receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-string-length-call-index-a1053-focused-red-20260723-230515.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a conservative optional empty-call suffix reader for static `.length`, `.size`, and `.bytesize` indexes while rejecting non-empty calls. Tests now cover dangerous shell `.length()` and `.size()` inserts, direct exec `.length()`/`.bytesize()` inserts, safe `.bytesize()` `rm --version`, dynamic insert values, dynamic receivers, non-empty length calls, and future insert guard behavior. Non-empty calls, escaped string literals, interpolated/dynamic receivers, variable-derived lengths, non-plain quote-like literals, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-string-length-call-index-a1053-focused-green-20260723-230603.log` (2 files / 4 tests), `agent-core-ruby-insert-string-length-call-index-a1053-related-20260723-230615.log` (2 files / 4 tests), `agent-core-build-a1053-20260723-230615.log`, `ruby-insert-string-length-call-index-dist-fixed-a1053-20260723-230627.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1053-20260723-230637.log` (229 files / 689 tests), and `agent-core-full-test-a1053-20260723-230637.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 196, `ruby-static-string-length-integer.utils.ts` 56, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1054 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses parenthesized static string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(("xx").length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(("xx").size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(("x").length, "m"); flag = "-f"; flag.insert(("x").bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-parenthesized-string-length-index-runtime-a1054-20260723-231023.log` confirmed real Ruby evaluates parenthesized static string receiver length indexes before shell strings and direct argv variables, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-parenthesized-string-length-index-dist-before-a1054-20260723-231023.log` reproduced 3 false-negative mismatches where dangerous shell/direct parenthesized receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, dynamic parenthesized receiver, non-empty calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-parenthesized-string-length-index-a1054-focused-red-20260723-231121.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a conservative parenthesized plain-string receiver reader and `ruby-static-integer-expression.utils.ts` tries static string-length integer parsing before falling back to parenthesized numeric expressions. Tests now cover dangerous shell `("xx").length` and `("xx").size()` inserts, direct exec `("x").length`/`("x").bytesize()` inserts, safe parenthesized `.bytesize()` `rm --version`, dynamic insert values, dynamic parenthesized receivers, non-empty calls, and future insert guard behavior. Non-empty calls, escaped string literals, interpolated/dynamic receivers, variable-derived lengths, non-plain quote-like literals, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-parenthesized-string-length-index-a1054-focused-green-20260723-231200.log` (2 files / 4 tests), `agent-core-ruby-insert-parenthesized-string-length-index-a1054-related-20260723-231216.log` (2 files / 4 tests), `agent-core-build-a1054-20260723-231216.log`, `ruby-insert-parenthesized-string-length-index-dist-fixed-a1054-20260723-231227.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1054-20260723-231238.log` (229 files / 689 tests), and `agent-core-full-test-a1054-20260723-231238.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 69, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-integer-operator.utils.ts` 27, `ruby-static-syntax.utils.ts` 5, `ruby-static-insert-assignment.utils.ts` 94, `ruby-static-mutation-assignment.utils.ts` 103, and `interpreter-static-string.utils.ts` 199. |
| A1055 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses quote-like string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert(%q{xx}.length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert(%Q[xx].size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert(%q(x).length, "m"); flag = "-f"; flag.insert(%q{x}.bytesize, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-quote-like-string-length-index-runtime-a1055-20260723-231559.log` confirmed real Ruby evaluates `%q`/non-interpolated `%Q` quote-like string length indexes before shell strings and direct argv variables, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-quote-like-string-length-index-dist-before-a1055-20260723-231559.log` reproduced 3 false-negative mismatches where dangerous shell/direct quote-like receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated `%Q` receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-quote-like-string-length-index-a1055-focused-red-20260723-231655.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` reused `readRubyQuoteLikeStringLiteral` as an additional static string-length receiver reader while keeping interpolated `%Q` unresolved. Tests now cover dangerous shell `%q{xx}.length` and `%Q[xx].size()` inserts, direct exec `%q(...).length`/`%q{...}.bytesize` inserts, safe quote-like `.bytesize()` `rm --version`, dynamic insert values, interpolated quote-like receivers, non-empty calls, and future insert guard behavior. Interpolated `%Q` receivers, non-empty calls, escaped quote-like edge cases beyond the shared quote-like reader, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-quote-like-string-length-index-a1055-focused-green-20260723-231753.log` (2 files / 4 tests), `agent-core-ruby-insert-quote-like-string-length-index-a1055-related-20260723-231805.log` (2 files / 4 tests), `agent-core-build-a1055-20260723-231805.log`, `ruby-insert-quote-like-string-length-index-dist-fixed-a1055-20260723-231819.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1055-20260723-231832.log` (229 files / 689 tests), and `agent-core-full-test-a1055-20260723-231832.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 77, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1056 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses ASCII hex-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\x78\x78".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\x78\x78".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\x78".length, "m"); flag = "-f"; flag.insert("\x78".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-hex-escaped-string-length-index-runtime-a1056-20260723-232254.log` confirmed real Ruby evaluates double-quoted `\xHH` ASCII escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-hex-escaped-string-length-index-dist-before-a1056-20260723-232254.log` reproduced 3 false-negative mismatches where dangerous shell/direct hex-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated escaped receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-hex-escaped-string-length-index-a1056-focused-red-20260723-232353.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow double-quoted `\xHH` parser that only accepts two-digit ASCII hex escapes and still rejects other escape forms. Tests now cover dangerous shell `"\x78\x78".length` and `"\x78\x78".size()` inserts, direct exec `"\x78".length`/`"\x78".bytesize()` inserts, safe hex-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated escaped receivers, non-empty calls, and future insert guard behavior. Non-ASCII hex escapes, other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-hex-escaped-string-length-index-a1056-focused-green-20260723-232442.log` (2 files / 4 tests), `agent-core-ruby-insert-hex-escaped-string-length-index-a1056-related-20260723-232501.log` (2 files / 4 tests), `agent-core-build-a1056-20260723-232501.log`, `ruby-insert-hex-escaped-string-length-index-dist-fixed-a1056-20260723-232513.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1056-20260723-232526.log` (229 files / 689 tests), and `agent-core-full-test-a1056-20260723-232526.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 93, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1057 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses newline-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\n\n".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\n\n".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\n".length, "m"); flag = "-f"; flag.insert("\n".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-newline-escaped-string-length-index-runtime-a1057-20260723-232903.log` confirmed real Ruby evaluates double-quoted `\n` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-newline-escaped-string-length-index-dist-before-a1057-20260723-232903.log` reproduced 3 false-negative mismatches where dangerous shell/direct newline-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated newline receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-newline-escaped-string-length-index-a1057-focused-red-20260723-232954.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\n` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\n\n".length` and `"\n\n".size()` inserts, direct exec `"\n".length`/`"\n".bytesize()` inserts, safe newline-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated newline receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-newline-escaped-string-length-index-a1057-focused-green-20260723-233048.log` (2 files / 4 tests), `agent-core-ruby-insert-newline-escaped-string-length-index-a1057-related-20260723-233104.log` (2 files / 4 tests), `agent-core-build-a1057-20260723-233104.log`, `ruby-insert-newline-escaped-string-length-index-dist-fixed-a1057-20260723-233119.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1057-20260723-233133.log` (229 files / 689 tests), and `agent-core-full-test-a1057-20260723-233136.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 95, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1058 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses tab-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\t\t".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\t\t".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\t".length, "m"); flag = "-f"; flag.insert("\t".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-tab-escaped-string-length-index-runtime-a1058-20260723-233743.log` confirmed real Ruby evaluates double-quoted `\t` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-tab-escaped-string-length-index-dist-before-a1058-20260723-233744.log` reproduced 3 false-negative mismatches where dangerous shell/direct tab-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated tab receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-tab-escaped-string-length-index-a1058-focused-red-20260723-233944.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\t` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\t\t".length` and `"\t\t".size()` inserts, direct exec `"\t".length`/`"\t".bytesize()` inserts, safe tab-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated tab receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-tab-escaped-string-length-index-a1058-focused-green-20260723-234020.log` (2 files / 4 tests), `agent-core-ruby-insert-tab-escaped-string-length-index-a1058-related-20260723-234036.log` (2 files / 4 tests), `agent-core-build-a1058-20260723-234037.log`, `ruby-insert-tab-escaped-string-length-index-dist-fixed-a1058-20260723-234048.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1058-20260723-234154.log` (230 files / 706 tests), and `agent-core-full-test-a1058-20260723-234102.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 96, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1059 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses carriage-return-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\r\r".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\r\r".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\r".length, "m"); flag = "-f"; flag.insert("\r".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-carriage-return-escaped-string-length-index-runtime-a1059-20260723-234603.log` confirmed real Ruby evaluates double-quoted `\r` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-carriage-return-escaped-string-length-index-dist-before-a1059-20260723-234604.log` reproduced 3 false-negative mismatches where dangerous shell/direct carriage-return-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated carriage-return receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-carriage-return-escaped-string-length-index-a1059-focused-red-20260723-234701.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\r` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\r\r".length` and `"\r\r".size()` inserts, direct exec `"\r".length`/`"\r".bytesize()` inserts, safe carriage-return-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated carriage-return receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-carriage-return-escaped-string-length-index-a1059-focused-green-20260723-234742.log` (2 files / 4 tests), `agent-core-ruby-insert-carriage-return-escaped-string-length-index-a1059-related-20260723-234800.log` (2 files / 4 tests), `agent-core-build-a1059-20260723-234800.log`, `ruby-insert-carriage-return-escaped-string-length-index-dist-fixed-a1059-20260723-234810.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1059-20260723-234826.log` (230 files / 706 tests), and `agent-core-full-test-a1059-20260723-234826.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 97, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1060 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses form-feed-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\f\f".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\f\f".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\f".length, "m"); flag = "-f"; flag.insert("\f".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-form-feed-escaped-string-length-index-runtime-a1060-20260723-235231.log` confirmed real Ruby evaluates double-quoted `\f` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-form-feed-escaped-string-length-index-dist-before-a1060-20260723-235232.log` reproduced 3 false-negative mismatches where dangerous shell/direct form-feed-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated form-feed receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-form-feed-escaped-string-length-index-a1060-focused-red-20260723-235332.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\f` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\f\f".length` and `"\f\f".size()` inserts, direct exec `"\f".length`/`"\f".bytesize()` inserts, safe form-feed-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated form-feed receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-form-feed-escaped-string-length-index-a1060-focused-green-20260723-235403.log` (2 files / 4 tests), `agent-core-ruby-insert-form-feed-escaped-string-length-index-a1060-related-20260723-235419.log` (2 files / 4 tests), `agent-core-build-a1060-20260723-235419.log`, `ruby-insert-form-feed-escaped-string-length-index-dist-fixed-a1060-20260723-235432.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1060-20260723-235445.log` (230 files / 706 tests), and `agent-core-full-test-a1060-20260723-235445.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 98, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1061 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses vertical-tab-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\v\v".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\v\v".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\v".length, "m"); flag = "-f"; flag.insert("\v".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-vertical-tab-escaped-string-length-index-runtime-a1061-20260723-235857.log` confirmed real Ruby evaluates double-quoted `\v` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-vertical-tab-escaped-string-length-index-dist-before-a1061-20260723-235857.log` reproduced 3 false-negative mismatches where dangerous shell/direct vertical-tab-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated vertical-tab receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-vertical-tab-escaped-string-length-index-a1061-focused-red-20260724-000009.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\v` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\v\v".length` and `"\v\v".size()` inserts, direct exec `"\v".length`/`"\v".bytesize()` inserts, safe vertical-tab-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated vertical-tab receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-vertical-tab-escaped-string-length-index-a1061-focused-green-20260724-000051.log` (2 files / 4 tests), `agent-core-ruby-insert-vertical-tab-escaped-string-length-index-a1061-related-20260724-000103.log` (2 files / 4 tests), `agent-core-build-a1061-20260724-000103.log`, `ruby-insert-vertical-tab-escaped-string-length-index-dist-fixed-a1061-20260724-000116.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1061-20260724-000130.log` (230 files / 706 tests), and `agent-core-full-test-a1061-20260724-000130.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 99, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1062 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses alert/bell-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\a\a".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\a\a".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\a".length, "m"); flag = "-f"; flag.insert("\a".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-alert-escaped-string-length-index-runtime-a1062-20260724-000550.log` confirmed real Ruby evaluates double-quoted `\a` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-alert-escaped-string-length-index-dist-before-a1062-20260724-000553.log` reproduced 3 false-negative mismatches where dangerous shell/direct alert-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated alert receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-alert-escaped-string-length-index-a1062-focused-red-20260724-000709.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\a` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\a\a".length` and `"\a\a".size()` inserts, direct exec `"\a".length`/`"\a".bytesize()` inserts, safe alert-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated alert receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-alert-escaped-string-length-index-a1062-focused-green-20260724-000745.log` (2 files / 4 tests), `agent-core-ruby-insert-alert-escaped-string-length-index-a1062-related-20260724-000757.log` (2 files / 4 tests), `agent-core-build-a1062-20260724-000757.log`, `ruby-insert-alert-escaped-string-length-index-dist-fixed-a1062-20260724-000809.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1062-20260724-000824.log` (230 files / 706 tests), and `agent-core-full-test-a1062-20260724-000824.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 100, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1063 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses escape-character-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\e\e".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\e\e".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\e".length, "m"); flag = "-f"; flag.insert("\e".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-escape-escaped-string-length-index-runtime-a1063-20260724-001230.log` confirmed real Ruby evaluates double-quoted `\e` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-escape-escaped-string-length-index-dist-before-a1063-20260724-001230.log` reproduced 3 false-negative mismatches where dangerous shell/direct escape-character receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated escape-character receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-escape-escaped-string-length-index-a1063-focused-red-20260724-001340.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\e` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\e\e".length` and `"\e\e".size()` inserts, direct exec `"\e".length`/`"\e".bytesize()` inserts, safe escape-character `.bytesize()` `rm --version`, dynamic insert values, interpolated escape-character receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-escape-escaped-string-length-index-a1063-focused-green-20260724-001418.log` (2 files / 4 tests), `agent-core-ruby-insert-escape-escaped-string-length-index-a1063-related-20260724-001433.log` (2 files / 4 tests), `agent-core-build-a1063-20260724-001434.log`, `ruby-insert-escape-escaped-string-length-index-dist-fixed-a1063-20260724-001449.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1063-20260724-001507.log` (230 files / 706 tests), and `agent-core-full-test-a1063-20260724-001508.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 101, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1064 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses space-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\s\s".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\s\s".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\s".length, "m"); flag = "-f"; flag.insert("\s".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-space-escaped-string-length-index-runtime-a1064-20260724-001917.log` confirmed real Ruby evaluates double-quoted `\s` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-space-escaped-string-length-index-dist-before-a1064-20260724-001918.log` reproduced 3 false-negative mismatches where dangerous shell/direct space-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated space receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-space-escaped-string-length-index-a1064-focused-red-20260724-002023.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\s` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\s\s".length` and `"\s\s".size()` inserts, direct exec `"\s".length`/`"\s".bytesize()` inserts, safe space-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated space receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-space-escaped-string-length-index-a1064-focused-green-20260724-002100.log` (2 files / 4 tests), `agent-core-ruby-insert-space-escaped-string-length-index-a1064-related-20260724-002117.log` (2 files / 4 tests), `agent-core-build-a1064-20260724-002117.log`, `ruby-insert-space-escaped-string-length-index-dist-fixed-a1064-20260724-002137.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1064-20260724-002148.log` (230 files / 706 tests), and `agent-core-full-test-a1064-20260724-002149.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 102, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1065 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses backspace-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\b\b".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\b\b".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\b".length, "m"); flag = "-f"; flag.insert("\b".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-backspace-escaped-string-length-index-runtime-a1065-20260724-002557.log` confirmed real Ruby evaluates double-quoted `\b` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-backspace-escaped-string-length-index-dist-before-a1065-20260724-002557.log` reproduced 3 false-negative mismatches where dangerous shell/direct backspace-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated backspace receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-backspace-escaped-string-length-index-a1065-focused-red-20260724-002701.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\b` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\b\b".length` and `"\b\b".size()` inserts, direct exec `"\b".length`/`"\b".bytesize()` inserts, safe backspace-escaped `.bytesize()` `rm --version`, dynamic insert values, interpolated backspace receivers, non-empty calls, and future insert guard behavior. Other Ruby escape forms, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-backspace-escaped-string-length-index-a1065-focused-green-20260724-002747.log` (2 files / 4 tests), `agent-core-ruby-insert-backspace-escaped-string-length-index-a1065-related-20260724-002807.log` (2 files / 4 tests), `agent-core-build-a1065-20260724-002807.log`, `ruby-insert-backspace-escaped-string-length-index-dist-fixed-a1065-20260724-002822.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1065-20260724-002838.log` (230 files / 706 tests), and `agent-core-full-test-a1065-20260724-002838.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 103, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1066 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses backslash-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\\\\\\\\".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\\\\\\\\".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\\\\".length, "m"); flag = "-f"; flag.insert("\\\\".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-backslash-escaped-string-length-index-runtime-a1066-20260724-003528.log` confirmed real Ruby evaluates double-quoted `\\` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-backslash-escaped-string-length-index-dist-before-a1066-20260724-003528.log` reproduced 3 false-negative mismatches where dangerous shell/direct backslash-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated backslash receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-backslash-escaped-string-length-index-a1066-focused-red-20260724-003623.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\\` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\\\\\\\\".length` and `"\\\\\\\\".size()` inserts plus direct exec `"\\\\".length`/`"\\\\".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-backslash-escaped-string-length-index-a1066-focused-green-20260724-003715.log` (2 files / 4 tests), `agent-core-ruby-insert-backslash-escaped-string-length-index-a1066-related-20260724-003730.log` (2 files / 4 tests), `agent-core-build-a1066-20260724-003731.log`, `ruby-insert-backslash-escaped-string-length-index-dist-fixed-a1066-20260724-003742.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1066-20260724-003754.log` (230 files / 706 tests), and `agent-core-full-test-a1066-20260724-003754.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 104, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1067 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses quote-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\"\"".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\"\"".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\"".length, "m"); flag = "-f"; flag.insert("\"".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-quote-escaped-string-length-index-runtime-a1067-20260724-004257.log` confirmed real Ruby evaluates double-quoted `\"` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-quote-escaped-string-length-index-dist-before-a1067-20260724-004258.log` reproduced 3 false-negative mismatches where dangerous shell/direct quote-escaped receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated quote receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-quote-escaped-string-length-index-a1067-focused-red-20260724-004343.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added `\"` to the narrow double-quoted escape parser while continuing to reject other escape forms. Tests now cover dangerous shell `"\"\"".length` and `"\"\"".size()` inserts plus direct exec `"\"".length`/`"\"".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, encoding-sensitive or side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-quote-escaped-string-length-index-a1067-focused-green-20260724-004424.log` (2 files / 4 tests), `agent-core-ruby-insert-quote-escaped-string-length-index-a1067-related-20260724-004438.log` (2 files / 4 tests), `agent-core-build-a1067-20260724-004439.log`, `ruby-insert-quote-escaped-string-length-index-dist-fixed-a1067-20260724-004449.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1067-20260724-004503.log` (230 files / 706 tests), and `agent-core-full-test-a1067-20260724-004503.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 105, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1068 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses ASCII octal-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\170\170".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\170\170".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\170".length, "m"); flag = "-f"; flag.insert("\170".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-octal-escaped-string-length-index-runtime-a1068-20260724-004850.log` confirmed real Ruby evaluates double-quoted `\170` octal escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-octal-escaped-string-length-index-dist-before-a1068-20260724-004850.log` reproduced 3 false-negative mismatches where dangerous shell/direct octal receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated octal receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-octal-escaped-string-length-index-a1068-focused-red-20260724-005002.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow 1-to-3 digit ASCII octal escape parser while continuing to reject encoding-sensitive wider octal values and other unimplemented escape forms. Tests now cover dangerous shell `"\170\170".length` and `"\170\170".size()` inserts plus direct exec `"\170".length`/`"\170".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, non-ASCII octal/encoding-sensitive escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-octal-escaped-string-length-index-a1068-focused-green-20260724-005046.log` (2 files / 4 tests), `agent-core-ruby-insert-octal-escaped-string-length-index-a1068-related-20260724-005100.log` (2 files / 4 tests), `agent-core-build-a1068-20260724-005100.log`, `ruby-insert-octal-escaped-string-length-index-dist-fixed-a1068-20260724-005112.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1068-20260724-005126.log` (230 files / 706 tests), and `agent-core-full-test-a1068-20260724-005126.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 120, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1069 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses fixed-width ASCII Unicode-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\u0078\u0078".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\u0078\u0078".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\u0078".length, "m"); flag = "-f"; flag.insert("\u0078".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-unicode-escaped-string-length-index-runtime-a1069-20260724-005507.log` confirmed real Ruby evaluates double-quoted `\u0078` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-unicode-escaped-string-length-index-dist-before-a1069-20260724-005507.log` reproduced 3 false-negative mismatches where dangerous shell/direct Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated Unicode receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-unicode-escaped-string-length-index-a1069-focused-red-20260724-005550.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow fixed-width `\uHHHH` ASCII parser while continuing to reject `\u{...}`, non-ASCII codepoints, and other encoding-sensitive escape forms. Tests now cover dangerous shell `"\u0078\u0078".length` and `"\u0078\u0078".size()` inserts plus direct exec `"\u0078".length`/`"\u0078".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, `\u{...}` and non-ASCII Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-unicode-escaped-string-length-index-a1069-focused-green-20260724-005626.log` (2 files / 4 tests), `agent-core-ruby-insert-unicode-escaped-string-length-index-a1069-related-20260724-005639.log` (2 files / 4 tests), `agent-core-build-a1069-20260724-005639.log`, `ruby-insert-unicode-escaped-string-length-index-dist-fixed-a1069-20260724-005651.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1069-20260724-005700.log` (230 files / 706 tests), and `agent-core-full-test-a1069-20260724-005701.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 132, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1070 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses single-codepoint ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\u{78}\u{78}".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\u{78}\u{78}".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\u{78}".length, "m"); flag = "-f"; flag.insert("\u{78}".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-braced-unicode-escaped-string-length-index-runtime-a1070-20260724-010036.log` confirmed real Ruby evaluates double-quoted `\u{78}` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-braced-unicode-escaped-string-length-index-dist-before-a1070-20260724-010036.log` reproduced 3 false-negative mismatches where dangerous shell/direct braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated braced-Unicode receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-braced-unicode-escaped-string-length-index-a1070-focused-red-20260724-010125.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow `\u{hex}` single ASCII codepoint parser while continuing to reject multi-codepoint braced Unicode, braced whitespace forms, non-ASCII codepoints, and other encoding-sensitive escape forms. Tests now cover dangerous shell `"\u{78}\u{78}".length` and `"\u{78}\u{78}".size()` inserts plus direct exec `"\u{78}".length`/`"\u{78}".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, multi-codepoint/whitespace/non-ASCII braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-braced-unicode-escaped-string-length-index-a1070-focused-green-20260724-010208.log` (2 files / 4 tests), `agent-core-ruby-insert-braced-unicode-escaped-string-length-index-a1070-related-20260724-010224.log` (2 files / 4 tests), `agent-core-build-a1070-20260724-010224.log`, `ruby-insert-braced-unicode-escaped-string-length-index-dist-fixed-a1070-20260724-010235.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1070-20260724-010243.log` (230 files / 706 tests), and `agent-core-full-test-a1070-20260724-010243.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 147, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1071 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses whitespace-separated multi-codepoint ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\u{78 78}".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\u{78 78}".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\u{78 78}".length - 1, "m"); flag = "-f"; flag.insert("\u{78 78}".bytesize() - 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-multi-braced-unicode-string-length-index-runtime-a1071-20260724-010611.log` confirmed real Ruby evaluates double-quoted `\u{78 78}` into two characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-multi-braced-unicode-string-length-index-dist-before-a1071-20260724-010611.log` reproduced 3 false-negative mismatches where dangerous shell/direct multi-codepoint braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-multi-braced-unicode-string-length-index-a1071-focused-red-20260724-010652.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded the narrow braced Unicode parser to concatenate space-separated ASCII codepoints while continuing to reject empty tokens, non-ASCII codepoints, and other encoding-sensitive escape forms. Tests now cover dangerous shell `"\u{78 78}".length` and `"\u{78 78}".size()` inserts plus direct exec `"\u{78 78}".length - 1`/`"\u{78 78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-multi-braced-unicode-string-length-index-a1071-focused-green-20260724-010745.log` (2 files / 4 tests), `agent-core-ruby-insert-multi-braced-unicode-string-length-index-a1071-related-20260724-010756.log` (2 files / 4 tests), `agent-core-build-a1071-20260724-010756.log`, `ruby-insert-multi-braced-unicode-string-length-index-dist-fixed-a1071-20260724-010807.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1071-20260724-010816.log` (230 files / 706 tests), and `agent-core-full-test-a1071-20260724-010816.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 154, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1072 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses single-digit ASCII hex-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\x7\x7".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\x7\x7".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\x7".length, "m"); flag = "-f"; flag.insert("\x7".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-single-hex-escaped-string-length-index-runtime-a1072-20260724-011132.log` confirmed real Ruby evaluates double-quoted one-digit `\xH` escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-single-hex-escaped-string-length-index-dist-before-a1072-20260724-011132.log` reproduced 3 false-negative mismatches where dangerous shell/direct one-digit hex receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-single-hex-escaped-string-length-index-a1072-focused-red-20260724-011227.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` replaced the fixed two-digit hex branch with a narrow `\xH`/`\xHH` ASCII parser while preserving existing two-digit behavior and rejecting non-ASCII escapes. Tests now cover dangerous shell `"\x7\x7".length` and `"\x7\x7".size()` inserts plus direct exec `"\x7".length`/`"\x7".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, non-ASCII/malformed hex escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-single-hex-escaped-string-length-index-a1072-focused-green-20260724-011302.log` (2 files / 4 tests), `agent-core-ruby-insert-single-hex-escaped-string-length-index-a1072-related-20260724-011315.log` (2 files / 4 tests), `agent-core-build-a1072-20260724-011315.log`, `ruby-insert-single-hex-escaped-string-length-index-dist-fixed-a1072-20260724-011325.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1072-20260724-011338.log` (230 files / 706 tests), and `agent-core-full-test-a1072-20260724-011338.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 160, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1073 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses ASCII-letter control-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\cA\cA".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\cA\cA".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\cA".length, "m"); flag = "-f"; flag.insert("\cA".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-control-escaped-string-length-index-runtime-a1073-20260724-011657.log` confirmed real Ruby evaluates double-quoted `\cA` control escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-control-escaped-string-length-index-dist-before-a1073-20260724-011657.log` reproduced 3 false-negative mismatches where dangerous shell/direct control receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-control-escaped-string-length-index-a1073-focused-red-20260724-011737.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow `\c[A-Za-z]` parser while continuing to reject `\C-`, `\M-`, symbol control escapes, and other complex control/meta escape forms. Tests now cover dangerous shell `"\cA\cA".length` and `"\cA\cA".size()` inserts plus direct exec `"\cA".length`/`"\cA".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, non-letter control/meta escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-control-escaped-string-length-index-a1073-focused-green-20260724-011820.log` (2 files / 4 tests), `agent-core-ruby-insert-control-escaped-string-length-index-a1073-related-20260724-011831.log` (2 files / 4 tests), `agent-core-build-a1073-20260724-011831.log`, `ruby-insert-control-escaped-string-length-index-dist-fixed-a1073-20260724-011841.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1073-20260724-011849.log` (230 files / 706 tests), and `agent-core-full-test-a1073-20260724-011850.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 172, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1074 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses capital control-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\C-A\C-A".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\C-A\C-A".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\C-A".length, "m"); flag = "-f"; flag.insert("\C-A".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-capital-control-escaped-string-length-index-runtime-a1074-20260724-012304.log` confirmed real Ruby evaluates double-quoted `\C-A` control escapes before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-capital-control-escaped-string-length-index-dist-before-a1074-rerun-20260724-012458.log` reproduced 3 false-negative mismatches where dangerous shell/direct capital control receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-capital-control-escaped-string-length-index-a1074-focused-red-20260724-012518.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded the narrow control escape parser to accept both `\c[A-Za-z]` and `\C-[A-Za-z]` while continuing to reject non-letter control/meta escape forms. Tests now cover dangerous shell `"\C-A\C-A".length` and `"\C-A\C-A".size()` inserts plus direct exec `"\C-A".length`/`"\C-A".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, non-letter control/meta escapes such as `\M-`, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-capital-control-escaped-string-length-index-a1074-focused-green-20260724-012542.log` (2 files / 4 tests), `agent-core-ruby-insert-capital-control-escaped-string-length-index-a1074-related-20260724-012556.log` (2 files / 4 tests), `agent-core-build-a1074-20260724-012556.log`, `ruby-insert-capital-control-escaped-string-length-index-dist-fixed-a1074-20260724-012623.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1074-20260724-012632.log` (230 files / 706 tests), and `agent-core-full-test-a1074-20260724-012635.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 173, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1075 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses DEL control-escaped double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\c?\c?".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\C-?\C-?".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\c?".length, "m"); flag = "-f"; flag.insert("\C-?".bytesize(), "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-del-control-escaped-string-length-index-runtime-a1075-20260724-013021.log` confirmed real Ruby evaluates double-quoted `\c?` and `\C-?` as single DEL control characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-del-control-escaped-string-length-index-dist-before-a1075-20260724-013050.log` reproduced 3 false-negative mismatches where dangerous shell/direct DEL-control receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-del-control-escaped-string-length-index-a1075-focused-red-20260724-013117.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow `?` control-character branch for `\c?` and `\C-?` while preserving the existing ASCII-letter control escape handling and continuing to reject broader control/meta escape forms. Tests now cover dangerous shell `"\c?\c?".length` and `"\C-?\C-?".size()` inserts plus direct exec `"\c?".length`/`"\C-?".bytesize()` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, non-DEL non-letter control/meta escapes such as `\M-`, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-del-control-escaped-string-length-index-a1075-focused-green-20260724-013145.log` (2 files / 4 tests), `agent-core-ruby-insert-del-control-escaped-string-length-index-a1075-related-20260724-013154.log` (2 files / 4 tests), `agent-core-build-a1075-20260724-013157.log`, `ruby-insert-del-control-escaped-string-length-index-dist-fixed-a1075-20260724-013221.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1075-20260724-013229.log` (230 files / 706 tests), and `agent-core-full-test-a1075-20260724-013230.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1076 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses multi-space-separated ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\u{78  78}".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\u{78   78}".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\u{78  78}".length - 1, "m"); flag = "-f"; flag.insert("\u{78  78}".bytesize() - 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-multi-space-braced-unicode-string-length-index-runtime-a1076-20260724-013533.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints separated by multiple ordinary spaces before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-multi-space-braced-unicode-string-length-index-dist-before-a1076-20260724-013615.log` reproduced 3 false-negative mismatches where dangerous shell/direct multi-space braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-multi-space-braced-unicode-string-length-index-a1076-focused-red-20260724-013643.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` changed braced-Unicode tokenization from single-space splitting to one-or-more ordinary-space splitting while preserving ASCII codepoint validation and rejecting empty tokens. Tests now cover dangerous shell `"\u{78  78}".length` and `"\u{78   78}".size()` inserts plus direct exec `"\u{78  78}".length - 1`/`"\u{78  78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, tab/newline/leading/trailing whitespace braced-Unicode forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-multi-space-braced-unicode-string-length-index-a1076-focused-green-20260724-013710.log` (2 files / 4 tests), `agent-core-ruby-insert-multi-space-braced-unicode-string-length-index-a1076-related-20260724-013722.log` (2 files / 4 tests), `agent-core-build-a1076-20260724-013722.log`, `ruby-insert-multi-space-braced-unicode-string-length-index-dist-fixed-a1076-20260724-013746.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1076-20260724-013754.log` (230 files / 706 tests), and `agent-core-full-test-a1076-20260724-013755.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1077 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses leading/trailing-space ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so `ruby -e 'cmd = "rm /"; cmd.insert("\u{ 78 78 }".length, " -rf"); system(cmd)'`, `ruby -e 'cmd = "rm /"; cmd.insert("\u{  78 78  }".size(), " -rf"); system(cmd)'`, and `ruby -e 'cmd = "r"; cmd.insert("\u{ 78 78 }".length - 1, "m"); flag = "-f"; flag.insert("\u{ 78 78 }".bytesize() - 1, "r"); exec(cmd, flag, "/")'` deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-edge-space-braced-unicode-string-length-index-runtime-a1077-20260724-014042.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints with leading/trailing ordinary spaces before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-edge-space-braced-unicode-string-length-index-dist-before-a1077-20260724-014113.log` reproduced 3 false-negative mismatches where dangerous shell/direct edge-space braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-edge-space-braced-unicode-string-length-index-a1077-focused-red-20260724-014140.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` trims ordinary spaces before one-or-more ordinary-space braced-Unicode token splitting while preserving ASCII codepoint validation and rejecting empty tokens after trim. Tests now cover dangerous shell `"\u{ 78 78 }".length` and `"\u{  78 78  }".size()` inserts plus direct exec `"\u{ 78 78 }".length - 1`/`"\u{ 78 78 }".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, tab/newline braced-Unicode whitespace forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-edge-space-braced-unicode-string-length-index-a1077-focused-green-20260724-014207.log` (2 files / 4 tests), `agent-core-ruby-insert-edge-space-braced-unicode-string-length-index-a1077-related-20260724-014218.log` (2 files / 4 tests), `agent-core-build-a1077-20260724-014218.log`, `ruby-insert-edge-space-braced-unicode-string-length-index-dist-fixed-a1077-20260724-014245.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1077-20260724-014257.log` (230 files / 706 tests), and `agent-core-full-test-a1077-20260724-014258.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1078 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses tab-separated ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so Ruby source containing `cmd.insert("\u{78<TAB>78}".length, " -rf")`, `cmd.insert("\u{<TAB>78<TAB>78<TAB>}".size(), " -rf")`, and direct exec `"\u{78<TAB>78}".length - 1` / `"\u{78<TAB>78}".bytesize() - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-tab-braced-unicode-string-length-index-runtime-a1078-20260724-014539.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints separated by real tab characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Corrected pre-fix current-dist probe `ruby-insert-tab-braced-unicode-string-length-index-dist-before-a1078-rerun-20260724-014818.log` reproduced 3 false-negative mismatches where dangerous shell/direct real-tab braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed with corrected real-tab command strings in `agent-core-ruby-insert-tab-braced-unicode-string-length-index-a1078-focused-red-rerun-20260724-014825.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded braced-Unicode trimming/splitting from ordinary spaces to ordinary spaces or tabs while preserving ASCII codepoint validation and rejecting empty tokens after trim. Tests now cover dangerous shell real-tab `"\u{78<TAB>78}".length` and `"\u{<TAB>78<TAB>78<TAB>}".size()` inserts plus direct exec real-tab `"\u{78<TAB>78}".length - 1`/`"\u{78<TAB>78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, newline braced-Unicode whitespace forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-tab-braced-unicode-string-length-index-a1078-focused-green-rerun-20260724-014843.log` (2 files / 4 tests), `agent-core-ruby-insert-tab-braced-unicode-string-length-index-a1078-related-20260724-014852.log` (2 files / 4 tests), `agent-core-build-a1078-20260724-014852.log`, `ruby-insert-tab-braced-unicode-string-length-index-dist-fixed-a1078-20260724-014916.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1078-20260724-014924.log` (230 files / 706 tests), and `agent-core-full-test-a1078-20260724-014924.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1079 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses newline-separated ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so Ruby source containing `cmd.insert("\u{78<NL>78}".length, " -rf")`, `cmd.insert("\u{<NL>78<NL>78<NL>}".size(), " -rf")`, and direct exec `"\u{78<NL>78}".length - 1` / `"\u{78<NL>78}".bytesize() - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-newline-braced-unicode-string-length-index-runtime-a1079-20260724-015219.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints separated by real newline characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-newline-braced-unicode-string-length-index-dist-before-a1079-20260724-015248.log` reproduced 3 false-negative mismatches where dangerous shell/direct real-newline braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-newline-braced-unicode-string-length-index-a1079-focused-red-20260724-015312.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded braced-Unicode trimming/splitting from ordinary spaces/tabs to ordinary spaces, tabs, or newlines while preserving ASCII codepoint validation and rejecting empty tokens after trim. Tests now cover dangerous shell real-newline `"\u{78<NL>78}".length` and `"\u{<NL>78<NL>78<NL>}".size()` inserts plus direct exec real-newline `"\u{78<NL>78}".length - 1`/`"\u{78<NL>78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, other braced-Unicode whitespace forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-newline-braced-unicode-string-length-index-a1079-focused-green-20260724-015335.log` (2 files / 4 tests), `agent-core-ruby-insert-newline-braced-unicode-string-length-index-a1079-related-20260724-015347.log` (2 files / 4 tests), `agent-core-build-a1079-20260724-015348.log`, `ruby-insert-newline-braced-unicode-string-length-index-dist-fixed-a1079-20260724-015413.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1079-20260724-015421.log` (230 files / 706 tests), and `agent-core-full-test-a1079-20260724-015421.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1080 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses carriage-return-separated ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so Ruby source containing `cmd.insert("\u{78<CR>78}".length, " -rf")`, `cmd.insert("\u{<CR>78<CR>78<CR>}".size(), " -rf")`, and direct exec `"\u{78<CR>78}".length - 1` / `"\u{78<CR>78}".bytesize() - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-carriage-return-braced-unicode-string-length-index-runtime-a1080-20260724-015700.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints separated by real carriage return characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-carriage-return-braced-unicode-string-length-index-dist-before-a1080-20260724-015729.log` reproduced 3 false-negative mismatches where dangerous shell/direct real-carriage-return braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-carriage-return-braced-unicode-string-length-index-a1080-focused-red-20260724-015751.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded braced-Unicode trimming/splitting from ordinary spaces/tabs/newlines to ordinary spaces, tabs, newlines, or carriage returns while preserving ASCII codepoint validation and rejecting empty tokens after trim. Tests now cover dangerous shell real-carriage-return `"\u{78<CR>78}".length` and `"\u{<CR>78<CR>78<CR>}".size()` inserts plus direct exec real-carriage-return `"\u{78<CR>78}".length - 1`/`"\u{78<CR>78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, other braced-Unicode whitespace forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-carriage-return-braced-unicode-string-length-index-a1080-focused-green-20260724-015812.log` (2 files / 4 tests), `agent-core-ruby-insert-carriage-return-braced-unicode-string-length-index-a1080-related-20260724-015823.log` (2 files / 4 tests), `agent-core-build-a1080-20260724-015823.log`, `ruby-insert-carriage-return-braced-unicode-string-length-index-dist-fixed-a1080-20260724-015848.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1080-20260724-015856.log` (230 files / 706 tests), and `agent-core-full-test-a1080-20260724-015856.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1081 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses form-feed-separated ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so Ruby source containing `cmd.insert("\u{78<FF>78}".length, " -rf")`, `cmd.insert("\u{<FF>78<FF>78<FF>}".size(), " -rf")`, and direct exec `"\u{78<FF>78}".length - 1` / `"\u{78<FF>78}".bytesize() - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-form-feed-braced-unicode-string-length-index-runtime-a1081-20260724-020149.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints separated by real form feed characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-form-feed-braced-unicode-string-length-index-dist-before-a1081-20260724-020221.log` reproduced 3 false-negative mismatches where dangerous shell/direct real-form-feed braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-form-feed-braced-unicode-string-length-index-a1081-focused-red-20260724-020254.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded braced-Unicode trimming/splitting from ordinary spaces/tabs/newlines/carriage returns to ordinary spaces, tabs, newlines, carriage returns, or form feeds while preserving ASCII codepoint validation and rejecting empty tokens after trim. Tests now cover dangerous shell real-form-feed `"\u{78<FF>78}".length` and `"\u{<FF>78<FF>78<FF>}".size()` inserts plus direct exec real-form-feed `"\u{78<FF>78}".length - 1`/`"\u{78<FF>78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, other braced-Unicode whitespace forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-form-feed-braced-unicode-string-length-index-a1081-focused-green-20260724-020326.log` (2 files / 4 tests), `agent-core-ruby-insert-form-feed-braced-unicode-string-length-index-a1081-related-20260724-020337.log` (2 files / 4 tests), `agent-core-build-a1081-20260724-020338.log`, `ruby-insert-form-feed-braced-unicode-string-length-index-dist-fixed-a1081-20260724-020403.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1081-20260724-020412.log` (230 files / 706 tests), and `agent-core-full-test-a1081-20260724-020412.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1082 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses vertical-tab-separated ASCII braced-Unicode double-quoted string receivers before length/size methods and shell/direct process execution, so Ruby source containing `cmd.insert("\u{78<VT>78}".length, " -rf")`, `cmd.insert("\u{<VT>78<VT>78<VT>}".size(), " -rf")`, and direct exec `"\u{78<VT>78}".length - 1` / `"\u{78<VT>78}".bytesize() - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-vertical-tab-braced-unicode-string-length-index-runtime-a1082-20260724-020829.log` confirmed real Ruby evaluates double-quoted braced Unicode codepoints separated by real vertical tab characters before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-vertical-tab-braced-unicode-string-length-index-dist-before-a1082-20260724-020931.log` reproduced 3 false-negative mismatches where dangerous shell/direct real-vertical-tab braced-Unicode receiver length index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-vertical-tab-braced-unicode-string-length-index-a1082-focused-red-20260724-021005.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` expanded braced-Unicode trimming/splitting from ordinary spaces/tabs/newlines/carriage returns/form feeds to ordinary spaces, tabs, newlines, carriage returns, form feeds, or vertical tabs while preserving ASCII codepoint validation and rejecting empty tokens after trim. Tests now cover dangerous shell real-vertical-tab `"\u{78<VT>78}".length` and `"\u{<VT>78<VT>78<VT>}".size()` inserts plus direct exec real-vertical-tab `"\u{78<VT>78}".length - 1`/`"\u{78<VT>78}".bytesize() - 1` inserts. Other Ruby escape forms not explicitly implemented, single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, other braced-Unicode whitespace forms, non-ASCII/malformed braced Unicode escapes, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-vertical-tab-braced-unicode-string-length-index-a1082-focused-green-20260724-021020.log` (2 files / 4 tests), `agent-core-ruby-insert-vertical-tab-braced-unicode-string-length-index-a1082-related-20260724-021030.log` (2 files / 4 tests), `agent-core-build-a1082-20260724-021030.log`, `ruby-insert-vertical-tab-braced-unicode-string-length-index-dist-fixed-a1082-20260724-021052.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1082-20260724-021059.log` (230 files / 706 tests), and `agent-core-full-test-a1082-20260724-021113.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 174, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1083 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses simple double-quoted meta escapes before length/size methods and shell/direct process execution, so Ruby source containing `cmd.insert("\M-a\M-a".length, " -rf")`, `cmd.insert("\M-a\M-a".size, " -rf")`, and direct exec `"\M-a\M-a".length - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-meta-escape-string-length-index-runtime-a1083-20260724-021428.log` confirmed real Ruby evaluates simple `\M-<char>` meta escapes as one character per escape before string length indexes, reaches fake `rm -rf /`, can build safe `rm --version`, and can be dangerous when the insert value is dynamic (5 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-meta-escape-string-length-index-dist-before-a1083-20260724-021447.log` reproduced 3 false-negative mismatches where dangerous shell/direct simple-meta-escape receiver length/size index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-meta-escape-string-length-index-a1083-focused-red-20260724-021525.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` added a narrow `\M-<ascii-char>` double-quoted escape reader while preserving existing simple/control/octal/hex/Unicode parsing and rejecting missing, newline, quote-ending, backslash-prefixed, or non-ASCII operands. Tests now cover dangerous shell `"\M-a\M-a".length` and `"\M-a\M-a".size` inserts plus direct exec `"\M-a\M-a".length - 1` inserts. Single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, complex meta-control escapes such as `\M-\C-A`, meta escape bytesize-specific behavior, malformed meta escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-meta-escape-string-length-index-a1083-focused-green-20260724-021558.log` (2 files / 4 tests), `agent-core-ruby-insert-meta-escape-string-length-index-a1083-related-20260724-021608.log` (2 files / 4 tests), `agent-core-build-a1083-20260724-021609.log`, `ruby-insert-meta-escape-string-length-index-dist-fixed-a1083-20260724-021629.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1083-20260724-021637.log` (230 files / 706 tests), and `agent-core-full-test-a1083-20260724-021638.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 186, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1084 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses simple double-quoted meta escapes before `bytesize` methods and shell/direct process execution, so Ruby source containing `cmd.insert("\M-a\M-a".bytesize, " -rf")`, `cmd.insert("\M-a\M-a".bytesize(), " -rf")`, and direct exec `"\M-a\M-a".bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-meta-escape-bytesize-index-runtime-a1084-20260724-021945.log` confirmed real Ruby evaluates simple `\M-<char>` meta escapes as one character and one byte per escape before bytesize indexes, reaches fake `rm -rf /`, can build safe `rm --version`, can be dangerous when the insert value is dynamic, and reports `["\M-a\M-a".length, "\M-a\M-a".bytesize]` as `[2, 2]` (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-meta-escape-bytesize-index-dist-before-a1084-20260724-022007.log` reproduced 3 false-negative mismatches where dangerous shell/direct simple-meta-escape receiver bytesize index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty bytesize calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-meta-escape-bytesize-index-a1084-focused-red-20260724-022047.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` changed the narrow `\M-<ascii-char>` escape reader to contribute an ASCII-width placeholder for length/bytesize-only evaluation, preserving the same operand guards and avoiding UTF-8 byte overcounting. Tests now cover dangerous shell `"\M-a\M-a".bytesize` and `"\M-a\M-a".bytesize()` inserts plus direct exec `"\M-a\M-a".bytesize - 1` inserts. Single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, complex meta-control escapes such as `\M-\C-A`, malformed meta escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-meta-escape-bytesize-index-a1084-focused-green-20260724-022119.log` (2 files / 4 tests), `agent-core-ruby-insert-meta-escape-bytesize-index-a1084-related-20260724-022128.log` (2 files / 4 tests), `agent-core-build-a1084-20260724-022129.log`, `ruby-insert-meta-escape-bytesize-index-dist-fixed-a1084-20260724-022152.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1084-20260724-022159.log` (230 files / 706 tests), and `agent-core-full-test-a1084-20260724-022200.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 186, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1085 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses double-quoted meta-control escapes before length/bytesize methods and shell/direct process execution, so Ruby source containing `cmd.insert("\M-\C-A\M-\C-A".length, " -rf")`, `cmd.insert("\M-\C-A\M-\C-A".bytesize, " -rf")`, and direct exec `"\M-\C-A\M-\C-A".length - 1` / `"\M-\C-A\M-\C-A".bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-meta-control-escape-string-length-index-runtime-a1085-20260724-022519.log` confirmed real Ruby evaluates `\M-` wrapped control escapes as one character and one byte per escape before length/bytesize indexes, reaches fake `rm -rf /`, can build safe `rm --version`, can be dangerous when the insert value is dynamic, and reports `["\M-\C-A\M-\C-A".length, "\M-\C-A\M-\C-A".bytesize]` as `[2, 2]` (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-meta-control-escape-string-length-index-dist-before-a1085-20260724-022539.log` reproduced 3 false-negative mismatches where dangerous shell/direct meta-control receiver length/bytesize index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-meta-control-escape-string-length-index-a1085-focused-red-20260724-022611.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` taught the narrow meta escape reader to delegate `\M-` followed by an already-supported Ruby control escape to `readControlEscape` and contribute a one-character/one-byte placeholder for length/bytesize-only evaluation. Tests now cover dangerous shell `"\M-\C-A\M-\C-A".length` and `"\M-\C-A\M-\C-A".bytesize` inserts plus direct exec mixed length/bytesize indexes. Single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, other complex meta forms not backed by `readControlEscape`, malformed meta escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-meta-control-escape-string-length-index-a1085-focused-green-20260724-022638.log` (2 files / 4 tests), `agent-core-ruby-insert-meta-control-escape-string-length-index-a1085-related-20260724-022648.log` (2 files / 4 tests), `agent-core-build-a1085-20260724-022649.log`, `ruby-insert-meta-control-escape-string-length-index-dist-fixed-a1085-20260724-022711.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1085-20260724-022720.log` (230 files / 706 tests), and `agent-core-full-test-a1085-20260724-022721.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 191, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1086 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses double-quoted control-meta escapes before length/bytesize methods and shell/direct process execution, so Ruby source containing `cmd.insert("\C-\M-A\C-\M-A".length, " -rf")`, `cmd.insert("\C-\M-A\C-\M-A".bytesize, " -rf")`, and direct exec `"\C-\M-A\C-\M-A".length - 1` / `"\C-\M-A\C-\M-A".bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-control-meta-escape-string-length-index-runtime-a1086-20260724-023031.log` confirmed real Ruby evaluates `\C-` wrapped meta escapes as one character and one byte per escape before length/bytesize indexes, reaches fake `rm -rf /`, can build safe `rm --version`, can be dangerous when the insert value is dynamic, and reports `["\C-\M-A\C-\M-A".length, "\C-\M-A\C-\M-A".bytesize]` as `[2, 2]` (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-control-meta-escape-string-length-index-dist-before-a1086-20260724-023053.log` reproduced 3 false-negative mismatches where dangerous shell/direct control-meta receiver length/bytesize index flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-control-meta-escape-string-length-index-a1086-focused-red-20260724-023134.log` (2 failing files / 2 failing tests), then passed after `ruby-static-string-length-integer.utils.ts` taught the control escape reader to delegate `\C-` followed by an already-supported Ruby meta escape to `readMetaEscape` and contribute a one-character/one-byte placeholder for length/bytesize-only evaluation. Tests now cover dangerous shell `"\C-\M-A\C-\M-A".length` and `"\C-\M-A\C-\M-A".bytesize` inserts plus direct exec mixed length/bytesize indexes. Single-quoted escapes, interpolated receivers, non-empty calls, variable-derived lengths, other complex control/meta forms not backed by the narrow readers, malformed control/meta escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-control-meta-escape-string-length-index-a1086-focused-green-20260724-023211.log` (2 files / 4 tests), `agent-core-ruby-insert-control-meta-escape-string-length-index-a1086-related-20260724-023221.log` (2 files / 4 tests), `agent-core-build-a1086-20260724-023221.log`, `ruby-insert-control-meta-escape-string-length-index-dist-fixed-a1086-20260724-023244.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1086-20260724-023253.log` (230 files / 706 tests), and `agent-core-full-test-a1086-20260724-023254.log` (327 files / 1979 tests). Production source line counts remain under or at 200: `ruby-static-integer-expression.utils.ts` 198, `ruby-static-string-length-integer.utils.ts` 195, `interpreter-quote-like-string.utils.ts` 122, `ruby-static-integer-literal.utils.ts` 35, `ruby-static-insert-assignment.utils.ts` 94, and `interpreter-static-string.utils.ts` 199. |
| A1087 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses double-quoted backslash-LF line-continuation receivers before length/bytesize methods and shell/direct process execution, so Ruby source containing `cmd.insert("x\<LF>x".length, " -rf")`, `cmd.insert("x\<LF>x".bytesize, " -rf")`, and direct exec `"x\<LF>x".length - 1` / `"x\<LF>x".bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-line-continuation-string-length-index-runtime-a1087-20260724-023817.log` confirmed real Ruby treats a backslash followed by LF inside double-quoted strings as a line continuation contributing no character, so `"x\<LF>x".length` and `.bytesize` both evaluate to 2, dangerous shell/direct insert flows reach fake `rm -rf /`, safe `rm --version` can still be built, and a dynamic insert value remains runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-line-continuation-string-length-index-dist-before-a1087-20260724-024014.log` reproduced 3 false-negative mismatches where dangerous shell length, shell bytesize, and direct mixed length/bytesize flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-line-continuation-string-length-index-a1087-focused-red-20260724-024051.log` (2 failing files / 2 failing tests), then passed after Ruby double-quoted escape parsing was extracted to `ruby-double-quoted-escape.utils.ts` and taught to return a zero-length value for backslash-LF line continuations while `ruby-static-string-length-integer.utils.ts` now delegates escape evaluation instead of owning every escape helper. Tests now cover dangerous shell `"x\<LF>x".length` and `"x\<LF>x".bytesize` inserts plus direct exec mixed length/bytesize indexes. Single-quoted strings, interpolated receivers, non-empty calls, variable-derived lengths, CRLF continuations, malformed escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-line-continuation-string-length-index-a1087-focused-green-20260724-024220.log` (2 files / 4 tests), `agent-core-ruby-insert-line-continuation-string-length-index-a1087-related-20260724-024235.log` (2 files / 4 tests), `agent-core-build-a1087-20260724-024242.log`, `ruby-insert-line-continuation-string-length-index-dist-fixed-a1087-20260724-024306.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1087-20260724-024315.log` (230 files / 706 tests), and `agent-core-full-test-a1087-20260724-024329.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `ruby-static-string-length-integer.utils.ts` 85, `ruby-double-quoted-escape.utils.ts` 116, `interpreter-static-string.utils.ts` 199, `interpreter-quote-like-string.utils.ts` 122, and `ruby-static-insert-assignment.utils.ts` 94. |
| A1088 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses double-quoted backslash-CRLF line-continuation receivers before length/bytesize methods and shell/direct process execution, so Ruby source containing `cmd.insert("x\<CRLF>x".length, " -rf")`, `cmd.insert("x\<CRLF>x".bytesize, " -rf")`, and direct exec `"x\<CRLF>x".length - 1` / `"x\<CRLF>x".bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-crlf-continuation-string-length-index-runtime-a1088-20260724-024711.log` confirmed real Ruby treats a backslash followed by CRLF inside double-quoted strings as a line continuation contributing no character, so `"x\<CRLF>x".length` and `.bytesize` both evaluate to 2, dangerous shell/direct insert flows reach fake `rm -rf /`, safe `rm --version` can still be built, and a dynamic insert value remains runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-crlf-continuation-string-length-index-dist-before-a1088-20260724-024731.log` reproduced 3 false-negative mismatches where dangerous shell length, shell bytesize, and direct mixed length/bytesize flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-crlf-continuation-string-length-index-a1088-focused-red-20260724-024801.log` (2 failing files / 2 failing tests), then passed after `ruby-double-quoted-escape.utils.ts` taught the double-quoted escape reader to return a zero-length value for backslash-CRLF line continuations before the existing backslash-LF continuation branch. Tests now cover dangerous shell `"x\<CRLF>x".length` and `"x\<CRLF>x".bytesize` inserts plus direct exec mixed length/bytesize indexes. Single-quoted strings, interpolated receivers, non-empty calls, variable-derived lengths, lone CR continuations, malformed escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-crlf-continuation-string-length-index-a1088-focused-green-20260724-024815.log` (2 files / 4 tests), `agent-core-ruby-insert-crlf-continuation-string-length-index-a1088-related-20260724-024830.log` (2 files / 4 tests), `agent-core-build-a1088-20260724-024837.log`, `ruby-insert-crlf-continuation-string-length-index-dist-fixed-a1088-20260724-024859.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1088-20260724-024904.log` (230 files / 706 tests), and `agent-core-full-test-a1088-20260724-024917.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `ruby-double-quoted-escape.utils.ts` 119, `ruby-static-string-length-integer.utils.ts` 85, `interpreter-static-string.utils.ts` 199, `interpreter-quote-like-string.utils.ts` 122, and `ruby-static-insert-assignment.utils.ts` 94. |
| A1089 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses `%Q[...]` quote-like string receivers with backslash-LF line continuations before length/bytesize methods and shell/direct process execution, so Ruby source containing `cmd.insert(%Q[x\<LF>x].length, " -rf")`, `cmd.insert(%Q[x\<LF>x].bytesize, " -rf")`, and direct exec `%Q[x\<LF>x].length - 1` / `%Q[x\<LF>x].bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-quote-like-line-continuation-string-length-index-runtime-a1089-20260724-025252.log` confirmed real Ruby treats a backslash followed by LF inside `%Q[...]` strings as a line continuation contributing no character, so `%Q[x\<LF>x].length` and `.bytesize` both evaluate to 2, dangerous shell/direct insert flows reach fake `rm -rf /`, safe `rm --version` can still be built, and a dynamic insert value remains runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-quote-like-line-continuation-string-length-index-dist-before-a1089-20260724-025315.log` reproduced 3 false-negative mismatches where dangerous shell length, shell bytesize, and direct mixed length/bytesize flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-quote-like-line-continuation-string-length-index-a1089-focused-red-20260724-025348.log` (2 failing files / 2 failing tests), then passed after `interpreter-quote-like-string.utils.ts` taught the Ruby quote-like body reader to skip backslash-LF line continuations before the generic escaped-character branch while leaving Perl and non-line-continuation escape handling unchanged. Tests now cover dangerous shell `%Q[x\<LF>x].length` and `%Q[x\<LF>x].bytesize` inserts plus direct exec mixed length/bytesize indexes. A lone backslash-CR candidate was runtime-probed separately in `ruby-insert-lone-cr-continuation-string-length-index-runtime-a1089-candidate-20260724-025154.log` and rejected for this slice because Ruby treats lone CR as a byte with a warning rather than a zero-length continuation. Single-quoted `%q`, interpolated receivers, non-empty calls, variable-derived lengths, `%Q` backslash-CRLF continuations, other quote-like delimiters not covered here, malformed escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-quote-like-line-continuation-string-length-index-a1089-focused-green-20260724-025408.log` (2 files / 4 tests), `agent-core-ruby-insert-quote-like-line-continuation-string-length-index-a1089-related-20260724-025428.log` (2 files / 4 tests), `agent-core-build-a1089-20260724-025436.log`, `ruby-insert-quote-like-line-continuation-string-length-index-dist-fixed-a1089-20260724-025458.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1089-20260724-025505.log` (230 files / 706 tests), and `agent-core-full-test-a1089-20260724-025519.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `interpreter-quote-like-string.utils.ts` 126, `ruby-double-quoted-escape.utils.ts` 119, `ruby-static-string-length-integer.utils.ts` 85, `interpreter-static-string.utils.ts` 199, and `ruby-static-insert-assignment.utils.ts` 94. |
| A1090 | done | Core E2E | Detect Ruby static `String#insert` variable mutations whose index uses `%Q[...]` quote-like string receivers with backslash-CRLF line continuations before length/bytesize methods and shell/direct process execution, so Ruby source containing `cmd.insert(%Q[x\<CRLF>x].length, " -rf")`, `cmd.insert(%Q[x\<CRLF>x].bytesize, " -rf")`, and direct exec `%Q[x\<CRLF>x].length - 1` / `%Q[x\<CRLF>x].bytesize - 1` indexes deny instead of slipping through as user-reviewable. | Runtime probe `ruby-insert-quote-like-crlf-continuation-string-length-index-runtime-a1090-20260724-025803.log` confirmed real Ruby treats a backslash followed by CRLF inside `%Q[...]` strings as a line continuation contributing no character, so `%Q[x\<CRLF>x].length` and `.bytesize` both evaluate to 2, dangerous shell/direct insert flows reach fake `rm -rf /`, safe `rm --version` can still be built, and a dynamic insert value remains runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `ruby-insert-quote-like-crlf-continuation-string-length-index-dist-before-a1090-20260724-025829.log` reproduced 3 false-negative mismatches where dangerous shell length, shell bytesize, and direct mixed length/bytesize flows returned `ask_user`; safe `--version`, dynamic insert value, interpolated receiver, non-empty length calls, and future insert guards stayed `ask_user`. Focused E2E first failed in `agent-core-ruby-insert-quote-like-crlf-continuation-string-length-index-a1090-focused-red-20260724-025904.log` (2 failing files / 2 failing tests), then passed after `interpreter-quote-like-string.utils.ts` taught the Ruby quote-like body reader to skip backslash-CRLF line continuations before the existing backslash-LF continuation branch and before the generic escaped-character branch, while leaving Perl and non-line-continuation escape handling unchanged. Tests now cover dangerous shell `%Q[x\<CRLF>x].length` and `%Q[x\<CRLF>x].bytesize` inserts plus direct exec mixed length/bytesize indexes. Single-quoted `%q`, interpolated receivers, non-empty calls, variable-derived lengths, other quote-like delimiters not covered here, malformed escapes, non-ASCII operands, side-effecting length expressions, out-of-range indexes, aliases, list-assignment RHS mutation chains, block/keyword forms, non-string receiver coercion, and broader Ruby expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-ruby-insert-quote-like-crlf-continuation-string-length-index-a1090-focused-green-20260724-025921.log` (2 files / 4 tests), `agent-core-ruby-insert-quote-like-crlf-continuation-string-length-index-a1090-related-20260724-025936.log` (2 files / 4 tests), `agent-core-build-a1090-20260724-025942.log`, `ruby-insert-quote-like-crlf-continuation-string-length-index-dist-fixed-a1090-20260724-030005.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1090-20260724-030012.log` (230 files / 706 tests), and `agent-core-full-test-a1090-20260724-030028.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `interpreter-quote-like-string.utils.ts` 130, `ruby-double-quoted-escape.utils.ts` 119, `ruby-static-string-length-integer.utils.ts` 85, `interpreter-static-string.utils.ts` 199, and `ruby-static-insert-assignment.utils.ts` 94. |
| A1091 | done | Core E2E | Detect Python static strings with backslash-LF line continuations in shell/direct process commands, so `os.system("rm\<LF> -rf /")`, `subprocess.run("rm\<LF> -rf /", shell=True)`, and direct argv `subprocess.run(["r\<LF>m", "-r\<LF>f", "/"])` deny instead of slipping through as user-reviewable. | Runtime probe `python-line-continuation-string-command-runtime-a1091-20260724-030327.log` confirmed real Python folds a backslash followed by LF inside non-raw string literals to no character, so `"x\<LF>x" == "xx"` and `len("x\<LF>x") == 2`, shell strings reach fake `rm -rf /`, direct argv reaches fake `["rm", "-rf", "/"]`, safe `rm --version` can still be built through adjacent literals, and a dynamic flag can be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `python-line-continuation-string-command-dist-before-a1091-20260724-030353.log` reproduced 3 false-negative mismatches where dangerous `os.system`, shell-backed `subprocess.run`, and direct argv line-continuation strings returned `ask_user`; safe `--version`, dynamic flag, raw string, and future assignment guards stayed `ask_user`. Focused E2E first failed in `agent-core-python-line-continuation-string-command-a1091-focused-red-20260724-030439.log` (2 failing files / 2 failing tests), then passed after `python-string-literal.utils.ts` added a Python-specific non-raw quoted literal reader that skips backslash-LF and backslash-CRLF continuations before the generic escaped-character branch, while raw strings still delegate to the existing conservative quoted literal reader. Tests now cover dangerous shell `os.system("rm\<LF> -rf /")`, `subprocess.run("rm\<LF> -rf /", shell=True)`, and direct argv `["r\<LF>m", "-r\<LF>f", "/"]`. Triple-quoted strings, raw strings, f-strings with expressions, dynamic variables, future assignments, malformed escapes, non-ASCII operands, side-effecting expressions, alias/coercion paths, and broader Python expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-line-continuation-string-command-a1091-focused-green-20260724-030518.log` (2 files / 4 tests), `agent-core-python-line-continuation-string-command-a1091-related-20260724-030533.log` (2 files / 4 tests), `agent-core-build-a1091-20260724-030540.log`, `python-line-continuation-string-command-dist-fixed-a1091-20260724-030601.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1091-20260724-030609.log` (230 files / 706 tests), and `agent-core-full-test-a1091-20260724-030624.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `python-string-literal.utils.ts` 137, `python-static-string.utils.ts` 96, `python-shell-command.utils.ts` 160, `python-direct-command.utils.ts` 143, and `interpreter-script-token.utils.ts` 76. |
| A1092 | done | Core E2E | Preserve Python raw triple-quoted direct-process argv boundaries while still detecting shell-executed backslash-newline continuations, so direct `subprocess.run([r"""r\<LF>m""", r"""-r\<LF>f""", r"""/"""])` stays user-reviewable instead of being falsely denied, while non-raw triple direct argv and raw triple `shell=True` command strings that shell-fold to `rm -rf /` still deny. | Runtime probe `python-raw-triple-direct-line-continuation-runtime-a1092-20260724-031442.log` confirmed real Python direct argv preserves backslash-LF inside raw triple strings (`["r\\<LF>m", "-r\\<LF>f", "/"]`), folds it inside non-raw triple strings to `["rm", "-rf", "/"]`, and preserves raw single-string behavior (3 cases / 0 failures). Pre-fix current-dist probe `python-raw-triple-direct-line-continuation-dist-before-a1092-20260724-031413.log` reproduced 1 false-positive mismatch where raw triple direct argv returned `deny` while raw single direct argv stayed `ask_user`. Focused E2E first failed in `agent-core-python-raw-triple-direct-line-continuation-a1092-focused-red-20260724-031536.log` (1 file / 1 failed, 1 passed test), then passed after `python-string-literal.utils.ts` taught the Python string reader to consume triple delimiters as one literal and to preserve escapes for raw strings instead of accidentally reparsing the middle quote as a non-raw adjacent chunk. A shell boundary rerun showed raw triple `shell=True` must remain dangerous because the receiving shell removes backslash-newline continuations, so `hosted-shell-command.utils.ts` now normalizes shell-command-string backslash-LF/CRLF continuations only after a string is known to be shell-executed, leaving direct argv tokens untouched. Tests now cover non-raw triple direct argv deny, raw triple direct argv `ask_user`, and raw triple shell command deny. Expression f-strings, dynamic raw-string variables, raw direct argv that resolves through aliases/mutations, malformed triple delimiters, non-ASCII operands, shell dialect differences beyond backslash line continuation, side-effecting expressions, future assignments, and broader Python expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-python-raw-triple-line-continuation-a1092-focused-final-rerun-20260724-032012.log` (2 files / 4 tests), `agent-core-python-raw-triple-line-continuation-a1092-related-final-20260724-032022.log` (2 files / 4 tests), `agent-core-build-a1092-final-20260724-032029.log`, `python-raw-triple-direct-line-continuation-dist-fixed-a1092-final-20260724-032040.log` (2 cases / 0 mismatches), `python-triple-line-continuation-string-command-boundary-dist-fixed-a1092-final-20260724-032041.log` (6 cases / 0 mismatches), `agent-core-auto-reviewer-a1092-final-20260724-032050.log` (230 files / 706 tests), and `agent-core-full-test-a1092-final-20260724-032105.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `python-string-literal.utils.ts` 149, `hosted-shell-command.utils.ts` 34, `python-static-string.utils.ts` 96, `python-direct-command.utils.ts` 143, `python-shell-command.utils.ts` 160, and `shell-command.utils.ts` 187. |
| A1093 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.slice()` calls with non-negative literal indexes, so `"xrm".slice(1)`, `"x-rf".slice(1)`, and `"xrm -rf /".slice(1)` deny in direct argv, `execSync`, and `{ shell: true }` paths instead of slipping through as user-reviewable. | Runtime probe `node-static-slice-method-runtime-a1093-20260724-032419.log` confirmed real Node evaluates static `.slice(1)` receivers into child-process direct argv and shell command strings, safe `rm --version` operands, and dynamic index runtime-dangerous cases (4 cases / 0 failures). Pre-fix current-dist probe `node-static-slice-method-dist-before-a1093-20260724-032443.log` reproduced 4 false-negative mismatches where dangerous direct argv slice, static ref slice, `execSync` slice, and shell-option slice returned `ask_user`; safe `--version`, dynamic index, and negative-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-slice-method-a1093-focused-red-20260724-032627.log` (3 files / 3 failed, 3 passed tests), then passed after new `javascript-static-slice-method.utils.ts` added a pure static slice chain reader that only accepts decimal non-negative literal start/end indexes, and `javascript-static-string.utils.ts` delegates through it before the existing concat chain. Tests now cover direct `spawnSync("xrm".slice(1), ["x-rf".slice(1), "/"])`, static refs, `execSync("xrm -rf /".slice(1))`, shell-option `spawnSync("xrm -rf /".slice(1), [], {shell:true})`, safe `--version`, and dynamic index guards. Negative indexes, omitted args, `substring`/`substr`, optional or bracketed slice method calls, dynamic indexes, side-effecting receivers, non-decimal numeric expressions, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-static-slice-method-a1093-focused-green-20260724-032723.log` (3 files / 6 tests), `agent-core-node-static-slice-method-a1093-related-20260724-032745.log` (3 files / 6 tests), `agent-core-build-a1093-20260724-032753.log`, `node-static-slice-method-dist-fixed-a1093-20260724-032801.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1093-20260724-032811.log` (230 files / 706 tests), and `agent-core-full-test-a1093-20260724-032825.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 57, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, and `interpreter-shell-command.utils.ts` 143. |
| A1094 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.substring()` calls with non-negative literal indexes in normal order, so `"xrm".substring(1)`, `"x-rf".substring(1)`, `"xrmx".substring(1, 3)`, and `"xrm -rf /".substring(1)` deny in direct argv, `execSync`, and `{ shell: true }` paths instead of slipping through as user-reviewable. | Runtime probe `node-static-substring-method-runtime-a1094-20260724-033104.log` confirmed real Node evaluates static `.substring(1)` and `.substring(1, end)` receivers into child-process direct argv and shell command strings, safe `rm --version` operands, dynamic index runtime-dangerous cases, and swapped-index runtime-dangerous cases (6 cases / 0 failures). Pre-fix current-dist probe `node-static-substring-method-dist-before-a1094-20260724-033132.log` reproduced 5 false-negative mismatches where dangerous direct argv substring, static ref substring, end-index substring, `execSync` substring, and shell-option substring returned `ask_user`; safe `--version`, dynamic index, and swapped-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-substring-method-a1094-focused-red-20260724-033226.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-slice-method.utils.ts` expanded the pure static method-chain reader to accept `substring` alongside `slice`, while only accepting decimal non-negative literal start/end indexes and rejecting `substring(end < start)` instead of guessing runtime argument swapping. Tests now cover direct `spawnSync("xrm".substring(1), ["x-rf".substring(1), "/"])`, static refs, end-index substring, `execSync("xrm -rf /".substring(1))`, shell-option `spawnSync("xrm -rf /".substring(1), [], {shell:true})`, safe `--version`, and dynamic index guards. Swapped substring indexes, negative indexes, omitted/empty args, `substr`, optional or bracketed method calls, dynamic indexes, side-effecting receivers, non-decimal numeric expressions, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-static-substring-method-a1094-focused-green-20260724-033315.log` (3 files / 6 tests), `agent-core-node-static-substring-method-a1094-related-20260724-033330.log` (3 files / 6 tests), `agent-core-build-a1094-20260724-033341.log`, `node-static-substring-method-dist-fixed-a1094-20260724-033350.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1094-20260724-033359.log` (230 files / 706 tests), and `agent-core-full-test-a1094-20260724-033416.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 74, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, and `interpreter-shell-command.utils.ts` 143. |
| A1095 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.substr()` calls with non-negative literal start/length arguments, so `"xrm".substr(1)`, `"x-rf".substr(1)`, `"xrmx".substr(1, 2)`, and `"xrm -rf /".substr(1)` deny in direct argv, `execSync`, and `{ shell: true }` paths instead of slipping through as user-reviewable. | Runtime probe `node-static-substr-method-runtime-a1095-20260724-033655.log` confirmed real Node evaluates static `.substr(1)` and `.substr(1, length)` receivers into child-process direct argv and shell command strings, safe `rm --version` operands, dynamic-start runtime-dangerous cases, and negative-start runtime-dangerous cases (6 cases / 0 failures). Pre-fix current-dist probe `node-static-substr-method-dist-before-a1095-20260724-033722.log` reproduced 5 false-negative mismatches where dangerous direct argv substr, static ref substr, length substr, `execSync` substr, and shell-option substr returned `ask_user`; safe `--version`, dynamic start, and negative-start guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-substr-method-a1095-focused-red-20260724-033816.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-slice-method.utils.ts` expanded the pure static slice-like method-chain reader to accept `substr` alongside `slice` and `substring`, while treating the second `substr` argument as length and only accepting decimal non-negative literal start/length arguments. Tests now cover direct `spawnSync("xrm".substr(1), ["x-rf".substr(1), "/"])`, static refs, length substr, `execSync("xrm -rf /".substr(1))`, shell-option `spawnSync("xrm -rf /".substr(1), [], {shell:true})`, safe `--version`, and dynamic start guards. Negative starts, negative lengths, omitted/empty args, optional or bracketed method calls, dynamic starts/lengths, side-effecting receivers, non-decimal numeric expressions, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-static-substr-method-a1095-focused-green-20260724-033854.log` (3 files / 6 tests), `agent-core-node-static-substr-method-a1095-related-20260724-033912.log` (3 files / 6 tests), `agent-core-build-a1095-20260724-033921.log`, `node-static-substr-method-dist-fixed-a1095-20260724-033933.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1095-20260724-033940.log` (230 files / 706 tests), and `agent-core-full-test-a1095-20260724-033957.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 88, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, and `interpreter-shell-command.utils.ts` 143. |
| A1096 | done | Core E2E | Detect Node child process direct/shell commands built from static JS quoted bracket-key `slice`/`substring`/`substr` method calls, so `"xrm"["slice"](1)`, `"xrm"["substring"](1)`, `"xrm"["substr"](1)`, static refs, `execSync("xrm -rf /"["slice"](1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-bracket-slice-like-method-runtime-a1096-20260724-034236.log` confirmed real Node evaluates static quoted bracket keys for `slice`/`substring`/`substr` receivers into direct argv and shell command strings, preserves safe `rm --version`, and shows a dynamic bracket key can still be runtime-dangerous (6 cases / 0 failures). Pre-fix current-dist probe `node-bracket-slice-like-method-dist-before-a1096-20260724-034304.log` reproduced 6 false-negative mismatches where direct argv bracket `slice`/`substring`/`substr`, direct static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-bracket-slice-like-method-a1096-focused-red-20260724-034527.log` (3 files / 3 failed tests), then passed after `javascript-static-slice-method.utils.ts` added a static quoted bracket-key method branch before the existing slice-like argument evaluator, accepting only literal `slice`/`substring`/`substr` keys and preserving dynamic-key fallback. Tests now cover dangerous direct argv inline/static refs, shell `execSync`, shell-option command strings, safe operands, and dynamic-key guards. Dynamic bracket keys, computed static keys like `["sl" + "ice"]`, optional chaining before bracket method calls, negative/dynamic indexes, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-bracket-slice-like-method-a1096-focused-green-20260724-034605.log` (3 files / 6 tests), `agent-core-node-bracket-slice-like-method-a1096-related-20260724-034629.log` (4 files / 8 tests), `agent-core-build-a1096-20260724-034637.log`, `node-bracket-slice-like-method-dist-fixed-a1096-20260724-034647.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1096-20260724-034654.log` (230 files / 706 tests), and `agent-core-full-test-a1096-20260724-034708.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 105, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1097 | done | Core E2E | Detect Node child process direct/shell commands built from static JS optional-chained `slice`/`substring`/`substr` method calls, so `"xrm"?.slice(1)`, `"xrm"?.substring(1)`, `"xrm"?.["substr"](1)`, static refs, `execSync("xrm -rf /"?.slice(1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-optional-slice-like-method-runtime-a1097-20260724-035029.log` confirmed real Node evaluates optional-chained static `slice`/`substring`/`substr` receivers into child-process direct argv and shell command strings, preserves safe `rm --version`, and reports 6 cases / 0 failures. Pre-fix current-dist probe `node-optional-slice-like-method-dist-before-a1097-20260724-035035.log` reproduced 6 false-negative mismatches where optional dot/bracket direct argv, static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-optional-slice-like-method-a1097-focused-red-20260724-035130.log` (3 files / 3 failed tests), then passed after `javascript-static-slice-method.utils.ts` added narrow optional-chain entry parsing for `?.method(...)` and `?.["method"](...)` before the existing slice-like argument evaluator, preserving literal method names and decimal non-negative index guards. Tests now cover dangerous direct argv inline/static refs, shell `execSync`, shell-option command strings, safe operands, and dynamic-index guards. Nullish or side-effecting receivers, computed static keys like `?.["sl" + "ice"]`, optional method-call forms like `.slice?.(1)`, negative/dynamic indexes, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-optional-slice-like-method-a1097-focused-green-20260724-035212.log` (3 files / 6 tests), `agent-core-node-optional-slice-like-method-a1097-related-20260724-035220.log` (4 files / 8 tests), `agent-core-build-a1097-20260724-035227.log`, `node-optional-slice-like-method-dist-fixed-a1097-20260724-035236.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1097-20260724-035243.log` (230 files / 706 tests), and `agent-core-full-test-a1097-20260724-035258.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 125, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1098 | done | Core E2E | Detect Node child process direct/shell commands built from static JS optional method-call `slice`/`substring`/`substr` forms, so `"xrm".slice?.(1)`, `"xrm".substring?.(1)`, `"xrm"["substr"]?.(1)`, static refs with `?.slice?.(1)`, `execSync("xrm -rf /".slice?.(1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-optional-call-slice-like-method-runtime-a1098-20260724-035546.log` confirmed real Node evaluates optional method-call static `slice`/`substring`/`substr` receivers into child-process direct argv and shell command strings, preserves safe `rm --version`, and reports 6 cases / 0 failures. Pre-fix current-dist probe `node-optional-call-slice-like-method-dist-before-a1098-20260724-035552.log` reproduced 6 false-negative mismatches where optional-call dot/bracket direct argv, optional access plus optional-call static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-optional-call-slice-like-method-a1098-focused-red-20260724-035645.log` (3 files / 3 failed tests), then passed after `javascript-static-slice-method.utils.ts` reused the existing literal method-name branches while allowing a narrow `?.(` call opener before the existing slice-like argument evaluator, preserving decimal non-negative index and literal key guards. Tests now cover dangerous direct argv inline/static refs, shell `execSync`, shell-option command strings, safe operands, and dynamic-index guards. Nullish or side-effecting receivers, computed static keys like `["sl" + "ice"]?.(1)`, dynamic optional-call targets, negative/dynamic indexes, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-optional-call-slice-like-method-a1098-focused-green-20260724-035717.log` (3 files / 6 tests), `agent-core-node-optional-call-slice-like-method-a1098-related-20260724-035730.log` (4 files / 8 tests), `agent-core-build-a1098-20260724-035737.log`, `node-optional-call-slice-like-method-dist-fixed-a1098-20260724-035745.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1098-20260724-035752.log` (230 files / 706 tests), and `agent-core-full-test-a1098-20260724-035806.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 133, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1099 | done | Core E2E | Detect Node child process direct/shell commands built from static JS computed bracket keys for `slice`/`substring`/`substr`, so `"xrm"["sl" + "ice"](1)`, `"xrm"["sub" + "string"](1)`, `"xrm"["sub" + "str"](1)`, optional static refs with `?.["sl" + "ice"]?.(1)`, `execSync("xrm -rf /"["sl" + "ice"](1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-computed-slice-like-method-key-runtime-a1099-20260724-040054.log` confirmed real Node evaluates pure static computed bracket keys for `slice`/`substring`/`substr` receivers into child-process direct argv and shell command strings, preserves safe `rm --version`, and reports 6 cases / 0 failures. Pre-fix current-dist probe `node-computed-slice-like-method-key-dist-before-a1099-20260724-040100.log` reproduced 6 false-negative mismatches where computed-key direct argv, optional static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-computed-slice-like-method-key-a1099-focused-red-20260724-040202.log` (3 files / 3 failed tests), then passed after `javascript-static-slice-method.utils.ts` expanded bracket method-key parsing from a single quoted literal to a pure quoted-literal `+` chain before applying the existing literal method-name, optional call, and decimal non-negative index guards. Tests now cover dangerous direct argv inline/static refs, shell `execSync`, shell-option command strings, safe operands, and dynamic-key guards. Dynamic key parts, computed keys with non-string operands or template expressions, side-effecting receivers, negative/dynamic indexes, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-computed-slice-like-method-key-a1099-focused-green-20260724-040232.log` (3 files / 6 tests), `agent-core-node-computed-slice-like-method-key-a1099-related-20260724-040248.log` (4 files / 8 tests), `agent-core-build-a1099-20260724-040256.log`, `node-computed-slice-like-method-key-dist-fixed-a1099-20260724-040303.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1099-20260724-040309.log` (230 files / 706 tests), and `agent-core-full-test-a1099-20260724-040323.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 152, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1100 | done | Core E2E | Detect Node child process direct/shell commands built from static JS no-substitution template-literal bracket keys for `slice`/`substring`/`substr`, so `"xrm"[`slice`](1)`, `"xrm"[`substring`](1)`, `"xrm"[`substr`](1)`, optional static refs with `?.[`slice`]?.(1)`, `execSync("xrm -rf /"[`slice`](1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-template-slice-like-method-key-runtime-a1100-20260724-040603.log` confirmed real Node evaluates no-substitution template method keys into child-process direct argv and shell command strings for direct `slice`/`substring`/`substr`, optional static refs, and shell `execSync`, while preserving safe `rm --version` operands (6 cases / 0 failures). Pre-fix current-dist probe `node-template-slice-like-method-key-dist-before-a1100-20260724-040609.log` reproduced 6 false-negative mismatches where template-key direct argv, optional static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and interpolated-template-key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-template-slice-like-method-key-a1100-focused-red-20260724-040821.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-slice-method.utils.ts` expanded static bracket method-key parsing from quoted literals and quoted `+` chains to also accept no-substitution template literal key parts before applying the existing literal method-name, optional call, and decimal non-negative index guards. Tests now cover dangerous direct argv inline keys, substring/substr variants, optional static refs, shell `execSync`, shell-option command strings, safe operands, and interpolated-template-key guards. Interpolated template keys like `` `sl${"ice"}` ``, non-string key operands, side-effecting receivers, negative/dynamic indexes, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-template-slice-like-method-key-a1100-focused-green-20260724-040912.log` (3 files / 6 tests), `agent-core-node-template-slice-like-method-key-a1100-related-20260724-040923.log` (4 files / 8 tests), `agent-core-build-a1100-20260724-040924.log`, `node-template-slice-like-method-key-dist-fixed-a1100-20260724-040934.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1100-20260724-040935.log` (230 files / 706 tests), and `agent-core-full-test-a1100-20260724-040949.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 195, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1101 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.trim()` / `trimStart()` / `trimEnd()` calls, so `spawnSync(" rm ".trim(), [" -rf ".trim(), "/"])`, `spawnSync(" rm".trimStart(), [" -rf".trimStart(), "/"])`, `spawnSync("rm ".trimEnd(), ["-rf ".trimEnd(), "/"])`, `execSync(" rm -rf / ".trim())`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-static-trim-method-runtime-a1101-20260724-041403.log` confirmed real Node evaluates static trim method chains into child-process direct argv and shell command strings for `trim`, `trimStart`, `trimEnd`, static refs, and shell `execSync`, while preserving safe `rm --version` operands (6 cases / 0 failures). Pre-fix current-dist probe `node-static-trim-method-dist-before-a1101-20260724-041403.log` reproduced 6 false-negative mismatches where trim direct argv, trimStart/trimEnd direct argv, static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-method guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-static-trim-method-a1101-focused-red-20260724-041457.log` (3 files / 3 failed, 3 passed tests), then passed after new `javascript-static-trim-method.utils.ts` added a narrow static empty-argument trim method-chain reader and `javascript-static-string.utils.ts` now applies it after the existing slice-like method reader and before concat handling. Tests now cover dangerous direct argv inline trim/trimStart/trimEnd, static refs, shell `execSync`, shell-option command strings, safe operands, and dynamic-method guards. Bracket-key trim calls, optional trim calls, extra-argument trim calls, dynamic method names, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-static-trim-method-a1101-focused-green-20260724-041554.log` (3 files / 6 tests), `agent-core-node-static-trim-method-a1101-related-20260724-041605.log` (4 files / 8 tests), `agent-core-build-a1101-20260724-041605.log`, `node-static-trim-method-dist-fixed-a1101-20260724-041615.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1101-20260724-041615.log` (230 files / 706 tests), and `agent-core-full-test-a1101-20260724-041630.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 197, `javascript-static-trim-method.utils.ts` 57, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1102 | done | Core E2E | Detect Node child process direct/shell commands built from static JS bracket-key and optional `trim` / `trimStart` / `trimEnd` method calls, so `spawnSync(" rm "["trim"](), [" -rf "["trim"](), "/"])`, `spawnSync(" rm"["trimStart"](), ["-rf "["trimEnd"](), "/"])`, `spawnSync(" rm "?.trim(), [" -rf "?.trim(), "/"])`, `spawnSync(" rm ".trim?.(), [" -rf ".trim?.(), "/"])`, `execSync(" rm -rf / "?.["trim"]?.())`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-bracket-optional-trim-method-runtime-a1102-20260724-041951.log` confirmed real Node evaluates static bracket-key and optional trim method calls into child-process direct argv and shell command strings for bracket `trim`, bracket `trimStart`/`trimEnd`, optional access, optional method-call, optional bracket access/call, and shell `execSync`, while preserving safe `rm --version` operands (7 cases / 0 failures). Pre-fix current-dist probe `node-bracket-optional-trim-method-dist-before-a1102-20260724-041951.log` reproduced 7 false-negative mismatches where bracket trim direct argv, bracket trimStart/trimEnd direct argv, optional access, optional call, optional bracket direct argv, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-method guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-bracket-optional-trim-method-a1102-focused-red-20260724-042042.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-trim-method.utils.ts` split trim call parsing into named, quoted bracket-key, and empty-call-opener helpers, accepting narrow `?.method()`, `.method?.()`, `["method"]()`, `["method"]?.()`, and `?.["method"]?.()` forms while preserving static method-name guards. Tests now cover dangerous direct argv inline bracket/optional forms, shell `execSync`, shell-option command strings, safe operands, and dynamic-method guards. Computed trim keys like `["tr" + "im"]`, template-literal trim keys, non-string key operands, extra-argument trim calls, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-bracket-optional-trim-method-a1102-focused-green-20260724-042144.log` (3 files / 6 tests), `agent-core-node-bracket-optional-trim-method-a1102-related-20260724-042156.log` (4 files / 8 tests), `agent-core-build-a1102-20260724-042157.log`, `node-bracket-optional-trim-method-dist-fixed-a1102-20260724-042207.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1102-20260724-042207.log` (230 files / 706 tests), and `agent-core-full-test-a1102-20260724-042224.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 197, `javascript-static-trim-method.utils.ts` 102, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1103 | done | Core E2E | Detect Node child process direct/shell commands built from static JS computed quoted bracket keys for `trim` / `trimStart` / `trimEnd`, so `spawnSync(" rm "["tr" + "im"](), [" -rf "["tr" + "im"](), "/"])`, `spawnSync(" rm"["trim" + "Start"](), ["-rf "["trim" + "End"](), "/"])`, optional computed bracket calls with `?.["tr" + "im"]?.()`, `execSync(" rm -rf / "["tr" + "im"]())`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-computed-trim-method-key-runtime-a1103-20260724-042539.log` confirmed real Node evaluates pure static computed quoted trim keys into child-process direct argv and shell command strings for computed `trim`, computed `trimStart`/`trimEnd`, optional computed bracket calls, and shell `execSync`, while preserving safe `rm --version` operands (5 cases / 0 failures). Pre-fix current-dist probe `node-computed-trim-method-key-dist-before-a1103-20260724-042539.log` reproduced 5 false-negative mismatches where computed trim direct argv, computed trimStart/trimEnd direct argv, optional computed bracket direct argv, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-key-part guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-computed-trim-method-key-a1103-focused-red-20260724-042622.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-trim-method.utils.ts` expanded bracket trim method-key parsing from a single quoted literal to a pure quoted-literal `+` chain before applying the existing static method-name and empty-call guards. Tests now cover dangerous direct argv inline computed keys, optional computed bracket forms, shell `execSync`, shell-option command strings, safe operands, and dynamic-key-part guards. Dynamic key parts, template-literal trim keys, non-string key operands, extra-argument trim calls, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-computed-trim-method-key-a1103-focused-green-20260724-042652.log` (3 files / 6 tests), `agent-core-node-computed-trim-method-key-a1103-related-20260724-042702.log` (4 files / 8 tests), `agent-core-build-a1103-20260724-042703.log`, `node-computed-trim-method-key-dist-fixed-a1103-20260724-042714.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1103-20260724-042714.log` (230 files / 706 tests), and `agent-core-full-test-a1103-20260724-042729.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 197, `javascript-static-trim-method.utils.ts` 121, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1104 | done | Core E2E | Detect Node child process direct/shell commands built from static JS no-substitution template-literal bracket keys for `trim` / `trimStart` / `trimEnd`, so `spawnSync(" rm "[`trim`](), [" -rf "[`trim`](), "/"])`, `spawnSync(" rm"[`trimStart`](), ["-rf "[`trimEnd`](), "/"])`, optional template bracket calls with `?.[`trim`]?.()`, `execSync(" rm -rf / "[`trim`]())`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-template-trim-method-key-runtime-a1104-20260724-043012.log` confirmed real Node evaluates no-substitution template trim keys into child-process direct argv and shell command strings for template `trim`, template `trimStart`/`trimEnd`, optional template bracket calls, and shell `execSync`, while preserving safe `rm --version` operands (5 cases / 0 failures). Pre-fix current-dist probe `node-template-trim-method-key-dist-before-a1104-20260724-043013.log` reproduced 5 false-negative mismatches where template trim direct argv, template trimStart/trimEnd direct argv, optional template bracket direct argv, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and interpolated-template-key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-template-trim-method-key-a1104-focused-red-20260724-043053.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-trim-method.utils.ts` expanded bracket trim method-key parsing from quoted literals and quoted `+` chains to also accept no-substitution template literal key parts before applying the existing static method-name and empty-call guards. Tests now cover dangerous direct argv inline template keys, optional template bracket forms, shell `execSync`, shell-option command strings, safe operands, and interpolated-template-key guards. Interpolated template keys like `` `tr${"im"}` ``, non-string key operands, extra-argument trim calls, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-template-trim-method-key-a1104-focused-green-20260724-043123.log` (3 files / 6 tests), `agent-core-node-template-trim-method-key-a1104-related-20260724-043135.log` (4 files / 8 tests), `agent-core-build-a1104-20260724-043135.log`, `node-template-trim-method-key-dist-fixed-a1104-20260724-043147.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1104-20260724-043147.log` (230 files / 706 tests), and `agent-core-full-test-a1104-20260724-043202.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 197, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1105 | done | Core E2E | Detect Node child process direct/shell commands built from static JS trim-like methods followed by slice-like methods, so `spawnSync(" xrm ".trim().slice(1), [" x-rf ".trim().slice(1), "/"])`, `spawnSync(" xrm".trimStart().substring(1), [" x-rf".trimStart().substring(1), "/"])`, `spawnSync("xrm ".trimEnd().substr(1), ["x-rf ".trimEnd().substr(1), "/"])`, static refs, `execSync(" xrm -rf / ".trim().slice(1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-trim-then-slice-chain-runtime-a1105-20260724-043509.log` confirmed real Node evaluates static `trim`/`trimStart`/`trimEnd` followed by `slice`/`substring`/`substr` into child-process direct argv and shell command strings, while preserving safe `rm --version` operands (6 cases / 0 failures). Pre-fix current-dist probe `node-trim-then-slice-chain-dist-before-a1105-20260724-043509.log` reproduced 6 false-negative mismatches where trim-then-slice direct argv, trimStart-then-substring direct argv, trimEnd-then-substr direct argv, static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe `--version` and dynamic-index guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-trim-then-slice-chain-a1105-focused-red-20260724-043552.log` (3 files / 3 failed, 3 passed tests), then passed after new `javascript-static-string-method-chain.utils.ts` took ownership of static string method-chain ordering, repeatedly applying the existing slice-like and trim-like readers until no more static method advances remain before handing off to concat. Tests now cover dangerous direct argv inline/static refs, shell `execSync`, shell-option command strings, safe operands, and dynamic-index guards. Concat followed by slice/trim, concat arguments with later chained methods, dynamic indexes, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-trim-then-slice-chain-a1105-focused-green-20260724-043641.log` (3 files / 6 tests), `agent-core-node-trim-then-slice-chain-a1105-related-20260724-043652.log` (4 files / 8 tests), `agent-core-build-a1105-20260724-043653.log`, `node-trim-then-slice-chain-dist-fixed-a1105-20260724-043703.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1105-20260724-043704.log` (230 files / 706 tests), and `agent-core-full-test-a1105-20260724-043720.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-static-string-method-chain.utils.ts` 33, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1106 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `concat()` followed by slice-like or trim-like methods, so `spawnSync("x".concat("rm").slice(1), ["x".concat("-rf").slice(1), "/"])`, `spawnSync(" rm".concat(" ").trim(), [" -rf".concat(" ").trim(), "/"])`, static refs, `execSync("x".concat("rm -rf /").slice(1))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-concat-then-method-chain-runtime-a1106-20260724-044018.log` confirmed real Node evaluates `concat()` followed by `slice()` or `trim()` into child-process direct argv and shell command strings, while preserving safe `rm --version` operands (5 cases / 0 failures). Pre-fix current-dist probe `node-concat-then-method-chain-dist-before-a1106-20260724-044019.log` reproduced 5 false-negative mismatches out of 7 cases where direct argv slice, direct argv trim, static refs, shell `execSync`, and shell-option command cases returned `ask_user`; safe operands and the dynamic concat guard stayed `ask_user`. Focused E2E first failed in `agent-core-node-concat-then-method-chain-a1106-focused-red-20260724-044111.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-static-string-method-chain.utils.ts` started treating concat as a repeatable static method-chain step: slice-like, trim-like, and concat readers now advance in one loop, and any concat advance is fed back into later slice/trim parsing. During focused verification, the new dynamic guard was corrected from a resolvable const literal to `getTail()` so unresolved concat arguments remain user-reviewable rather than falsely denied. Tests now cover dangerous direct argv inline/static refs, shell `execSync`, shell-option command strings, safe operands, and truly dynamic concat-argument guards. Dynamic concat arguments, non-static receivers, side-effecting receivers, mutation/alias chains, symbolic method names, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-concat-then-method-chain-a1106-focused-green-20260724-044411.log` (3 files / 6 tests), `agent-core-node-concat-then-method-chain-a1106-related-20260724-044420.log` (4 files / 8 tests), `agent-core-build-a1106-20260724-044426.log`, `node-concat-then-method-chain-dist-fixed-a1106-20260724-044433.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1106-20260724-044439.log` (230 files / 706 tests), and `agent-core-full-test-a1106-20260724-044453.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-static-string-method-chain.utils.ts` 42, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1107 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.concat.call(...)` and `.apply(...)`, so `spawnSync(String.prototype.concat.call("r", "m"), [String.prototype.concat.call("-r", "f"), "/"])`, `spawnSync(String.prototype.concat.apply("r", ["m"]), [String.prototype.concat.apply("-r", ["f"]), "/"])`, `execSync(String.prototype.concat.call("rm", " -rf /"))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-string-prototype-concat-call-runtime-a1107-20260724-044923.log` confirmed real Node evaluates `String.prototype.concat.call/apply` into child-process direct argv and shell command strings, while preserving safe `rm --version` operands (5 cases / 0 failures). Pre-fix current-dist probe `node-string-prototype-concat-call-dist-before-a1107-20260724-044929.log` reproduced 4 false-negative mismatches out of 7 cases where direct argv call, direct argv apply, shell `execSync`, and shell-option command cases returned `ask_user`; safe operands, dynamic call args, and dynamic apply arrays stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-prototype-concat-call-a1107-focused-red-20260724-045033.log` (3 files / 3 failed, 3 passed tests), then passed after new `javascript-string-prototype-concat.utils.ts` added a narrow static parser for direct `String.prototype.concat.call(receiver, ...args)` and `.apply(receiver, [args])`, and `javascript-static-string.utils.ts` delegates to it as a primary string expression before method-chain parsing. Tests now cover dangerous direct argv call/apply, shell `execSync`, shell-option command strings, safe operands, dynamic call-argument guards, and dynamic apply-array guards. Aliased concat functions, optional/bracket `call` or `apply`, non-static receivers, dynamic apply arrays, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-string-prototype-concat-call-a1107-focused-green-20260724-045223.log` (3 files / 6 tests), `agent-core-node-string-prototype-concat-call-a1107-related-20260724-045231.log` (4 files / 8 tests), `agent-core-build-a1107-20260724-045238.log`, `node-string-prototype-concat-call-dist-fixed-a1107-20260724-045245.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1107-20260724-045251.log` (230 files / 706 tests), and `agent-core-full-test-a1107-20260724-045304.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-string-prototype-concat.utils.ts` 167, `javascript-static-string-method-chain.utils.ts` 42, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1108 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.concat` static wrapper variants, so `String.prototype.concat["call"]("r", "m")`, `String.prototype.concat["ca" + "ll"]("r", "m")`, `String.prototype.concat?.["call"]?.("r", "m")`, `String.prototype.concat.call?.("rm", " -rf /")`, and `String.prototype.concat["apply"]("rm", [" -rf /"])` deny instead of slipping through as user-reviewable. | Runtime probe `node-string-prototype-concat-wrapper-runtime-a1108-20260724-045630.log` confirmed real Node evaluates static bracket, computed quoted-key, optional bracket/call, and bracket apply wrapper forms into child-process direct argv and shell command strings, while preserving safe `rm --version` operands (6 cases / 0 failures). Pre-fix current-dist probe `node-string-prototype-concat-wrapper-dist-before-a1108-20260724-045636.log` reproduced 5 false-negative mismatches out of 8 cases where bracket call, computed call, optional bracket call, optional `call?.(...)`, and bracket apply shell-option cases returned `ask_user`; safe operands, dynamic wrapper keys, and dynamic apply arrays stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-prototype-concat-wrapper-a1108-focused-red-20260724-045726.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-string-prototype-concat.utils.ts` replaced dot-only wrapper parsing with a narrow static wrapper member reader that accepts `.call`, `?.call`, `["call"]`, `["ca" + "ll"]`, `?.["call"]`, and `?.(` invocation forms for `call`/`apply`, while dynamic wrapper keys remain unresolved. Tests now cover dangerous direct argv bracket/computed/optional call, shell `execSync` optional call, shell-option bracket apply, safe operands, dynamic wrapper-key guards, and dynamic apply-array guards. Template wrapper keys, aliased concat functions, optional/bracket base `String.prototype.concat` members beyond the wrapper, non-static receivers, dynamic apply arrays, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-string-prototype-concat-wrapper-a1108-focused-green-20260724-045808.log` (3 files / 6 tests), `agent-core-node-string-prototype-concat-wrapper-a1108-related-20260724-045825.log` (4 files / 8 tests), `agent-core-build-a1108-20260724-045832.log`, `node-string-prototype-concat-wrapper-dist-fixed-a1108-20260724-045841.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1108-20260724-045847.log` (230 files / 706 tests), and `agent-core-full-test-a1108-20260724-045901.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-string-prototype-concat.utils.ts` 199, `javascript-static-string-method-chain.utils.ts` 42, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1109 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.concat` no-substitution template wrapper keys, so ``String.prototype.concat[`call`]("r", "m")``, ``String.prototype.concat["ca" + `ll`]("r", "m")``, ``String.prototype.concat?.[`call`]?.("r", "m")``, ``String.prototype.concat[`call`]("rm", " -rf /")``, and ``String.prototype.concat[`apply`]("rm", [" -rf /"])`` deny instead of slipping through as user-reviewable. | Runtime probe `node-string-prototype-concat-template-wrapper-runtime-a1109-20260724-050213.log` confirmed real Node evaluates no-substitution template wrapper keys and mixed quoted/template key concatenation into child-process direct argv and shell command strings, while preserving safe `rm --version` operands (6 cases / 0 failures). Pre-fix current-dist probe `node-string-prototype-concat-template-wrapper-dist-before-a1109-20260724-050219.log` reproduced 5 false-negative mismatches out of 7 cases where template call, mixed template call, optional template call, shell `execSync`, and shell-option template apply cases returned `ask_user`; safe operands and interpolated-template-key guards stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-prototype-concat-template-wrapper-a1109-focused-red-20260724-050315.log` (3 files / 3 failed, 3 passed tests), then passed after new `javascript-static-member-key.utils.ts` extracted static member-key parsing for quoted literals plus no-substitution template literals, and `javascript-string-prototype-concat.utils.ts` now uses that helper for wrapper `call`/`apply` keys instead of its quoted-only local reader. Tests now cover dangerous direct argv template/mixed/optional wrapper calls, shell `execSync` template call, shell-option template apply, safe operands, and interpolated-template-key guards. Interpolated template wrapper keys, aliased concat functions, optional/bracket base `String.prototype.concat` members beyond the wrapper, non-static receivers, dynamic apply arrays, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-string-prototype-concat-template-wrapper-a1109-focused-green-20260724-050410.log` (3 files / 6 tests), `agent-core-node-string-prototype-concat-template-wrapper-a1109-related-20260724-050427.log` (4 files / 8 tests), `agent-core-build-a1109-20260724-050435.log`, `node-string-prototype-concat-template-wrapper-dist-fixed-a1109-20260724-050444.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1109-20260724-050450.log` (230 files / 706 tests), and `agent-core-full-test-a1109-20260724-050505.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-string-prototype-concat.utils.ts` 184, `javascript-static-member-key.utils.ts` 53, `javascript-static-string-method-chain.utils.ts` 42, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1110 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.concat` base member variants, so `String["prototype"].concat.call("r", "m")`, `String.prototype["concat"].call("r", "m")`, `String["proto" + "type"]["con" + "cat"]["call"]("r", "m")`, `String?.["prototype"]?.["concat"]?.["call"]?.("r", "m")`, ``String[`prototype`][`concat`].call("rm", " -rf /")``, and `String["prototype"]["concat"].apply("rm", [" -rf /"])` deny instead of slipping through as user-reviewable. | Runtime probe `node-string-prototype-concat-base-runtime-a1110-20260724-050929.log` confirmed real Node evaluates bracket, computed quoted-key, optional, and no-substitution template base members for `String.prototype.concat` into child-process direct argv and shell command strings, while preserving safe `rm --version` operands (7 cases / 0 failures). Pre-fix current-dist probe `node-string-prototype-concat-base-dist-before-a1110-20260724-050937.log` reproduced 6 false-negative mismatches out of 9 cases where bracket prototype, bracket concat, computed base, optional base, template shell `execSync`, and shell-option bracket-base apply cases returned `ask_user`; safe operands, dynamic prototype keys, and interpolated concat keys stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-prototype-concat-base-a1110-focused-red-20260724-051030.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-string-prototype-concat.utils.ts` replaced dot-only base-path parsing with the same static member-name reader used for wrapper keys, allowing `.prototype`, `["prototype"]`, `["proto" + "type"]`, `?.["prototype"]`, `.concat`, `["concat"]`, template keys, and optional chaining while dynamic/interpolated keys remain unresolved. Tests now cover dangerous direct argv bracket/computed/optional base calls, shell `execSync` template base, shell-option bracket-base apply, safe operands, dynamic prototype-key guards, and interpolated concat-key guards. Aliased `String`/`concat` references, non-static base keys, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-string-prototype-concat-base-a1110-focused-green-20260724-051125.log` (3 files / 6 tests), `agent-core-node-string-prototype-concat-base-a1110-related-20260724-051142.log` (4 files / 8 tests), `agent-core-build-a1110-20260724-051150.log`, `node-string-prototype-concat-base-dist-fixed-a1110-20260724-051158.log` (9 cases / 0 mismatches), `agent-core-auto-reviewer-a1110-20260724-051207.log` (230 files / 706 tests), and `agent-core-full-test-a1110-20260724-051222.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-string-prototype-concat.utils.ts` 179, `javascript-static-member-key.utils.ts` 53, `javascript-static-string-method-chain.utils.ts` 42, `javascript-static-trim-method.utils.ts` 151, `javascript-static-slice-method.utils.ts` 182, `javascript-static-concat-method.utils.ts` 76, `javascript-static-decimal-code.utils.ts` 49, `node-direct-command.utils.ts` 64, `interpreter-shell-command.utils.ts` 143, `node-shell-option.utils.ts` 70, and `interpreter-script-token.utils.ts` 76. |
| A1111 | done | Core E2E | Detect Node child process direct/shell commands built from immediately invoked static JS `String.prototype.concat.bind(...)`, so `spawnSync(String.prototype.concat.bind("r", "m")(), [String.prototype.concat.bind("-r", "f")(), "/"])`, `spawnSync(String.prototype.concat.bind("r")("m"), [String.prototype.concat.bind("-r")("f"), "/"])`, `execSync(String.prototype.concat.bind("rm")(" -rf /"))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-string-prototype-concat-bind-runtime-a1111-20260724-051801.log` confirmed real Node evaluates bound concat receivers, bound concat arguments, invocation-time concat arguments, shell `execSync`, shell-option command strings, and safe `--version` operands through child_process calls (5 cases / 0 failures). Pre-fix current-dist probe `node-string-prototype-concat-bind-dist-before-a1111-20260724-051802.log` reproduced 4 false-negative mismatches out of 7 cases where dangerous direct argv bound args, direct argv invocation args, shell `execSync`, and shell-option command strings returned `ask_user`; safe operands, dynamic bind receivers, and dynamic invocation args stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-prototype-concat-bind-a1111-focused-red-2-20260724-051920.log` (3 files / 3 failed, 3 passed tests), then passed after `javascript-string-prototype-concat.utils.ts` added an immediately-invoked `bind` reader that joins static receiver, bound args, and invocation args, and new `javascript-static-member-access.utils.ts` extracted static member-name and JS call-start parsing shared by base/wrapper/bind handling. Tests now cover dangerous direct argv bound/invocation args, shell `execSync`, shell-option command strings, safe operands, dynamic bind receiver guards, and dynamic invocation-argument guards. Non-invoked bound functions, aliased `String`/`concat` references, non-static receivers, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-string-prototype-concat-bind-a1111-focused-green-20260724-052030.log` (3 files / 6 tests), `agent-core-node-string-prototype-concat-bind-a1111-related-20260724-052056.log` (4 files / 8 tests), `agent-core-build-a1111-20260724-052056.log`, `node-string-prototype-concat-bind-dist-fixed-a1111-20260724-052110.log` (7 cases / 0 mismatches), `agent-core-auto-reviewer-a1111-rerun-20260724-052152.log` (229 files / 689 tests), and `agent-core-full-test-a1111-20260724-052111.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-string-prototype-concat.utils.ts` 192, `javascript-static-member-access.utils.ts` 41, and `javascript-static-member-key.utils.ts` 53. |
| A1112 | done | Core E2E | Detect Node child process direct/shell commands built from static JS `String.prototype.concat.bind.call/apply(...)` wrappers, so `spawnSync(String.prototype.concat.bind.call(String.prototype.concat, "r", "m")(), ...)`, `spawnSync(String.prototype.concat.bind.apply(String.prototype.concat, ["r", "m"])(), ...)`, `execSync(String.prototype.concat.bind.call(String.prototype.concat, "rm")(" -rf /"))`, and `{ shell: true }` command strings deny instead of slipping through as user-reviewable. | Runtime probe `node-string-prototype-concat-bind-wrapper-runtime-a1112-rerun-20260724-052702.log` confirmed real Node evaluates `bind.call` and `bind.apply` wrapper forms into child-process direct argv and shell command strings while preserving safe `rm --version` operands (5 cases / 0 failures). Pre-fix current-dist probe `node-string-prototype-concat-bind-wrapper-dist-before-a1112-20260724-052625.log` reproduced 4 false-negative mismatches out of 8 cases where dangerous direct argv `bind.call`, direct argv `bind.apply`, shell `execSync`, and shell-option command strings returned `ask_user`; safe operands, dynamic wrapper target functions, dynamic apply arrays, and dynamic invocation args stayed `ask_user`. Focused E2E first failed in `agent-core-node-string-prototype-concat-bind-wrapper-a1112-focused-red-20260724-052742.log` (3 files / 3 failed, 3 passed tests), then passed after direct and wrapper bind parsing moved into new `javascript-string-prototype-concat-bind.utils.ts`, static string-list parsing moved into `javascript-static-string-list.utils.ts`, and `javascript-string-prototype-concat.utils.ts` delegates `bind`, `bind.call`, and `bind.apply` handling without exceeding its responsibility or line budget. Tests now cover dangerous direct argv `bind.call/apply`, shell `execSync` `bind.call`, shell-option `bind.apply`, safe operands, dynamic target guards, dynamic apply-array guards, and dynamic invocation-argument guards. Non-invoked bound functions, aliased `String`/`concat` references, non-static wrapper targets, side-effecting receivers, mutation/alias chains, and broader JS expression evaluation remain unresolved/user-reviewable rather than guessed. Verification passed in `agent-core-node-string-prototype-concat-bind-wrapper-a1112-focused-green-20260724-053006.log` (3 files / 6 tests), `agent-core-node-string-prototype-concat-bind-wrapper-a1112-related-20260724-053023.log` (4 files / 8 tests), `agent-core-build-a1112-20260724-053023.log`, `node-string-prototype-concat-bind-wrapper-dist-fixed-a1112-20260724-053035.log` (8 cases / 0 mismatches), `agent-core-auto-reviewer-a1112-20260724-053035.log` (229 files / 689 tests), and `agent-core-full-test-a1112-20260724-053035.log` (327 files / 1979 tests). Production source line counts remain under the 200-line ceiling: `javascript-static-string.utils.ts` 199, `javascript-string-prototype-concat.utils.ts` 118, `javascript-string-prototype-concat-bind.utils.ts` 158, `javascript-static-string-list.utils.ts` 50, and `javascript-static-member-access.utils.ts` 41. |
