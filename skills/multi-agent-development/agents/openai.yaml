interface:
  display_name: "Multi-Agent Development"
  short_description: "Coordinate explicitly authorized bounded agents"
  default_prompt: "Use $multi-agent-development to coordinate only the bounded agent roles this large development task actually needs."

policy:
  allow_implicit_invocation: true
