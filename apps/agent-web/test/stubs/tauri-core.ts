/** Test stub for @tauri-apps/api/core.
 *  Returns null for any invoke — tests that need real behaviour mock it
 *  per-test with vi.mock('@tauri-apps/api/core', ...). */
export async function invoke(): Promise<unknown> {
  return null;
}
